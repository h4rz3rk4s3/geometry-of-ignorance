,Repo,GitHub_Type,Nr,original_statement,rephrased_statement,Non_Knowledge_type,original_Unnamed: 0.1,original_Unnamed: 0,original_Repo,original_Nr,original_GitHub_Type,original_final_example,original_label,original_label_detailed,label_detailed,label,statement
0,pytorch_pytorch,issue,154997,"I accidentally discovered that nn.Linear with datatype float16/bfloat16 can produce significant numerical errors on sliced tensors:
 import torch
 torch.random.manual_seed(42)
 from torch import nn
 

 dtype = torch.float16
 batch, seq, embed_dim, delay = 2, 16, 4, 2
 x1 = torch.randn(batch, seq, embed_dim, device=""cuda"", dtype=dtype)
 x2 = x1[:, delay:]
 assert torch.allclose(x1[:, delay:], x2)
 net = nn.Linear(embed_dim, embed_dim).to(""cuda"").to(dtype=dtype)
 out1 = net(x1)
 out2 = net(x2)
 print((out1[:, delay:] - out2).abs().max())
 assert torch.allclose(out1[:, delay:], out2)
 

 I got the output like this:
 tensor(0.0010, device='cuda:0', dtype=torch.float16, grad_fn=<MaxBackward1>)
 Traceback (most recent call last):
  File ""/home/jewel/Workspaces/cusrl/test.py"", line 14, in <module>
  assert torch.allclose(out1[:, delay:], out2)
 AssertionError
 

 If the type is bfloat16, the error is even greater:
 tensor(0.0078, device='cuda:0', dtype=torch.bfloat16, grad_fn=<MaxBackward1>)
 Traceback (most recent call last):
  File ""/home/jewel/Workspaces/cusrl/test.py"", line 14, in <module>
  assert torch.allclose(out1[:, delay:], out2)
 AssertionError
 

 However, if the tensor is cloned after sliced x2 = x1[:, delay:].clone() or the type is float32, it will produce an error of exactly zero.
 tensor(0., device='cuda:0', grad_fn=<MaxBackward1>)
 

 I don't know if this is an allowable margin of error or a bug.","I noticed that using nn.Linear with float16 or bfloat16 datatypes can lead to significant numerical discrepancies when applied to sliced tensors. The code `x2 = x1[:, delay:]` and `out2 = net(x2)` produces different results compared to `out1[:, delay:]` from `out1 = net(x1)`. The differences are small but significant enough to cause assertion errors. I'm unsure if this is a known issue or a bug in PyTorch's half-precision implementation.",Known,0,0,pytorch_pytorch,154997,issue,,1,1,1,1,"I noticed that using nn.Linear with float16 or bfloat16 datatypes can lead to significant numerical discrepancies when applied to sliced tensors. The code `x2 = x1[:, delay:]` and `out2 = net(x2)` produces different results compared to `out1[:, delay:]` from `out1 = net(x1)`. The differences are small but significant enough to cause assertion errors. I'm unsure if this is a known issue or a bug in PyTorch's half-precision implementation."
1,pytorch_pytorch,issue,154997,"I accidentally discovered that nn.Linear with datatype float16/bfloat16 can produce significant numerical errors on sliced tensors:
 import torch
 torch.random.manual_seed(42)
 from torch import nn
 

 dtype = torch.float16
 batch, seq, embed_dim, delay = 2, 16, 4, 2
 x1 = torch.randn(batch, seq, embed_dim, device=""cuda"", dtype=dtype)
 x2 = x1[:, delay:]
 assert torch.allclose(x1[:, delay:], x2)
 net = nn.Linear(embed_dim, embed_dim).to(""cuda"").to(dtype=dtype)
 out1 = net(x1)
 out2 = net(x2)
 print((out1[:, delay:] - out2).abs().max())
 assert torch.allclose(out1[:, delay:], out2)
 

 I got the output like this:
 tensor(0.0010, device='cuda:0', dtype=torch.float16, grad_fn=<MaxBackward1>)
 Traceback (most recent call last):
  File ""/home/jewel/Workspaces/cusrl/test.py"", line 14, in <module>
  assert torch.allclose(out1[:, delay:], out2)
 AssertionError
 

 If the type is bfloat16, the error is even greater:
 tensor(0.0078, device='cuda:0', dtype=torch.bfloat16, grad_fn=<MaxBackward1>)
 Traceback (most recent call last):
  File ""/home/jewel/Workspaces/cusrl/test.py"", line 14, in <module>
  assert torch.allclose(out1[:, delay:], out2)
 AssertionError
 

 However, if the tensor is cloned after sliced x2 = x1[:, delay:].clone() or the type is float32, it will produce an error of exactly zero.
 tensor(0., device='cuda:0', grad_fn=<MaxBackward1>)
 

 I don't know if this is an allowable margin of error or a bug.","I'm encountering unexpected numerical errors when using nn.Linear with float16 and bfloat16 datatypes on sliced tensors. The assertion `torch.allclose(out1[:, delay:], out2)` fails, even though `torch.allclose(x1[:, delay:], x2)` passes. The error is more pronounced with bfloat16. I'm not sure if this is within the acceptable range of precision for these datatypes or if it's a bug.",Known,0,0,pytorch_pytorch,154997,issue,,1,1,1,1,"I'm encountering unexpected numerical errors when using nn.Linear with float16 and bfloat16 datatypes on sliced tensors. The assertion `torch.allclose(out1[:, delay:], out2)` fails, even though `torch.allclose(x1[:, delay:], x2)` passes. The error is more pronounced with bfloat16. I'm not sure if this is within the acceptable range of precision for these datatypes or if it's a bug."
2,pytorch_pytorch,issue,154997,"I accidentally discovered that nn.Linear with datatype float16/bfloat16 can produce significant numerical errors on sliced tensors:
 import torch
 torch.random.manual_seed(42)
 from torch import nn
 

 dtype = torch.float16
 batch, seq, embed_dim, delay = 2, 16, 4, 2
 x1 = torch.randn(batch, seq, embed_dim, device=""cuda"", dtype=dtype)
 x2 = x1[:, delay:]
 assert torch.allclose(x1[:, delay:], x2)
 net = nn.Linear(embed_dim, embed_dim).to(""cuda"").to(dtype=dtype)
 out1 = net(x1)
 out2 = net(x2)
 print((out1[:, delay:] - out2).abs().max())
 assert torch.allclose(out1[:, delay:], out2)
 

 I got the output like this:
 tensor(0.0010, device='cuda:0', dtype=torch.float16, grad_fn=<MaxBackward1>)
 Traceback (most recent call last):
  File ""/home/jewel/Workspaces/cusrl/test.py"", line 14, in <module>
  assert torch.allclose(out1[:, delay:], out2)
 AssertionError
 

 If the type is bfloat16, the error is even greater:
 tensor(0.0078, device='cuda:0', dtype=torch.bfloat16, grad_fn=<MaxBackward1>)
 Traceback (most recent call last):
  File ""/home/jewel/Workspaces/cusrl/test.py"", line 14, in <module>
  assert torch.allclose(out1[:, delay:], out2)
 AssertionError
 

 However, if the tensor is cloned after sliced x2 = x1[:, delay:].clone() or the type is float32, it will produce an error of exactly zero.
 tensor(0., device='cuda:0', grad_fn=<MaxBackward1>)
 

 I don't know if this is an allowable margin of error or a bug.","I've found that nn.Linear can produce different outputs for mathematically identical float16 and bfloat16 tensors when they are sliced. The code `x2 = x1[:, delay:]` and `out2 = net(x2)` results in a significant numerical difference compared to `out1[:, delay:]` from `out1 = net(x1)`. The error is particularly large with bfloat16. I'm not sure if this is a precision issue or a bug in PyTorch.",Known,0,0,pytorch_pytorch,154997,issue,,1,1,1,1,"I've found that nn.Linear can produce different outputs for mathematically identical float16 and bfloat16 tensors when they are sliced. The code `x2 = x1[:, delay:]` and `out2 = net(x2)` results in a significant numerical difference compared to `out1[:, delay:]` from `out1 = net(x1)`. The error is particularly large with bfloat16. I'm not sure if this is a precision issue or a bug in PyTorch."
3,pytorch_pytorch,issue,154997,"I accidentally discovered that nn.Linear with datatype float16/bfloat16 can produce significant numerical errors on sliced tensors:
 import torch
 torch.random.manual_seed(42)
 from torch import nn
 

 dtype = torch.float16
 batch, seq, embed_dim, delay = 2, 16, 4, 2
 x1 = torch.randn(batch, seq, embed_dim, device=""cuda"", dtype=dtype)
 x2 = x1[:, delay:]
 assert torch.allclose(x1[:, delay:], x2)
 net = nn.Linear(embed_dim, embed_dim).to(""cuda"").to(dtype=dtype)
 out1 = net(x1)
 out2 = net(x2)
 print((out1[:, delay:] - out2).abs().max())
 assert torch.allclose(out1[:, delay:], out2)
 

 I got the output like this:
 tensor(0.0010, device='cuda:0', dtype=torch.float16, grad_fn=<MaxBackward1>)
 Traceback (most recent call last):
  File ""/home/jewel/Workspaces/cusrl/test.py"", line 14, in <module>
  assert torch.allclose(out1[:, delay:], out2)
 AssertionError
 

 If the type is bfloat16, the error is even greater:
 tensor(0.0078, device='cuda:0', dtype=torch.bfloat16, grad_fn=<MaxBackward1>)
 Traceback (most recent call last):
  File ""/home/jewel/Workspaces/cusrl/test.py"", line 14, in <module>
  assert torch.allclose(out1[:, delay:], out2)
 AssertionError
 

 However, if the tensor is cloned after sliced x2 = x1[:, delay:].clone() or the type is float32, it will produce an error of exactly zero.
 tensor(0., device='cuda:0', grad_fn=<MaxBackward1>)
 

 I don't know if this is an allowable margin of error or a bug.","I'm seeing significant numerical discrepancies when using nn.Linear with float16 and bfloat16 datatypes on sliced tensors. The code `x2 = x1[:, delay:]` and `out2 = net(x2)` produces different results compared to `out1[:, delay:]` from `out1 = net(x1)`. The differences are small but enough to cause assertion errors. I'm unsure if this is a known issue or a bug in PyTorch's half-precision implementation.",Known,0,0,pytorch_pytorch,154997,issue,,1,1,1,1,"I'm seeing significant numerical discrepancies when using nn.Linear with float16 and bfloat16 datatypes on sliced tensors. The code `x2 = x1[:, delay:]` and `out2 = net(x2)` produces different results compared to `out1[:, delay:]` from `out1 = net(x1)`. The differences are small but enough to cause assertion errors. I'm unsure if this is a known issue or a bug in PyTorch's half-precision implementation."
4,pytorch_pytorch,issue,154997,"I accidentally discovered that nn.Linear with datatype float16/bfloat16 can produce significant numerical errors on sliced tensors:
 import torch
 torch.random.manual_seed(42)
 from torch import nn
 

 dtype = torch.float16
 batch, seq, embed_dim, delay = 2, 16, 4, 2
 x1 = torch.randn(batch, seq, embed_dim, device=""cuda"", dtype=dtype)
 x2 = x1[:, delay:]
 assert torch.allclose(x1[:, delay:], x2)
 net = nn.Linear(embed_dim, embed_dim).to(""cuda"").to(dtype=dtype)
 out1 = net(x1)
 out2 = net(x2)
 print((out1[:, delay:] - out2).abs().max())
 assert torch.allclose(out1[:, delay:], out2)
 

 I got the output like this:
 tensor(0.0010, device='cuda:0', dtype=torch.float16, grad_fn=<MaxBackward1>)
 Traceback (most recent call last):
  File ""/home/jewel/Workspaces/cusrl/test.py"", line 14, in <module>
  assert torch.allclose(out1[:, delay:], out2)
 AssertionError
 

 If the type is bfloat16, the error is even greater:
 tensor(0.0078, device='cuda:0', dtype=torch.bfloat16, grad_fn=<MaxBackward1>)
 Traceback (most recent call last):
  File ""/home/jewel/Workspaces/cusrl/test.py"", line 14, in <module>
  assert torch.allclose(out1[:, delay:], out2)
 AssertionError
 

 However, if the tensor is cloned after sliced x2 = x1[:, delay:].clone() or the type is float32, it will produce an error of exactly zero.
 tensor(0., device='cuda:0', grad_fn=<MaxBackward1>)
 

 I don't know if this is an allowable margin of error or a bug.","I've encountered a numerical precision issue when using nn.Linear with float16 and bfloat16 datatypes on sliced tensors. The code `x2 = x1[:, delay:]` and `out2 = net(x2)` results in a significant numerical difference compared to `out1[:, delay:]` from `out1 = net(x1)`. The error is more pronounced with bfloat16. I'm not sure if this is a precision issue or a bug in PyTorch.",Known,0,0,pytorch_pytorch,154997,issue,,1,1,1,1,"I've encountered a numerical precision issue when using nn.Linear with float16 and bfloat16 datatypes on sliced tensors. The code `x2 = x1[:, delay:]` and `out2 = net(x2)` results in a significant numerical difference compared to `out1[:, delay:]` from `out1 = net(x1)`. The error is more pronounced with bfloat16. I'm not sure if this is a precision issue or a bug in PyTorch."
5,pytorch_pytorch,issue,154997,"I accidentally discovered that nn.Linear with datatype float16/bfloat16 can produce significant numerical errors on sliced tensors:
 import torch
 torch.random.manual_seed(42)
 from torch import nn
 

 dtype = torch.float16
 batch, seq, embed_dim, delay = 2, 16, 4, 2
 x1 = torch.randn(batch, seq, embed_dim, device=""cuda"", dtype=dtype)
 x2 = x1[:, delay:]
 assert torch.allclose(x1[:, delay:], x2)
 net = nn.Linear(embed_dim, embed_dim).to(""cuda"").to(dtype=dtype)
 out1 = net(x1)
 out2 = net(x2)
 print((out1[:, delay:] - out2).abs().max())
 assert torch.allclose(out1[:, delay:], out2)
 

 I got the output like this:
 tensor(0.0010, device='cuda:0', dtype=torch.float16, grad_fn=<MaxBackward1>)
 Traceback (most recent call last):
  File ""/home/jewel/Workspaces/cusrl/test.py"", line 14, in <module>
  assert torch.allclose(out1[:, delay:], out2)
 AssertionError
 

 If the type is bfloat16, the error is even greater:
 tensor(0.0078, device='cuda:0', dtype=torch.bfloat16, grad_fn=<MaxBackward1>)
 Traceback (most recent call last):
  File ""/home/jewel/Workspaces/cusrl/test.py"", line 14, in <module>
  assert torch.allclose(out1[:, delay:], out2)
 AssertionError
 

 However, if the tensor is cloned after sliced x2 = x1[:, delay:].clone() or the type is float32, it will produce an error of exactly zero.
 tensor(0., device='cuda:0', grad_fn=<MaxBackward1>)
 

 I don't know if this is an allowable margin of error or a bug.","I'm running into numerical precision issues when applying nn.Linear to sliced float16 and bfloat16 tensors. The code `x2 = x1[:, delay:]` and `out2 = net(x2)` produces different results compared to `out1[:, delay:]` from `out1 = net(x1)`. The differences are small but significant enough to cause assertion errors. I'm unsure if this is a known issue or a bug in PyTorch's half-precision implementation.",Known,0,0,pytorch_pytorch,154997,issue,,1,1,1,1,"I'm running into numerical precision issues when applying nn.Linear to sliced float16 and bfloat16 tensors. The code `x2 = x1[:, delay:]` and `out2 = net(x2)` produces different results compared to `out1[:, delay:]` from `out1 = net(x1)`. The differences are small but significant enough to cause assertion errors. I'm unsure if this is a known issue or a bug in PyTorch's half-precision implementation."
6,pytorch_pytorch,issue,154997,"I accidentally discovered that nn.Linear with datatype float16/bfloat16 can produce significant numerical errors on sliced tensors:
 import torch
 torch.random.manual_seed(42)
 from torch import nn
 

 dtype = torch.float16
 batch, seq, embed_dim, delay = 2, 16, 4, 2
 x1 = torch.randn(batch, seq, embed_dim, device=""cuda"", dtype=dtype)
 x2 = x1[:, delay:]
 assert torch.allclose(x1[:, delay:], x2)
 net = nn.Linear(embed_dim, embed_dim).to(""cuda"").to(dtype=dtype)
 out1 = net(x1)
 out2 = net(x2)
 print((out1[:, delay:] - out2).abs().max())
 assert torch.allclose(out1[:, delay:], out2)
 

 I got the output like this:
 tensor(0.0010, device='cuda:0', dtype=torch.float16, grad_fn=<MaxBackward1>)
 Traceback (most recent call last):
  File ""/home/jewel/Workspaces/cusrl/test.py"", line 14, in <module>
  assert torch.allclose(out1[:, delay:], out2)
 AssertionError
 

 If the type is bfloat16, the error is even greater:
 tensor(0.0078, device='cuda:0', dtype=torch.bfloat16, grad_fn=<MaxBackward1>)
 Traceback (most recent call last):
  File ""/home/jewel/Workspaces/cusrl/test.py"", line 14, in <module>
  assert torch.allclose(out1[:, delay:], out2)
 AssertionError
 

 However, if the tensor is cloned after sliced x2 = x1[:, delay:].clone() or the type is float32, it will produce an error of exactly zero.
 tensor(0., device='cuda:0', grad_fn=<MaxBackward1>)
 

 I don't know if this is an allowable margin of error or a bug.","I've noticed that nn.Linear can produce different outputs for mathematically identical float16 and bfloat16 tensors when they are sliced. The code `x2 = x1[:, delay:]` and `out2 = net(x2)` results in a significant numerical difference compared to `out1[:, delay:]` from `out1 = net(x1)`. The error is particularly large with bfloat16. I'm not sure if this is a precision issue or a bug in PyTorch.",Known,0,0,pytorch_pytorch,154997,issue,,1,1,1,1,"I've noticed that nn.Linear can produce different outputs for mathematically identical float16 and bfloat16 tensors when they are sliced. The code `x2 = x1[:, delay:]` and `out2 = net(x2)` results in a significant numerical difference compared to `out1[:, delay:]` from `out1 = net(x1)`. The error is particularly large with bfloat16. I'm not sure if this is a precision issue or a bug in PyTorch."
7,pytorch_pytorch,issue,154997,"I accidentally discovered that nn.Linear with datatype float16/bfloat16 can produce significant numerical errors on sliced tensors:
 import torch
 torch.random.manual_seed(42)
 from torch import nn
 

 dtype = torch.float16
 batch, seq, embed_dim, delay = 2, 16, 4, 2
 x1 = torch.randn(batch, seq, embed_dim, device=""cuda"", dtype=dtype)
 x2 = x1[:, delay:]
 assert torch.allclose(x1[:, delay:], x2)
 net = nn.Linear(embed_dim, embed_dim).to(""cuda"").to(dtype=dtype)
 out1 = net(x1)
 out2 = net(x2)
 print((out1[:, delay:] - out2).abs().max())
 assert torch.allclose(out1[:, delay:], out2)
 

 I got the output like this:
 tensor(0.0010, device='cuda:0', dtype=torch.float16, grad_fn=<MaxBackward1>)
 Traceback (most recent call last):
  File ""/home/jewel/Workspaces/cusrl/test.py"", line 14, in <module>
  assert torch.allclose(out1[:, delay:], out2)
 AssertionError
 

 If the type is bfloat16, the error is even greater:
 tensor(0.0078, device='cuda:0', dtype=torch.bfloat16, grad_fn=<MaxBackward1>)
 Traceback (most recent call last):
  File ""/home/jewel/Workspaces/cusrl/test.py"", line 14, in <module>
  assert torch.allclose(out1[:, delay:], out2)
 AssertionError
 

 However, if the tensor is cloned after sliced x2 = x1[:, delay:].clone() or the type is float32, it will produce an error of exactly zero.
 tensor(0., device='cuda:0', grad_fn=<MaxBackward1>)
 

 I don't know if this is an allowable margin of error or a bug.","I'm encountering unexpected numerical errors when using nn.Linear with float16 and bfloat16 datatypes on sliced tensors. The assertion `torch.allclose(out1[:, delay:], out2)` fails, even though `torch.allclose(x1[:, delay:], x2)` passes. The error is more pronounced with bfloat16. I'm not sure if this is within the acceptable range of precision for these datatypes or if it's a bug in PyTorch.",Known,0,0,pytorch_pytorch,154997,issue,,1,1,1,1,"I'm encountering unexpected numerical errors when using nn.Linear with float16 and bfloat16 datatypes on sliced tensors. The assertion `torch.allclose(out1[:, delay:], out2)` fails, even though `torch.allclose(x1[:, delay:], x2)` passes. The error is more pronounced with bfloat16. I'm not sure if this is within the acceptable range of precision for these datatypes or if it's a bug in PyTorch."
8,pytorch_pytorch,issue,154997,"I accidentally discovered that nn.Linear with datatype float16/bfloat16 can produce significant numerical errors on sliced tensors:
 import torch
 torch.random.manual_seed(42)
 from torch import nn
 

 dtype = torch.float16
 batch, seq, embed_dim, delay = 2, 16, 4, 2
 x1 = torch.randn(batch, seq, embed_dim, device=""cuda"", dtype=dtype)
 x2 = x1[:, delay:]
 assert torch.allclose(x1[:, delay:], x2)
 net = nn.Linear(embed_dim, embed_dim).to(""cuda"").to(dtype=dtype)
 out1 = net(x1)
 out2 = net(x2)
 print((out1[:, delay:] - out2).abs().max())
 assert torch.allclose(out1[:, delay:], out2)
 

 I got the output like this:
 tensor(0.0010, device='cuda:0', dtype=torch.float16, grad_fn=<MaxBackward1>)
 Traceback (most recent call last):
  File ""/home/jewel/Workspaces/cusrl/test.py"", line 14, in <module>
  assert torch.allclose(out1[:, delay:], out2)
 AssertionError
 

 If the type is bfloat16, the error is even greater:
 tensor(0.0078, device='cuda:0', dtype=torch.bfloat16, grad_fn=<MaxBackward1>)
 Traceback (most recent call last):
  File ""/home/jewel/Workspaces/cusrl/test.py"", line 14, in <module>
  assert torch.allclose(out1[:, delay:], out2)
 AssertionError
 

 However, if the tensor is cloned after sliced x2 = x1[:, delay:].clone() or the type is float32, it will produce an error of exactly zero.
 tensor(0., device='cuda:0', grad_fn=<MaxBackward1>)
 

 I don't know if this is an allowable margin of error or a bug.","I've found that nn.Linear can produce different outputs for mathematically identical float16 and bfloat16 tensors when they are sliced. The code `x2 = x1[:, delay:]` and `out2 = net(x2)` results in a significant numerical difference compared to `out1[:, delay:]` from `out1 = net(x1)`. The error is particularly large with bfloat16. I'm not sure if this is a precision issue or a bug in PyTorch.",Known,0,0,pytorch_pytorch,154997,issue,,1,1,1,1,"I've found that nn.Linear can produce different outputs for mathematically identical float16 and bfloat16 tensors when they are sliced. The code `x2 = x1[:, delay:]` and `out2 = net(x2)` results in a significant numerical difference compared to `out1[:, delay:]` from `out1 = net(x1)`. The error is particularly large with bfloat16. I'm not sure if this is a precision issue or a bug in PyTorch."
9,pytorch_pytorch,issue,154997,"I accidentally discovered that nn.Linear with datatype float16/bfloat16 can produce significant numerical errors on sliced tensors:
 import torch
 torch.random.manual_seed(42)
 from torch import nn
 

 dtype = torch.float16
 batch, seq, embed_dim, delay = 2, 16, 4, 2
 x1 = torch.randn(batch, seq, embed_dim, device=""cuda"", dtype=dtype)
 x2 = x1[:, delay:]
 assert torch.allclose(x1[:, delay:], x2)
 net = nn.Linear(embed_dim, embed_dim).to(""cuda"").to(dtype=dtype)
 out1 = net(x1)
 out2 = net(x2)
 print((out1[:, delay:] - out2).abs().max())
 assert torch.allclose(out1[:, delay:], out2)
 

 I got the output like this:
 tensor(0.0010, device='cuda:0', dtype=torch.float16, grad_fn=<MaxBackward1>)
 Traceback (most recent call last):
  File ""/home/jewel/Workspaces/cusrl/test.py"", line 14, in <module>
  assert torch.allclose(out1[:, delay:], out2)
 AssertionError
 

 If the type is bfloat16, the error is even greater:
 tensor(0.0078, device='cuda:0', dtype=torch.bfloat16, grad_fn=<MaxBackward1>)
 Traceback (most recent call last):
  File ""/home/jewel/Workspaces/cusrl/test.py"", line 14, in <module>
  assert torch.allclose(out1[:, delay:], out2)
 AssertionError
 

 However, if the tensor is cloned after sliced x2 = x1[:, delay:].clone() or the type is float32, it will produce an error of exactly zero.
 tensor(0., device='cuda:0', grad_fn=<MaxBackward1>)
 

 I don't know if this is an allowable margin of error or a bug.","I'm seeing significant numerical discrepancies when using nn.Linear with float16 and bfloat16 datatypes on sliced tensors. The code `x2 = x1[:, delay:]` and `out2 = net(x2)` produces different results compared to `out1[:, delay:]` from `out1 = net(x1)`. The differences are small but enough to cause assertion errors. I'm unsure if this is a known issue or a bug in PyTorch's half-precision implementation.",Known,0,0,pytorch_pytorch,154997,issue,,1,1,1,1,"I'm seeing significant numerical discrepancies when using nn.Linear with float16 and bfloat16 datatypes on sliced tensors. The code `x2 = x1[:, delay:]` and `out2 = net(x2)` produces different results compared to `out1[:, delay:]` from `out1 = net(x1)`. The differences are small but enough to cause assertion errors. I'm unsure if this is a known issue or a bug in PyTorch's half-precision implementation."
10,pytorch_pytorch,Issue,154556,"When I use torch.compile(flex_attention) with NJT inputs I get the following error:
 torch._dynamo.exc.BackendCompilerFailed: backend='inductor' raised:
 RuntimeError: Attempting to use FunctionalTensor on its own. Instead, please use it with a corresponding FunctionalTensorMode()
 

 This happens both on cpu and cuda devices (A100, 6000 ADA, 2080Ti) and independent of setting dynamic=True/False.
 Depending on how K,V are constructed, their jagged dimensions might have shapes j2,j3 which are not detected as matching. I made some effort to get both equal to j2, but that didn't help.
 Note that Q has a different shape j1. Running self-attention flex_attention(Q,Q,Q) with all jagged axes equal to j1 didn't help either.
 A minimal reproducer for this issue:
 import torch
 from torch.nested import nested_tensor_from_jagged
 from torch.nn.attention.flex_attention import flex_attention
 

 device = 'cuda' if torch.cuda.is_available() else 'cpu'
 H, E_inner, E_value= 8, 16, 32
 lengths_Q = torch.tensor([11, 13, 15])
 lengths_KV = torch.tensor([10, 15, 11])
 offsets_Q = torch.cat([torch.tensor([0]), torch.cumsum(lengths_Q, dim=0)])
 offsets_KV = torch.cat([torch.tensor([0]), torch.cumsum(lengths_KV, dim=0)])
 

 Q = torch.cat([torch.randn(Ni,H,E_inner) for Ni in lengths_Q], dim=0)
 K = torch.cat([torch.randn(Ni,H,E_inner) for Ni in lengths_KV], dim=0)
 V = torch.cat([torch.randn(Ni,H,E_value) for Ni in lengths_KV], dim=0)
 Q = nested_tensor_from_jagged(Q, offsets=offsets_Q )
 K = nested_tensor_from_jagged(K, offsets=offsets_KV)
 V = nested_tensor_from_jagged(V, offsets=offsets_KV)
 Q = Q.moveaxis(1,2).contiguous().to(device) # size=(B, H, j1, E_inner), contiguous=True
 K = K.moveaxis(1,2).contiguous().to(device) # size=(B, H, j2, E_inner), contiguous=True
 V = V.moveaxis(1,2).contiguous().to(device) # size=(B, H, j2, E_value), contiguous=True
 

 flex_attention = torch.compile(flex_attention)
 attn_out = flex_attention(Q,K,V) # size=(B, H, j1, E_value), crashes when flex_attention is compiled
 print(attn_out)","I'm encountering an error when using `torch.compile` with the `flex_attention` function and NJT inputs. The error message is `RuntimeError: Attempting to use FunctionalTensor on its own. Instead, please use it with a corresponding FunctionalTensorMode()`. This issue occurs on both CPU and CUDA devices, regardless of whether `dynamic` is set to `True` or `False`. I've tried to ensure that the jagged dimensions of K and V match, but the error persists. A minimal reproducer is provided below, which demonstrates the issue with nested tensors and the `flex_attention` function.",Irrelevant,1,1,pytorch_pytorch,154556,Issue,,1,2,2,1,"I'm encountering an error when using `torch.compile` with the `flex_attention` function and NJT inputs. The error message is `RuntimeError: Attempting to use FunctionalTensor on its own. Instead, please use it with a corresponding FunctionalTensorMode()`. This issue occurs on both CPU and CUDA devices, regardless of whether `dynamic` is set to `True` or `False`. I've tried to ensure that the jagged dimensions of K and V match, but the error persists. A minimal reproducer is provided below, which demonstrates the issue with nested tensors and the `flex_attention` function."
11,pytorch_pytorch,Issue,154556,"When I use torch.compile(flex_attention) with NJT inputs I get the following error:
 torch._dynamo.exc.BackendCompilerFailed: backend='inductor' raised:
 RuntimeError: Attempting to use FunctionalTensor on its own. Instead, please use it with a corresponding FunctionalTensorMode()
 

 This happens both on cpu and cuda devices (A100, 6000 ADA, 2080Ti) and independent of setting dynamic=True/False.
 Depending on how K,V are constructed, their jagged dimensions might have shapes j2,j3 which are not detected as matching. I made some effort to get both equal to j2, but that didn't help.
 Note that Q has a different shape j1. Running self-attention flex_attention(Q,Q,Q) with all jagged axes equal to j1 didn't help either.
 A minimal reproducer for this issue:
 import torch
 from torch.nested import nested_tensor_from_jagged
 from torch.nn.attention.flex_attention import flex_attention
 

 device = 'cuda' if torch.cuda.is_available() else 'cpu'
 H, E_inner, E_value= 8, 16, 32
 lengths_Q = torch.tensor([11, 13, 15])
 lengths_KV = torch.tensor([10, 15, 11])
 offsets_Q = torch.cat([torch.tensor([0]), torch.cumsum(lengths_Q, dim=0)])
 offsets_KV = torch.cat([torch.tensor([0]), torch.cumsum(lengths_KV, dim=0)])
 

 Q = torch.cat([torch.randn(Ni,H,E_inner) for Ni in lengths_Q], dim=0)
 K = torch.cat([torch.randn(Ni,H,E_inner) for Ni in lengths_KV], dim=0)
 V = torch.cat([torch.randn(Ni,H,E_value) for Ni in lengths_KV], dim=0)
 Q = nested_tensor_from_jagged(Q, offsets=offsets_Q )
 K = nested_tensor_from_jagged(K, offsets=offsets_KV)
 V = nested_tensor_from_jagged(V, offsets=offsets_KV)
 Q = Q.moveaxis(1,2).contiguous().to(device) # size=(B, H, j1, E_inner), contiguous=True
 K = K.moveaxis(1,2).contiguous().to(device) # size=(B, H, j2, E_inner), contiguous=True
 V = V.moveaxis(1,2).contiguous().to(device) # size=(B, H, j2, E_value), contiguous=True
 

 flex_attention = torch.compile(flex_attention)
 attn_out = flex_attention(Q,K,V) # size=(B, H, j1, E_value), crashes when flex_attention is compiled
 print(attn_out)","I'm facing a runtime error when compiling the `flex_attention` function with NJT inputs using `torch.compile`. The error message indicates that `FunctionalTensor` should be used with a `FunctionalTensorMode()`. This error is consistent across different devices (CPU, A100, 6000 ADA, 2080Ti) and settings of `dynamic`. I've attempted to align the jagged dimensions of K and V, but the issue remains. The provided minimal code snippet reproduces the error, showing that the problem occurs even when all jagged axes are equal.",Irrelevant,1,1,pytorch_pytorch,154556,Issue,,1,2,2,1,"I'm facing a runtime error when compiling the `flex_attention` function with NJT inputs using `torch.compile`. The error message indicates that `FunctionalTensor` should be used with a `FunctionalTensorMode()`. This error is consistent across different devices (CPU, A100, 6000 ADA, 2080Ti) and settings of `dynamic`. I've attempted to align the jagged dimensions of K and V, but the issue remains. The provided minimal code snippet reproduces the error, showing that the problem occurs even when all jagged axes are equal."
12,pytorch_pytorch,Issue,154556,"When I use torch.compile(flex_attention) with NJT inputs I get the following error:
 torch._dynamo.exc.BackendCompilerFailed: backend='inductor' raised:
 RuntimeError: Attempting to use FunctionalTensor on its own. Instead, please use it with a corresponding FunctionalTensorMode()
 

 This happens both on cpu and cuda devices (A100, 6000 ADA, 2080Ti) and independent of setting dynamic=True/False.
 Depending on how K,V are constructed, their jagged dimensions might have shapes j2,j3 which are not detected as matching. I made some effort to get both equal to j2, but that didn't help.
 Note that Q has a different shape j1. Running self-attention flex_attention(Q,Q,Q) with all jagged axes equal to j1 didn't help either.
 A minimal reproducer for this issue:
 import torch
 from torch.nested import nested_tensor_from_jagged
 from torch.nn.attention.flex_attention import flex_attention
 

 device = 'cuda' if torch.cuda.is_available() else 'cpu'
 H, E_inner, E_value= 8, 16, 32
 lengths_Q = torch.tensor([11, 13, 15])
 lengths_KV = torch.tensor([10, 15, 11])
 offsets_Q = torch.cat([torch.tensor([0]), torch.cumsum(lengths_Q, dim=0)])
 offsets_KV = torch.cat([torch.tensor([0]), torch.cumsum(lengths_KV, dim=0)])
 

 Q = torch.cat([torch.randn(Ni,H,E_inner) for Ni in lengths_Q], dim=0)
 K = torch.cat([torch.randn(Ni,H,E_inner) for Ni in lengths_KV], dim=0)
 V = torch.cat([torch.randn(Ni,H,E_value) for Ni in lengths_KV], dim=0)
 Q = nested_tensor_from_jagged(Q, offsets=offsets_Q )
 K = nested_tensor_from_jagged(K, offsets=offsets_KV)
 V = nested_tensor_from_jagged(V, offsets=offsets_KV)
 Q = Q.moveaxis(1,2).contiguous().to(device) # size=(B, H, j1, E_inner), contiguous=True
 K = K.moveaxis(1,2).contiguous().to(device) # size=(B, H, j2, E_inner), contiguous=True
 V = V.moveaxis(1,2).contiguous().to(device) # size=(B, H, j2, E_value), contiguous=True
 

 flex_attention = torch.compile(flex_attention)
 attn_out = flex_attention(Q,K,V) # size=(B, H, j1, E_value), crashes when flex_attention is compiled
 print(attn_out)","I'm getting a `RuntimeError` when using `torch.compile` with the `flex_attention` function and nested jagged tensors (NJT). The error message suggests that `FunctionalTensor` should be used with a `FunctionalTensorMode()`. This error is consistent across both CPU and CUDA devices, and it doesn't matter whether `dynamic` is set to `True` or `False`. I've tried to ensure that the jagged dimensions of K and V match, but the error still occurs. The minimal code snippet provided demonstrates the issue with nested tensors and the `flex_attention` function.",Irrelevant,1,1,pytorch_pytorch,154556,Issue,,1,2,2,1,"I'm getting a `RuntimeError` when using `torch.compile` with the `flex_attention` function and nested jagged tensors (NJT). The error message suggests that `FunctionalTensor` should be used with a `FunctionalTensorMode()`. This error is consistent across both CPU and CUDA devices, and it doesn't matter whether `dynamic` is set to `True` or `False`. I've tried to ensure that the jagged dimensions of K and V match, but the error still occurs. The minimal code snippet provided demonstrates the issue with nested tensors and the `flex_attention` function."
13,pytorch_pytorch,Issue,154556,"When I use torch.compile(flex_attention) with NJT inputs I get the following error:
 torch._dynamo.exc.BackendCompilerFailed: backend='inductor' raised:
 RuntimeError: Attempting to use FunctionalTensor on its own. Instead, please use it with a corresponding FunctionalTensorMode()
 

 This happens both on cpu and cuda devices (A100, 6000 ADA, 2080Ti) and independent of setting dynamic=True/False.
 Depending on how K,V are constructed, their jagged dimensions might have shapes j2,j3 which are not detected as matching. I made some effort to get both equal to j2, but that didn't help.
 Note that Q has a different shape j1. Running self-attention flex_attention(Q,Q,Q) with all jagged axes equal to j1 didn't help either.
 A minimal reproducer for this issue:
 import torch
 from torch.nested import nested_tensor_from_jagged
 from torch.nn.attention.flex_attention import flex_attention
 

 device = 'cuda' if torch.cuda.is_available() else 'cpu'
 H, E_inner, E_value= 8, 16, 32
 lengths_Q = torch.tensor([11, 13, 15])
 lengths_KV = torch.tensor([10, 15, 11])
 offsets_Q = torch.cat([torch.tensor([0]), torch.cumsum(lengths_Q, dim=0)])
 offsets_KV = torch.cat([torch.tensor([0]), torch.cumsum(lengths_KV, dim=0)])
 

 Q = torch.cat([torch.randn(Ni,H,E_inner) for Ni in lengths_Q], dim=0)
 K = torch.cat([torch.randn(Ni,H,E_inner) for Ni in lengths_KV], dim=0)
 V = torch.cat([torch.randn(Ni,H,E_value) for Ni in lengths_KV], dim=0)
 Q = nested_tensor_from_jagged(Q, offsets=offsets_Q )
 K = nested_tensor_from_jagged(K, offsets=offsets_KV)
 V = nested_tensor_from_jagged(V, offsets=offsets_KV)
 Q = Q.moveaxis(1,2).contiguous().to(device) # size=(B, H, j1, E_inner), contiguous=True
 K = K.moveaxis(1,2).contiguous().to(device) # size=(B, H, j2, E_inner), contiguous=True
 V = V.moveaxis(1,2).contiguous().to(device) # size=(B, H, j2, E_value), contiguous=True
 

 flex_attention = torch.compile(flex_attention)
 attn_out = flex_attention(Q,K,V) # size=(B, H, j1, E_value), crashes when flex_attention is compiled
 print(attn_out)","I'm running into a `RuntimeError` when compiling the `flex_attention` function with nested jagged tensors (NJT) using `torch.compile`. The error message states that `FunctionalTensor` should be used with a `FunctionalTensorMode()`. This issue is consistent across different devices (CPU, A100, 6000 ADA, 2080Ti) and settings of `dynamic`. I've tried to ensure that the jagged dimensions of K and V match, but the error persists. The provided minimal code snippet reproduces the error, showing that the problem occurs even when all jagged axes are equal.",Irrelevant,1,1,pytorch_pytorch,154556,Issue,,1,2,2,1,"I'm running into a `RuntimeError` when compiling the `flex_attention` function with nested jagged tensors (NJT) using `torch.compile`. The error message states that `FunctionalTensor` should be used with a `FunctionalTensorMode()`. This issue is consistent across different devices (CPU, A100, 6000 ADA, 2080Ti) and settings of `dynamic`. I've tried to ensure that the jagged dimensions of K and V match, but the error persists. The provided minimal code snippet reproduces the error, showing that the problem occurs even when all jagged axes are equal."
14,pytorch_pytorch,Issue,154556,"When I use torch.compile(flex_attention) with NJT inputs I get the following error:
 torch._dynamo.exc.BackendCompilerFailed: backend='inductor' raised:
 RuntimeError: Attempting to use FunctionalTensor on its own. Instead, please use it with a corresponding FunctionalTensorMode()
 

 This happens both on cpu and cuda devices (A100, 6000 ADA, 2080Ti) and independent of setting dynamic=True/False.
 Depending on how K,V are constructed, their jagged dimensions might have shapes j2,j3 which are not detected as matching. I made some effort to get both equal to j2, but that didn't help.
 Note that Q has a different shape j1. Running self-attention flex_attention(Q,Q,Q) with all jagged axes equal to j1 didn't help either.
 A minimal reproducer for this issue:
 import torch
 from torch.nested import nested_tensor_from_jagged
 from torch.nn.attention.flex_attention import flex_attention
 

 device = 'cuda' if torch.cuda.is_available() else 'cpu'
 H, E_inner, E_value= 8, 16, 32
 lengths_Q = torch.tensor([11, 13, 15])
 lengths_KV = torch.tensor([10, 15, 11])
 offsets_Q = torch.cat([torch.tensor([0]), torch.cumsum(lengths_Q, dim=0)])
 offsets_KV = torch.cat([torch.tensor([0]), torch.cumsum(lengths_KV, dim=0)])
 

 Q = torch.cat([torch.randn(Ni,H,E_inner) for Ni in lengths_Q], dim=0)
 K = torch.cat([torch.randn(Ni,H,E_inner) for Ni in lengths_KV], dim=0)
 V = torch.cat([torch.randn(Ni,H,E_value) for Ni in lengths_KV], dim=0)
 Q = nested_tensor_from_jagged(Q, offsets=offsets_Q )
 K = nested_tensor_from_jagged(K, offsets=offsets_KV)
 V = nested_tensor_from_jagged(V, offsets=offsets_KV)
 Q = Q.moveaxis(1,2).contiguous().to(device) # size=(B, H, j1, E_inner), contiguous=True
 K = K.moveaxis(1,2).contiguous().to(device) # size=(B, H, j2, E_inner), contiguous=True
 V = V.moveaxis(1,2).contiguous().to(device) # size=(B, H, j2, E_value), contiguous=True
 

 flex_attention = torch.compile(flex_attention)
 attn_out = flex_attention(Q,K,V) # size=(B, H, j1, E_value), crashes when flex_attention is compiled
 print(attn_out)","I'm encountering a `RuntimeError` when using `torch.compile` with the `flex_attention` function and nested jagged tensors (NJT). The error message indicates that `FunctionalTensor` should be used with a `FunctionalTensorMode()`. This issue is consistent across both CPU and CUDA devices, and it doesn't matter whether `dynamic` is set to `True` or `False`. I've tried to ensure that the jagged dimensions of K and V match, but the error still occurs. The minimal code snippet provided demonstrates the issue with nested tensors and the `flex_attention` function.",Irrelevant,1,1,pytorch_pytorch,154556,Issue,,1,2,2,1,"I'm encountering a `RuntimeError` when using `torch.compile` with the `flex_attention` function and nested jagged tensors (NJT). The error message indicates that `FunctionalTensor` should be used with a `FunctionalTensorMode()`. This issue is consistent across both CPU and CUDA devices, and it doesn't matter whether `dynamic` is set to `True` or `False`. I've tried to ensure that the jagged dimensions of K and V match, but the error still occurs. The minimal code snippet provided demonstrates the issue with nested tensors and the `flex_attention` function."
15,pytorch_pytorch,Issue,154556,The symptom comes from the functionalization implementation for the flex_attention HOP,The issue seems to stem from the functionalization of the flex_attention HOP implementation.,Knowledge,2,2,pytorch_pytorch,154556,Issue,,0,0,0,0,The issue seems to stem from the functionalization of the flex_attention HOP implementation.
16,pytorch_pytorch,Issue,154556,The symptom comes from the functionalization implementation for the flex_attention HOP,I believe the problem is related to how the flex_attention HOP is functionalized in the code.,Knowledge,2,2,pytorch_pytorch,154556,Issue,,0,0,0,0,I believe the problem is related to how the flex_attention HOP is functionalized in the code.
17,pytorch_pytorch,Issue,154556,The symptom comes from the functionalization implementation for the flex_attention HOP,The root cause of the symptom appears to be the functionalization process used for the flex_attention HOP.,Knowledge,2,2,pytorch_pytorch,154556,Issue,,0,0,0,0,The root cause of the symptom appears to be the functionalization process used for the flex_attention HOP.
18,pytorch_pytorch,Issue,154556,The symptom comes from the functionalization implementation for the flex_attention HOP,The functionalization of the flex_attention HOP might be the source of the observed behavior.,Knowledge,2,2,pytorch_pytorch,154556,Issue,,0,0,0,0,The functionalization of the flex_attention HOP might be the source of the observed behavior.
19,pytorch_pytorch,Issue,154556,The symptom comes from the functionalization implementation for the flex_attention HOP,It looks like the functionalization of the flex_attention HOP is causing the issue.,Knowledge,2,2,pytorch_pytorch,154556,Issue,,0,0,0,0,It looks like the functionalization of the flex_attention HOP is causing the issue.
20,pytorch_pytorch,Issue,154556,The symptom comes from the functionalization implementation for the flex_attention HOP,The symptom is likely due to the way the flex_attention HOP is implemented in the functionalization step.,Knowledge,2,2,pytorch_pytorch,154556,Issue,,0,0,0,0,The symptom is likely due to the way the flex_attention HOP is implemented in the functionalization step.
21,pytorch_pytorch,Issue,154556,The symptom comes from the functionalization implementation for the flex_attention HOP,The problem might be originating from the functionalization of the flex_attention HOP.,Knowledge,2,2,pytorch_pytorch,154556,Issue,,0,0,0,0,The problem might be originating from the functionalization of the flex_attention HOP.
22,pytorch_pytorch,Issue,154556,The symptom comes from the functionalization implementation for the flex_attention HOP,I suspect the functionalization of the flex_attention HOP is responsible for the symptom.,Knowledge,2,2,pytorch_pytorch,154556,Issue,,0,0,0,0,I suspect the functionalization of the flex_attention HOP is responsible for the symptom.
23,pytorch_pytorch,Issue,154556,The symptom comes from the functionalization implementation for the flex_attention HOP,The functionalization implementation for the flex_attention HOP could be the reason for the observed issue.,Knowledge,2,2,pytorch_pytorch,154556,Issue,,0,0,0,0,The functionalization implementation for the flex_attention HOP could be the reason for the observed issue.
24,pytorch_pytorch,Issue,154556,The symptom comes from the functionalization implementation for the flex_attention HOP,The symptom appears to be a result of the functionalization of the flex_attention HOP.,Knowledge,2,2,pytorch_pytorch,154556,Issue,,0,0,0,0,The symptom appears to be a result of the functionalization of the flex_attention HOP.
25,pytorch_pytorch,issue,154556,"On the surface, it seems like ctx.unwrap_tensors() (with ctx being a PythonFunctionalizeAPI in this case) is not smart enough to handle tensor subclasses? Should it be handled there or more ad-hoc within flex_attention's impl?","It appears that ctx.unwrap_tensors() might not be fully equipped to deal with tensor subclasses when using the PythonFunctionalizeAPI. Is this something that should be addressed within the ctx.unwrap_tensors() method, or should the handling be more specific to the flex_attention implementation?",Irrelevant,3,3,pytorch_pytorch,154556,issue,,1,2,2,1,"It appears that ctx.unwrap_tensors() might not be fully equipped to deal with tensor subclasses when using the PythonFunctionalizeAPI. Is this something that should be addressed within the ctx.unwrap_tensors() method, or should the handling be more specific to the flex_attention implementation?"
26,pytorch_pytorch,issue,154556,"On the surface, it seems like ctx.unwrap_tensors() (with ctx being a PythonFunctionalizeAPI in this case) is not smart enough to handle tensor subclasses? Should it be handled there or more ad-hoc within flex_attention's impl?","From what I can tell, ctx.unwrap_tensors() doesn't seem to handle tensor subclasses effectively when used with the PythonFunctionalizeAPI. Should this be a responsibility of the ctx.unwrap_tensors() function, or should the flex_attention implementation take care of it?",Irrelevant,3,3,pytorch_pytorch,154556,issue,,1,2,2,1,"From what I can tell, ctx.unwrap_tensors() doesn't seem to handle tensor subclasses effectively when used with the PythonFunctionalizeAPI. Should this be a responsibility of the ctx.unwrap_tensors() function, or should the flex_attention implementation take care of it?"
27,pytorch_pytorch,issue,154556,"On the surface, it seems like ctx.unwrap_tensors() (with ctx being a PythonFunctionalizeAPI in this case) is not smart enough to handle tensor subclasses? Should it be handled there or more ad-hoc within flex_attention's impl?","It looks like ctx.unwrap_tensors() isn't handling tensor subclasses as expected in the context of the PythonFunctionalizeAPI. Is this a limitation of the ctx.unwrap_tensors() method, or should the flex_attention implementation include additional logic to handle this?",Irrelevant,3,3,pytorch_pytorch,154556,issue,,1,2,2,1,"It looks like ctx.unwrap_tensors() isn't handling tensor subclasses as expected in the context of the PythonFunctionalizeAPI. Is this a limitation of the ctx.unwrap_tensors() method, or should the flex_attention implementation include additional logic to handle this?"
28,pytorch_pytorch,issue,154556,"On the surface, it seems like ctx.unwrap_tensors() (with ctx being a PythonFunctionalizeAPI in this case) is not smart enough to handle tensor subclasses? Should it be handled there or more ad-hoc within flex_attention's impl?","I'm noticing that ctx.unwrap_tensors() doesn't seem to work well with tensor subclasses when using the PythonFunctionalizeAPI. Should this be a concern for the ctx.unwrap_tensors() method, or should the flex_attention implementation have a more tailored approach?",Irrelevant,3,3,pytorch_pytorch,154556,issue,,1,2,2,1,"I'm noticing that ctx.unwrap_tensors() doesn't seem to work well with tensor subclasses when using the PythonFunctionalizeAPI. Should this be a concern for the ctx.unwrap_tensors() method, or should the flex_attention implementation have a more tailored approach?"
29,pytorch_pytorch,issue,154556,"On the surface, it seems like ctx.unwrap_tensors() (with ctx being a PythonFunctionalizeAPI in this case) is not smart enough to handle tensor subclasses? Should it be handled there or more ad-hoc within flex_attention's impl?","It seems that ctx.unwrap_tensors() isn't smart enough to handle tensor subclasses when used with the PythonFunctionalizeAPI. Should this be a general fix in ctx.unwrap_tensors(), or should the flex_attention implementation include specific handling for this?",Irrelevant,3,3,pytorch_pytorch,154556,issue,,1,2,2,1,"It seems that ctx.unwrap_tensors() isn't smart enough to handle tensor subclasses when used with the PythonFunctionalizeAPI. Should this be a general fix in ctx.unwrap_tensors(), or should the flex_attention implementation include specific handling for this?"
30,pytorch_pytorch,issue,154556,"On the surface, it seems like ctx.unwrap_tensors() (with ctx being a PythonFunctionalizeAPI in this case) is not smart enough to handle tensor subclasses? Should it be handled there or more ad-hoc within flex_attention's impl?","From my observations, ctx.unwrap_tensors() doesn't appear to handle tensor subclasses correctly when used with the PythonFunctionalizeAPI. Should this be a responsibility of the ctx.unwrap_tensors() method, or should the flex_attention implementation have a more ad-hoc solution?",Irrelevant,3,3,pytorch_pytorch,154556,issue,,1,2,2,1,"From my observations, ctx.unwrap_tensors() doesn't appear to handle tensor subclasses correctly when used with the PythonFunctionalizeAPI. Should this be a responsibility of the ctx.unwrap_tensors() method, or should the flex_attention implementation have a more ad-hoc solution?"
31,pytorch_pytorch,issue,154556,"On the surface, it seems like ctx.unwrap_tensors() (with ctx being a PythonFunctionalizeAPI in this case) is not smart enough to handle tensor subclasses? Should it be handled there or more ad-hoc within flex_attention's impl?","It appears that ctx.unwrap_tensors() isn't fully capable of handling tensor subclasses in the context of the PythonFunctionalizeAPI. Should this be addressed in the ctx.unwrap_tensors() method, or should the flex_attention implementation include additional logic?",Irrelevant,3,3,pytorch_pytorch,154556,issue,,1,2,2,1,"It appears that ctx.unwrap_tensors() isn't fully capable of handling tensor subclasses in the context of the PythonFunctionalizeAPI. Should this be addressed in the ctx.unwrap_tensors() method, or should the flex_attention implementation include additional logic?"
32,pytorch_pytorch,issue,154556,"On the surface, it seems like ctx.unwrap_tensors() (with ctx being a PythonFunctionalizeAPI in this case) is not smart enough to handle tensor subclasses? Should it be handled there or more ad-hoc within flex_attention's impl?","I'm seeing that ctx.unwrap_tensors() doesn't seem to handle tensor subclasses properly when used with the PythonFunctionalizeAPI. Should this be a general fix in ctx.unwrap_tensors(), or should the flex_attention implementation have a more specific solution?",Irrelevant,3,3,pytorch_pytorch,154556,issue,,1,2,2,1,"I'm seeing that ctx.unwrap_tensors() doesn't seem to handle tensor subclasses properly when used with the PythonFunctionalizeAPI. Should this be a general fix in ctx.unwrap_tensors(), or should the flex_attention implementation have a more specific solution?"
33,pytorch_pytorch,issue,154556,"On the surface, it seems like ctx.unwrap_tensors() (with ctx being a PythonFunctionalizeAPI in this case) is not smart enough to handle tensor subclasses? Should it be handled there or more ad-hoc within flex_attention's impl?","It seems that ctx.unwrap_tensors() isn't equipped to handle tensor subclasses when used with the PythonFunctionalizeAPI. Should this be a responsibility of the ctx.unwrap_tensors() method, or should the flex_attention implementation include additional handling?",Irrelevant,3,3,pytorch_pytorch,154556,issue,,1,2,2,1,"It seems that ctx.unwrap_tensors() isn't equipped to handle tensor subclasses when used with the PythonFunctionalizeAPI. Should this be a responsibility of the ctx.unwrap_tensors() method, or should the flex_attention implementation include additional handling?"
34,pytorch_pytorch,issue,154556,"On the surface, it seems like ctx.unwrap_tensors() (with ctx being a PythonFunctionalizeAPI in this case) is not smart enough to handle tensor subclasses? Should it be handled there or more ad-hoc within flex_attention's impl?","From what I can gather, ctx.unwrap_tensors() doesn't seem to handle tensor subclasses effectively when used with the PythonFunctionalizeAPI. Should this be a concern for the ctx.unwrap_tensors() method, or should the flex_attention implementation have a more tailored approach?",Irrelevant,3,3,pytorch_pytorch,154556,issue,,1,2,2,1,"From what I can gather, ctx.unwrap_tensors() doesn't seem to handle tensor subclasses effectively when used with the PythonFunctionalizeAPI. Should this be a concern for the ctx.unwrap_tensors() method, or should the flex_attention implementation have a more tailored approach?"
35,pytorch_pytorch,Issue,154093,"I want to export a model to onnx, the simple code like the following:
 from torch import nn
 import torch
 

 class Model(nn.Module):
  def forward(self, x, y):
  y = torch.repeat_interleave(y, x.shape[0], dim=0)
  return y
 

 device = 'cuda:1'
 model = Model()
 model.to(device)
 x = torch.zeros((3, 10)).to(device)
 y = torch.zeros((1, 10)).to(device)
 torch.onnx.export(model=model, f='model.onnx', args=(x, y))
 

 Running this code, and I got an exception:
 Expected all tensors to be on the same device, but found at least two devices, cuda:1 and cpu! (when checking argument for argument index in method wrapper_CUDA__index_select)
 

 After debugging, I found that x.shape[0] is a tensor not a int, and its device is 'cpu' not 'cuda:1'
 Is that a bug, and how can I fix it?","I'm trying to export a PyTorch model to ONNX using the following code, but I encounter an exception. The error message indicates that the tensors are on different devices, even though I've moved them to 'cuda:1'. Specifically, `x.shape[0]` is a tensor on the CPU, causing the issue. Is this a bug, and how can I resolve it?",Known,4,4,pytorch_pytorch,154093,Issue,,1,1,1,1,"I'm trying to export a PyTorch model to ONNX using the following code, but I encounter an exception. The error message indicates that the tensors are on different devices, even though I've moved them to 'cuda:1'. Specifically, `x.shape[0]` is a tensor on the CPU, causing the issue. Is this a bug, and how can I resolve it?"
36,pytorch_pytorch,Issue,154093,"I want to export a model to onnx, the simple code like the following:
 from torch import nn
 import torch
 

 class Model(nn.Module):
  def forward(self, x, y):
  y = torch.repeat_interleave(y, x.shape[0], dim=0)
  return y
 

 device = 'cuda:1'
 model = Model()
 model.to(device)
 x = torch.zeros((3, 10)).to(device)
 y = torch.zeros((1, 10)).to(device)
 torch.onnx.export(model=model, f='model.onnx', args=(x, y))
 

 Running this code, and I got an exception:
 Expected all tensors to be on the same device, but found at least two devices, cuda:1 and cpu! (when checking argument for argument index in method wrapper_CUDA__index_select)
 

 After debugging, I found that x.shape[0] is a tensor not a int, and its device is 'cpu' not 'cuda:1'
 Is that a bug, and how can I fix it?","I'm facing an issue when exporting a PyTorch model to ONNX. The code snippet provided works fine until the export step, where I get an error about tensors being on different devices. It seems that `x.shape[0]` is a CPU tensor, while the rest of the tensors are on 'cuda:1'. Is this a known bug, and what's the best way to fix it?",Known,4,4,pytorch_pytorch,154093,Issue,,1,1,1,1,"I'm facing an issue when exporting a PyTorch model to ONNX. The code snippet provided works fine until the export step, where I get an error about tensors being on different devices. It seems that `x.shape[0]` is a CPU tensor, while the rest of the tensors are on 'cuda:1'. Is this a known bug, and what's the best way to fix it?"
37,pytorch_pytorch,Issue,154093,"I want to export a model to onnx, the simple code like the following:
 from torch import nn
 import torch
 

 class Model(nn.Module):
  def forward(self, x, y):
  y = torch.repeat_interleave(y, x.shape[0], dim=0)
  return y
 

 device = 'cuda:1'
 model = Model()
 model.to(device)
 x = torch.zeros((3, 10)).to(device)
 y = torch.zeros((1, 10)).to(device)
 torch.onnx.export(model=model, f='model.onnx', args=(x, y))
 

 Running this code, and I got an exception:
 Expected all tensors to be on the same device, but found at least two devices, cuda:1 and cpu! (when checking argument for argument index in method wrapper_CUDA__index_select)
 

 After debugging, I found that x.shape[0] is a tensor not a int, and its device is 'cpu' not 'cuda:1'
 Is that a bug, and how can I fix it?","I'm attempting to export a PyTorch model to ONNX, but I'm running into a device mismatch error. The error message suggests that `x.shape[0]` is a CPU tensor, while the other tensors are on 'cuda:1'. This is causing the export to fail. Is this a bug in PyTorch, or am I missing something in my code?",Known,4,4,pytorch_pytorch,154093,Issue,,1,1,1,1,"I'm attempting to export a PyTorch model to ONNX, but I'm running into a device mismatch error. The error message suggests that `x.shape[0]` is a CPU tensor, while the other tensors are on 'cuda:1'. This is causing the export to fail. Is this a bug in PyTorch, or am I missing something in my code?"
38,pytorch_pytorch,Issue,154093,"I want to export a model to onnx, the simple code like the following:
 from torch import nn
 import torch
 

 class Model(nn.Module):
  def forward(self, x, y):
  y = torch.repeat_interleave(y, x.shape[0], dim=0)
  return y
 

 device = 'cuda:1'
 model = Model()
 model.to(device)
 x = torch.zeros((3, 10)).to(device)
 y = torch.zeros((1, 10)).to(device)
 torch.onnx.export(model=model, f='model.onnx', args=(x, y))
 

 Running this code, and I got an exception:
 Expected all tensors to be on the same device, but found at least two devices, cuda:1 and cpu! (when checking argument for argument index in method wrapper_CUDA__index_select)
 

 After debugging, I found that x.shape[0] is a tensor not a int, and its device is 'cpu' not 'cuda:1'
 Is that a bug, and how can I fix it?","I'm having trouble exporting a PyTorch model to ONNX. The error I'm encountering is related to tensors being on different devices. Specifically, `x.shape[0]` is a CPU tensor, while the rest are on 'cuda:1'. Is this a bug, and how can I ensure all tensors are on the same device during the export process?",Known,4,4,pytorch_pytorch,154093,Issue,,1,1,1,1,"I'm having trouble exporting a PyTorch model to ONNX. The error I'm encountering is related to tensors being on different devices. Specifically, `x.shape[0]` is a CPU tensor, while the rest are on 'cuda:1'. Is this a bug, and how can I ensure all tensors are on the same device during the export process?"
39,pytorch_pytorch,Issue,154093,"I want to export a model to onnx, the simple code like the following:
 from torch import nn
 import torch
 

 class Model(nn.Module):
  def forward(self, x, y):
  y = torch.repeat_interleave(y, x.shape[0], dim=0)
  return y
 

 device = 'cuda:1'
 model = Model()
 model.to(device)
 x = torch.zeros((3, 10)).to(device)
 y = torch.zeros((1, 10)).to(device)
 torch.onnx.export(model=model, f='model.onnx', args=(x, y))
 

 Running this code, and I got an exception:
 Expected all tensors to be on the same device, but found at least two devices, cuda:1 and cpu! (when checking argument for argument index in method wrapper_CUDA__index_select)
 

 After debugging, I found that x.shape[0] is a tensor not a int, and its device is 'cpu' not 'cuda:1'
 Is that a bug, and how can I fix it?","I'm trying to export a PyTorch model to ONNX, but I'm getting an error about tensors being on different devices. The issue seems to be with `x.shape[0]`, which is a CPU tensor, while the others are on 'cuda:1'. Is this a bug, and what steps can I take to fix it?",Known,4,4,pytorch_pytorch,154093,Issue,,1,1,1,1,"I'm trying to export a PyTorch model to ONNX, but I'm getting an error about tensors being on different devices. The issue seems to be with `x.shape[0]`, which is a CPU tensor, while the others are on 'cuda:1'. Is this a bug, and what steps can I take to fix it?"
40,pytorch_pytorch,Issue,154093,"I want to export a model to onnx, the simple code like the following:
 from torch import nn
 import torch
 

 class Model(nn.Module):
  def forward(self, x, y):
  y = torch.repeat_interleave(y, x.shape[0], dim=0)
  return y
 

 device = 'cuda:1'
 model = Model()
 model.to(device)
 x = torch.zeros((3, 10)).to(device)
 y = torch.zeros((1, 10)).to(device)
 torch.onnx.export(model=model, f='model.onnx', args=(x, y))
 

 Running this code, and I got an exception:
 Expected all tensors to be on the same device, but found at least two devices, cuda:1 and cpu! (when checking argument for argument index in method wrapper_CUDA__index_select)
 

 After debugging, I found that x.shape[0] is a tensor not a int, and its device is 'cpu' not 'cuda:1'
 Is that a bug, and how can I fix it?","I'm running into a device mismatch error when exporting a PyTorch model to ONNX. The error message indicates that `x.shape[0]` is a CPU tensor, while the rest of the tensors are on 'cuda:1'. This is causing the export to fail. Is this a bug, and how can I resolve it?",Known,4,4,pytorch_pytorch,154093,Issue,,1,1,1,1,"I'm running into a device mismatch error when exporting a PyTorch model to ONNX. The error message indicates that `x.shape[0]` is a CPU tensor, while the rest of the tensors are on 'cuda:1'. This is causing the export to fail. Is this a bug, and how can I resolve it?"
41,pytorch_pytorch,Issue,154093,"I want to export a model to onnx, the simple code like the following:
 from torch import nn
 import torch
 

 class Model(nn.Module):
  def forward(self, x, y):
  y = torch.repeat_interleave(y, x.shape[0], dim=0)
  return y
 

 device = 'cuda:1'
 model = Model()
 model.to(device)
 x = torch.zeros((3, 10)).to(device)
 y = torch.zeros((1, 10)).to(device)
 torch.onnx.export(model=model, f='model.onnx', args=(x, y))
 

 Running this code, and I got an exception:
 Expected all tensors to be on the same device, but found at least two devices, cuda:1 and cpu! (when checking argument for argument index in method wrapper_CUDA__index_select)
 

 After debugging, I found that x.shape[0] is a tensor not a int, and its device is 'cpu' not 'cuda:1'
 Is that a bug, and how can I fix it?","I'm facing a device mismatch issue when exporting a PyTorch model to ONNX. The error message suggests that `x.shape[0]` is a CPU tensor, while the other tensors are on 'cuda:1'. This is preventing the export from completing successfully. Is this a bug, and what can I do to fix it?",Known,4,4,pytorch_pytorch,154093,Issue,,1,1,1,1,"I'm facing a device mismatch issue when exporting a PyTorch model to ONNX. The error message suggests that `x.shape[0]` is a CPU tensor, while the other tensors are on 'cuda:1'. This is preventing the export from completing successfully. Is this a bug, and what can I do to fix it?"
42,pytorch_pytorch,issue,153992,"By default AOTI generated code doesn't perform input shape checking for efficiency reason. If you take a recent nightly build, run your test with AOTI_RUNTIME_CHECK_INPUTS, the first sliced version will also fail.","The AOTI-generated code skips input shape validation by default to optimize performance. However, if you use a recent nightly build and set the AOTI_RUNTIME_CHECK_INPUTS environment variable, the first sliced version of your test will also fail due to shape mismatches.",Knowledge,5,5,pytorch_pytorch,153992,issue,,0,0,0,0,"The AOTI-generated code skips input shape validation by default to optimize performance. However, if you use a recent nightly build and set the AOTI_RUNTIME_CHECK_INPUTS environment variable, the first sliced version of your test will also fail due to shape mismatches."
43,pytorch_pytorch,issue,153992,"By default AOTI generated code doesn't perform input shape checking for efficiency reason. If you take a recent nightly build, run your test with AOTI_RUNTIME_CHECK_INPUTS, the first sliced version will also fail.","In the AOTI-generated code, input shape checks are disabled for efficiency. To enable these checks, use a recent nightly build and set AOTI_RUNTIME_CHECK_INPUTS. This will cause the first sliced version of your test to fail if there are any shape inconsistencies.",Knowledge,5,5,pytorch_pytorch,153992,issue,,0,0,0,0,"In the AOTI-generated code, input shape checks are disabled for efficiency. To enable these checks, use a recent nightly build and set AOTI_RUNTIME_CHECK_INPUTS. This will cause the first sliced version of your test to fail if there are any shape inconsistencies."
44,pytorch_pytorch,issue,153992,"By default AOTI generated code doesn't perform input shape checking for efficiency reason. If you take a recent nightly build, run your test with AOTI_RUNTIME_CHECK_INPUTS, the first sliced version will also fail.","By default, AOTI-generated code does not validate input shapes to improve performance. If you want to enable these checks, use a recent nightly build and set the AOTI_RUNTIME_CHECK_INPUTS flag. This will make the first sliced version of your test fail if there are any shape issues.",Knowledge,5,5,pytorch_pytorch,153992,issue,,0,0,0,0,"By default, AOTI-generated code does not validate input shapes to improve performance. If you want to enable these checks, use a recent nightly build and set the AOTI_RUNTIME_CHECK_INPUTS flag. This will make the first sliced version of your test fail if there are any shape issues."
45,pytorch_pytorch,issue,153992,"By default AOTI generated code doesn't perform input shape checking for efficiency reason. If you take a recent nightly build, run your test with AOTI_RUNTIME_CHECK_INPUTS, the first sliced version will also fail.","The AOTI-generated code omits input shape checks to enhance efficiency. To include these checks, use a recent nightly build and set the AOTI_RUNTIME_CHECK_INPUTS environment variable. This will cause the first sliced version of your test to fail if the input shapes are incorrect.",Knowledge,5,5,pytorch_pytorch,153992,issue,,0,0,0,0,"The AOTI-generated code omits input shape checks to enhance efficiency. To include these checks, use a recent nightly build and set the AOTI_RUNTIME_CHECK_INPUTS environment variable. This will cause the first sliced version of your test to fail if the input shapes are incorrect."
46,pytorch_pytorch,issue,153992,"By default AOTI generated code doesn't perform input shape checking for efficiency reason. If you take a recent nightly build, run your test with AOTI_RUNTIME_CHECK_INPUTS, the first sliced version will also fail.","AOTI-generated code does not perform input shape validation by default to optimize performance. To enable these checks, use a recent nightly build and set the AOTI_RUNTIME_CHECK_INPUTS flag. This will result in the first sliced version of your test failing if there are any shape mismatches.",Knowledge,5,5,pytorch_pytorch,153992,issue,,0,0,0,0,"AOTI-generated code does not perform input shape validation by default to optimize performance. To enable these checks, use a recent nightly build and set the AOTI_RUNTIME_CHECK_INPUTS flag. This will result in the first sliced version of your test failing if there are any shape mismatches."
47,pytorch_pytorch,issue,153992,"By default AOTI generated code doesn't perform input shape checking for efficiency reason. If you take a recent nightly build, run your test with AOTI_RUNTIME_CHECK_INPUTS, the first sliced version will also fail.","To ensure input shape consistency in AOTI-generated code, you need to use a recent nightly build and set the AOTI_RUNTIME_CHECK_INPUTS environment variable. By default, these checks are disabled for performance reasons, but enabling them will cause the first sliced version of your test to fail if there are any shape issues.",Knowledge,5,5,pytorch_pytorch,153992,issue,,0,0,0,0,"To ensure input shape consistency in AOTI-generated code, you need to use a recent nightly build and set the AOTI_RUNTIME_CHECK_INPUTS environment variable. By default, these checks are disabled for performance reasons, but enabling them will cause the first sliced version of your test to fail if there are any shape issues."
48,pytorch_pytorch,issue,153992,"By default AOTI generated code doesn't perform input shape checking for efficiency reason. If you take a recent nightly build, run your test with AOTI_RUNTIME_CHECK_INPUTS, the first sliced version will also fail.","The AOTI-generated code skips input shape checks to improve efficiency. If you encounter issues, you can enable these checks by using a recent nightly build and setting the AOTI_RUNTIME_CHECK_INPUTS flag. This will make the first sliced version of your test fail if the input shapes are incorrect.",Knowledge,5,5,pytorch_pytorch,153992,issue,,0,0,0,0,"The AOTI-generated code skips input shape checks to improve efficiency. If you encounter issues, you can enable these checks by using a recent nightly build and setting the AOTI_RUNTIME_CHECK_INPUTS flag. This will make the first sliced version of your test fail if the input shapes are incorrect."
49,pytorch_pytorch,issue,153992,"By default AOTI generated code doesn't perform input shape checking for efficiency reason. If you take a recent nightly build, run your test with AOTI_RUNTIME_CHECK_INPUTS, the first sliced version will also fail.","By default, AOTI-generated code does not validate input shapes to optimize performance. To enable these checks, use a recent nightly build and set the AOTI_RUNTIME_CHECK_INPUTS environment variable. This will cause the first sliced version of your test to fail if the input shapes are not as expected.",Knowledge,5,5,pytorch_pytorch,153992,issue,,0,0,0,0,"By default, AOTI-generated code does not validate input shapes to optimize performance. To enable these checks, use a recent nightly build and set the AOTI_RUNTIME_CHECK_INPUTS environment variable. This will cause the first sliced version of your test to fail if the input shapes are not as expected."
50,pytorch_pytorch,issue,153992,"By default AOTI generated code doesn't perform input shape checking for efficiency reason. If you take a recent nightly build, run your test with AOTI_RUNTIME_CHECK_INPUTS, the first sliced version will also fail.","AOTI-generated code does not perform input shape validation by default to enhance performance. To enable these checks, use a recent nightly build and set the AOTI_RUNTIME_CHECK_INPUTS flag. This will result in the first sliced version of your test failing if there are any shape mismatches.",Knowledge,5,5,pytorch_pytorch,153992,issue,,0,0,0,0,"AOTI-generated code does not perform input shape validation by default to enhance performance. To enable these checks, use a recent nightly build and set the AOTI_RUNTIME_CHECK_INPUTS flag. This will result in the first sliced version of your test failing if there are any shape mismatches."
51,pytorch_pytorch,issue,153992,"By default AOTI generated code doesn't perform input shape checking for efficiency reason. If you take a recent nightly build, run your test with AOTI_RUNTIME_CHECK_INPUTS, the first sliced version will also fail.","The AOTI-generated code omits input shape checks to improve efficiency. To include these checks, use a recent nightly build and set the AOTI_RUNTIME_CHECK_INPUTS environment variable. This will cause the first sliced version of your test to fail if the input shapes are incorrect.",Knowledge,5,5,pytorch_pytorch,153992,issue,,0,0,0,0,"The AOTI-generated code omits input shape checks to improve efficiency. To include these checks, use a recent nightly build and set the AOTI_RUNTIME_CHECK_INPUTS environment variable. This will cause the first sliced version of your test to fail if the input shapes are incorrect."
52,pytorch_pytorch,issue,153960,"Upgrading to PyTorch 2.7 makes the following script randomly hang in distributed mode:
 import argparse
 import os
 import torch
 

 

 def main(bad = False):
  local_rank = int(os.environ[""LOCAL_RANK""])
  device = torch.device(""cuda"", local_rank)
  torch.cuda.set_device(device)
  if bad:
  torch.distributed.init_process_group(""nccl"", device_id=device)
  else:
  torch.distributed.init_process_group(""nccl"")
 

  device = torch.device(""cuda"")
  rank = torch.distributed.get_rank()
 

  for i in range(10000):
  if rank == 0:
  if i % 10 == 0:
  print(i)
  a = torch.ones(3, device=device)
  else:
  a = torch.zeros(3, device=device)
 

  torch.distributed.broadcast(a, src=0)
 

  torch.distributed.destroy_process_group()
 

 

 if __name__ == ""__main__"":
  parser = argparse.ArgumentParser()
  parser.add_argument(""--bad"", action=""store_true"", help=""Shows the bug"")
  args = parser.parse_args()
  main(args.bad)
 

 More precisely torchrun --nproc_per_node 8 -- script.py --bad will randomly start hanging (you might need to launch it a couple of times) whereas torchrun --nproc_per_node 8 -- script.py is fine.
 torchrun --nproc_per_node 8 -- script.py --bad completes normally in 2.6.0, so this looks like a regression in PyTorch 2.7. We tried different kinds of GPUs and it looks like the bug appears on all of them.","After upgrading to PyTorch 2.7, I've noticed that a distributed training script occasionally hangs. The script initializes a process group and performs a broadcast operation in a loop. When run with the `--bad` flag, `torchrun --nproc_per_node 8 -- script.py --bad` hangs randomly, while `torchrun --nproc_per_node 8 -- script.py` works fine. This issue didn't occur in PyTorch 2.6.0, suggesting a regression in the latest version. The problem persists across different GPU models.",Known,6,6,pytorch_pytorch,153960,issue,,1,1,1,1,"After upgrading to PyTorch 2.7, I've noticed that a distributed training script occasionally hangs. The script initializes a process group and performs a broadcast operation in a loop. When run with the `--bad` flag, `torchrun --nproc_per_node 8 -- script.py --bad` hangs randomly, while `torchrun --nproc_per_node 8 -- script.py` works fine. This issue didn't occur in PyTorch 2.6.0, suggesting a regression in the latest version. The problem persists across different GPU models."
53,pytorch_pytorch,issue,153960,"Upgrading to PyTorch 2.7 makes the following script randomly hang in distributed mode:
 import argparse
 import os
 import torch
 

 

 def main(bad = False):
  local_rank = int(os.environ[""LOCAL_RANK""])
  device = torch.device(""cuda"", local_rank)
  torch.cuda.set_device(device)
  if bad:
  torch.distributed.init_process_group(""nccl"", device_id=device)
  else:
  torch.distributed.init_process_group(""nccl"")
 

  device = torch.device(""cuda"")
  rank = torch.distributed.get_rank()
 

  for i in range(10000):
  if rank == 0:
  if i % 10 == 0:
  print(i)
  a = torch.ones(3, device=device)
  else:
  a = torch.zeros(3, device=device)
 

  torch.distributed.broadcast(a, src=0)
 

  torch.distributed.destroy_process_group()
 

 

 if __name__ == ""__main__"":
  parser = argparse.ArgumentParser()
  parser.add_argument(""--bad"", action=""store_true"", help=""Shows the bug"")
  args = parser.parse_args()
  main(args.bad)
 

 More precisely torchrun --nproc_per_node 8 -- script.py --bad will randomly start hanging (you might need to launch it a couple of times) whereas torchrun --nproc_per_node 8 -- script.py is fine.
 torchrun --nproc_per_node 8 -- script.py --bad completes normally in 2.6.0, so this looks like a regression in PyTorch 2.7. We tried different kinds of GPUs and it looks like the bug appears on all of them.","I've encountered a hanging issue in a distributed training script after upgrading to PyTorch 2.7. The script initializes a process group and performs a broadcast operation in a loop. Running `torchrun --nproc_per_node 8 -- script.py --bad` causes the script to hang randomly, whereas `torchrun --nproc_per_node 8 -- script.py` runs without issues. This behavior was not present in PyTorch 2.6.0, indicating a potential regression. The issue is consistent across various GPU types.",Known,6,6,pytorch_pytorch,153960,issue,,1,1,1,1,"I've encountered a hanging issue in a distributed training script after upgrading to PyTorch 2.7. The script initializes a process group and performs a broadcast operation in a loop. Running `torchrun --nproc_per_node 8 -- script.py --bad` causes the script to hang randomly, whereas `torchrun --nproc_per_node 8 -- script.py` runs without issues. This behavior was not present in PyTorch 2.6.0, indicating a potential regression. The issue is consistent across various GPU types."
54,pytorch_pytorch,issue,153960,"Upgrading to PyTorch 2.7 makes the following script randomly hang in distributed mode:
 import argparse
 import os
 import torch
 

 

 def main(bad = False):
  local_rank = int(os.environ[""LOCAL_RANK""])
  device = torch.device(""cuda"", local_rank)
  torch.cuda.set_device(device)
  if bad:
  torch.distributed.init_process_group(""nccl"", device_id=device)
  else:
  torch.distributed.init_process_group(""nccl"")
 

  device = torch.device(""cuda"")
  rank = torch.distributed.get_rank()
 

  for i in range(10000):
  if rank == 0:
  if i % 10 == 0:
  print(i)
  a = torch.ones(3, device=device)
  else:
  a = torch.zeros(3, device=device)
 

  torch.distributed.broadcast(a, src=0)
 

  torch.distributed.destroy_process_group()
 

 

 if __name__ == ""__main__"":
  parser = argparse.ArgumentParser()
  parser.add_argument(""--bad"", action=""store_true"", help=""Shows the bug"")
  args = parser.parse_args()
  main(args.bad)
 

 More precisely torchrun --nproc_per_node 8 -- script.py --bad will randomly start hanging (you might need to launch it a couple of times) whereas torchrun --nproc_per_node 8 -- script.py is fine.
 torchrun --nproc_per_node 8 -- script.py --bad completes normally in 2.6.0, so this looks like a regression in PyTorch 2.7. We tried different kinds of GPUs and it looks like the bug appears on all of them.","Upgrading to PyTorch 2.7 has introduced a hanging issue in a distributed training script. The script initializes a process group and performs a broadcast operation in a loop. When the `--bad` flag is used, `torchrun --nproc_per_node 8 -- script.py --bad` hangs randomly, while `torchrun --nproc_per_node 8 -- script.py` runs smoothly. This issue was not observed in PyTorch 2.6.0, suggesting a regression. The problem is reproducible on different GPU models.",Known,6,6,pytorch_pytorch,153960,issue,,1,1,1,1,"Upgrading to PyTorch 2.7 has introduced a hanging issue in a distributed training script. The script initializes a process group and performs a broadcast operation in a loop. When the `--bad` flag is used, `torchrun --nproc_per_node 8 -- script.py --bad` hangs randomly, while `torchrun --nproc_per_node 8 -- script.py` runs smoothly. This issue was not observed in PyTorch 2.6.0, suggesting a regression. The problem is reproducible on different GPU models."
55,pytorch_pytorch,issue,153960,"Upgrading to PyTorch 2.7 makes the following script randomly hang in distributed mode:
 import argparse
 import os
 import torch
 

 

 def main(bad = False):
  local_rank = int(os.environ[""LOCAL_RANK""])
  device = torch.device(""cuda"", local_rank)
  torch.cuda.set_device(device)
  if bad:
  torch.distributed.init_process_group(""nccl"", device_id=device)
  else:
  torch.distributed.init_process_group(""nccl"")
 

  device = torch.device(""cuda"")
  rank = torch.distributed.get_rank()
 

  for i in range(10000):
  if rank == 0:
  if i % 10 == 0:
  print(i)
  a = torch.ones(3, device=device)
  else:
  a = torch.zeros(3, device=device)
 

  torch.distributed.broadcast(a, src=0)
 

  torch.distributed.destroy_process_group()
 

 

 if __name__ == ""__main__"":
  parser = argparse.ArgumentParser()
  parser.add_argument(""--bad"", action=""store_true"", help=""Shows the bug"")
  args = parser.parse_args()
  main(args.bad)
 

 More precisely torchrun --nproc_per_node 8 -- script.py --bad will randomly start hanging (you might need to launch it a couple of times) whereas torchrun --nproc_per_node 8 -- script.py is fine.
 torchrun --nproc_per_node 8 -- script.py --bad completes normally in 2.6.0, so this looks like a regression in PyTorch 2.7. We tried different kinds of GPUs and it looks like the bug appears on all of them.","I'm experiencing random hangs in a distributed training script after upgrading to PyTorch 2.7. The script initializes a process group and performs a broadcast operation in a loop. Running `torchrun --nproc_per_node 8 -- script.py --bad` causes the script to hang, while `torchrun --nproc_per_node 8 -- script.py` works as expected. This issue was not present in PyTorch 2.6.0, indicating a regression. The problem is consistent across different GPU models.",Known,6,6,pytorch_pytorch,153960,issue,,1,1,1,1,"I'm experiencing random hangs in a distributed training script after upgrading to PyTorch 2.7. The script initializes a process group and performs a broadcast operation in a loop. Running `torchrun --nproc_per_node 8 -- script.py --bad` causes the script to hang, while `torchrun --nproc_per_node 8 -- script.py` works as expected. This issue was not present in PyTorch 2.6.0, indicating a regression. The problem is consistent across different GPU models."
56,pytorch_pytorch,issue,153960,"Upgrading to PyTorch 2.7 makes the following script randomly hang in distributed mode:
 import argparse
 import os
 import torch
 

 

 def main(bad = False):
  local_rank = int(os.environ[""LOCAL_RANK""])
  device = torch.device(""cuda"", local_rank)
  torch.cuda.set_device(device)
  if bad:
  torch.distributed.init_process_group(""nccl"", device_id=device)
  else:
  torch.distributed.init_process_group(""nccl"")
 

  device = torch.device(""cuda"")
  rank = torch.distributed.get_rank()
 

  for i in range(10000):
  if rank == 0:
  if i % 10 == 0:
  print(i)
  a = torch.ones(3, device=device)
  else:
  a = torch.zeros(3, device=device)
 

  torch.distributed.broadcast(a, src=0)
 

  torch.distributed.destroy_process_group()
 

 

 if __name__ == ""__main__"":
  parser = argparse.ArgumentParser()
  parser.add_argument(""--bad"", action=""store_true"", help=""Shows the bug"")
  args = parser.parse_args()
  main(args.bad)
 

 More precisely torchrun --nproc_per_node 8 -- script.py --bad will randomly start hanging (you might need to launch it a couple of times) whereas torchrun --nproc_per_node 8 -- script.py is fine.
 torchrun --nproc_per_node 8 -- script.py --bad completes normally in 2.6.0, so this looks like a regression in PyTorch 2.7. We tried different kinds of GPUs and it looks like the bug appears on all of them.","A distributed training script that worked fine in PyTorch 2.6.0 now hangs randomly after upgrading to PyTorch 2.7. The script initializes a process group and performs a broadcast operation in a loop. When the `--bad` flag is used, `torchrun --nproc_per_node 8 -- script.py --bad` hangs, while `torchrun --nproc_per_node 8 -- script.py` runs without issues. This suggests a regression in PyTorch 2.7. The problem is reproducible on various GPU models.",Known,6,6,pytorch_pytorch,153960,issue,,1,1,1,1,"A distributed training script that worked fine in PyTorch 2.6.0 now hangs randomly after upgrading to PyTorch 2.7. The script initializes a process group and performs a broadcast operation in a loop. When the `--bad` flag is used, `torchrun --nproc_per_node 8 -- script.py --bad` hangs, while `torchrun --nproc_per_node 8 -- script.py` runs without issues. This suggests a regression in PyTorch 2.7. The problem is reproducible on various GPU models."
57,pytorch_pytorch,issue,153960,"Upgrading to PyTorch 2.7 makes the following script randomly hang in distributed mode:
 import argparse
 import os
 import torch
 

 

 def main(bad = False):
  local_rank = int(os.environ[""LOCAL_RANK""])
  device = torch.device(""cuda"", local_rank)
  torch.cuda.set_device(device)
  if bad:
  torch.distributed.init_process_group(""nccl"", device_id=device)
  else:
  torch.distributed.init_process_group(""nccl"")
 

  device = torch.device(""cuda"")
  rank = torch.distributed.get_rank()
 

  for i in range(10000):
  if rank == 0:
  if i % 10 == 0:
  print(i)
  a = torch.ones(3, device=device)
  else:
  a = torch.zeros(3, device=device)
 

  torch.distributed.broadcast(a, src=0)
 

  torch.distributed.destroy_process_group()
 

 

 if __name__ == ""__main__"":
  parser = argparse.ArgumentParser()
  parser.add_argument(""--bad"", action=""store_true"", help=""Shows the bug"")
  args = parser.parse_args()
  main(args.bad)
 

 More precisely torchrun --nproc_per_node 8 -- script.py --bad will randomly start hanging (you might need to launch it a couple of times) whereas torchrun --nproc_per_node 8 -- script.py is fine.
 torchrun --nproc_per_node 8 -- script.py --bad completes normally in 2.6.0, so this looks like a regression in PyTorch 2.7. We tried different kinds of GPUs and it looks like the bug appears on all of them.","I've noticed a hanging issue in a distributed training script after upgrading to PyTorch 2.7. The script initializes a process group and performs a broadcast operation in a loop. Running `torchrun --nproc_per_node 8 -- script.py --bad` causes the script to hang randomly, while `torchrun --nproc_per_node 8 -- script.py` runs smoothly. This issue was not present in PyTorch 2.6.0, indicating a regression. The problem is consistent across different GPU models.",Known,6,6,pytorch_pytorch,153960,issue,,1,1,1,1,"I've noticed a hanging issue in a distributed training script after upgrading to PyTorch 2.7. The script initializes a process group and performs a broadcast operation in a loop. Running `torchrun --nproc_per_node 8 -- script.py --bad` causes the script to hang randomly, while `torchrun --nproc_per_node 8 -- script.py` runs smoothly. This issue was not present in PyTorch 2.6.0, indicating a regression. The problem is consistent across different GPU models."
58,pytorch_pytorch,issue,153960,"Upgrading to PyTorch 2.7 makes the following script randomly hang in distributed mode:
 import argparse
 import os
 import torch
 

 

 def main(bad = False):
  local_rank = int(os.environ[""LOCAL_RANK""])
  device = torch.device(""cuda"", local_rank)
  torch.cuda.set_device(device)
  if bad:
  torch.distributed.init_process_group(""nccl"", device_id=device)
  else:
  torch.distributed.init_process_group(""nccl"")
 

  device = torch.device(""cuda"")
  rank = torch.distributed.get_rank()
 

  for i in range(10000):
  if rank == 0:
  if i % 10 == 0:
  print(i)
  a = torch.ones(3, device=device)
  else:
  a = torch.zeros(3, device=device)
 

  torch.distributed.broadcast(a, src=0)
 

  torch.distributed.destroy_process_group()
 

 

 if __name__ == ""__main__"":
  parser = argparse.ArgumentParser()
  parser.add_argument(""--bad"", action=""store_true"", help=""Shows the bug"")
  args = parser.parse_args()
  main(args.bad)
 

 More precisely torchrun --nproc_per_node 8 -- script.py --bad will randomly start hanging (you might need to launch it a couple of times) whereas torchrun --nproc_per_node 8 -- script.py is fine.
 torchrun --nproc_per_node 8 -- script.py --bad completes normally in 2.6.0, so this looks like a regression in PyTorch 2.7. We tried different kinds of GPUs and it looks like the bug appears on all of them.","After upgrading to PyTorch 2.7, a distributed training script that initializes a process group and performs a broadcast operation in a loop now hangs randomly. Running `torchrun --nproc_per_node 8 -- script.py --bad` causes the script to hang, while `torchrun --nproc_per_node 8 -- script.py` works as expected. This issue was not present in PyTorch 2.6.0, suggesting a regression. The problem is reproducible on various GPU models.",Known,6,6,pytorch_pytorch,153960,issue,,1,1,1,1,"After upgrading to PyTorch 2.7, a distributed training script that initializes a process group and performs a broadcast operation in a loop now hangs randomly. Running `torchrun --nproc_per_node 8 -- script.py --bad` causes the script to hang, while `torchrun --nproc_per_node 8 -- script.py` works as expected. This issue was not present in PyTorch 2.6.0, suggesting a regression. The problem is reproducible on various GPU models."
59,pytorch_pytorch,issue,153960,"Upgrading to PyTorch 2.7 makes the following script randomly hang in distributed mode:
 import argparse
 import os
 import torch
 

 

 def main(bad = False):
  local_rank = int(os.environ[""LOCAL_RANK""])
  device = torch.device(""cuda"", local_rank)
  torch.cuda.set_device(device)
  if bad:
  torch.distributed.init_process_group(""nccl"", device_id=device)
  else:
  torch.distributed.init_process_group(""nccl"")
 

  device = torch.device(""cuda"")
  rank = torch.distributed.get_rank()
 

  for i in range(10000):
  if rank == 0:
  if i % 10 == 0:
  print(i)
  a = torch.ones(3, device=device)
  else:
  a = torch.zeros(3, device=device)
 

  torch.distributed.broadcast(a, src=0)
 

  torch.distributed.destroy_process_group()
 

 

 if __name__ == ""__main__"":
  parser = argparse.ArgumentParser()
  parser.add_argument(""--bad"", action=""store_true"", help=""Shows the bug"")
  args = parser.parse_args()
  main(args.bad)
 

 More precisely torchrun --nproc_per_node 8 -- script.py --bad will randomly start hanging (you might need to launch it a couple of times) whereas torchrun --nproc_per_node 8 -- script.py is fine.
 torchrun --nproc_per_node 8 -- script.py --bad completes normally in 2.6.0, so this looks like a regression in PyTorch 2.7. We tried different kinds of GPUs and it looks like the bug appears on all of them.","I've encountered a hanging issue in a distributed training script after upgrading to PyTorch 2.7. The script initializes a process group and performs a broadcast operation in a loop. When the `--bad` flag is used, `torchrun --nproc_per_node 8 -- script.py --bad` hangs randomly, while `torchrun --nproc_per_node 8 -- script.py` runs without issues. This behavior was not observed in PyTorch 2.6.0, indicating a regression. The problem is consistent across different GPU models.",Known,6,6,pytorch_pytorch,153960,issue,,1,1,1,1,"I've encountered a hanging issue in a distributed training script after upgrading to PyTorch 2.7. The script initializes a process group and performs a broadcast operation in a loop. When the `--bad` flag is used, `torchrun --nproc_per_node 8 -- script.py --bad` hangs randomly, while `torchrun --nproc_per_node 8 -- script.py` runs without issues. This behavior was not observed in PyTorch 2.6.0, indicating a regression. The problem is consistent across different GPU models."
60,pytorch_pytorch,issue,153960,"Upgrading to PyTorch 2.7 makes the following script randomly hang in distributed mode:
 import argparse
 import os
 import torch
 

 

 def main(bad = False):
  local_rank = int(os.environ[""LOCAL_RANK""])
  device = torch.device(""cuda"", local_rank)
  torch.cuda.set_device(device)
  if bad:
  torch.distributed.init_process_group(""nccl"", device_id=device)
  else:
  torch.distributed.init_process_group(""nccl"")
 

  device = torch.device(""cuda"")
  rank = torch.distributed.get_rank()
 

  for i in range(10000):
  if rank == 0:
  if i % 10 == 0:
  print(i)
  a = torch.ones(3, device=device)
  else:
  a = torch.zeros(3, device=device)
 

  torch.distributed.broadcast(a, src=0)
 

  torch.distributed.destroy_process_group()
 

 

 if __name__ == ""__main__"":
  parser = argparse.ArgumentParser()
  parser.add_argument(""--bad"", action=""store_true"", help=""Shows the bug"")
  args = parser.parse_args()
  main(args.bad)
 

 More precisely torchrun --nproc_per_node 8 -- script.py --bad will randomly start hanging (you might need to launch it a couple of times) whereas torchrun --nproc_per_node 8 -- script.py is fine.
 torchrun --nproc_per_node 8 -- script.py --bad completes normally in 2.6.0, so this looks like a regression in PyTorch 2.7. We tried different kinds of GPUs and it looks like the bug appears on all of them.","A distributed training script that initializes a process group and performs a broadcast operation in a loop now hangs randomly after upgrading to PyTorch 2.7. Running `torchrun --nproc_per_node 8 -- script.py --bad` causes the script to hang, while `torchrun --nproc_per_node 8 -- script.py` runs smoothly. This issue was not present in PyTorch 2.6.0, suggesting a regression. The problem is reproducible on various GPU models.",Known,6,6,pytorch_pytorch,153960,issue,,1,1,1,1,"A distributed training script that initializes a process group and performs a broadcast operation in a loop now hangs randomly after upgrading to PyTorch 2.7. Running `torchrun --nproc_per_node 8 -- script.py --bad` causes the script to hang, while `torchrun --nproc_per_node 8 -- script.py` runs smoothly. This issue was not present in PyTorch 2.6.0, suggesting a regression. The problem is reproducible on various GPU models."
61,pytorch_pytorch,issue,153960,"Upgrading to PyTorch 2.7 makes the following script randomly hang in distributed mode:
 import argparse
 import os
 import torch
 

 

 def main(bad = False):
  local_rank = int(os.environ[""LOCAL_RANK""])
  device = torch.device(""cuda"", local_rank)
  torch.cuda.set_device(device)
  if bad:
  torch.distributed.init_process_group(""nccl"", device_id=device)
  else:
  torch.distributed.init_process_group(""nccl"")
 

  device = torch.device(""cuda"")
  rank = torch.distributed.get_rank()
 

  for i in range(10000):
  if rank == 0:
  if i % 10 == 0:
  print(i)
  a = torch.ones(3, device=device)
  else:
  a = torch.zeros(3, device=device)
 

  torch.distributed.broadcast(a, src=0)
 

  torch.distributed.destroy_process_group()
 

 

 if __name__ == ""__main__"":
  parser = argparse.ArgumentParser()
  parser.add_argument(""--bad"", action=""store_true"", help=""Shows the bug"")
  args = parser.parse_args()
  main(args.bad)
 

 More precisely torchrun --nproc_per_node 8 -- script.py --bad will randomly start hanging (you might need to launch it a couple of times) whereas torchrun --nproc_per_node 8 -- script.py is fine.
 torchrun --nproc_per_node 8 -- script.py --bad completes normally in 2.6.0, so this looks like a regression in PyTorch 2.7. We tried different kinds of GPUs and it looks like the bug appears on all of them.","I'm experiencing random hangs in a distributed training script after upgrading to PyTorch 2.7. The script initializes a process group and performs a broadcast operation in a loop. When the `--bad` flag is used, `torchrun --nproc_per_node 8 -- script.py --bad` hangs, while `torchrun --nproc_per_node 8 -- script.py` runs without issues. This issue was not present in PyTorch 2.6.0, indicating a regression. The problem is consistent across different GPU models.",Known,6,6,pytorch_pytorch,153960,issue,,1,1,1,1,"I'm experiencing random hangs in a distributed training script after upgrading to PyTorch 2.7. The script initializes a process group and performs a broadcast operation in a loop. When the `--bad` flag is used, `torchrun --nproc_per_node 8 -- script.py --bad` hangs, while `torchrun --nproc_per_node 8 -- script.py` runs without issues. This issue was not present in PyTorch 2.6.0, indicating a regression. The problem is consistent across different GPU models."
62,pytorch_pytorch,Issue,153960,"What does the device_id do behind the scenes? What does it change with respect to NCCL?
 Can we get a NCCL debug log (NCCL_DEBUG=INFO)? Or does the race go away then?",I'm curious about the inner workings of the device_id. How does it affect NCCL operations? Is it possible to enable NCCL debug logs by setting NCCL_DEBUG=INFO to get more insights? Does this help in resolving any race conditions?,Known,7,7,pytorch_pytorch,153960,Issue,,1,1,1,1,I'm curious about the inner workings of the device_id. How does it affect NCCL operations? Is it possible to enable NCCL debug logs by setting NCCL_DEBUG=INFO to get more insights? Does this help in resolving any race conditions?
63,pytorch_pytorch,Issue,153960,"What does the device_id do behind the scenes? What does it change with respect to NCCL?
 Can we get a NCCL debug log (NCCL_DEBUG=INFO)? Or does the race go away then?",Can someone explain what the device_id does under the hood and how it impacts NCCL? Is there a way to enable detailed NCCL logs to see what's happening during execution? Does setting NCCL_DEBUG=INFO help in identifying and resolving race conditions?,Known,7,7,pytorch_pytorch,153960,Issue,,1,1,1,1,Can someone explain what the device_id does under the hood and how it impacts NCCL? Is there a way to enable detailed NCCL logs to see what's happening during execution? Does setting NCCL_DEBUG=INFO help in identifying and resolving race conditions?
64,pytorch_pytorch,Issue,153960,"What does the device_id do behind the scenes? What does it change with respect to NCCL?
 Can we get a NCCL debug log (NCCL_DEBUG=INFO)? Or does the race go away then?",I'm trying to understand the role of device_id in the context of NCCL. What changes does it introduce? Can we enable NCCL debug logs to get more detailed information? Does this setting help in diagnosing and fixing race conditions?,Known,7,7,pytorch_pytorch,153960,Issue,,1,1,1,1,I'm trying to understand the role of device_id in the context of NCCL. What changes does it introduce? Can we enable NCCL debug logs to get more detailed information? Does this setting help in diagnosing and fixing race conditions?
65,pytorch_pytorch,Issue,153960,"What does the device_id do behind the scenes? What does it change with respect to NCCL?
 Can we get a NCCL debug log (NCCL_DEBUG=INFO)? Or does the race go away then?",I'm not sure what the device_id does behind the scenes and how it affects NCCL. Is there a way to get more detailed logs by setting NCCL_DEBUG=INFO? Does this help in identifying and resolving any race conditions that might be present?,Known,7,7,pytorch_pytorch,153960,Issue,,1,1,1,1,I'm not sure what the device_id does behind the scenes and how it affects NCCL. Is there a way to get more detailed logs by setting NCCL_DEBUG=INFO? Does this help in identifying and resolving any race conditions that might be present?
66,pytorch_pytorch,Issue,153960,"What does the device_id do behind the scenes? What does it change with respect to NCCL?
 Can we get a NCCL debug log (NCCL_DEBUG=INFO)? Or does the race go away then?",I'm having trouble understanding the impact of device_id on NCCL. What exactly does it change? Can we enable NCCL debug logs to get more information? Does setting NCCL_DEBUG=INFO help in diagnosing and resolving race conditions?,Known,7,7,pytorch_pytorch,153960,Issue,,1,1,1,1,I'm having trouble understanding the impact of device_id on NCCL. What exactly does it change? Can we enable NCCL debug logs to get more information? Does setting NCCL_DEBUG=INFO help in diagnosing and resolving race conditions?
67,pytorch_pytorch,Issue,153960,"What does the device_id do behind the scenes? What does it change with respect to NCCL?
 Can we get a NCCL debug log (NCCL_DEBUG=INFO)? Or does the race go away then?",I'm confused about the function of device_id and its effect on NCCL. Can we enable NCCL debug logs to get more details? Does setting NCCL_DEBUG=INFO help in identifying and resolving any race conditions that might be occurring?,Known,7,7,pytorch_pytorch,153960,Issue,,1,1,1,1,I'm confused about the function of device_id and its effect on NCCL. Can we enable NCCL debug logs to get more details? Does setting NCCL_DEBUG=INFO help in identifying and resolving any race conditions that might be occurring?
68,pytorch_pytorch,Issue,153960,"What does the device_id do behind the scenes? What does it change with respect to NCCL?
 Can we get a NCCL debug log (NCCL_DEBUG=INFO)? Or does the race go away then?",I'm trying to figure out what the device_id does and how it influences NCCL. Is there a way to get more detailed logs by setting NCCL_DEBUG=INFO? Does this help in diagnosing and fixing any race conditions?,Known,7,7,pytorch_pytorch,153960,Issue,,1,1,1,1,I'm trying to figure out what the device_id does and how it influences NCCL. Is there a way to get more detailed logs by setting NCCL_DEBUG=INFO? Does this help in diagnosing and fixing any race conditions?
69,pytorch_pytorch,Issue,153960,"What does the device_id do behind the scenes? What does it change with respect to NCCL?
 Can we get a NCCL debug log (NCCL_DEBUG=INFO)? Or does the race go away then?",I'm unsure about the role of device_id in NCCL. What changes does it make? Can we enable NCCL debug logs to get more insights? Does setting NCCL_DEBUG=INFO help in identifying and resolving race conditions?,Known,7,7,pytorch_pytorch,153960,Issue,,1,1,1,1,I'm unsure about the role of device_id in NCCL. What changes does it make? Can we enable NCCL debug logs to get more insights? Does setting NCCL_DEBUG=INFO help in identifying and resolving race conditions?
70,pytorch_pytorch,Issue,153960,"What does the device_id do behind the scenes? What does it change with respect to NCCL?
 Can we get a NCCL debug log (NCCL_DEBUG=INFO)? Or does the race go away then?",I'm curious about the device_id and its impact on NCCL. What does it change? Can we enable NCCL debug logs to get more detailed information? Does setting NCCL_DEBUG=INFO help in diagnosing and fixing any race conditions?,Known,7,7,pytorch_pytorch,153960,Issue,,1,1,1,1,I'm curious about the device_id and its impact on NCCL. What does it change? Can we enable NCCL debug logs to get more detailed information? Does setting NCCL_DEBUG=INFO help in diagnosing and fixing any race conditions?
71,pytorch_pytorch,Issue,153960,"What does the device_id do behind the scenes? What does it change with respect to NCCL?
 Can we get a NCCL debug log (NCCL_DEBUG=INFO)? Or does the race go away then?",I'm trying to understand the device_id and its effect on NCCL. What does it do behind the scenes? Can we enable NCCL debug logs to get more details? Does setting NCCL_DEBUG=INFO help in identifying and resolving race conditions?,Known,7,7,pytorch_pytorch,153960,Issue,,1,1,1,1,I'm trying to understand the device_id and its effect on NCCL. What does it do behind the scenes? Can we enable NCCL debug logs to get more details? Does setting NCCL_DEBUG=INFO help in identifying and resolving race conditions?
72,pytorch_pytorch,issue,153960,"It would initialize NCCL communicator at that moment, because we know the device id at that moment, instead of delaying the init till the first collective (the traditional pytorch way). To compensate for ""ahead-of-time bill"", it uses NCCL's non-blocking API mode.","The NCCL communicator is initialized immediately when the device ID is known, rather than waiting for the first collective operation as is typical in PyTorch. This approach uses NCCL's non-blocking API to manage the initialization cost upfront.",Knowledge,8,8,pytorch_pytorch,153960,issue,,0,0,0,0,"The NCCL communicator is initialized immediately when the device ID is known, rather than waiting for the first collective operation as is typical in PyTorch. This approach uses NCCL's non-blocking API to manage the initialization cost upfront."
73,pytorch_pytorch,issue,153960,"It would initialize NCCL communicator at that moment, because we know the device id at that moment, instead of delaying the init till the first collective (the traditional pytorch way). To compensate for ""ahead-of-time bill"", it uses NCCL's non-blocking API mode.","Instead of delaying the initialization of the NCCL communicator until the first collective operation, it is set up as soon as the device ID is available. This is done to avoid the overhead later by leveraging NCCL's non-blocking API.",Knowledge,8,8,pytorch_pytorch,153960,issue,,0,0,0,0,"Instead of delaying the initialization of the NCCL communicator until the first collective operation, it is set up as soon as the device ID is available. This is done to avoid the overhead later by leveraging NCCL's non-blocking API."
74,pytorch_pytorch,issue,153960,"It would initialize NCCL communicator at that moment, because we know the device id at that moment, instead of delaying the init till the first collective (the traditional pytorch way). To compensate for ""ahead-of-time bill"", it uses NCCL's non-blocking API mode.","The NCCL communicator is initialized right away when the device ID is determined, rather than waiting for the first collective call. This is achieved using NCCL's non-blocking API to handle the initialization cost efficiently.",Knowledge,8,8,pytorch_pytorch,153960,issue,,0,0,0,0,"The NCCL communicator is initialized right away when the device ID is determined, rather than waiting for the first collective call. This is achieved using NCCL's non-blocking API to handle the initialization cost efficiently."
75,pytorch_pytorch,issue,153960,"It would initialize NCCL communicator at that moment, because we know the device id at that moment, instead of delaying the init till the first collective (the traditional pytorch way). To compensate for ""ahead-of-time bill"", it uses NCCL's non-blocking API mode.","To optimize performance, the NCCL communicator is initialized immediately when the device ID is known, rather than waiting for the first collective operation. This is managed using NCCL's non-blocking API to handle the initialization cost upfront.",Knowledge,8,8,pytorch_pytorch,153960,issue,,0,0,0,0,"To optimize performance, the NCCL communicator is initialized immediately when the device ID is known, rather than waiting for the first collective operation. This is managed using NCCL's non-blocking API to handle the initialization cost upfront."
76,pytorch_pytorch,issue,153960,"It would initialize NCCL communicator at that moment, because we know the device id at that moment, instead of delaying the init till the first collective (the traditional pytorch way). To compensate for ""ahead-of-time bill"", it uses NCCL's non-blocking API mode.","The NCCL communicator is set up as soon as the device ID is available, rather than waiting for the first collective operation. This is done to avoid the initialization overhead later by using NCCL's non-blocking API.",Knowledge,8,8,pytorch_pytorch,153960,issue,,0,0,0,0,"The NCCL communicator is set up as soon as the device ID is available, rather than waiting for the first collective operation. This is done to avoid the initialization overhead later by using NCCL's non-blocking API."
77,pytorch_pytorch,issue,153960,"It would initialize NCCL communicator at that moment, because we know the device id at that moment, instead of delaying the init till the first collective (the traditional pytorch way). To compensate for ""ahead-of-time bill"", it uses NCCL's non-blocking API mode.","The NCCL communicator is initialized at the moment the device ID is known, instead of waiting for the first collective operation. This is done to manage the initialization cost upfront using NCCL's non-blocking API.",Knowledge,8,8,pytorch_pytorch,153960,issue,,0,0,0,0,"The NCCL communicator is initialized at the moment the device ID is known, instead of waiting for the first collective operation. This is done to manage the initialization cost upfront using NCCL's non-blocking API."
78,pytorch_pytorch,issue,153960,"It would initialize NCCL communicator at that moment, because we know the device id at that moment, instead of delaying the init till the first collective (the traditional pytorch way). To compensate for ""ahead-of-time bill"", it uses NCCL's non-blocking API mode.","To ensure efficient initialization, the NCCL communicator is set up immediately when the device ID is known, rather than waiting for the first collective operation. This is achieved by using NCCL's non-blocking API to handle the initialization cost.",Knowledge,8,8,pytorch_pytorch,153960,issue,,0,0,0,0,"To ensure efficient initialization, the NCCL communicator is set up immediately when the device ID is known, rather than waiting for the first collective operation. This is achieved by using NCCL's non-blocking API to handle the initialization cost."
79,pytorch_pytorch,issue,153960,"It would initialize NCCL communicator at that moment, because we know the device id at that moment, instead of delaying the init till the first collective (the traditional pytorch way). To compensate for ""ahead-of-time bill"", it uses NCCL's non-blocking API mode.","The NCCL communicator is initialized right when the device ID is known, rather than delaying it until the first collective operation. This is done to manage the initialization cost upfront using NCCL's non-blocking API.",Knowledge,8,8,pytorch_pytorch,153960,issue,,0,0,0,0,"The NCCL communicator is initialized right when the device ID is known, rather than delaying it until the first collective operation. This is done to manage the initialization cost upfront using NCCL's non-blocking API."
80,pytorch_pytorch,issue,153960,"It would initialize NCCL communicator at that moment, because we know the device id at that moment, instead of delaying the init till the first collective (the traditional pytorch way). To compensate for ""ahead-of-time bill"", it uses NCCL's non-blocking API mode.","The NCCL communicator is initialized as soon as the device ID is available, rather than waiting for the first collective operation. This is done to handle the initialization cost upfront using NCCL's non-blocking API.",Knowledge,8,8,pytorch_pytorch,153960,issue,,0,0,0,0,"The NCCL communicator is initialized as soon as the device ID is available, rather than waiting for the first collective operation. This is done to handle the initialization cost upfront using NCCL's non-blocking API."
81,pytorch_pytorch,issue,153960,"It would initialize NCCL communicator at that moment, because we know the device id at that moment, instead of delaying the init till the first collective (the traditional pytorch way). To compensate for ""ahead-of-time bill"", it uses NCCL's non-blocking API mode.","To avoid the overhead of delayed initialization, the NCCL communicator is set up immediately when the device ID is known. This is managed using NCCL's non-blocking API to handle the initialization cost upfront.",Knowledge,8,8,pytorch_pytorch,153960,issue,,0,0,0,0,"To avoid the overhead of delayed initialization, the NCCL communicator is set up immediately when the device ID is known. This is managed using NCCL's non-blocking API to handle the initialization cost upfront."
82,pytorch_pytorch,issue,153960,"Let me give you some background on this issue from the NCCL team's perspective:
 Until recently, ncclCommGetAsyncError was, as @ptrblck said, a pure read-op. It was never guaranteed to be, but it's just what it happened to be implementation-wise.
 This changed in NCCL 2.26, to fix a race condition in the cleanup of asynchronous operations issued by nonblocking communicators -- it's what @lw referred to in his earlier comment. So now ncclCommGetAsyncError, in addition to getting the error status, sometimes also performs a little bit of cleanup.","To provide some context from the NCCL team's viewpoint: Until the recent update, ncclCommGetAsyncError was essentially a read-only operation, though this wasn't officially guaranteed. However, in NCCL 2.26, we made changes to address a race condition in the cleanup of asynchronous operations from nonblocking communicators. As a result, ncclCommGetAsyncError now not only retrieves the error status but also occasionally performs some cleanup tasks.",Knowledge,9,9,pytorch_pytorch,153960,issue,,0,0,0,0,"To provide some context from the NCCL team's viewpoint: Until the recent update, ncclCommGetAsyncError was essentially a read-only operation, though this wasn't officially guaranteed. However, in NCCL 2.26, we made changes to address a race condition in the cleanup of asynchronous operations from nonblocking communicators. As a result, ncclCommGetAsyncError now not only retrieves the error status but also occasionally performs some cleanup tasks."
83,pytorch_pytorch,issue,153960,"Let me give you some background on this issue from the NCCL team's perspective:
 Until recently, ncclCommGetAsyncError was, as @ptrblck said, a pure read-op. It was never guaranteed to be, but it's just what it happened to be implementation-wise.
 This changed in NCCL 2.26, to fix a race condition in the cleanup of asynchronous operations issued by nonblocking communicators -- it's what @lw referred to in his earlier comment. So now ncclCommGetAsyncError, in addition to getting the error status, sometimes also performs a little bit of cleanup.","From the NCCL team's perspective, ncclCommGetAsyncError was previously a read-only function, as @ptrblck mentioned. This was the implementation detail, even though it wasn't officially documented. In NCCL 2.26, we introduced changes to fix a race condition during the cleanup of asynchronous operations from nonblocking communicators, as @lw noted. Now, ncclCommGetAsyncError also handles some cleanup in addition to fetching the error status.",Knowledge,9,9,pytorch_pytorch,153960,issue,,0,0,0,0,"From the NCCL team's perspective, ncclCommGetAsyncError was previously a read-only function, as @ptrblck mentioned. This was the implementation detail, even though it wasn't officially documented. In NCCL 2.26, we introduced changes to fix a race condition during the cleanup of asynchronous operations from nonblocking communicators, as @lw noted. Now, ncclCommGetAsyncError also handles some cleanup in addition to fetching the error status."
84,pytorch_pytorch,issue,153960,"Let me give you some background on this issue from the NCCL team's perspective:
 Until recently, ncclCommGetAsyncError was, as @ptrblck said, a pure read-op. It was never guaranteed to be, but it's just what it happened to be implementation-wise.
 This changed in NCCL 2.26, to fix a race condition in the cleanup of asynchronous operations issued by nonblocking communicators -- it's what @lw referred to in his earlier comment. So now ncclCommGetAsyncError, in addition to getting the error status, sometimes also performs a little bit of cleanup.","To give you some background from the NCCL team: Until recently, ncclCommGetAsyncError was effectively a read operation, though this wasn't guaranteed. In NCCL 2.26, we made changes to resolve a race condition in the cleanup of asynchronous operations from nonblocking communicators. As a result, ncclCommGetAsyncError now performs some cleanup tasks in addition to retrieving the error status.",Knowledge,9,9,pytorch_pytorch,153960,issue,,0,0,0,0,"To give you some background from the NCCL team: Until recently, ncclCommGetAsyncError was effectively a read operation, though this wasn't guaranteed. In NCCL 2.26, we made changes to resolve a race condition in the cleanup of asynchronous operations from nonblocking communicators. As a result, ncclCommGetAsyncError now performs some cleanup tasks in addition to retrieving the error status."
85,pytorch_pytorch,issue,153960,"Let me give you some background on this issue from the NCCL team's perspective:
 Until recently, ncclCommGetAsyncError was, as @ptrblck said, a pure read-op. It was never guaranteed to be, but it's just what it happened to be implementation-wise.
 This changed in NCCL 2.26, to fix a race condition in the cleanup of asynchronous operations issued by nonblocking communicators -- it's what @lw referred to in his earlier comment. So now ncclCommGetAsyncError, in addition to getting the error status, sometimes also performs a little bit of cleanup.","From the NCCL team's standpoint, ncclCommGetAsyncError was previously a read-only function, as @ptrblck pointed out. However, in NCCL 2.26, we addressed a race condition in the cleanup of asynchronous operations from nonblocking communicators, as @lw mentioned. Now, ncclCommGetAsyncError not only retrieves the error status but also performs some cleanup operations.",Knowledge,9,9,pytorch_pytorch,153960,issue,,0,0,0,0,"From the NCCL team's standpoint, ncclCommGetAsyncError was previously a read-only function, as @ptrblck pointed out. However, in NCCL 2.26, we addressed a race condition in the cleanup of asynchronous operations from nonblocking communicators, as @lw mentioned. Now, ncclCommGetAsyncError not only retrieves the error status but also performs some cleanup operations."
86,pytorch_pytorch,issue,153960,"Let me give you some background on this issue from the NCCL team's perspective:
 Until recently, ncclCommGetAsyncError was, as @ptrblck said, a pure read-op. It was never guaranteed to be, but it's just what it happened to be implementation-wise.
 This changed in NCCL 2.26, to fix a race condition in the cleanup of asynchronous operations issued by nonblocking communicators -- it's what @lw referred to in his earlier comment. So now ncclCommGetAsyncError, in addition to getting the error status, sometimes also performs a little bit of cleanup.","To provide some context from the NCCL team: Until the recent update, ncclCommGetAsyncError was a read-only operation, though this wasn't officially guaranteed. In NCCL 2.26, we made changes to fix a race condition in the cleanup of asynchronous operations from nonblocking communicators. Now, ncclCommGetAsyncError also handles some cleanup tasks in addition to fetching the error status.",Knowledge,9,9,pytorch_pytorch,153960,issue,,0,0,0,0,"To provide some context from the NCCL team: Until the recent update, ncclCommGetAsyncError was a read-only operation, though this wasn't officially guaranteed. In NCCL 2.26, we made changes to fix a race condition in the cleanup of asynchronous operations from nonblocking communicators. Now, ncclCommGetAsyncError also handles some cleanup tasks in addition to fetching the error status."
87,pytorch_pytorch,issue,153960,"Let me give you some background on this issue from the NCCL team's perspective:
 Until recently, ncclCommGetAsyncError was, as @ptrblck said, a pure read-op. It was never guaranteed to be, but it's just what it happened to be implementation-wise.
 This changed in NCCL 2.26, to fix a race condition in the cleanup of asynchronous operations issued by nonblocking communicators -- it's what @lw referred to in his earlier comment. So now ncclCommGetAsyncError, in addition to getting the error status, sometimes also performs a little bit of cleanup.","From the NCCL team's perspective, ncclCommGetAsyncError was previously a read-only function, as @ptrblck noted. However, in NCCL 2.26, we introduced changes to address a race condition in the cleanup of asynchronous operations from nonblocking communicators, as @lw mentioned. Now, ncclCommGetAsyncError not only retrieves the error status but also performs some cleanup tasks.",Knowledge,9,9,pytorch_pytorch,153960,issue,,0,0,0,0,"From the NCCL team's perspective, ncclCommGetAsyncError was previously a read-only function, as @ptrblck noted. However, in NCCL 2.26, we introduced changes to address a race condition in the cleanup of asynchronous operations from nonblocking communicators, as @lw mentioned. Now, ncclCommGetAsyncError not only retrieves the error status but also performs some cleanup tasks."
88,pytorch_pytorch,Issue,153955,"I've been trying to export a pytorch decoder layer of a command pipeline to onnx to run a deployment environment that supports onnxruntime-gpu but I had been unsuccessful. I've tried breaking it down into specifically just the autoregressive components.
 I’m trying to export a simplified “step‐by‐step” decoder (a single–step pass through nn.TransformerDecoder) to ONNX with fully dynamic sequence lengths, but I keep hitting a runtime reshape failure when I feed in shorter sequences than my dummy export shape.","I'm attempting to export a PyTorch decoder layer to ONNX for deployment in an environment that supports onnxruntime-gpu, but I'm encountering issues. Specifically, I'm trying to export a simplified single-step pass through nn.TransformerDecoder with fully dynamic sequence lengths, but I keep getting a runtime reshape error when I input sequences shorter than the dummy export shape.",Unknown,10,10,pytorch_pytorch,153955,Issue,,1,3,3,1,"I'm attempting to export a PyTorch decoder layer to ONNX for deployment in an environment that supports onnxruntime-gpu, but I'm encountering issues. Specifically, I'm trying to export a simplified single-step pass through nn.TransformerDecoder with fully dynamic sequence lengths, but I keep getting a runtime reshape error when I input sequences shorter than the dummy export shape."
89,pytorch_pytorch,Issue,153955,"I've been trying to export a pytorch decoder layer of a command pipeline to onnx to run a deployment environment that supports onnxruntime-gpu but I had been unsuccessful. I've tried breaking it down into specifically just the autoregressive components.
 I’m trying to export a simplified “step‐by‐step” decoder (a single–step pass through nn.TransformerDecoder) to ONNX with fully dynamic sequence lengths, but I keep hitting a runtime reshape failure when I feed in shorter sequences than my dummy export shape.","I've been struggling to export a PyTorch decoder layer to ONNX for use with onnxruntime-gpu. I've broken it down to just the autoregressive components and am trying to export a single-step pass through nn.TransformerDecoder with dynamic sequence lengths. However, I consistently run into a reshape error at runtime when the input sequences are shorter than the dummy export shape.",Unknown,10,10,pytorch_pytorch,153955,Issue,,1,3,3,1,"I've been struggling to export a PyTorch decoder layer to ONNX for use with onnxruntime-gpu. I've broken it down to just the autoregressive components and am trying to export a single-step pass through nn.TransformerDecoder with dynamic sequence lengths. However, I consistently run into a reshape error at runtime when the input sequences are shorter than the dummy export shape."
90,pytorch_pytorch,Issue,153955,"I've been trying to export a pytorch decoder layer of a command pipeline to onnx to run a deployment environment that supports onnxruntime-gpu but I had been unsuccessful. I've tried breaking it down into specifically just the autoregressive components.
 I’m trying to export a simplified “step‐by‐step” decoder (a single–step pass through nn.TransformerDecoder) to ONNX with fully dynamic sequence lengths, but I keep hitting a runtime reshape failure when I feed in shorter sequences than my dummy export shape.","I'm having trouble exporting a PyTorch decoder layer to ONNX for deployment in an environment that supports onnxruntime-gpu. I've simplified the model to a single-step pass through nn.TransformerDecoder with dynamic sequence lengths, but I keep encountering a runtime reshape error when the input sequences are shorter than the dummy export shape.",Unknown,10,10,pytorch_pytorch,153955,Issue,,1,3,3,1,"I'm having trouble exporting a PyTorch decoder layer to ONNX for deployment in an environment that supports onnxruntime-gpu. I've simplified the model to a single-step pass through nn.TransformerDecoder with dynamic sequence lengths, but I keep encountering a runtime reshape error when the input sequences are shorter than the dummy export shape."
91,pytorch_pytorch,Issue,153955,"I've been trying to export a pytorch decoder layer of a command pipeline to onnx to run a deployment environment that supports onnxruntime-gpu but I had been unsuccessful. I've tried breaking it down into specifically just the autoregressive components.
 I’m trying to export a simplified “step‐by‐step” decoder (a single–step pass through nn.TransformerDecoder) to ONNX with fully dynamic sequence lengths, but I keep hitting a runtime reshape failure when I feed in shorter sequences than my dummy export shape.","I'm trying to export a PyTorch decoder layer to ONNX for use with onnxruntime-gpu, but I'm running into issues. I've simplified the model to a single-step pass through nn.TransformerDecoder with dynamic sequence lengths, but I get a runtime reshape error when the input sequences are shorter than the dummy export shape.",Unknown,10,10,pytorch_pytorch,153955,Issue,,1,3,3,1,"I'm trying to export a PyTorch decoder layer to ONNX for use with onnxruntime-gpu, but I'm running into issues. I've simplified the model to a single-step pass through nn.TransformerDecoder with dynamic sequence lengths, but I get a runtime reshape error when the input sequences are shorter than the dummy export shape."
92,pytorch_pytorch,Issue,153955,"I've been trying to export a pytorch decoder layer of a command pipeline to onnx to run a deployment environment that supports onnxruntime-gpu but I had been unsuccessful. I've tried breaking it down into specifically just the autoregressive components.
 I’m trying to export a simplified “step‐by‐step” decoder (a single–step pass through nn.TransformerDecoder) to ONNX with fully dynamic sequence lengths, but I keep hitting a runtime reshape failure when I feed in shorter sequences than my dummy export shape.","I'm attempting to export a PyTorch decoder layer to ONNX for deployment in an environment that supports onnxruntime-gpu. I've broken it down to just the autoregressive components and am trying to export a single-step pass through nn.TransformerDecoder with dynamic sequence lengths, but I keep hitting a runtime reshape error when the input sequences are shorter than the dummy export shape.",Unknown,10,10,pytorch_pytorch,153955,Issue,,1,3,3,1,"I'm attempting to export a PyTorch decoder layer to ONNX for deployment in an environment that supports onnxruntime-gpu. I've broken it down to just the autoregressive components and am trying to export a single-step pass through nn.TransformerDecoder with dynamic sequence lengths, but I keep hitting a runtime reshape error when the input sequences are shorter than the dummy export shape."
93,pytorch_pytorch,Issue,153955,"I've been trying to export a pytorch decoder layer of a command pipeline to onnx to run a deployment environment that supports onnxruntime-gpu but I had been unsuccessful. I've tried breaking it down into specifically just the autoregressive components.
 I’m trying to export a simplified “step‐by‐step” decoder (a single–step pass through nn.TransformerDecoder) to ONNX with fully dynamic sequence lengths, but I keep hitting a runtime reshape failure when I feed in shorter sequences than my dummy export shape.","I'm facing challenges exporting a PyTorch decoder layer to ONNX for use with onnxruntime-gpu. I've simplified the model to a single-step pass through nn.TransformerDecoder with dynamic sequence lengths, but I keep getting a runtime reshape error when the input sequences are shorter than the dummy export shape.",Unknown,10,10,pytorch_pytorch,153955,Issue,,1,3,3,1,"I'm facing challenges exporting a PyTorch decoder layer to ONNX for use with onnxruntime-gpu. I've simplified the model to a single-step pass through nn.TransformerDecoder with dynamic sequence lengths, but I keep getting a runtime reshape error when the input sequences are shorter than the dummy export shape."
94,pytorch_pytorch,Issue,153955,"I've been trying to export a pytorch decoder layer of a command pipeline to onnx to run a deployment environment that supports onnxruntime-gpu but I had been unsuccessful. I've tried breaking it down into specifically just the autoregressive components.
 I’m trying to export a simplified “step‐by‐step” decoder (a single–step pass through nn.TransformerDecoder) to ONNX with fully dynamic sequence lengths, but I keep hitting a runtime reshape failure when I feed in shorter sequences than my dummy export shape.","I'm trying to export a PyTorch decoder layer to ONNX for deployment in an environment that supports onnxruntime-gpu, but I'm encountering a runtime reshape error. I've simplified the model to a single-step pass through nn.TransformerDecoder with dynamic sequence lengths, and the error occurs when the input sequences are shorter than the dummy export shape.",Unknown,10,10,pytorch_pytorch,153955,Issue,,1,3,3,1,"I'm trying to export a PyTorch decoder layer to ONNX for deployment in an environment that supports onnxruntime-gpu, but I'm encountering a runtime reshape error. I've simplified the model to a single-step pass through nn.TransformerDecoder with dynamic sequence lengths, and the error occurs when the input sequences are shorter than the dummy export shape."
95,pytorch_pytorch,Issue,153955,"I've been trying to export a pytorch decoder layer of a command pipeline to onnx to run a deployment environment that supports onnxruntime-gpu but I had been unsuccessful. I've tried breaking it down into specifically just the autoregressive components.
 I’m trying to export a simplified “step‐by‐step” decoder (a single–step pass through nn.TransformerDecoder) to ONNX with fully dynamic sequence lengths, but I keep hitting a runtime reshape failure when I feed in shorter sequences than my dummy export shape.","I'm having difficulty exporting a PyTorch decoder layer to ONNX for use with onnxruntime-gpu. I've broken it down to just the autoregressive components and am trying to export a single-step pass through nn.TransformerDecoder with dynamic sequence lengths, but I keep hitting a runtime reshape error when the input sequences are shorter than the dummy export shape.",Unknown,10,10,pytorch_pytorch,153955,Issue,,1,3,3,1,"I'm having difficulty exporting a PyTorch decoder layer to ONNX for use with onnxruntime-gpu. I've broken it down to just the autoregressive components and am trying to export a single-step pass through nn.TransformerDecoder with dynamic sequence lengths, but I keep hitting a runtime reshape error when the input sequences are shorter than the dummy export shape."
96,pytorch_pytorch,Issue,153955,"I've been trying to export a pytorch decoder layer of a command pipeline to onnx to run a deployment environment that supports onnxruntime-gpu but I had been unsuccessful. I've tried breaking it down into specifically just the autoregressive components.
 I’m trying to export a simplified “step‐by‐step” decoder (a single–step pass through nn.TransformerDecoder) to ONNX with fully dynamic sequence lengths, but I keep hitting a runtime reshape failure when I feed in shorter sequences than my dummy export shape.","I'm attempting to export a PyTorch decoder layer to ONNX for deployment in an environment that supports onnxruntime-gpu. I've simplified the model to a single-step pass through nn.TransformerDecoder with dynamic sequence lengths, but I keep encountering a runtime reshape error when the input sequences are shorter than the dummy export shape.",Unknown,10,10,pytorch_pytorch,153955,Issue,,1,3,3,1,"I'm attempting to export a PyTorch decoder layer to ONNX for deployment in an environment that supports onnxruntime-gpu. I've simplified the model to a single-step pass through nn.TransformerDecoder with dynamic sequence lengths, but I keep encountering a runtime reshape error when the input sequences are shorter than the dummy export shape."
97,pytorch_pytorch,Issue,153955,"You would need to specify dynamic_shapes/dynamic_axes explicitly.
 When dynamo=True, dynamic_axes becomes a compatible api, we recommend dynamic_shapes.","To use dynamic shapes, you need to explicitly specify dynamic_shapes or dynamic_axes. When dynamo is enabled, dynamic_axes is supported, but we recommend using dynamic_shapes for better compatibility.",Knowledge,11,11,pytorch_pytorch,153955,Issue,,0,0,0,0,"To use dynamic shapes, you need to explicitly specify dynamic_shapes or dynamic_axes. When dynamo is enabled, dynamic_axes is supported, but we recommend using dynamic_shapes for better compatibility."
98,pytorch_pytorch,Issue,153955,"You would need to specify dynamic_shapes/dynamic_axes explicitly.
 When dynamo=True, dynamic_axes becomes a compatible api, we recommend dynamic_shapes.","If you want to enable dynamic shapes, you must explicitly set dynamic_shapes or dynamic_axes. With dynamo=True, dynamic_axes is a compatible API, but we advise using dynamic_shapes for optimal performance.",Knowledge,11,11,pytorch_pytorch,153955,Issue,,0,0,0,0,"If you want to enable dynamic shapes, you must explicitly set dynamic_shapes or dynamic_axes. With dynamo=True, dynamic_axes is a compatible API, but we advise using dynamic_shapes for optimal performance."
99,pytorch_pytorch,Issue,153955,"You would need to specify dynamic_shapes/dynamic_axes explicitly.
 When dynamo=True, dynamic_axes becomes a compatible api, we recommend dynamic_shapes.","For dynamic shape support, you need to explicitly define dynamic_shapes or dynamic_axes. When dynamo is active, dynamic_axes is supported, but we recommend dynamic_shapes for the best results.",Knowledge,11,11,pytorch_pytorch,153955,Issue,,0,0,0,0,"For dynamic shape support, you need to explicitly define dynamic_shapes or dynamic_axes. When dynamo is active, dynamic_axes is supported, but we recommend dynamic_shapes for the best results."
100,pytorch_pytorch,Issue,153955,"You would need to specify dynamic_shapes/dynamic_axes explicitly.
 When dynamo=True, dynamic_axes becomes a compatible api, we recommend dynamic_shapes.","To handle dynamic shapes, you should explicitly specify dynamic_shapes or dynamic_axes. When dynamo is set to True, dynamic_axes is a compatible option, but we suggest using dynamic_shapes for the best experience.",Knowledge,11,11,pytorch_pytorch,153955,Issue,,0,0,0,0,"To handle dynamic shapes, you should explicitly specify dynamic_shapes or dynamic_axes. When dynamo is set to True, dynamic_axes is a compatible option, but we suggest using dynamic_shapes for the best experience."
101,pytorch_pytorch,Issue,153955,"You would need to specify dynamic_shapes/dynamic_axes explicitly.
 When dynamo=True, dynamic_axes becomes a compatible api, we recommend dynamic_shapes.","To enable dynamic shapes in your model, you must explicitly set dynamic_shapes or dynamic_axes. With dynamo enabled, dynamic_axes is a valid option, but we recommend dynamic_shapes for better performance and compatibility.",Knowledge,11,11,pytorch_pytorch,153955,Issue,,0,0,0,0,"To enable dynamic shapes in your model, you must explicitly set dynamic_shapes or dynamic_axes. With dynamo enabled, dynamic_axes is a valid option, but we recommend dynamic_shapes for better performance and compatibility."
102,pytorch_pytorch,Issue,153955,"You would need to specify dynamic_shapes/dynamic_axes explicitly.
 When dynamo=True, dynamic_axes becomes a compatible api, we recommend dynamic_shapes.","For dynamic shape handling, you need to explicitly specify dynamic_shapes or dynamic_axes. When dynamo is True, dynamic_axes is a compatible API, but we strongly recommend using dynamic_shapes.",Knowledge,11,11,pytorch_pytorch,153955,Issue,,0,0,0,0,"For dynamic shape handling, you need to explicitly specify dynamic_shapes or dynamic_axes. When dynamo is True, dynamic_axes is a compatible API, but we strongly recommend using dynamic_shapes."
103,pytorch_pytorch,Issue,153955,"You would need to specify dynamic_shapes/dynamic_axes explicitly.
 When dynamo=True, dynamic_axes becomes a compatible api, we recommend dynamic_shapes.","To support dynamic shapes, you must explicitly define dynamic_shapes or dynamic_axes. When dynamo is enabled, dynamic_axes is a valid option, but we advise using dynamic_shapes for the best results.",Knowledge,11,11,pytorch_pytorch,153955,Issue,,0,0,0,0,"To support dynamic shapes, you must explicitly define dynamic_shapes or dynamic_axes. When dynamo is enabled, dynamic_axes is a valid option, but we advise using dynamic_shapes for the best results."
104,pytorch_pytorch,Issue,153955,"You would need to specify dynamic_shapes/dynamic_axes explicitly.
 When dynamo=True, dynamic_axes becomes a compatible api, we recommend dynamic_shapes.","To ensure dynamic shape support, you need to explicitly set dynamic_shapes or dynamic_axes. With dynamo=True, dynamic_axes is a compatible API, but we recommend dynamic_shapes for the best performance.",Knowledge,11,11,pytorch_pytorch,153955,Issue,,0,0,0,0,"To ensure dynamic shape support, you need to explicitly set dynamic_shapes or dynamic_axes. With dynamo=True, dynamic_axes is a compatible API, but we recommend dynamic_shapes for the best performance."
105,pytorch_pytorch,Issue,153955,"You would need to specify dynamic_shapes/dynamic_axes explicitly.
 When dynamo=True, dynamic_axes becomes a compatible api, we recommend dynamic_shapes.","For dynamic shape handling, you should explicitly specify dynamic_shapes or dynamic_axes. When dynamo is enabled, dynamic_axes is a valid option, but we recommend dynamic_shapes for the best compatibility and performance.",Knowledge,11,11,pytorch_pytorch,153955,Issue,,0,0,0,0,"For dynamic shape handling, you should explicitly specify dynamic_shapes or dynamic_axes. When dynamo is enabled, dynamic_axes is a valid option, but we recommend dynamic_shapes for the best compatibility and performance."
106,pytorch_pytorch,Issue,153955,"You would need to specify dynamic_shapes/dynamic_axes explicitly.
 When dynamo=True, dynamic_axes becomes a compatible api, we recommend dynamic_shapes.","To enable dynamic shapes in your model, you must explicitly specify dynamic_shapes or dynamic_axes. When dynamo is set to True, dynamic_axes is a compatible API, but we strongly recommend using dynamic_shapes for the best results.",Knowledge,11,11,pytorch_pytorch,153955,Issue,,0,0,0,0,"To enable dynamic shapes in your model, you must explicitly specify dynamic_shapes or dynamic_axes. When dynamo is set to True, dynamic_axes is a compatible API, but we strongly recommend using dynamic_shapes for the best results."
107,pytorch_pytorch,issue,21819,I met a strange illegal memory access error. It happens randomly without any regular pattern.,"I'm encountering a sporadic illegal memory access error that doesn't seem to follow any specific pattern. It's difficult to debug because it happens randomly, making it hard to pinpoint the exact cause.",Unknown,12,12,pytorch_pytorch,21819,issue,,1,3,3,1,"I'm encountering a sporadic illegal memory access error that doesn't seem to follow any specific pattern. It's difficult to debug because it happens randomly, making it hard to pinpoint the exact cause."
108,pytorch_pytorch,issue,21819,I met a strange illegal memory access error. It happens randomly without any regular pattern.,"I've been facing an intermittent illegal memory access issue that appears to occur at random. There doesn't seem to be a consistent trigger, which makes it challenging to determine the root cause.",Unknown,12,12,pytorch_pytorch,21819,issue,,1,3,3,1,"I've been facing an intermittent illegal memory access issue that appears to occur at random. There doesn't seem to be a consistent trigger, which makes it challenging to determine the root cause."
109,pytorch_pytorch,issue,21819,I met a strange illegal memory access error. It happens randomly without any regular pattern.,"I've run into a strange illegal memory access error that pops up randomly. It doesn't follow any predictable pattern, which is making it very difficult to diagnose and fix.",Unknown,12,12,pytorch_pytorch,21819,issue,,1,3,3,1,"I've run into a strange illegal memory access error that pops up randomly. It doesn't follow any predictable pattern, which is making it very difficult to diagnose and fix."
110,pytorch_pytorch,issue,21819,I met a strange illegal memory access error. It happens randomly without any regular pattern.,I'm seeing an illegal memory access error that happens sporadically. The lack of a clear pattern is making it hard to understand what's causing the issue.,Unknown,12,12,pytorch_pytorch,21819,issue,,1,3,3,1,I'm seeing an illegal memory access error that happens sporadically. The lack of a clear pattern is making it hard to understand what's causing the issue.
111,pytorch_pytorch,issue,21819,I met a strange illegal memory access error. It happens randomly without any regular pattern.,I've encountered an illegal memory access error that appears randomly and doesn't seem to have a consistent trigger. This makes it particularly challenging to debug and resolve.,Unknown,12,12,pytorch_pytorch,21819,issue,,1,3,3,1,I've encountered an illegal memory access error that appears randomly and doesn't seem to have a consistent trigger. This makes it particularly challenging to debug and resolve.
112,pytorch_pytorch,issue,21819,I met a strange illegal memory access error. It happens randomly without any regular pattern.,I'm dealing with a random illegal memory access error that doesn't follow any regular pattern. It's making it very difficult to identify the underlying problem.,Unknown,12,12,pytorch_pytorch,21819,issue,,1,3,3,1,I'm dealing with a random illegal memory access error that doesn't follow any regular pattern. It's making it very difficult to identify the underlying problem.
113,pytorch_pytorch,issue,21819,I met a strange illegal memory access error. It happens randomly without any regular pattern.,I've been struggling with an intermittent illegal memory access error that doesn't seem to have a predictable pattern. It's making it hard to figure out what's going wrong.,Unknown,12,12,pytorch_pytorch,21819,issue,,1,3,3,1,I've been struggling with an intermittent illegal memory access error that doesn't seem to have a predictable pattern. It's making it hard to figure out what's going wrong.
114,pytorch_pytorch,issue,21819,I met a strange illegal memory access error. It happens randomly without any regular pattern.,I've run into a strange illegal memory access issue that occurs at random times. The lack of a consistent pattern is making it difficult to diagnose.,Unknown,12,12,pytorch_pytorch,21819,issue,,1,3,3,1,I've run into a strange illegal memory access issue that occurs at random times. The lack of a consistent pattern is making it difficult to diagnose.
115,pytorch_pytorch,issue,21819,I met a strange illegal memory access error. It happens randomly without any regular pattern.,I'm facing an illegal memory access error that happens sporadically and without any clear pattern. It's making it hard to determine the cause and find a solution.,Unknown,12,12,pytorch_pytorch,21819,issue,,1,3,3,1,I'm facing an illegal memory access error that happens sporadically and without any clear pattern. It's making it hard to determine the cause and find a solution.
116,pytorch_pytorch,issue,21819,I met a strange illegal memory access error. It happens randomly without any regular pattern.,"I've encountered a random illegal memory access error that doesn't follow any regular pattern, which is making it very challenging to debug and fix.",Unknown,12,12,pytorch_pytorch,21819,issue,,1,3,3,1,"I've encountered a random illegal memory access error that doesn't follow any regular pattern, which is making it very challenging to debug and fix."
117,pytorch_pytorch,issue,21819,"I know some wrong indexing operations and some wrong usage method of loss function may lead to illegal memory access error. But in this script, there is no such kind of operation.
 I am quite sure this error is not because of out of memory since only about 2G GPU memory is used, and I have totally 12G GPU memory.","I'm aware that incorrect indexing or misuse of a loss function can cause illegal memory access errors. However, I've reviewed the script and can confirm that none of these operations are present. I'm also certain that the error isn't due to running out of memory, as the script only uses around 2GB of the 12GB available.",Unknown,13,13,pytorch_pytorch,21819,issue,,1,3,3,1,"I'm aware that incorrect indexing or misuse of a loss function can cause illegal memory access errors. However, I've reviewed the script and can confirm that none of these operations are present. I'm also certain that the error isn't due to running out of memory, as the script only uses around 2GB of the 12GB available."
118,pytorch_pytorch,issue,21819,"I know some wrong indexing operations and some wrong usage method of loss function may lead to illegal memory access error. But in this script, there is no such kind of operation.
 I am quite sure this error is not because of out of memory since only about 2G GPU memory is used, and I have totally 12G GPU memory.","I understand that illegal memory access errors can often be caused by incorrect indexing or improper use of loss functions. In this case, though, I've double-checked the script and found no such issues. Additionally, the GPU memory usage is only about 2GB, which is far below the 12GB limit, so I don't think it's an out-of-memory problem.",Unknown,13,13,pytorch_pytorch,21819,issue,,1,3,3,1,"I understand that illegal memory access errors can often be caused by incorrect indexing or improper use of loss functions. In this case, though, I've double-checked the script and found no such issues. Additionally, the GPU memory usage is only about 2GB, which is far below the 12GB limit, so I don't think it's an out-of-memory problem."
119,pytorch_pytorch,issue,21819,"I know some wrong indexing operations and some wrong usage method of loss function may lead to illegal memory access error. But in this script, there is no such kind of operation.
 I am quite sure this error is not because of out of memory since only about 2G GPU memory is used, and I have totally 12G GPU memory.","I know that illegal memory access errors can arise from wrong indexing or incorrect loss function usage. However, I've thoroughly examined the script and can confidently say that these issues are not present. The error is unlikely to be due to memory constraints, as the script uses only 2GB of the 12GB available GPU memory.",Unknown,13,13,pytorch_pytorch,21819,issue,,1,3,3,1,"I know that illegal memory access errors can arise from wrong indexing or incorrect loss function usage. However, I've thoroughly examined the script and can confidently say that these issues are not present. The error is unlikely to be due to memory constraints, as the script uses only 2GB of the 12GB available GPU memory."
120,pytorch_pytorch,issue,21819,"I know some wrong indexing operations and some wrong usage method of loss function may lead to illegal memory access error. But in this script, there is no such kind of operation.
 I am quite sure this error is not because of out of memory since only about 2G GPU memory is used, and I have totally 12G GPU memory.","I'm familiar with the common causes of illegal memory access errors, such as incorrect indexing or misuse of loss functions. In this script, however, I've verified that none of these issues are present. The error is not due to insufficient memory, as the script only uses about 2GB of the 12GB available.",Unknown,13,13,pytorch_pytorch,21819,issue,,1,3,3,1,"I'm familiar with the common causes of illegal memory access errors, such as incorrect indexing or misuse of loss functions. In this script, however, I've verified that none of these issues are present. The error is not due to insufficient memory, as the script only uses about 2GB of the 12GB available."
121,pytorch_pytorch,issue,21819,"I know some wrong indexing operations and some wrong usage method of loss function may lead to illegal memory access error. But in this script, there is no such kind of operation.
 I am quite sure this error is not because of out of memory since only about 2G GPU memory is used, and I have totally 12G GPU memory.","I recognize that illegal memory access errors can be caused by incorrect indexing or improper loss function usage. However, I've reviewed the script and can confirm that these issues are not the cause. The GPU memory usage is only around 2GB, which is well within the 12GB limit, so I don't believe it's an out-of-memory issue.",Unknown,13,13,pytorch_pytorch,21819,issue,,1,3,3,1,"I recognize that illegal memory access errors can be caused by incorrect indexing or improper loss function usage. However, I've reviewed the script and can confirm that these issues are not the cause. The GPU memory usage is only around 2GB, which is well within the 12GB limit, so I don't believe it's an out-of-memory issue."
122,pytorch_pytorch,issue,21819,"I know some wrong indexing operations and some wrong usage method of loss function may lead to illegal memory access error. But in this script, there is no such kind of operation.
 I am quite sure this error is not because of out of memory since only about 2G GPU memory is used, and I have totally 12G GPU memory.","I'm aware that illegal memory access errors can result from wrong indexing or incorrect loss function usage. In this script, I've checked for these issues and found none. The error is not due to memory constraints, as the script only uses about 2GB of the 12GB available GPU memory.",Unknown,13,13,pytorch_pytorch,21819,issue,,1,3,3,1,"I'm aware that illegal memory access errors can result from wrong indexing or incorrect loss function usage. In this script, I've checked for these issues and found none. The error is not due to memory constraints, as the script only uses about 2GB of the 12GB available GPU memory."
123,pytorch_pytorch,issue,21819,"I know some wrong indexing operations and some wrong usage method of loss function may lead to illegal memory access error. But in this script, there is no such kind of operation.
 I am quite sure this error is not because of out of memory since only about 2G GPU memory is used, and I have totally 12G GPU memory.","I know that illegal memory access errors can be caused by incorrect indexing or improper use of loss functions. However, I've reviewed the script and can confirm that these issues are not present. The error is unlikely to be due to memory constraints, as the script only uses about 2GB of the 12GB available GPU memory.",Unknown,13,13,pytorch_pytorch,21819,issue,,1,3,3,1,"I know that illegal memory access errors can be caused by incorrect indexing or improper use of loss functions. However, I've reviewed the script and can confirm that these issues are not present. The error is unlikely to be due to memory constraints, as the script only uses about 2GB of the 12GB available GPU memory."
124,pytorch_pytorch,issue,21819,I solved by CUDA_LAUNCH_BLOCKING=1.,Setting CUDA_LAUNCH_BLOCKING=1 resolved the issue for me.,Knowledge,14,14,pytorch_pytorch,21819,issue,This error can be solved by setting CUDA_LAUNCH_BLOCKING=1.,0,0,0,0,Setting CUDA_LAUNCH_BLOCKING=1 resolved the issue for me.
125,pytorch_pytorch,issue,21819,I solved by CUDA_LAUNCH_BLOCKING=1.,I was able to fix the problem by enabling CUDA_LAUNCH_BLOCKING=1.,Knowledge,14,14,pytorch_pytorch,21819,issue,This error can be solved by setting CUDA_LAUNCH_BLOCKING=1.,0,0,0,0,I was able to fix the problem by enabling CUDA_LAUNCH_BLOCKING=1.
126,pytorch_pytorch,issue,21819,I solved by CUDA_LAUNCH_BLOCKING=1.,The solution was to set the environment variable CUDA_LAUNCH_BLOCKING to 1.,Knowledge,14,14,pytorch_pytorch,21819,issue,This error can be solved by setting CUDA_LAUNCH_BLOCKING=1.,0,0,0,0,The solution was to set the environment variable CUDA_LAUNCH_BLOCKING to 1.
127,pytorch_pytorch,issue,21819,I solved by CUDA_LAUNCH_BLOCKING=1.,I managed to solve the issue by configuring CUDA_LAUNCH_BLOCKING=1.,Knowledge,14,14,pytorch_pytorch,21819,issue,This error can be solved by setting CUDA_LAUNCH_BLOCKING=1.,0,0,0,0,I managed to solve the issue by configuring CUDA_LAUNCH_BLOCKING=1.
128,pytorch_pytorch,issue,21819,I solved by CUDA_LAUNCH_BLOCKING=1.,"By setting CUDA_LAUNCH_BLOCKING=1, I was able to resolve the problem.",Knowledge,14,14,pytorch_pytorch,21819,issue,This error can be solved by setting CUDA_LAUNCH_BLOCKING=1.,0,0,0,0,"By setting CUDA_LAUNCH_BLOCKING=1, I was able to resolve the problem."
129,pytorch_pytorch,issue,21819,I solved by CUDA_LAUNCH_BLOCKING=1.,I found that setting CUDA_LAUNCH_BLOCKING=1 fixed the issue I was facing.,Knowledge,14,14,pytorch_pytorch,21819,issue,This error can be solved by setting CUDA_LAUNCH_BLOCKING=1.,0,0,0,0,I found that setting CUDA_LAUNCH_BLOCKING=1 fixed the issue I was facing.
130,pytorch_pytorch,issue,21819,I solved by CUDA_LAUNCH_BLOCKING=1.,The issue was resolved when I set CUDA_LAUNCH_BLOCKING to 1.,Knowledge,14,14,pytorch_pytorch,21819,issue,This error can be solved by setting CUDA_LAUNCH_BLOCKING=1.,0,0,0,0,The issue was resolved when I set CUDA_LAUNCH_BLOCKING to 1.
131,pytorch_pytorch,issue,21819,I solved by CUDA_LAUNCH_BLOCKING=1.,I solved the problem by setting the CUDA_LAUNCH_BLOCKING environment variable to 1.,Knowledge,14,14,pytorch_pytorch,21819,issue,This error can be solved by setting CUDA_LAUNCH_BLOCKING=1.,0,0,0,0,I solved the problem by setting the CUDA_LAUNCH_BLOCKING environment variable to 1.
132,pytorch_pytorch,issue,21819,I solved by CUDA_LAUNCH_BLOCKING=1.,Setting CUDA_LAUNCH_BLOCKING=1 did the trick for me.,Knowledge,14,14,pytorch_pytorch,21819,issue,This error can be solved by setting CUDA_LAUNCH_BLOCKING=1.,0,0,0,0,Setting CUDA_LAUNCH_BLOCKING=1 did the trick for me.
133,pytorch_pytorch,issue,21819,I solved by CUDA_LAUNCH_BLOCKING=1.,I was able to address the issue by setting CUDA_LAUNCH_BLOCKING=1.,Knowledge,14,14,pytorch_pytorch,21819,issue,This error can be solved by setting CUDA_LAUNCH_BLOCKING=1.,0,0,0,0,I was able to address the issue by setting CUDA_LAUNCH_BLOCKING=1.
134,pytorch_pytorch,Issue,21819,"It looks like pytorch-2.x. need some hostcall, that hostcall need PCIe Atomic feature, So if we can find where the hostcall, and remove them. Maybe we can use pytorch-2.x without PCIe3 Atomic requirement?","I noticed that PyTorch 2.x seems to rely on hostcalls that require PCIe Atomic operations. If we can identify and remove these hostcalls, it might be possible to use PyTorch 2.x without the PCIe3 Atomic requirement. Is this a feasible approach?",Known,15,15,pytorch_pytorch,21819,Issue,,1,1,1,1,"I noticed that PyTorch 2.x seems to rely on hostcalls that require PCIe Atomic operations. If we can identify and remove these hostcalls, it might be possible to use PyTorch 2.x without the PCIe3 Atomic requirement. Is this a feasible approach?"
135,pytorch_pytorch,Issue,21819,"It looks like pytorch-2.x. need some hostcall, that hostcall need PCIe Atomic feature, So if we can find where the hostcall, and remove them. Maybe we can use pytorch-2.x without PCIe3 Atomic requirement?","It appears that PyTorch 2.x uses hostcalls that depend on PCIe Atomic features. By locating and eliminating these hostcalls, we might be able to run PyTorch 2.x without needing PCIe3 Atomic support. Has anyone tried this before?",Known,15,15,pytorch_pytorch,21819,Issue,,1,1,1,1,"It appears that PyTorch 2.x uses hostcalls that depend on PCIe Atomic features. By locating and eliminating these hostcalls, we might be able to run PyTorch 2.x without needing PCIe3 Atomic support. Has anyone tried this before?"
136,pytorch_pytorch,Issue,21819,"It looks like pytorch-2.x. need some hostcall, that hostcall need PCIe Atomic feature, So if we can find where the hostcall, and remove them. Maybe we can use pytorch-2.x without PCIe3 Atomic requirement?","I've observed that PyTorch 2.x has some hostcalls that require PCIe Atomic operations. If we can pinpoint and remove these hostcalls, it could allow us to use PyTorch 2.x without the PCIe3 Atomic requirement. Is this a viable solution?",Known,15,15,pytorch_pytorch,21819,Issue,,1,1,1,1,"I've observed that PyTorch 2.x has some hostcalls that require PCIe Atomic operations. If we can pinpoint and remove these hostcalls, it could allow us to use PyTorch 2.x without the PCIe3 Atomic requirement. Is this a viable solution?"
137,pytorch_pytorch,Issue,21819,"It looks like pytorch-2.x. need some hostcall, that hostcall need PCIe Atomic feature, So if we can find where the hostcall, and remove them. Maybe we can use pytorch-2.x without PCIe3 Atomic requirement?","It seems that PyTorch 2.x includes hostcalls that need PCIe Atomic features. If we can find and remove these hostcalls, we might be able to use PyTorch 2.x without the PCIe3 Atomic requirement. Has anyone explored this possibility?",Known,15,15,pytorch_pytorch,21819,Issue,,1,1,1,1,"It seems that PyTorch 2.x includes hostcalls that need PCIe Atomic features. If we can find and remove these hostcalls, we might be able to use PyTorch 2.x without the PCIe3 Atomic requirement. Has anyone explored this possibility?"
138,pytorch_pytorch,Issue,21819,"It looks like pytorch-2.x. need some hostcall, that hostcall need PCIe Atomic feature, So if we can find where the hostcall, and remove them. Maybe we can use pytorch-2.x without PCIe3 Atomic requirement?","I've found that PyTorch 2.x uses hostcalls that depend on PCIe Atomic operations. By identifying and removing these hostcalls, it might be possible to run PyTorch 2.x without the PCIe3 Atomic requirement. Is this a reasonable approach?",Known,15,15,pytorch_pytorch,21819,Issue,,1,1,1,1,"I've found that PyTorch 2.x uses hostcalls that depend on PCIe Atomic operations. By identifying and removing these hostcalls, it might be possible to run PyTorch 2.x without the PCIe3 Atomic requirement. Is this a reasonable approach?"
139,pytorch_pytorch,Issue,21819,"It looks like pytorch-2.x. need some hostcall, that hostcall need PCIe Atomic feature, So if we can find where the hostcall, and remove them. Maybe we can use pytorch-2.x without PCIe3 Atomic requirement?","It looks like PyTorch 2.x has some hostcalls that require PCIe Atomic features. If we can locate and remove these hostcalls, we might be able to use PyTorch 2.x without the PCIe3 Atomic requirement. Has anyone attempted this before?",Known,15,15,pytorch_pytorch,21819,Issue,,1,1,1,1,"It looks like PyTorch 2.x has some hostcalls that require PCIe Atomic features. If we can locate and remove these hostcalls, we might be able to use PyTorch 2.x without the PCIe3 Atomic requirement. Has anyone attempted this before?"
140,pytorch_pytorch,Issue,21819,"It looks like pytorch-2.x. need some hostcall, that hostcall need PCIe Atomic feature, So if we can find where the hostcall, and remove them. Maybe we can use pytorch-2.x without PCIe3 Atomic requirement?","I've noticed that PyTorch 2.x relies on hostcalls that need PCIe Atomic operations. If we can find and remove these hostcalls, it might be possible to use PyTorch 2.x without the PCIe3 Atomic requirement. Is this a practical solution?",Known,15,15,pytorch_pytorch,21819,Issue,,1,1,1,1,"I've noticed that PyTorch 2.x relies on hostcalls that need PCIe Atomic operations. If we can find and remove these hostcalls, it might be possible to use PyTorch 2.x without the PCIe3 Atomic requirement. Is this a practical solution?"
141,pytorch_pytorch,Issue,21819,"It looks like pytorch-2.x. need some hostcall, that hostcall need PCIe Atomic feature, So if we can find where the hostcall, and remove them. Maybe we can use pytorch-2.x without PCIe3 Atomic requirement?","It seems that PyTorch 2.x includes hostcalls that depend on PCIe Atomic features. By identifying and removing these hostcalls, we might be able to use PyTorch 2.x without the PCIe3 Atomic requirement. Has anyone tried this approach?",Known,15,15,pytorch_pytorch,21819,Issue,,1,1,1,1,"It seems that PyTorch 2.x includes hostcalls that depend on PCIe Atomic features. By identifying and removing these hostcalls, we might be able to use PyTorch 2.x without the PCIe3 Atomic requirement. Has anyone tried this approach?"
142,pytorch_pytorch,Issue,21819,"It looks like pytorch-2.x. need some hostcall, that hostcall need PCIe Atomic feature, So if we can find where the hostcall, and remove them. Maybe we can use pytorch-2.x without PCIe3 Atomic requirement?","I've observed that PyTorch 2.x has some hostcalls that require PCIe Atomic operations. If we can pinpoint and remove these hostcalls, it could allow us to use PyTorch 2.x without the PCIe3 Atomic requirement. Is this a feasible solution?",Known,15,15,pytorch_pytorch,21819,Issue,,1,1,1,1,"I've observed that PyTorch 2.x has some hostcalls that require PCIe Atomic operations. If we can pinpoint and remove these hostcalls, it could allow us to use PyTorch 2.x without the PCIe3 Atomic requirement. Is this a feasible solution?"
143,pytorch_pytorch,issue,21819,"Yes, for the hostcall with atomics issue, changing the motherboard with PCIe atomics support is a viable solution.","For the hostcall with atomics issue, replacing the motherboard with one that supports PCIe atomics can resolve the problem.",Knowledge,16,16,pytorch_pytorch,21819,issue,,0,0,0,0,"For the hostcall with atomics issue, replacing the motherboard with one that supports PCIe atomics can resolve the problem."
144,pytorch_pytorch,issue,21819,"Yes, for the hostcall with atomics issue, changing the motherboard with PCIe atomics support is a viable solution.","If you're facing the hostcall with atomics issue, upgrading to a motherboard that supports PCIe atomics is a valid solution.",Knowledge,16,16,pytorch_pytorch,21819,issue,,0,0,0,0,"If you're facing the hostcall with atomics issue, upgrading to a motherboard that supports PCIe atomics is a valid solution."
145,pytorch_pytorch,issue,21819,"Yes, for the hostcall with atomics issue, changing the motherboard with PCIe atomics support is a viable solution.","To address the hostcall with atomics issue, switching to a motherboard that has PCIe atomics support is a feasible approach.",Knowledge,16,16,pytorch_pytorch,21819,issue,,0,0,0,0,"To address the hostcall with atomics issue, switching to a motherboard that has PCIe atomics support is a feasible approach."
146,pytorch_pytorch,issue,21819,"Yes, for the hostcall with atomics issue, changing the motherboard with PCIe atomics support is a viable solution.",The hostcall with atomics issue can be fixed by changing the motherboard to one that supports PCIe atomics.,Knowledge,16,16,pytorch_pytorch,21819,issue,,0,0,0,0,The hostcall with atomics issue can be fixed by changing the motherboard to one that supports PCIe atomics.
147,pytorch_pytorch,issue,21819,"Yes, for the hostcall with atomics issue, changing the motherboard with PCIe atomics support is a viable solution.",A viable solution for the hostcall with atomics issue is to use a motherboard that supports PCIe atomics.,Knowledge,16,16,pytorch_pytorch,21819,issue,,0,0,0,0,A viable solution for the hostcall with atomics issue is to use a motherboard that supports PCIe atomics.
148,pytorch_pytorch,issue,21819,"Yes, for the hostcall with atomics issue, changing the motherboard with PCIe atomics support is a viable solution.","For the issue with hostcall and atomics, a motherboard that supports PCIe atomics can be a suitable replacement.",Knowledge,16,16,pytorch_pytorch,21819,issue,,0,0,0,0,"For the issue with hostcall and atomics, a motherboard that supports PCIe atomics can be a suitable replacement."
149,pytorch_pytorch,issue,21819,"Yes, for the hostcall with atomics issue, changing the motherboard with PCIe atomics support is a viable solution.","To resolve the hostcall with atomics issue, consider replacing your current motherboard with one that supports PCIe atomics.",Knowledge,16,16,pytorch_pytorch,21819,issue,,0,0,0,0,"To resolve the hostcall with atomics issue, consider replacing your current motherboard with one that supports PCIe atomics."
150,pytorch_pytorch,issue,21819,"Yes, for the hostcall with atomics issue, changing the motherboard with PCIe atomics support is a viable solution.","If you encounter the hostcall with atomics issue, a motherboard with PCIe atomics support is a recommended solution.",Knowledge,16,16,pytorch_pytorch,21819,issue,,0,0,0,0,"If you encounter the hostcall with atomics issue, a motherboard with PCIe atomics support is a recommended solution."
151,pytorch_pytorch,issue,21819,"Yes, for the hostcall with atomics issue, changing the motherboard with PCIe atomics support is a viable solution.",The hostcall with atomics issue can be effectively addressed by upgrading to a motherboard that supports PCIe atomics.,Knowledge,16,16,pytorch_pytorch,21819,issue,,0,0,0,0,The hostcall with atomics issue can be effectively addressed by upgrading to a motherboard that supports PCIe atomics.
152,pytorch_pytorch,issue,21819,"Yes, for the hostcall with atomics issue, changing the motherboard with PCIe atomics support is a viable solution.",A motherboard with PCIe atomics support is a practical solution for the hostcall with atomics issue.,Knowledge,16,16,pytorch_pytorch,21819,issue,,0,0,0,0,A motherboard with PCIe atomics support is a practical solution for the hostcall with atomics issue.
153,pytorch_pytorch,Issue,21819,"Using MPS for BERT inference appears to produce about a 2x slowdown compared to the CPU. Here is code to reproduce the issue:
 # MPS Version
 from transformers import AutoTokenizer, BertForSequenceClassification
 import timeit
 import torch
 

 tokenizer = AutoTokenizer.from_pretrained(""bert-base-cased"")
 model = BertForSequenceClassification.from_pretrained(""bert-base-cased"").to(torch.device(""mps""))
 

 tokens = tokenizer.tokenize(""Hello world, this is michael!"")
 tids = tokenizer.convert_tokens_to_ids(tokens)
 with torch.no_grad():
  t_tids = torch.tensor([tids]*64, device=torch.device(""mps""))
  res = timeit.timeit(lambda: model(input_ids=t_tids), number=100)
 print(res)
 

 torch.backends.mps.is_available() reports True as expected.
 I chose a batch size of 64, since that was mentioned on the blog post for the ~5x speedup in Huggingface BERT.
 Running this benchmark, swapping ""mps"" and ""cpu"" for the devices, I get:
 CPU 18.38 s
 MPS 36.00 s
 And, for comparison, running this same snippet on an A10G using CUDA 11.6 on an AWS g5.xlarge instance (ubuntu 20.04, pytorch 1.11.0 compiled from source), I get:
 CUDA 0.82 s
 I used the no_grad() construction due to the comment mentioned on #77753.
 I used today's nightly build. Furthermore, I also built PyTorch from source and observed no differences on the results here. Lastly, I confirmed with Activity Monitor and powermetrics that the GPU is in fact being used, seeing GPU usage at 98.8%, GPU power spike to 15711 mW when running the MPS benchmark, and CPU power spiking as expected when running the CPU benchmark.
 So it does seem like things are ""hooked up"" right, just not efficient.
 As a side note, when building PyTorch from source, I observed in the CMake logs that Accelerate.framework was selected as the BLAS library, but sadly I didn't observe any power consumption by the ANE module, which is too bad.","I've noticed a significant performance hit when using MPS for BERT inference compared to the CPU. The code snippet below demonstrates the issue: I set up a BERT model for sequence classification and measure the inference time on both devices. The results show a 2x slowdown on MPS compared to CPU. I've confirmed that the GPU is being utilized, but the efficiency is lacking. Here's the code for reference: [code snippet]",,17,17,pytorch_pytorch,21819,Issue,,-1,-1,-1,-1,"I've noticed a significant performance hit when using MPS for BERT inference compared to the CPU. The code snippet below demonstrates the issue: I set up a BERT model for sequence classification and measure the inference time on both devices. The results show a 2x slowdown on MPS compared to CPU. I've confirmed that the GPU is being utilized, but the efficiency is lacking. Here's the code for reference: [code snippet]"
154,pytorch_pytorch,Issue,21819,"Using MPS for BERT inference appears to produce about a 2x slowdown compared to the CPU. Here is code to reproduce the issue:
 # MPS Version
 from transformers import AutoTokenizer, BertForSequenceClassification
 import timeit
 import torch
 

 tokenizer = AutoTokenizer.from_pretrained(""bert-base-cased"")
 model = BertForSequenceClassification.from_pretrained(""bert-base-cased"").to(torch.device(""mps""))
 

 tokens = tokenizer.tokenize(""Hello world, this is michael!"")
 tids = tokenizer.convert_tokens_to_ids(tokens)
 with torch.no_grad():
  t_tids = torch.tensor([tids]*64, device=torch.device(""mps""))
  res = timeit.timeit(lambda: model(input_ids=t_tids), number=100)
 print(res)
 

 torch.backends.mps.is_available() reports True as expected.
 I chose a batch size of 64, since that was mentioned on the blog post for the ~5x speedup in Huggingface BERT.
 Running this benchmark, swapping ""mps"" and ""cpu"" for the devices, I get:
 CPU 18.38 s
 MPS 36.00 s
 And, for comparison, running this same snippet on an A10G using CUDA 11.6 on an AWS g5.xlarge instance (ubuntu 20.04, pytorch 1.11.0 compiled from source), I get:
 CUDA 0.82 s
 I used the no_grad() construction due to the comment mentioned on #77753.
 I used today's nightly build. Furthermore, I also built PyTorch from source and observed no differences on the results here. Lastly, I confirmed with Activity Monitor and powermetrics that the GPU is in fact being used, seeing GPU usage at 98.8%, GPU power spike to 15711 mW when running the MPS benchmark, and CPU power spiking as expected when running the CPU benchmark.
 So it does seem like things are ""hooked up"" right, just not efficient.
 As a side note, when building PyTorch from source, I observed in the CMake logs that Accelerate.framework was selected as the BLAS library, but sadly I didn't observe any power consumption by the ANE module, which is too bad.","When running BERT inference on the MPS device, I observed a 2x slowdown compared to the CPU. I used the transformers library and timed the inference for a batch size of 64. The results are consistent across multiple runs, and the GPU is indeed being used, as confirmed by Activity Monitor and powermetrics. Here's the code I used to reproduce the issue: [code snippet]",,17,17,pytorch_pytorch,21819,Issue,,-1,-1,-1,-1,"When running BERT inference on the MPS device, I observed a 2x slowdown compared to the CPU. I used the transformers library and timed the inference for a batch size of 64. The results are consistent across multiple runs, and the GPU is indeed being used, as confirmed by Activity Monitor and powermetrics. Here's the code I used to reproduce the issue: [code snippet]"
155,pytorch_pytorch,Issue,21819,"Using MPS for BERT inference appears to produce about a 2x slowdown compared to the CPU. Here is code to reproduce the issue:
 # MPS Version
 from transformers import AutoTokenizer, BertForSequenceClassification
 import timeit
 import torch
 

 tokenizer = AutoTokenizer.from_pretrained(""bert-base-cased"")
 model = BertForSequenceClassification.from_pretrained(""bert-base-cased"").to(torch.device(""mps""))
 

 tokens = tokenizer.tokenize(""Hello world, this is michael!"")
 tids = tokenizer.convert_tokens_to_ids(tokens)
 with torch.no_grad():
  t_tids = torch.tensor([tids]*64, device=torch.device(""mps""))
  res = timeit.timeit(lambda: model(input_ids=t_tids), number=100)
 print(res)
 

 torch.backends.mps.is_available() reports True as expected.
 I chose a batch size of 64, since that was mentioned on the blog post for the ~5x speedup in Huggingface BERT.
 Running this benchmark, swapping ""mps"" and ""cpu"" for the devices, I get:
 CPU 18.38 s
 MPS 36.00 s
 And, for comparison, running this same snippet on an A10G using CUDA 11.6 on an AWS g5.xlarge instance (ubuntu 20.04, pytorch 1.11.0 compiled from source), I get:
 CUDA 0.82 s
 I used the no_grad() construction due to the comment mentioned on #77753.
 I used today's nightly build. Furthermore, I also built PyTorch from source and observed no differences on the results here. Lastly, I confirmed with Activity Monitor and powermetrics that the GPU is in fact being used, seeing GPU usage at 98.8%, GPU power spike to 15711 mW when running the MPS benchmark, and CPU power spiking as expected when running the CPU benchmark.
 So it does seem like things are ""hooked up"" right, just not efficient.
 As a side note, when building PyTorch from source, I observed in the CMake logs that Accelerate.framework was selected as the BLAS library, but sadly I didn't observe any power consumption by the ANE module, which is too bad.","I'm experiencing a 2x performance degradation when using MPS for BERT inference compared to the CPU. The code I used to benchmark this involves setting up a BERT model and measuring the inference time on both devices. The GPU is being utilized, but the efficiency is not as expected. Here's the code for reference: [code snippet]",,17,17,pytorch_pytorch,21819,Issue,,-1,-1,-1,-1,"I'm experiencing a 2x performance degradation when using MPS for BERT inference compared to the CPU. The code I used to benchmark this involves setting up a BERT model and measuring the inference time on both devices. The GPU is being utilized, but the efficiency is not as expected. Here's the code for reference: [code snippet]"
156,pytorch_pytorch,Issue,21819,"Using MPS for BERT inference appears to produce about a 2x slowdown compared to the CPU. Here is code to reproduce the issue:
 # MPS Version
 from transformers import AutoTokenizer, BertForSequenceClassification
 import timeit
 import torch
 

 tokenizer = AutoTokenizer.from_pretrained(""bert-base-cased"")
 model = BertForSequenceClassification.from_pretrained(""bert-base-cased"").to(torch.device(""mps""))
 

 tokens = tokenizer.tokenize(""Hello world, this is michael!"")
 tids = tokenizer.convert_tokens_to_ids(tokens)
 with torch.no_grad():
  t_tids = torch.tensor([tids]*64, device=torch.device(""mps""))
  res = timeit.timeit(lambda: model(input_ids=t_tids), number=100)
 print(res)
 

 torch.backends.mps.is_available() reports True as expected.
 I chose a batch size of 64, since that was mentioned on the blog post for the ~5x speedup in Huggingface BERT.
 Running this benchmark, swapping ""mps"" and ""cpu"" for the devices, I get:
 CPU 18.38 s
 MPS 36.00 s
 And, for comparison, running this same snippet on an A10G using CUDA 11.6 on an AWS g5.xlarge instance (ubuntu 20.04, pytorch 1.11.0 compiled from source), I get:
 CUDA 0.82 s
 I used the no_grad() construction due to the comment mentioned on #77753.
 I used today's nightly build. Furthermore, I also built PyTorch from source and observed no differences on the results here. Lastly, I confirmed with Activity Monitor and powermetrics that the GPU is in fact being used, seeing GPU usage at 98.8%, GPU power spike to 15711 mW when running the MPS benchmark, and CPU power spiking as expected when running the CPU benchmark.
 So it does seem like things are ""hooked up"" right, just not efficient.
 As a side note, when building PyTorch from source, I observed in the CMake logs that Accelerate.framework was selected as the BLAS library, but sadly I didn't observe any power consumption by the ANE module, which is too bad.","I've encountered a 2x slowdown when running BERT inference on the MPS device compared to the CPU. I used the transformers library and measured the inference time for a batch size of 64. The GPU is being used, as confirmed by Activity Monitor and powermetrics, but the performance is not optimal. Here's the code I used: [code snippet]",,17,17,pytorch_pytorch,21819,Issue,,-1,-1,-1,-1,"I've encountered a 2x slowdown when running BERT inference on the MPS device compared to the CPU. I used the transformers library and measured the inference time for a batch size of 64. The GPU is being used, as confirmed by Activity Monitor and powermetrics, but the performance is not optimal. Here's the code I used: [code snippet]"
157,pytorch_pytorch,Issue,21819,"Using MPS for BERT inference appears to produce about a 2x slowdown compared to the CPU. Here is code to reproduce the issue:
 # MPS Version
 from transformers import AutoTokenizer, BertForSequenceClassification
 import timeit
 import torch
 

 tokenizer = AutoTokenizer.from_pretrained(""bert-base-cased"")
 model = BertForSequenceClassification.from_pretrained(""bert-base-cased"").to(torch.device(""mps""))
 

 tokens = tokenizer.tokenize(""Hello world, this is michael!"")
 tids = tokenizer.convert_tokens_to_ids(tokens)
 with torch.no_grad():
  t_tids = torch.tensor([tids]*64, device=torch.device(""mps""))
  res = timeit.timeit(lambda: model(input_ids=t_tids), number=100)
 print(res)
 

 torch.backends.mps.is_available() reports True as expected.
 I chose a batch size of 64, since that was mentioned on the blog post for the ~5x speedup in Huggingface BERT.
 Running this benchmark, swapping ""mps"" and ""cpu"" for the devices, I get:
 CPU 18.38 s
 MPS 36.00 s
 And, for comparison, running this same snippet on an A10G using CUDA 11.6 on an AWS g5.xlarge instance (ubuntu 20.04, pytorch 1.11.0 compiled from source), I get:
 CUDA 0.82 s
 I used the no_grad() construction due to the comment mentioned on #77753.
 I used today's nightly build. Furthermore, I also built PyTorch from source and observed no differences on the results here. Lastly, I confirmed with Activity Monitor and powermetrics that the GPU is in fact being used, seeing GPU usage at 98.8%, GPU power spike to 15711 mW when running the MPS benchmark, and CPU power spiking as expected when running the CPU benchmark.
 So it does seem like things are ""hooked up"" right, just not efficient.
 As a side note, when building PyTorch from source, I observed in the CMake logs that Accelerate.framework was selected as the BLAS library, but sadly I didn't observe any power consumption by the ANE module, which is too bad.","I'm seeing a 2x slowdown when using MPS for BERT inference compared to the CPU. I set up a BERT model and measured the inference time on both devices. The GPU is being utilized, but the performance is not as efficient as expected. Here's the code I used to reproduce the issue: [code snippet]",,17,17,pytorch_pytorch,21819,Issue,,-1,-1,-1,-1,"I'm seeing a 2x slowdown when using MPS for BERT inference compared to the CPU. I set up a BERT model and measured the inference time on both devices. The GPU is being utilized, but the performance is not as efficient as expected. Here's the code I used to reproduce the issue: [code snippet]"
158,pytorch_pytorch,Issue,21819,"Using MPS for BERT inference appears to produce about a 2x slowdown compared to the CPU. Here is code to reproduce the issue:
 # MPS Version
 from transformers import AutoTokenizer, BertForSequenceClassification
 import timeit
 import torch
 

 tokenizer = AutoTokenizer.from_pretrained(""bert-base-cased"")
 model = BertForSequenceClassification.from_pretrained(""bert-base-cased"").to(torch.device(""mps""))
 

 tokens = tokenizer.tokenize(""Hello world, this is michael!"")
 tids = tokenizer.convert_tokens_to_ids(tokens)
 with torch.no_grad():
  t_tids = torch.tensor([tids]*64, device=torch.device(""mps""))
  res = timeit.timeit(lambda: model(input_ids=t_tids), number=100)
 print(res)
 

 torch.backends.mps.is_available() reports True as expected.
 I chose a batch size of 64, since that was mentioned on the blog post for the ~5x speedup in Huggingface BERT.
 Running this benchmark, swapping ""mps"" and ""cpu"" for the devices, I get:
 CPU 18.38 s
 MPS 36.00 s
 And, for comparison, running this same snippet on an A10G using CUDA 11.6 on an AWS g5.xlarge instance (ubuntu 20.04, pytorch 1.11.0 compiled from source), I get:
 CUDA 0.82 s
 I used the no_grad() construction due to the comment mentioned on #77753.
 I used today's nightly build. Furthermore, I also built PyTorch from source and observed no differences on the results here. Lastly, I confirmed with Activity Monitor and powermetrics that the GPU is in fact being used, seeing GPU usage at 98.8%, GPU power spike to 15711 mW when running the MPS benchmark, and CPU power spiking as expected when running the CPU benchmark.
 So it does seem like things are ""hooked up"" right, just not efficient.
 As a side note, when building PyTorch from source, I observed in the CMake logs that Accelerate.framework was selected as the BLAS library, but sadly I didn't observe any power consumption by the ANE module, which is too bad.","I've noticed a significant performance drop when using MPS for BERT inference compared to the CPU. The code I used to benchmark this involves setting up a BERT model and measuring the inference time on both devices. The GPU is being used, but the efficiency is not as expected. Here's the code for reference: [code snippet]",,17,17,pytorch_pytorch,21819,Issue,,-1,-1,-1,-1,"I've noticed a significant performance drop when using MPS for BERT inference compared to the CPU. The code I used to benchmark this involves setting up a BERT model and measuring the inference time on both devices. The GPU is being used, but the efficiency is not as expected. Here's the code for reference: [code snippet]"
159,pytorch_pytorch,Issue,21819,"Using MPS for BERT inference appears to produce about a 2x slowdown compared to the CPU. Here is code to reproduce the issue:
 # MPS Version
 from transformers import AutoTokenizer, BertForSequenceClassification
 import timeit
 import torch
 

 tokenizer = AutoTokenizer.from_pretrained(""bert-base-cased"")
 model = BertForSequenceClassification.from_pretrained(""bert-base-cased"").to(torch.device(""mps""))
 

 tokens = tokenizer.tokenize(""Hello world, this is michael!"")
 tids = tokenizer.convert_tokens_to_ids(tokens)
 with torch.no_grad():
  t_tids = torch.tensor([tids]*64, device=torch.device(""mps""))
  res = timeit.timeit(lambda: model(input_ids=t_tids), number=100)
 print(res)
 

 torch.backends.mps.is_available() reports True as expected.
 I chose a batch size of 64, since that was mentioned on the blog post for the ~5x speedup in Huggingface BERT.
 Running this benchmark, swapping ""mps"" and ""cpu"" for the devices, I get:
 CPU 18.38 s
 MPS 36.00 s
 And, for comparison, running this same snippet on an A10G using CUDA 11.6 on an AWS g5.xlarge instance (ubuntu 20.04, pytorch 1.11.0 compiled from source), I get:
 CUDA 0.82 s
 I used the no_grad() construction due to the comment mentioned on #77753.
 I used today's nightly build. Furthermore, I also built PyTorch from source and observed no differences on the results here. Lastly, I confirmed with Activity Monitor and powermetrics that the GPU is in fact being used, seeing GPU usage at 98.8%, GPU power spike to 15711 mW when running the MPS benchmark, and CPU power spiking as expected when running the CPU benchmark.
 So it does seem like things are ""hooked up"" right, just not efficient.
 As a side note, when building PyTorch from source, I observed in the CMake logs that Accelerate.framework was selected as the BLAS library, but sadly I didn't observe any power consumption by the ANE module, which is too bad.","I'm experiencing a 2x slowdown when running BERT inference on the MPS device compared to the CPU. I used the transformers library and measured the inference time for a batch size of 64. The GPU is being utilized, but the performance is not optimal. Here's the code I used: [code snippet]",,17,17,pytorch_pytorch,21819,Issue,,-1,-1,-1,-1,"I'm experiencing a 2x slowdown when running BERT inference on the MPS device compared to the CPU. I used the transformers library and measured the inference time for a batch size of 64. The GPU is being utilized, but the performance is not optimal. Here's the code I used: [code snippet]"
160,pytorch_pytorch,Issue,21819,"Using MPS for BERT inference appears to produce about a 2x slowdown compared to the CPU. Here is code to reproduce the issue:
 # MPS Version
 from transformers import AutoTokenizer, BertForSequenceClassification
 import timeit
 import torch
 

 tokenizer = AutoTokenizer.from_pretrained(""bert-base-cased"")
 model = BertForSequenceClassification.from_pretrained(""bert-base-cased"").to(torch.device(""mps""))
 

 tokens = tokenizer.tokenize(""Hello world, this is michael!"")
 tids = tokenizer.convert_tokens_to_ids(tokens)
 with torch.no_grad():
  t_tids = torch.tensor([tids]*64, device=torch.device(""mps""))
  res = timeit.timeit(lambda: model(input_ids=t_tids), number=100)
 print(res)
 

 torch.backends.mps.is_available() reports True as expected.
 I chose a batch size of 64, since that was mentioned on the blog post for the ~5x speedup in Huggingface BERT.
 Running this benchmark, swapping ""mps"" and ""cpu"" for the devices, I get:
 CPU 18.38 s
 MPS 36.00 s
 And, for comparison, running this same snippet on an A10G using CUDA 11.6 on an AWS g5.xlarge instance (ubuntu 20.04, pytorch 1.11.0 compiled from source), I get:
 CUDA 0.82 s
 I used the no_grad() construction due to the comment mentioned on #77753.
 I used today's nightly build. Furthermore, I also built PyTorch from source and observed no differences on the results here. Lastly, I confirmed with Activity Monitor and powermetrics that the GPU is in fact being used, seeing GPU usage at 98.8%, GPU power spike to 15711 mW when running the MPS benchmark, and CPU power spiking as expected when running the CPU benchmark.
 So it does seem like things are ""hooked up"" right, just not efficient.
 As a side note, when building PyTorch from source, I observed in the CMake logs that Accelerate.framework was selected as the BLAS library, but sadly I didn't observe any power consumption by the ANE module, which is too bad.","I've observed a 2x performance degradation when using MPS for BERT inference compared to the CPU. The code I used to benchmark this involves setting up a BERT model and measuring the inference time on both devices. The GPU is being used, but the efficiency is not as expected. Here's the code for reference: [code snippet]",,17,17,pytorch_pytorch,21819,Issue,,-1,-1,-1,-1,"I've observed a 2x performance degradation when using MPS for BERT inference compared to the CPU. The code I used to benchmark this involves setting up a BERT model and measuring the inference time on both devices. The GPU is being used, but the efficiency is not as expected. Here's the code for reference: [code snippet]"
161,pytorch_pytorch,Issue,21819,"Using MPS for BERT inference appears to produce about a 2x slowdown compared to the CPU. Here is code to reproduce the issue:
 # MPS Version
 from transformers import AutoTokenizer, BertForSequenceClassification
 import timeit
 import torch
 

 tokenizer = AutoTokenizer.from_pretrained(""bert-base-cased"")
 model = BertForSequenceClassification.from_pretrained(""bert-base-cased"").to(torch.device(""mps""))
 

 tokens = tokenizer.tokenize(""Hello world, this is michael!"")
 tids = tokenizer.convert_tokens_to_ids(tokens)
 with torch.no_grad():
  t_tids = torch.tensor([tids]*64, device=torch.device(""mps""))
  res = timeit.timeit(lambda: model(input_ids=t_tids), number=100)
 print(res)
 

 torch.backends.mps.is_available() reports True as expected.
 I chose a batch size of 64, since that was mentioned on the blog post for the ~5x speedup in Huggingface BERT.
 Running this benchmark, swapping ""mps"" and ""cpu"" for the devices, I get:
 CPU 18.38 s
 MPS 36.00 s
 And, for comparison, running this same snippet on an A10G using CUDA 11.6 on an AWS g5.xlarge instance (ubuntu 20.04, pytorch 1.11.0 compiled from source), I get:
 CUDA 0.82 s
 I used the no_grad() construction due to the comment mentioned on #77753.
 I used today's nightly build. Furthermore, I also built PyTorch from source and observed no differences on the results here. Lastly, I confirmed with Activity Monitor and powermetrics that the GPU is in fact being used, seeing GPU usage at 98.8%, GPU power spike to 15711 mW when running the MPS benchmark, and CPU power spiking as expected when running the CPU benchmark.
 So it does seem like things are ""hooked up"" right, just not efficient.
 As a side note, when building PyTorch from source, I observed in the CMake logs that Accelerate.framework was selected as the BLAS library, but sadly I didn't observe any power consumption by the ANE module, which is too bad.","I'm seeing a 2x slowdown when using MPS for BERT inference compared to the CPU. I set up a BERT model and measured the inference time on both devices. The GPU is being utilized, but the performance is not as efficient as expected. Here's the code I used to reproduce the issue: [code snippet]",,17,17,pytorch_pytorch,21819,Issue,,-1,-1,-1,-1,"I'm seeing a 2x slowdown when using MPS for BERT inference compared to the CPU. I set up a BERT model and measured the inference time on both devices. The GPU is being utilized, but the performance is not as efficient as expected. Here's the code I used to reproduce the issue: [code snippet]"
162,pytorch_pytorch,Issue,21819,"Using MPS for BERT inference appears to produce about a 2x slowdown compared to the CPU. Here is code to reproduce the issue:
 # MPS Version
 from transformers import AutoTokenizer, BertForSequenceClassification
 import timeit
 import torch
 

 tokenizer = AutoTokenizer.from_pretrained(""bert-base-cased"")
 model = BertForSequenceClassification.from_pretrained(""bert-base-cased"").to(torch.device(""mps""))
 

 tokens = tokenizer.tokenize(""Hello world, this is michael!"")
 tids = tokenizer.convert_tokens_to_ids(tokens)
 with torch.no_grad():
  t_tids = torch.tensor([tids]*64, device=torch.device(""mps""))
  res = timeit.timeit(lambda: model(input_ids=t_tids), number=100)
 print(res)
 

 torch.backends.mps.is_available() reports True as expected.
 I chose a batch size of 64, since that was mentioned on the blog post for the ~5x speedup in Huggingface BERT.
 Running this benchmark, swapping ""mps"" and ""cpu"" for the devices, I get:
 CPU 18.38 s
 MPS 36.00 s
 And, for comparison, running this same snippet on an A10G using CUDA 11.6 on an AWS g5.xlarge instance (ubuntu 20.04, pytorch 1.11.0 compiled from source), I get:
 CUDA 0.82 s
 I used the no_grad() construction due to the comment mentioned on #77753.
 I used today's nightly build. Furthermore, I also built PyTorch from source and observed no differences on the results here. Lastly, I confirmed with Activity Monitor and powermetrics that the GPU is in fact being used, seeing GPU usage at 98.8%, GPU power spike to 15711 mW when running the MPS benchmark, and CPU power spiking as expected when running the CPU benchmark.
 So it does seem like things are ""hooked up"" right, just not efficient.
 As a side note, when building PyTorch from source, I observed in the CMake logs that Accelerate.framework was selected as the BLAS library, but sadly I didn't observe any power consumption by the ANE module, which is too bad.","I've noticed a significant performance hit when using MPS for BERT inference compared to the CPU. The code snippet below demonstrates the issue: I set up a BERT model for sequence classification and measure the inference time on both devices. The results show a 2x slowdown on MPS compared to CPU. I've confirmed that the GPU is being utilized, but the efficiency is lacking. Here's the code for reference: [code snippet]",,17,17,pytorch_pytorch,21819,Issue,,-1,-1,-1,-1,"I've noticed a significant performance hit when using MPS for BERT inference compared to the CPU. The code snippet below demonstrates the issue: I set up a BERT model for sequence classification and measure the inference time on both devices. The results show a 2x slowdown on MPS compared to CPU. I've confirmed that the GPU is being utilized, but the efficiency is lacking. Here's the code for reference: [code snippet]"
163,pytorch_pytorch,issue,21819,"I wonder if we could convert this to actual MPS code in Swift, then profile how long that code takes. That would determine whether the bottleneck is PyTorch’s fault and something that can be fixed.",I'm curious if we can translate this to MPS code in Swift and measure its performance. This would help us identify if the slowdown is due to PyTorch or if it's an issue that can be addressed.,Known,18,18,pytorch_pytorch,21819,issue,,1,1,1,1,I'm curious if we can translate this to MPS code in Swift and measure its performance. This would help us identify if the slowdown is due to PyTorch or if it's an issue that can be addressed.
164,pytorch_pytorch,issue,21819,"I wonder if we could convert this to actual MPS code in Swift, then profile how long that code takes. That would determine whether the bottleneck is PyTorch’s fault and something that can be fixed.",Would it be possible to rewrite this in Swift using the MPS framework and benchmark the execution time? This could shed light on whether the performance bottleneck lies with PyTorch or if it's something we can optimize.,Known,18,18,pytorch_pytorch,21819,issue,,1,1,1,1,Would it be possible to rewrite this in Swift using the MPS framework and benchmark the execution time? This could shed light on whether the performance bottleneck lies with PyTorch or if it's something we can optimize.
165,pytorch_pytorch,issue,21819,"I wonder if we could convert this to actual MPS code in Swift, then profile how long that code takes. That would determine whether the bottleneck is PyTorch’s fault and something that can be fixed.",I'm considering converting this to Swift with the Metal Performance Shaders (MPS) and profiling the code to see if the performance issues are specific to PyTorch. This would help us determine if the problem is within PyTorch or if it's a broader issue.,Known,18,18,pytorch_pytorch,21819,issue,,1,1,1,1,I'm considering converting this to Swift with the Metal Performance Shaders (MPS) and profiling the code to see if the performance issues are specific to PyTorch. This would help us determine if the problem is within PyTorch or if it's a broader issue.
166,pytorch_pytorch,issue,21819,"I wonder if we could convert this to actual MPS code in Swift, then profile how long that code takes. That would determine whether the bottleneck is PyTorch’s fault and something that can be fixed.",I'm wondering if we could implement this in Swift using the MPS library and time the execution to see if the performance issues are PyTorch-related. This would help us understand if the bottleneck is something we can fix.,Known,18,18,pytorch_pytorch,21819,issue,,1,1,1,1,I'm wondering if we could implement this in Swift using the MPS library and time the execution to see if the performance issues are PyTorch-related. This would help us understand if the bottleneck is something we can fix.
167,pytorch_pytorch,issue,21819,"I wonder if we could convert this to actual MPS code in Swift, then profile how long that code takes. That would determine whether the bottleneck is PyTorch’s fault and something that can be fixed.",Could we try rewriting this in Swift with the MPS framework and measure the execution time? This would help us determine if the performance issues are due to PyTorch or if they are inherent to the algorithm.,Known,18,18,pytorch_pytorch,21819,issue,,1,1,1,1,Could we try rewriting this in Swift with the MPS framework and measure the execution time? This would help us determine if the performance issues are due to PyTorch or if they are inherent to the algorithm.
168,pytorch_pytorch,issue,21819,"I wonder if we could convert this to actual MPS code in Swift, then profile how long that code takes. That would determine whether the bottleneck is PyTorch’s fault and something that can be fixed.",I'm thinking about converting this to Swift and using the MPS library to profile the code. This would help us figure out if the performance issues are caused by PyTorch or if they are something we can address.,Known,18,18,pytorch_pytorch,21819,issue,,1,1,1,1,I'm thinking about converting this to Swift and using the MPS library to profile the code. This would help us figure out if the performance issues are caused by PyTorch or if they are something we can address.
169,pytorch_pytorch,issue,21819,"I wonder if we could convert this to actual MPS code in Swift, then profile how long that code takes. That would determine whether the bottleneck is PyTorch’s fault and something that can be fixed.",I'm considering translating this to Swift and using the MPS framework to benchmark the performance. This would help us determine if the performance issues are due to PyTorch or if they are a result of the algorithm itself.,Known,18,18,pytorch_pytorch,21819,issue,,1,1,1,1,I'm considering translating this to Swift and using the MPS framework to benchmark the performance. This would help us determine if the performance issues are due to PyTorch or if they are a result of the algorithm itself.
170,pytorch_pytorch,issue,21819,"I wonder if we could convert this to actual MPS code in Swift, then profile how long that code takes. That would determine whether the bottleneck is PyTorch’s fault and something that can be fixed.",I'm curious if we can port this to Swift using the MPS library and measure the execution time. This would help us identify if the performance issues are specific to PyTorch or if they are a broader problem.,Known,18,18,pytorch_pytorch,21819,issue,,1,1,1,1,I'm curious if we can port this to Swift using the MPS library and measure the execution time. This would help us identify if the performance issues are specific to PyTorch or if they are a broader problem.
171,pytorch_pytorch,issue,21819,"I wonder if we could convert this to actual MPS code in Swift, then profile how long that code takes. That would determine whether the bottleneck is PyTorch’s fault and something that can be fixed.",I'm wondering if we could rewrite this in Swift with the MPS framework and profile the code to see if the performance issues are PyTorch-related. This would help us understand if the bottleneck is something we can fix.,Known,18,18,pytorch_pytorch,21819,issue,,1,1,1,1,I'm wondering if we could rewrite this in Swift with the MPS framework and profile the code to see if the performance issues are PyTorch-related. This would help us understand if the bottleneck is something we can fix.
172,pytorch_pytorch,issue,21819,"I wonder if we could convert this to actual MPS code in Swift, then profile how long that code takes. That would determine whether the bottleneck is PyTorch’s fault and something that can be fixed.",I'm considering converting this to Swift and using the MPS library to time the execution. This would help us determine if the performance issues are due to PyTorch or if they are inherent to the algorithm.,Known,18,18,pytorch_pytorch,21819,issue,,1,1,1,1,I'm considering converting this to Swift and using the MPS library to time the execution. This would help us determine if the performance issues are due to PyTorch or if they are inherent to the algorithm.
173,pytorch_pytorch,issue,21819,"Currently the graphs for random ops are not cached, which is leading to un-necessary recompiles slowing the randn operations. We are actively working on it and will update here with a fix","The graphs for random operations are not being cached, causing unnecessary recompilations and slowing down the randn functions. Our team is actively addressing this issue and will provide an update with a fix soon.",Knowledge,19,19,pytorch_pytorch,21819,issue,,0,0,0,0,"The graphs for random operations are not being cached, causing unnecessary recompilations and slowing down the randn functions. Our team is actively addressing this issue and will provide an update with a fix soon."
174,pytorch_pytorch,issue,21819,"Currently the graphs for random ops are not cached, which is leading to un-necessary recompiles slowing the randn operations. We are actively working on it and will update here with a fix","We've identified that the lack of caching for random operation graphs is leading to redundant recompilations, which is slowing down the randn operations. We are currently working on a solution and will post an update here once it's available.",Knowledge,19,19,pytorch_pytorch,21819,issue,,0,0,0,0,"We've identified that the lack of caching for random operation graphs is leading to redundant recompilations, which is slowing down the randn operations. We are currently working on a solution and will post an update here once it's available."
175,pytorch_pytorch,issue,21819,"Currently the graphs for random ops are not cached, which is leading to un-necessary recompiles slowing the randn operations. We are actively working on it and will update here with a fix","The current implementation does not cache the graphs for random operations, resulting in repeated recompilations and slower performance for randn. We are actively working on this and will share the fix here when it's ready.",Knowledge,19,19,pytorch_pytorch,21819,issue,,0,0,0,0,"The current implementation does not cache the graphs for random operations, resulting in repeated recompilations and slower performance for randn. We are actively working on this and will share the fix here when it's ready."
176,pytorch_pytorch,issue,21819,"Currently the graphs for random ops are not cached, which is leading to un-necessary recompiles slowing the randn operations. We are actively working on it and will update here with a fix","We've noticed that the graphs for random operations are not being cached, which is causing unnecessary recompilations and slowing down the randn functions. Our development team is working on a fix and will update this thread with the solution.",Knowledge,19,19,pytorch_pytorch,21819,issue,,0,0,0,0,"We've noticed that the graphs for random operations are not being cached, which is causing unnecessary recompilations and slowing down the randn functions. Our development team is working on a fix and will update this thread with the solution."
177,pytorch_pytorch,issue,21819,"Currently the graphs for random ops are not cached, which is leading to un-necessary recompiles slowing the randn operations. We are actively working on it and will update here with a fix","The graphs for random operations are not cached, leading to redundant recompilations and slower performance of randn. We are actively working on this issue and will provide an update with the fix as soon as possible.",Knowledge,19,19,pytorch_pytorch,21819,issue,,0,0,0,0,"The graphs for random operations are not cached, leading to redundant recompilations and slower performance of randn. We are actively working on this issue and will provide an update with the fix as soon as possible."
178,pytorch_pytorch,issue,21819,"Currently the graphs for random ops are not cached, which is leading to un-necessary recompiles slowing the randn operations. We are actively working on it and will update here with a fix","Currently, the graphs for random operations are not cached, causing unnecessary recompilations and slowing down the randn functions. Our team is actively working on a fix and will update this thread with the solution.",Knowledge,19,19,pytorch_pytorch,21819,issue,,0,0,0,0,"Currently, the graphs for random operations are not cached, causing unnecessary recompilations and slowing down the randn functions. Our team is actively working on a fix and will update this thread with the solution."
179,pytorch_pytorch,issue,21819,"Currently the graphs for random ops are not cached, which is leading to un-necessary recompiles slowing the randn operations. We are actively working on it and will update here with a fix","We are aware that the graphs for random operations are not being cached, which is leading to unnecessary recompilations and slower performance for randn. We are actively working on this and will provide an update with the fix soon.",Knowledge,19,19,pytorch_pytorch,21819,issue,,0,0,0,0,"We are aware that the graphs for random operations are not being cached, which is leading to unnecessary recompilations and slower performance for randn. We are actively working on this and will provide an update with the fix soon."
180,pytorch_pytorch,issue,21819,"Currently the graphs for random ops are not cached, which is leading to un-necessary recompiles slowing the randn operations. We are actively working on it and will update here with a fix",The lack of caching for random operation graphs is causing redundant recompilations and slowing down the randn functions. Our team is actively working on a solution and will update this thread with the fix.,Knowledge,19,19,pytorch_pytorch,21819,issue,,0,0,0,0,The lack of caching for random operation graphs is causing redundant recompilations and slowing down the randn functions. Our team is actively working on a solution and will update this thread with the fix.
181,pytorch_pytorch,issue,21819,"Currently the graphs for random ops are not cached, which is leading to un-necessary recompiles slowing the randn operations. We are actively working on it and will update here with a fix","We've identified that the graphs for random operations are not being cached, which is causing unnecessary recompilations and slowing down the randn functions. We are actively working on a fix and will update this thread with the solution.",Knowledge,19,19,pytorch_pytorch,21819,issue,,0,0,0,0,"We've identified that the graphs for random operations are not being cached, which is causing unnecessary recompilations and slowing down the randn functions. We are actively working on a fix and will update this thread with the solution."
182,pytorch_pytorch,issue,17501,"After testing on GPU, if I set the batch size low enough it runs very fast, but anything over 2 and it runs out of memory (8gb GPU). How would I go about debugging the memory usage?","I've noticed that my model runs very efficiently on the GPU with a batch size of 2 or less, but anything higher causes an out-of-memory error on my 8GB GPU. What steps should I take to diagnose and optimize the memory usage?",Known,20,20,pytorch_pytorch,17501,issue,,1,1,1,1,"I've noticed that my model runs very efficiently on the GPU with a batch size of 2 or less, but anything higher causes an out-of-memory error on my 8GB GPU. What steps should I take to diagnose and optimize the memory usage?"
183,pytorch_pytorch,issue,17501,"After testing on GPU, if I set the batch size low enough it runs very fast, but anything over 2 and it runs out of memory (8gb GPU). How would I go about debugging the memory usage?","When I run my model on the GPU, it performs well with a batch size of 2 or lower, but increasing it results in an out-of-memory error. My GPU has 8GB of memory. Can someone provide guidance on how to debug and reduce the memory consumption?",Known,20,20,pytorch_pytorch,17501,issue,,1,1,1,1,"When I run my model on the GPU, it performs well with a batch size of 2 or lower, but increasing it results in an out-of-memory error. My GPU has 8GB of memory. Can someone provide guidance on how to debug and reduce the memory consumption?"
184,pytorch_pytorch,issue,17501,"After testing on GPU, if I set the batch size low enough it runs very fast, but anything over 2 and it runs out of memory (8gb GPU). How would I go about debugging the memory usage?",I'm experiencing memory issues on my 8GB GPU when I set the batch size above 2. The model runs smoothly with smaller batch sizes but crashes with larger ones. How can I investigate and optimize the memory usage to handle larger batches?,Known,20,20,pytorch_pytorch,17501,issue,,1,1,1,1,I'm experiencing memory issues on my 8GB GPU when I set the batch size above 2. The model runs smoothly with smaller batch sizes but crashes with larger ones. How can I investigate and optimize the memory usage to handle larger batches?
185,pytorch_pytorch,issue,17501,"After testing on GPU, if I set the batch size low enough it runs very fast, but anything over 2 and it runs out of memory (8gb GPU). How would I go about debugging the memory usage?","My model runs quickly on the GPU with a batch size of 2 or less, but anything higher leads to an out-of-memory error on my 8GB GPU. What are some techniques to debug and manage the memory usage more effectively?",Known,20,20,pytorch_pytorch,17501,issue,,1,1,1,1,"My model runs quickly on the GPU with a batch size of 2 or less, but anything higher leads to an out-of-memory error on my 8GB GPU. What are some techniques to debug and manage the memory usage more effectively?"
186,pytorch_pytorch,issue,17501,"After testing on GPU, if I set the batch size low enough it runs very fast, but anything over 2 and it runs out of memory (8gb GPU). How would I go about debugging the memory usage?","I've observed that my model runs efficiently on the GPU with a batch size of 2 or lower, but increasing the batch size causes an out-of-memory error on my 8GB GPU. How can I troubleshoot and optimize the memory usage to support larger batch sizes?",Known,20,20,pytorch_pytorch,17501,issue,,1,1,1,1,"I've observed that my model runs efficiently on the GPU with a batch size of 2 or lower, but increasing the batch size causes an out-of-memory error on my 8GB GPU. How can I troubleshoot and optimize the memory usage to support larger batch sizes?"
187,pytorch_pytorch,issue,17501,"After testing on GPU, if I set the batch size low enough it runs very fast, but anything over 2 and it runs out of memory (8gb GPU). How would I go about debugging the memory usage?",I'm facing memory constraints on my 8GB GPU when I set the batch size above 2. The model runs fine with smaller batch sizes but crashes with larger ones. What are some strategies to debug and reduce the memory footprint?,Known,20,20,pytorch_pytorch,17501,issue,,1,1,1,1,I'm facing memory constraints on my 8GB GPU when I set the batch size above 2. The model runs fine with smaller batch sizes but crashes with larger ones. What are some strategies to debug and reduce the memory footprint?
188,pytorch_pytorch,issue,17501,"After testing on GPU, if I set the batch size low enough it runs very fast, but anything over 2 and it runs out of memory (8gb GPU). How would I go about debugging the memory usage?","My model runs smoothly on the GPU with a batch size of 2 or less, but anything higher results in an out-of-memory error on my 8GB GPU. How can I diagnose and optimize the memory usage to handle larger batch sizes?",Known,20,20,pytorch_pytorch,17501,issue,,1,1,1,1,"My model runs smoothly on the GPU with a batch size of 2 or less, but anything higher results in an out-of-memory error on my 8GB GPU. How can I diagnose and optimize the memory usage to handle larger batch sizes?"
189,pytorch_pytorch,issue,17501,"After testing on GPU, if I set the batch size low enough it runs very fast, but anything over 2 and it runs out of memory (8gb GPU). How would I go about debugging the memory usage?","I've noticed that my model runs very fast on the GPU with a batch size of 2 or lower, but increasing the batch size causes an out-of-memory error on my 8GB GPU. What are some methods to debug and manage the memory usage more effectively?",Known,20,20,pytorch_pytorch,17501,issue,,1,1,1,1,"I've noticed that my model runs very fast on the GPU with a batch size of 2 or lower, but increasing the batch size causes an out-of-memory error on my 8GB GPU. What are some methods to debug and manage the memory usage more effectively?"
190,pytorch_pytorch,issue,17501,"After testing on GPU, if I set the batch size low enough it runs very fast, but anything over 2 and it runs out of memory (8gb GPU). How would I go about debugging the memory usage?",I'm encountering out-of-memory errors on my 8GB GPU when I set the batch size above 2. The model runs well with smaller batch sizes but crashes with larger ones. How can I investigate and optimize the memory usage to support larger batches?,Known,20,20,pytorch_pytorch,17501,issue,,1,1,1,1,I'm encountering out-of-memory errors on my 8GB GPU when I set the batch size above 2. The model runs well with smaller batch sizes but crashes with larger ones. How can I investigate and optimize the memory usage to support larger batches?
191,pytorch_pytorch,issue,17501,The backward pass of an unfold will involved a lot of atomicAdd CUDA instructions in the unfolded dimension. That is what slows down the backward a lot.,"I noticed that the backward pass of an unfold operation involves numerous atomicAdd CUDA instructions in the unfolded dimension, which significantly slows down the process. Is this expected behavior, or is there a way to optimize it?",Knowledge,21,21,pytorch_pytorch,17501,issue,,0,0,0,0,"I noticed that the backward pass of an unfold operation involves numerous atomicAdd CUDA instructions in the unfolded dimension, which significantly slows down the process. Is this expected behavior, or is there a way to optimize it?"
192,pytorch_pytorch,issue,17501,The backward pass of an unfold will involved a lot of atomicAdd CUDA instructions in the unfolded dimension. That is what slows down the backward a lot.,"The backward pass of an unfold operation is quite slow, and I suspect it's due to the heavy use of atomicAdd CUDA instructions in the unfolded dimension. Can someone confirm if this is a known issue or if there are any workarounds?",Knowledge,21,21,pytorch_pytorch,17501,issue,,0,0,0,0,"The backward pass of an unfold operation is quite slow, and I suspect it's due to the heavy use of atomicAdd CUDA instructions in the unfolded dimension. Can someone confirm if this is a known issue or if there are any workarounds?"
193,pytorch_pytorch,issue,17501,The backward pass of an unfold will involved a lot of atomicAdd CUDA instructions in the unfolded dimension. That is what slows down the backward a lot.,"I'm experiencing a significant slowdown in the backward pass of an unfold operation, and I believe it's because of the many atomicAdd CUDA instructions being used in the unfolded dimension. Is this a common problem, or am I missing something in my implementation?",Knowledge,21,21,pytorch_pytorch,17501,issue,,0,0,0,0,"I'm experiencing a significant slowdown in the backward pass of an unfold operation, and I believe it's because of the many atomicAdd CUDA instructions being used in the unfolded dimension. Is this a common problem, or am I missing something in my implementation?"
194,pytorch_pytorch,issue,17501,The backward pass of an unfold will involved a lot of atomicAdd CUDA instructions in the unfolded dimension. That is what slows down the backward a lot.,"The backward pass of an unfold operation is taking a lot of time, and I traced it back to the extensive use of atomicAdd CUDA instructions in the unfolded dimension. Is this a known performance bottleneck, and are there any optimizations I can apply?",Knowledge,21,21,pytorch_pytorch,17501,issue,,0,0,0,0,"The backward pass of an unfold operation is taking a lot of time, and I traced it back to the extensive use of atomicAdd CUDA instructions in the unfolded dimension. Is this a known performance bottleneck, and are there any optimizations I can apply?"
195,pytorch_pytorch,issue,17501,The backward pass of an unfold will involved a lot of atomicAdd CUDA instructions in the unfolded dimension. That is what slows down the backward a lot.,"I've observed that the backward pass of an unfold operation is particularly slow, and it seems to be due to the large number of atomicAdd CUDA instructions in the unfolded dimension. Is this a known issue, or is there a way to improve the performance?",Knowledge,21,21,pytorch_pytorch,17501,issue,,0,0,0,0,"I've observed that the backward pass of an unfold operation is particularly slow, and it seems to be due to the large number of atomicAdd CUDA instructions in the unfolded dimension. Is this a known issue, or is there a way to improve the performance?"
196,pytorch_pytorch,issue,17501,The backward pass of an unfold will involved a lot of atomicAdd CUDA instructions in the unfolded dimension. That is what slows down the backward a lot.,"The backward pass of an unfold operation is much slower than expected, and I suspect it's because of the many atomicAdd CUDA instructions in the unfolded dimension. Can someone explain if this is a known limitation or if there are any optimizations available?",Knowledge,21,21,pytorch_pytorch,17501,issue,,0,0,0,0,"The backward pass of an unfold operation is much slower than expected, and I suspect it's because of the many atomicAdd CUDA instructions in the unfolded dimension. Can someone explain if this is a known limitation or if there are any optimizations available?"
197,pytorch_pytorch,issue,17501,The backward pass of an unfold will involved a lot of atomicAdd CUDA instructions in the unfolded dimension. That is what slows down the backward a lot.,"I'm seeing a significant performance hit in the backward pass of an unfold operation, and I think it's due to the heavy use of atomicAdd CUDA instructions in the unfolded dimension. Is this a known issue, or is there a way to optimize the backward pass?",Knowledge,21,21,pytorch_pytorch,17501,issue,,0,0,0,0,"I'm seeing a significant performance hit in the backward pass of an unfold operation, and I think it's due to the heavy use of atomicAdd CUDA instructions in the unfolded dimension. Is this a known issue, or is there a way to optimize the backward pass?"
198,pytorch_pytorch,issue,17501,The backward pass of an unfold will involved a lot of atomicAdd CUDA instructions in the unfolded dimension. That is what slows down the backward a lot.,"The backward pass of an unfold operation is quite slow, and I traced it back to the extensive use of atomicAdd CUDA instructions in the unfolded dimension. Is this a known performance issue, or is there a way to mitigate it?",Knowledge,21,21,pytorch_pytorch,17501,issue,,0,0,0,0,"The backward pass of an unfold operation is quite slow, and I traced it back to the extensive use of atomicAdd CUDA instructions in the unfolded dimension. Is this a known performance issue, or is there a way to mitigate it?"
199,pytorch_pytorch,issue,17501,The backward pass of an unfold will involved a lot of atomicAdd CUDA instructions in the unfolded dimension. That is what slows down the backward a lot.,"I'm experiencing a slowdown in the backward pass of an unfold operation, and I believe it's because of the many atomicAdd CUDA instructions in the unfolded dimension. Is this a known problem, or is there a way to optimize the backward pass?",Knowledge,21,21,pytorch_pytorch,17501,issue,,0,0,0,0,"I'm experiencing a slowdown in the backward pass of an unfold operation, and I believe it's because of the many atomicAdd CUDA instructions in the unfolded dimension. Is this a known problem, or is there a way to optimize the backward pass?"
200,pytorch_pytorch,issue,155309,The error is likely caused by how torch.export interacts with GPU-backed LSTM internals. It's possible that torch.export is not properly handling fused GPU kernels or CUDNN ops when fake tensors are involved during graph capture.,The error might be due to the way torch.export processes GPU-backed LSTM operations. It seems that torch.export may not be correctly managing fused GPU kernels or CUDNN operations when using fake tensors during the graph capture process.,Known,22,22,pytorch_pytorch,155309,issue,,1,1,1,1,The error might be due to the way torch.export processes GPU-backed LSTM operations. It seems that torch.export may not be correctly managing fused GPU kernels or CUDNN operations when using fake tensors during the graph capture process.
201,pytorch_pytorch,issue,155309,The error is likely caused by how torch.export interacts with GPU-backed LSTM internals. It's possible that torch.export is not properly handling fused GPU kernels or CUDNN ops when fake tensors are involved during graph capture.,"I suspect the issue is related to how torch.export handles the internal GPU operations of LSTM layers. It's possible that the export process is not properly dealing with fused GPU kernels or CUDNN operations, especially when fake tensors are used for graph capture.",Known,22,22,pytorch_pytorch,155309,issue,,1,1,1,1,"I suspect the issue is related to how torch.export handles the internal GPU operations of LSTM layers. It's possible that the export process is not properly dealing with fused GPU kernels or CUDNN operations, especially when fake tensors are used for graph capture."
202,pytorch_pytorch,issue,155309,The error is likely caused by how torch.export interacts with GPU-backed LSTM internals. It's possible that torch.export is not properly handling fused GPU kernels or CUDNN ops when fake tensors are involved during graph capture.,The error could be stemming from the interaction between torch.export and the GPU-backed LSTM internals. It appears that torch.export might not be correctly handling the fused GPU kernels or CUDNN operations when fake tensors are involved in the graph capture.,Known,22,22,pytorch_pytorch,155309,issue,,1,1,1,1,The error could be stemming from the interaction between torch.export and the GPU-backed LSTM internals. It appears that torch.export might not be correctly handling the fused GPU kernels or CUDNN operations when fake tensors are involved in the graph capture.
203,pytorch_pytorch,issue,155309,The error is likely caused by how torch.export interacts with GPU-backed LSTM internals. It's possible that torch.export is not properly handling fused GPU kernels or CUDNN ops when fake tensors are involved during graph capture.,"The problem might be that torch.export is not properly managing the GPU-specific operations of LSTM layers. It's possible that the export process is failing to handle fused GPU kernels or CUDNN operations correctly, particularly when fake tensors are used.",Known,22,22,pytorch_pytorch,155309,issue,,1,1,1,1,"The problem might be that torch.export is not properly managing the GPU-specific operations of LSTM layers. It's possible that the export process is failing to handle fused GPU kernels or CUDNN operations correctly, particularly when fake tensors are used."
204,pytorch_pytorch,issue,155309,The error is likely caused by how torch.export interacts with GPU-backed LSTM internals. It's possible that torch.export is not properly handling fused GPU kernels or CUDNN ops when fake tensors are involved during graph capture.,"I believe the error is likely caused by the way torch.export interacts with the GPU internals of LSTM layers. It seems that the export process might not be properly handling fused GPU kernels or CUDNN operations, especially when fake tensors are used during graph capture.",Known,22,22,pytorch_pytorch,155309,issue,,1,1,1,1,"I believe the error is likely caused by the way torch.export interacts with the GPU internals of LSTM layers. It seems that the export process might not be properly handling fused GPU kernels or CUDNN operations, especially when fake tensors are used during graph capture."
205,pytorch_pytorch,issue,155309,The error is likely caused by how torch.export interacts with GPU-backed LSTM internals. It's possible that torch.export is not properly handling fused GPU kernels or CUDNN ops when fake tensors are involved during graph capture.,"The issue might be due to the way torch.export processes the GPU-backed LSTM operations. It's possible that the export process is not correctly managing the fused GPU kernels or CUDNN operations, particularly when fake tensors are involved.",Known,22,22,pytorch_pytorch,155309,issue,,1,1,1,1,"The issue might be due to the way torch.export processes the GPU-backed LSTM operations. It's possible that the export process is not correctly managing the fused GPU kernels or CUDNN operations, particularly when fake tensors are involved."
206,pytorch_pytorch,issue,155309,The error is likely caused by how torch.export interacts with GPU-backed LSTM internals. It's possible that torch.export is not properly handling fused GPU kernels or CUDNN ops when fake tensors are involved during graph capture.,"The error could be a result of how torch.export handles the GPU-specific operations of LSTM layers. It seems that the export process might not be properly dealing with fused GPU kernels or CUDNN operations, especially when fake tensors are used.",Known,22,22,pytorch_pytorch,155309,issue,,1,1,1,1,"The error could be a result of how torch.export handles the GPU-specific operations of LSTM layers. It seems that the export process might not be properly dealing with fused GPU kernels or CUDNN operations, especially when fake tensors are used."
207,pytorch_pytorch,issue,155309,The error is likely caused by how torch.export interacts with GPU-backed LSTM internals. It's possible that torch.export is not properly handling fused GPU kernels or CUDNN ops when fake tensors are involved during graph capture.,"I suspect the problem is related to the interaction between torch.export and the GPU-backed LSTM internals. It's possible that the export process is not correctly handling the fused GPU kernels or CUDNN operations, particularly when fake tensors are involved.",Known,22,22,pytorch_pytorch,155309,issue,,1,1,1,1,"I suspect the problem is related to the interaction between torch.export and the GPU-backed LSTM internals. It's possible that the export process is not correctly handling the fused GPU kernels or CUDNN operations, particularly when fake tensors are involved."
208,pytorch_pytorch,issue,155309,The error is likely caused by how torch.export interacts with GPU-backed LSTM internals. It's possible that torch.export is not properly handling fused GPU kernels or CUDNN ops when fake tensors are involved during graph capture.,"The error might be caused by the way torch.export processes the GPU operations of LSTM layers. It seems that the export process might not be properly managing the fused GPU kernels or CUDNN operations, especially when fake tensors are used during graph capture.",Known,22,22,pytorch_pytorch,155309,issue,,1,1,1,1,"The error might be caused by the way torch.export processes the GPU operations of LSTM layers. It seems that the export process might not be properly managing the fused GPU kernels or CUDNN operations, especially when fake tensors are used during graph capture."
209,pytorch_pytorch,issue,155309,The error is likely caused by how torch.export interacts with GPU-backed LSTM internals. It's possible that torch.export is not properly handling fused GPU kernels or CUDNN ops when fake tensors are involved during graph capture.,"The issue could be due to the way torch.export interacts with the GPU-backed LSTM internals. It's possible that the export process is not correctly handling the fused GPU kernels or CUDNN operations, particularly when fake tensors are involved in the graph capture process.",Known,22,22,pytorch_pytorch,155309,issue,,1,1,1,1,"The issue could be due to the way torch.export interacts with the GPU-backed LSTM internals. It's possible that the export process is not correctly handling the fused GPU kernels or CUDNN operations, particularly when fake tensors are involved in the graph capture process."
210,pytorch_pytorch,issue,155309,"Thank you so much for the detailed and insightful analysis.

I followed your suggestion and tested with:

torch.backends.cudnn.enabled = False

As you anticipated, export on GPU succeeded when disabling cuDNN. This strongly indicates that the issue is indeed related to certain cuDNN-optimized fused kernels, likely being incorrectly traced during export when fake tensors are used.

For completeness, my environment reports the following cuDNN version:

torch.backends.cudnn.version() == 90100

This further supports the hypothesis that some low-level dispatch or fusion within cuDNN is not being properly marked as opaque or guarded during export tracing?

Additionally, I’d like to report that the same issue also occurs when using nn.RNN instead of nn.LSTM. Specifically, like:

self.rnn = nn.RNN(input_size, hidden_size, num_layers, batch_first=True)

This confirms that the problem is not limited to LSTM, but more broadly affects cuDNN-backed recurrent layers, including nn.RNN. Disabling torch.backends.cudnn.enabled continues to serve as a valid workaround in both cases.","Thank you for the thorough analysis. I followed your advice and disabled cuDNN by setting `torch.backends.cudnn.enabled = False`. As expected, the export on GPU worked without issues, confirming that the problem is related to cuDNN-optimized kernels. My environment uses cuDNN version 9.1, which suggests that some low-level cuDNN operations are not being properly traced during export. I also observed the same issue with nn.RNN, indicating that the problem affects cuDNN-backed recurrent layers in general.",Known,23,23,pytorch_pytorch,155309,issue,,1,1,1,1,"Thank you for the thorough analysis. I followed your advice and disabled cuDNN by setting `torch.backends.cudnn.enabled = False`. As expected, the export on GPU worked without issues, confirming that the problem is related to cuDNN-optimized kernels. My environment uses cuDNN version 9.1, which suggests that some low-level cuDNN operations are not being properly traced during export. I also observed the same issue with nn.RNN, indicating that the problem affects cuDNN-backed recurrent layers in general."
211,pytorch_pytorch,issue,155309,"Thank you so much for the detailed and insightful analysis.

I followed your suggestion and tested with:

torch.backends.cudnn.enabled = False

As you anticipated, export on GPU succeeded when disabling cuDNN. This strongly indicates that the issue is indeed related to certain cuDNN-optimized fused kernels, likely being incorrectly traced during export when fake tensors are used.

For completeness, my environment reports the following cuDNN version:

torch.backends.cudnn.version() == 90100

This further supports the hypothesis that some low-level dispatch or fusion within cuDNN is not being properly marked as opaque or guarded during export tracing?

Additionally, I’d like to report that the same issue also occurs when using nn.RNN instead of nn.LSTM. Specifically, like:

self.rnn = nn.RNN(input_size, hidden_size, num_layers, batch_first=True)

This confirms that the problem is not limited to LSTM, but more broadly affects cuDNN-backed recurrent layers, including nn.RNN. Disabling torch.backends.cudnn.enabled continues to serve as a valid workaround in both cases.","I appreciate the detailed explanation. Disabling cuDNN by setting `torch.backends.cudnn.enabled = False` resolved the export issue on GPU, as you predicted. This points to a problem with cuDNN-optimized kernels, particularly during export tracing with fake tensors. My cuDNN version is 9.1, which aligns with the hypothesis that certain cuDNN operations are not being correctly marked as opaque. Additionally, the issue persists with nn.RNN, confirming that it affects all cuDNN-backed recurrent layers.",Known,23,23,pytorch_pytorch,155309,issue,,1,1,1,1,"I appreciate the detailed explanation. Disabling cuDNN by setting `torch.backends.cudnn.enabled = False` resolved the export issue on GPU, as you predicted. This points to a problem with cuDNN-optimized kernels, particularly during export tracing with fake tensors. My cuDNN version is 9.1, which aligns with the hypothesis that certain cuDNN operations are not being correctly marked as opaque. Additionally, the issue persists with nn.RNN, confirming that it affects all cuDNN-backed recurrent layers."
212,pytorch_pytorch,issue,155309,"Thank you so much for the detailed and insightful analysis.

I followed your suggestion and tested with:

torch.backends.cudnn.enabled = False

As you anticipated, export on GPU succeeded when disabling cuDNN. This strongly indicates that the issue is indeed related to certain cuDNN-optimized fused kernels, likely being incorrectly traced during export when fake tensors are used.

For completeness, my environment reports the following cuDNN version:

torch.backends.cudnn.version() == 90100

This further supports the hypothesis that some low-level dispatch or fusion within cuDNN is not being properly marked as opaque or guarded during export tracing?

Additionally, I’d like to report that the same issue also occurs when using nn.RNN instead of nn.LSTM. Specifically, like:

self.rnn = nn.RNN(input_size, hidden_size, num_layers, batch_first=True)

This confirms that the problem is not limited to LSTM, but more broadly affects cuDNN-backed recurrent layers, including nn.RNN. Disabling torch.backends.cudnn.enabled continues to serve as a valid workaround in both cases.","Thank you for the insightful analysis. I tested the suggestion to disable cuDNN by setting `torch.backends.cudnn.enabled = False`, and the export on GPU succeeded. This strongly suggests that the issue is related to cuDNN-optimized kernels, possibly due to incorrect tracing of low-level operations. My environment uses cuDNN version 9.1, which supports this hypothesis. I also found that the same problem occurs with nn.RNN, indicating that it is not specific to LSTM but affects all cuDNN-backed recurrent layers.",Known,23,23,pytorch_pytorch,155309,issue,,1,1,1,1,"Thank you for the insightful analysis. I tested the suggestion to disable cuDNN by setting `torch.backends.cudnn.enabled = False`, and the export on GPU succeeded. This strongly suggests that the issue is related to cuDNN-optimized kernels, possibly due to incorrect tracing of low-level operations. My environment uses cuDNN version 9.1, which supports this hypothesis. I also found that the same problem occurs with nn.RNN, indicating that it is not specific to LSTM but affects all cuDNN-backed recurrent layers."
213,pytorch_pytorch,issue,155309,"Thank you so much for the detailed and insightful analysis.

I followed your suggestion and tested with:

torch.backends.cudnn.enabled = False

As you anticipated, export on GPU succeeded when disabling cuDNN. This strongly indicates that the issue is indeed related to certain cuDNN-optimized fused kernels, likely being incorrectly traced during export when fake tensors are used.

For completeness, my environment reports the following cuDNN version:

torch.backends.cudnn.version() == 90100

This further supports the hypothesis that some low-level dispatch or fusion within cuDNN is not being properly marked as opaque or guarded during export tracing?

Additionally, I’d like to report that the same issue also occurs when using nn.RNN instead of nn.LSTM. Specifically, like:

self.rnn = nn.RNN(input_size, hidden_size, num_layers, batch_first=True)

This confirms that the problem is not limited to LSTM, but more broadly affects cuDNN-backed recurrent layers, including nn.RNN. Disabling torch.backends.cudnn.enabled continues to serve as a valid workaround in both cases.","Your detailed analysis was very helpful. I followed your recommendation to disable cuDNN by setting `torch.backends.cudnn.enabled = False`, and the export on GPU worked as expected. This confirms that the issue is likely due to certain cuDNN-optimized kernels being incorrectly traced during export. My cuDNN version is 9.1, which further supports this hypothesis. Additionally, I observed the same issue with nn.RNN, suggesting that the problem is broader and affects all cuDNN-backed recurrent layers.",Known,23,23,pytorch_pytorch,155309,issue,,1,1,1,1,"Your detailed analysis was very helpful. I followed your recommendation to disable cuDNN by setting `torch.backends.cudnn.enabled = False`, and the export on GPU worked as expected. This confirms that the issue is likely due to certain cuDNN-optimized kernels being incorrectly traced during export. My cuDNN version is 9.1, which further supports this hypothesis. Additionally, I observed the same issue with nn.RNN, suggesting that the problem is broader and affects all cuDNN-backed recurrent layers."
214,pytorch_pytorch,issue,155309,"Thank you so much for the detailed and insightful analysis.

I followed your suggestion and tested with:

torch.backends.cudnn.enabled = False

As you anticipated, export on GPU succeeded when disabling cuDNN. This strongly indicates that the issue is indeed related to certain cuDNN-optimized fused kernels, likely being incorrectly traced during export when fake tensors are used.

For completeness, my environment reports the following cuDNN version:

torch.backends.cudnn.version() == 90100

This further supports the hypothesis that some low-level dispatch or fusion within cuDNN is not being properly marked as opaque or guarded during export tracing?

Additionally, I’d like to report that the same issue also occurs when using nn.RNN instead of nn.LSTM. Specifically, like:

self.rnn = nn.RNN(input_size, hidden_size, num_layers, batch_first=True)

This confirms that the problem is not limited to LSTM, but more broadly affects cuDNN-backed recurrent layers, including nn.RNN. Disabling torch.backends.cudnn.enabled continues to serve as a valid workaround in both cases.","Thank you for the comprehensive analysis. Disabling cuDNN by setting `torch.backends.cudnn.enabled = False` resolved the export issue on GPU, as you anticipated. This indicates that the problem is related to cuDNN-optimized kernels, possibly due to incorrect tracing of low-level operations. My cuDNN version is 9.1, which aligns with this hypothesis. I also found that the same issue occurs with nn.RNN, confirming that it affects all cuDNN-backed recurrent layers.",Known,23,23,pytorch_pytorch,155309,issue,,1,1,1,1,"Thank you for the comprehensive analysis. Disabling cuDNN by setting `torch.backends.cudnn.enabled = False` resolved the export issue on GPU, as you anticipated. This indicates that the problem is related to cuDNN-optimized kernels, possibly due to incorrect tracing of low-level operations. My cuDNN version is 9.1, which aligns with this hypothesis. I also found that the same issue occurs with nn.RNN, confirming that it affects all cuDNN-backed recurrent layers."
215,pytorch_pytorch,issue,155309,It is due to this line which is not PT2 compatible: ,"The issue stems from this line of code, which is not compatible with PyTorch 2.0.",Knowledge,24,24,pytorch_pytorch,155309,issue,,0,0,0,0,"The issue stems from this line of code, which is not compatible with PyTorch 2.0."
216,pytorch_pytorch,issue,155309,It is due to this line which is not PT2 compatible: ,This specific line is causing the problem because it is not PT2 compliant.,Knowledge,24,24,pytorch_pytorch,155309,issue,,0,0,0,0,This specific line is causing the problem because it is not PT2 compliant.
217,pytorch_pytorch,issue,155309,It is due to this line which is not PT2 compatible: ,"The incompatibility is due to this particular line, which does not work with PyTorch 2.0.",Knowledge,24,24,pytorch_pytorch,155309,issue,,0,0,0,0,"The incompatibility is due to this particular line, which does not work with PyTorch 2.0."
218,pytorch_pytorch,issue,155309,It is due to this line which is not PT2 compatible: ,"The root cause of the issue is this line, which is not supported in PyTorch 2.0.",Knowledge,24,24,pytorch_pytorch,155309,issue,,0,0,0,0,"The root cause of the issue is this line, which is not supported in PyTorch 2.0."
219,pytorch_pytorch,issue,155309,It is due to this line which is not PT2 compatible: ,"This line of code is the culprit, as it is not compatible with the latest PyTorch 2.0 release.",Knowledge,24,24,pytorch_pytorch,155309,issue,,0,0,0,0,"This line of code is the culprit, as it is not compatible with the latest PyTorch 2.0 release."
220,pytorch_pytorch,issue,155309,It is due to this line which is not PT2 compatible: ,"The problem arises from this line, which is not PT2 compatible.",Knowledge,24,24,pytorch_pytorch,155309,issue,,0,0,0,0,"The problem arises from this line, which is not PT2 compatible."
221,pytorch_pytorch,issue,155309,It is due to this line which is not PT2 compatible: ,"The issue is caused by this line, which is not compatible with the new PyTorch 2.0 version.",Knowledge,24,24,pytorch_pytorch,155309,issue,,0,0,0,0,"The issue is caused by this line, which is not compatible with the new PyTorch 2.0 version."
222,pytorch_pytorch,issue,155309,It is due to this line which is not PT2 compatible: ,"This line is the source of the problem, as it is not compatible with PyTorch 2.0.",Knowledge,24,24,pytorch_pytorch,155309,issue,,0,0,0,0,"This line is the source of the problem, as it is not compatible with PyTorch 2.0."
223,pytorch_pytorch,issue,155309,It is due to this line which is not PT2 compatible: ,"The incompatibility is due to this specific line, which does not work with PyTorch 2.0.",Knowledge,24,24,pytorch_pytorch,155309,issue,,0,0,0,0,"The incompatibility is due to this specific line, which does not work with PyTorch 2.0."
224,pytorch_pytorch,issue,155309,It is due to this line which is not PT2 compatible: ,"The issue is rooted in this line, which is not PT2 compatible.",Knowledge,24,24,pytorch_pytorch,155309,issue,,0,0,0,0,"The issue is rooted in this line, which is not PT2 compatible."
225,pytorch_pytorch,issue,155309,I think we need a better way to detect aliasing here.,I believe we should improve our method for detecting aliasing in this part of the code.,Known,25,25,pytorch_pytorch,155309,issue,,1,1,1,1,I believe we should improve our method for detecting aliasing in this part of the code.
226,pytorch_pytorch,issue,155309,I think we need a better way to detect aliasing here.,It seems like we require a more robust approach to identify aliasing issues in this section.,Known,25,25,pytorch_pytorch,155309,issue,,1,1,1,1,It seems like we require a more robust approach to identify aliasing issues in this section.
227,pytorch_pytorch,issue,155309,I think we need a better way to detect aliasing here.,I'm not sure if our current method for detecting aliasing is sufficient; we might need a better solution.,Known,25,25,pytorch_pytorch,155309,issue,,1,1,1,1,I'm not sure if our current method for detecting aliasing is sufficient; we might need a better solution.
228,pytorch_pytorch,issue,155309,I think we need a better way to detect aliasing here.,I think our current aliasing detection technique is lacking and we need a more effective way to handle it.,Known,25,25,pytorch_pytorch,155309,issue,,1,1,1,1,I think our current aliasing detection technique is lacking and we need a more effective way to handle it.
229,pytorch_pytorch,issue,155309,I think we need a better way to detect aliasing here.,We might need to rethink our aliasing detection strategy to ensure it's more accurate and reliable.,Known,25,25,pytorch_pytorch,155309,issue,,1,1,1,1,We might need to rethink our aliasing detection strategy to ensure it's more accurate and reliable.
230,pytorch_pytorch,issue,155309,I think we need a better way to detect aliasing here.,I feel that our current approach to detecting aliasing isn't robust enough and could benefit from an upgrade.,Known,25,25,pytorch_pytorch,155309,issue,,1,1,1,1,I feel that our current approach to detecting aliasing isn't robust enough and could benefit from an upgrade.
231,pytorch_pytorch,issue,155309,I think we need a better way to detect aliasing here.,I'm concerned that our aliasing detection isn't as effective as it should be; we need a better method.,Known,25,25,pytorch_pytorch,155309,issue,,1,1,1,1,I'm concerned that our aliasing detection isn't as effective as it should be; we need a better method.
232,pytorch_pytorch,issue,155309,I think we need a better way to detect aliasing here.,I think we should explore alternative methods for detecting aliasing to improve the reliability of our code.,Known,25,25,pytorch_pytorch,155309,issue,,1,1,1,1,I think we should explore alternative methods for detecting aliasing to improve the reliability of our code.
233,pytorch_pytorch,issue,155309,I think we need a better way to detect aliasing here.,Our current aliasing detection seems inadequate; we need a more sophisticated approach to ensure correctness.,Known,25,25,pytorch_pytorch,155309,issue,,1,1,1,1,Our current aliasing detection seems inadequate; we need a more sophisticated approach to ensure correctness.
234,pytorch_pytorch,issue,155309,I think we need a better way to detect aliasing here.,I believe our aliasing detection needs to be more advanced to catch all potential issues in this code.,Known,25,25,pytorch_pytorch,155309,issue,,1,1,1,1,I believe our aliasing detection needs to be more advanced to catch all potential issues in this code.
235,pytorch_pytorch,pull,155734,"torch.export.export() uses fake tensors during graph tracing, which don't have actual memory storage. The RNN module's flatten_parameters() method was calling p.data_ptr() for aliasing detection, causing ""Cannot access data pointer of Tensor"" errors on GPU.","I noticed that torch.export.export() uses fake tensors for graph tracing, which don't have actual memory storage. This causes issues with the RNN module's flatten_parameters() method, as it tries to call p.data_ptr() for aliasing detection, leading to 'Cannot access data pointer of Tensor' errors on the GPU.",Knowledge,26,26,pytorch_pytorch,155734,pull,,0,0,0,0,"I noticed that torch.export.export() uses fake tensors for graph tracing, which don't have actual memory storage. This causes issues with the RNN module's flatten_parameters() method, as it tries to call p.data_ptr() for aliasing detection, leading to 'Cannot access data pointer of Tensor' errors on the GPU."
236,pytorch_pytorch,pull,155734,"torch.export.export() uses fake tensors during graph tracing, which don't have actual memory storage. The RNN module's flatten_parameters() method was calling p.data_ptr() for aliasing detection, causing ""Cannot access data pointer of Tensor"" errors on GPU.","When using torch.export.export(), the RNN module's flatten_parameters() method fails because it calls p.data_ptr() on fake tensors, which don't have actual memory storage. This results in 'Cannot access data pointer of Tensor' errors on the GPU during graph tracing.",Knowledge,26,26,pytorch_pytorch,155734,pull,,0,0,0,0,"When using torch.export.export(), the RNN module's flatten_parameters() method fails because it calls p.data_ptr() on fake tensors, which don't have actual memory storage. This results in 'Cannot access data pointer of Tensor' errors on the GPU during graph tracing."
237,pytorch_pytorch,pull,155734,"torch.export.export() uses fake tensors during graph tracing, which don't have actual memory storage. The RNN module's flatten_parameters() method was calling p.data_ptr() for aliasing detection, causing ""Cannot access data pointer of Tensor"" errors on GPU.","I encountered an error when using torch.export.export() with an RNN module. The flatten_parameters() method calls p.data_ptr() for aliasing detection, but since the fake tensors used in graph tracing don't have actual memory storage, it results in 'Cannot access data pointer of Tensor' errors on the GPU.",Knowledge,26,26,pytorch_pytorch,155734,pull,,0,0,0,0,"I encountered an error when using torch.export.export() with an RNN module. The flatten_parameters() method calls p.data_ptr() for aliasing detection, but since the fake tensors used in graph tracing don't have actual memory storage, it results in 'Cannot access data pointer of Tensor' errors on the GPU."
238,pytorch_pytorch,pull,155734,"torch.export.export() uses fake tensors during graph tracing, which don't have actual memory storage. The RNN module's flatten_parameters() method was calling p.data_ptr() for aliasing detection, causing ""Cannot access data pointer of Tensor"" errors on GPU.","The RNN module's flatten_parameters() method is causing issues when used with torch.export.export(). It tries to call p.data_ptr() on fake tensors, which don't have actual memory storage, leading to 'Cannot access data pointer of Tensor' errors on the GPU.",Knowledge,26,26,pytorch_pytorch,155734,pull,,0,0,0,0,"The RNN module's flatten_parameters() method is causing issues when used with torch.export.export(). It tries to call p.data_ptr() on fake tensors, which don't have actual memory storage, leading to 'Cannot access data pointer of Tensor' errors on the GPU."
239,pytorch_pytorch,pull,155734,"torch.export.export() uses fake tensors during graph tracing, which don't have actual memory storage. The RNN module's flatten_parameters() method was calling p.data_ptr() for aliasing detection, causing ""Cannot access data pointer of Tensor"" errors on GPU.","I'm facing a problem with torch.export.export() where the RNN module's flatten_parameters() method attempts to call p.data_ptr() on fake tensors. Since these tensors don't have actual memory storage, it results in 'Cannot access data pointer of Tensor' errors on the GPU.",Knowledge,26,26,pytorch_pytorch,155734,pull,,0,0,0,0,"I'm facing a problem with torch.export.export() where the RNN module's flatten_parameters() method attempts to call p.data_ptr() on fake tensors. Since these tensors don't have actual memory storage, it results in 'Cannot access data pointer of Tensor' errors on the GPU."
240,pytorch_pytorch,pull,155734,"torch.export.export() uses fake tensors during graph tracing, which don't have actual memory storage. The RNN module's flatten_parameters() method was calling p.data_ptr() for aliasing detection, causing ""Cannot access data pointer of Tensor"" errors on GPU.","When I use torch.export.export() with an RNN module, the flatten_parameters() method fails because it calls p.data_ptr() on fake tensors, which don't have actual memory storage. This leads to 'Cannot access data pointer of Tensor' errors on the GPU.",Knowledge,26,26,pytorch_pytorch,155734,pull,,0,0,0,0,"When I use torch.export.export() with an RNN module, the flatten_parameters() method fails because it calls p.data_ptr() on fake tensors, which don't have actual memory storage. This leads to 'Cannot access data pointer of Tensor' errors on the GPU."
241,pytorch_pytorch,pull,155734,"torch.export.export() uses fake tensors during graph tracing, which don't have actual memory storage. The RNN module's flatten_parameters() method was calling p.data_ptr() for aliasing detection, causing ""Cannot access data pointer of Tensor"" errors on GPU.","I'm getting 'Cannot access data pointer of Tensor' errors on the GPU when using torch.export.export() with an RNN module. The issue arises because the flatten_parameters() method calls p.data_ptr() on fake tensors, which don't have actual memory storage.",Knowledge,26,26,pytorch_pytorch,155734,pull,,0,0,0,0,"I'm getting 'Cannot access data pointer of Tensor' errors on the GPU when using torch.export.export() with an RNN module. The issue arises because the flatten_parameters() method calls p.data_ptr() on fake tensors, which don't have actual memory storage."
242,pytorch_pytorch,pull,155734,"torch.export.export() uses fake tensors during graph tracing, which don't have actual memory storage. The RNN module's flatten_parameters() method was calling p.data_ptr() for aliasing detection, causing ""Cannot access data pointer of Tensor"" errors on GPU.","The RNN module's flatten_parameters() method is causing 'Cannot access data pointer of Tensor' errors on the GPU when used with torch.export.export(). This is because the method calls p.data_ptr() on fake tensors, which don't have actual memory storage.",Knowledge,26,26,pytorch_pytorch,155734,pull,,0,0,0,0,"The RNN module's flatten_parameters() method is causing 'Cannot access data pointer of Tensor' errors on the GPU when used with torch.export.export(). This is because the method calls p.data_ptr() on fake tensors, which don't have actual memory storage."
243,pytorch_pytorch,pull,155734,"torch.export.export() uses fake tensors during graph tracing, which don't have actual memory storage. The RNN module's flatten_parameters() method was calling p.data_ptr() for aliasing detection, causing ""Cannot access data pointer of Tensor"" errors on GPU.","I'm running into 'Cannot access data pointer of Tensor' errors on the GPU when using torch.export.export() with an RNN module. The flatten_parameters() method calls p.data_ptr() on fake tensors, which don't have actual memory storage, leading to these errors.",Knowledge,26,26,pytorch_pytorch,155734,pull,,0,0,0,0,"I'm running into 'Cannot access data pointer of Tensor' errors on the GPU when using torch.export.export() with an RNN module. The flatten_parameters() method calls p.data_ptr() on fake tensors, which don't have actual memory storage, leading to these errors."
244,pytorch_pytorch,issue,155218,"I'm wondering if there is a difference in the cuDNN version here, what does torch.backends.cudnn.version() show in your setup?",I'm curious if the cuDNN version might be causing this issue. Could you check what `torch.backends.cudnn.version()` returns in your environment?,Irrelevant,27,27,pytorch_pytorch,155218,issue,,1,2,2,1,I'm curious if the cuDNN version might be causing this issue. Could you check what `torch.backends.cudnn.version()` returns in your environment?
245,pytorch_pytorch,issue,155218,"I'm wondering if there is a difference in the cuDNN version here, what does torch.backends.cudnn.version() show in your setup?",I'm not sure if the cuDNN version is the same on your system. Can you run `torch.backends.cudnn.version()` and let me know the result?,Irrelevant,27,27,pytorch_pytorch,155218,issue,,1,2,2,1,I'm not sure if the cuDNN version is the same on your system. Can you run `torch.backends.cudnn.version()` and let me know the result?
246,pytorch_pytorch,issue,155218,"I'm wondering if there is a difference in the cuDNN version here, what does torch.backends.cudnn.version() show in your setup?",I'm trying to figure out if the cuDNN version could be the source of the problem. What does `torch.backends.cudnn.version()` return for you?,Irrelevant,27,27,pytorch_pytorch,155218,issue,,1,2,2,1,I'm trying to figure out if the cuDNN version could be the source of the problem. What does `torch.backends.cudnn.version()` return for you?
247,pytorch_pytorch,issue,155218,"I'm wondering if there is a difference in the cuDNN version here, what does torch.backends.cudnn.version() show in your setup?",I'm wondering if the cuDNN version might be different in your setup. Could you verify by running `torch.backends.cudnn.version()`?,Irrelevant,27,27,pytorch_pytorch,155218,issue,,1,2,2,1,I'm wondering if the cuDNN version might be different in your setup. Could you verify by running `torch.backends.cudnn.version()`?
248,pytorch_pytorch,issue,155218,"I'm wondering if there is a difference in the cuDNN version here, what does torch.backends.cudnn.version() show in your setup?",I'm unsure if the cuDNN version is consistent between our setups. Could you check the version using `torch.backends.cudnn.version()`?,Irrelevant,27,27,pytorch_pytorch,155218,issue,,1,2,2,1,I'm unsure if the cuDNN version is consistent between our setups. Could you check the version using `torch.backends.cudnn.version()`?
249,pytorch_pytorch,issue,155218,"I'm wondering if there is a difference in the cuDNN version here, what does torch.backends.cudnn.version() show in your setup?",I'm investigating if the cuDNN version could be causing the discrepancy. What does `torch.backends.cudnn.version()` show on your end?,Irrelevant,27,27,pytorch_pytorch,155218,issue,,1,2,2,1,I'm investigating if the cuDNN version could be causing the discrepancy. What does `torch.backends.cudnn.version()` show on your end?
250,pytorch_pytorch,issue,155218,"I'm wondering if there is a difference in the cuDNN version here, what does torch.backends.cudnn.version() show in your setup?",I'm considering if the cuDNN version might be a factor. Could you run `torch.backends.cudnn.version()` and share the output?,Irrelevant,27,27,pytorch_pytorch,155218,issue,,1,2,2,1,I'm considering if the cuDNN version might be a factor. Could you run `torch.backends.cudnn.version()` and share the output?
251,pytorch_pytorch,issue,155218,"I'm wondering if there is a difference in the cuDNN version here, what does torch.backends.cudnn.version() show in your setup?",I'm exploring if the cuDNN version is the same in your environment. What does `torch.backends.cudnn.version()` return for you?,Irrelevant,27,27,pytorch_pytorch,155218,issue,,1,2,2,1,I'm exploring if the cuDNN version is the same in your environment. What does `torch.backends.cudnn.version()` return for you?
252,pytorch_pytorch,issue,155218,"I'm wondering if there is a difference in the cuDNN version here, what does torch.backends.cudnn.version() show in your setup?",I'm checking if the cuDNN version could be the issue. Could you confirm by running `torch.backends.cudnn.version()`?,Irrelevant,27,27,pytorch_pytorch,155218,issue,,1,2,2,1,I'm checking if the cuDNN version could be the issue. Could you confirm by running `torch.backends.cudnn.version()`?
253,pytorch_pytorch,issue,155218,"I'm wondering if there is a difference in the cuDNN version here, what does torch.backends.cudnn.version() show in your setup?",I'm trying to rule out the cuDNN version as a potential cause. What does `torch.backends.cudnn.version()` show in your setup?,Irrelevant,27,27,pytorch_pytorch,155218,issue,,1,2,2,1,I'm trying to rule out the cuDNN version as a potential cause. What does `torch.backends.cudnn.version()` show in your setup?
254,pytorch_pytorch,issue,154721,"torch.export.export seems to produce invalid code for Tensor.split when used with meta device.

I am not sure if this is something that needs to be fixed, but here is the report anyway.","I noticed that torch.export.export generates incorrect code when using Tensor.split with the meta device. I'm unsure if this is a known issue or if it needs to be addressed, but I wanted to report it just in case.",Irrelevant,28,28,pytorch_pytorch,154721,issue,,1,2,2,1,"I noticed that torch.export.export generates incorrect code when using Tensor.split with the meta device. I'm unsure if this is a known issue or if it needs to be addressed, but I wanted to report it just in case."
255,pytorch_pytorch,issue,154721,"torch.export.export seems to produce invalid code for Tensor.split when used with meta device.

I am not sure if this is something that needs to be fixed, but here is the report anyway.","When I use torch.export.export with Tensor.split and the meta device, the generated code appears to be invalid. I'm not certain if this is a bug or if there's a specific reason for this behavior, but I thought it was worth mentioning.",Irrelevant,28,28,pytorch_pytorch,154721,issue,,1,2,2,1,"When I use torch.export.export with Tensor.split and the meta device, the generated code appears to be invalid. I'm not certain if this is a bug or if there's a specific reason for this behavior, but I thought it was worth mentioning."
256,pytorch_pytorch,issue,154721,"torch.export.export seems to produce invalid code for Tensor.split when used with meta device.

I am not sure if this is something that needs to be fixed, but here is the report anyway.","I encountered an issue where torch.export.export produces invalid code when Tensor.split is used with the meta device. I'm not sure if this is a bug or if there's a workaround, but I wanted to document it for further investigation.",Irrelevant,28,28,pytorch_pytorch,154721,issue,,1,2,2,1,"I encountered an issue where torch.export.export produces invalid code when Tensor.split is used with the meta device. I'm not sure if this is a bug or if there's a workaround, but I wanted to document it for further investigation."
257,pytorch_pytorch,issue,154721,"torch.export.export seems to produce invalid code for Tensor.split when used with meta device.

I am not sure if this is something that needs to be fixed, but here is the report anyway.","It seems that torch.export.export does not handle Tensor.split correctly when the meta device is involved. The generated code is invalid, and I'm not sure if this is a known limitation or a bug that needs to be fixed.",Irrelevant,28,28,pytorch_pytorch,154721,issue,,1,2,2,1,"It seems that torch.export.export does not handle Tensor.split correctly when the meta device is involved. The generated code is invalid, and I'm not sure if this is a known limitation or a bug that needs to be fixed."
258,pytorch_pytorch,issue,154721,"torch.export.export seems to produce invalid code for Tensor.split when used with meta device.

I am not sure if this is something that needs to be fixed, but here is the report anyway.","I'm seeing invalid code generation from torch.export.export when using Tensor.split with the meta device. I'm not certain if this is a bug or if there's a specific reason for this behavior, but I wanted to report it.",Irrelevant,28,28,pytorch_pytorch,154721,issue,,1,2,2,1,"I'm seeing invalid code generation from torch.export.export when using Tensor.split with the meta device. I'm not certain if this is a bug or if there's a specific reason for this behavior, but I wanted to report it."
259,pytorch_pytorch,issue,154721,"torch.export.export seems to produce invalid code for Tensor.split when used with meta device.

I am not sure if this is something that needs to be fixed, but here is the report anyway.","When I tried to use torch.export.export with Tensor.split and the meta device, the resulting code was invalid. I'm not sure if this is a bug or if there's a known issue with this combination, but I thought it was worth reporting.",Irrelevant,28,28,pytorch_pytorch,154721,issue,,1,2,2,1,"When I tried to use torch.export.export with Tensor.split and the meta device, the resulting code was invalid. I'm not sure if this is a bug or if there's a known issue with this combination, but I thought it was worth reporting."
260,pytorch_pytorch,issue,154721,"torch.export.export seems to produce invalid code for Tensor.split when used with meta device.

I am not sure if this is something that needs to be fixed, but here is the report anyway.","I found that torch.export.export generates invalid code when Tensor.split is used with the meta device. I'm not sure if this is a bug or if there's a specific reason for this, but I wanted to bring it to your attention.",Irrelevant,28,28,pytorch_pytorch,154721,issue,,1,2,2,1,"I found that torch.export.export generates invalid code when Tensor.split is used with the meta device. I'm not sure if this is a bug or if there's a specific reason for this, but I wanted to bring it to your attention."
261,pytorch_pytorch,issue,154721,"torch.export.export seems to produce invalid code for Tensor.split when used with meta device.

I am not sure if this is something that needs to be fixed, but here is the report anyway.","I ran into an issue where torch.export.export produces invalid code for Tensor.split when the meta device is used. I'm not sure if this is a known problem or if it needs to be fixed, but I wanted to report it.",Irrelevant,28,28,pytorch_pytorch,154721,issue,,1,2,2,1,"I ran into an issue where torch.export.export produces invalid code for Tensor.split when the meta device is used. I'm not sure if this is a known problem or if it needs to be fixed, but I wanted to report it."
262,pytorch_pytorch,issue,154721,"torch.export.export seems to produce invalid code for Tensor.split when used with meta device.

I am not sure if this is something that needs to be fixed, but here is the report anyway.","It appears that torch.export.export does not work correctly with Tensor.split and the meta device, as the generated code is invalid. I'm not sure if this is a bug or if there's a specific reason for this behavior, but I wanted to document it.",Irrelevant,28,28,pytorch_pytorch,154721,issue,,1,2,2,1,"It appears that torch.export.export does not work correctly with Tensor.split and the meta device, as the generated code is invalid. I'm not sure if this is a bug or if there's a specific reason for this behavior, but I wanted to document it."
263,pytorch_pytorch,issue,154721,"torch.export.export seems to produce invalid code for Tensor.split when used with meta device.

I am not sure if this is something that needs to be fixed, but here is the report anyway.","I noticed that using torch.export.export with Tensor.split and the meta device results in invalid code. I'm not sure if this is a bug or if there's a known issue, but I thought it was worth reporting.",Irrelevant,28,28,pytorch_pytorch,154721,issue,,1,2,2,1,"I noticed that using torch.export.export with Tensor.split and the meta device results in invalid code. I'm not sure if this is a bug or if there's a known issue, but I thought it was worth reporting."
264,pytorch_pytorch,issue,154456,"I'm running torch.compile on a transformer-based model. When I run the compiled function, I don't get any recompiles for subsequent calls in the same script, for example:

m = torch.compile(self.transformer.forward, fullgraph=True, dynamic=True, mode=""max-autotune"", disable=False)
m(...)
m(...)

works fine, with no recompiles. But when I run the script again, the torch.compile warmup takes a reasonably long time (running twice each for batch sizes 1, 2, and 4) -- about 10 seconds per input batch size. I ran the script with TORCH_LOGS=+dynamo,+inductor,+recompiles,+aot, and it seems like most of the time is spent tracing the code and running shape solving, even though the model code is unchanged.

Files are written to the disk cache, and the disk cache does speed up compile times, but I'm not sure why it still takes so long to trace and run the shape solver when the model code is unchanged, are the traced graphs not cached?","I'm using torch.compile on a transformer model, and while the initial compilation works fine without recompiles in the same script, subsequent runs of the script take a long time to warm up. Despite the disk cache being used, the tracing and shape solving still take a significant amount of time, even though the model code hasn't changed. I'm not sure if the traced graphs are being cached effectively.",Unknown,29,29,pytorch_pytorch,154456,issue,,1,3,3,1,"I'm using torch.compile on a transformer model, and while the initial compilation works fine without recompiles in the same script, subsequent runs of the script take a long time to warm up. Despite the disk cache being used, the tracing and shape solving still take a significant amount of time, even though the model code hasn't changed. I'm not sure if the traced graphs are being cached effectively."
265,pytorch_pytorch,issue,154456,"I'm running torch.compile on a transformer-based model. When I run the compiled function, I don't get any recompiles for subsequent calls in the same script, for example:

m = torch.compile(self.transformer.forward, fullgraph=True, dynamic=True, mode=""max-autotune"", disable=False)
m(...)
m(...)

works fine, with no recompiles. But when I run the script again, the torch.compile warmup takes a reasonably long time (running twice each for batch sizes 1, 2, and 4) -- about 10 seconds per input batch size. I ran the script with TORCH_LOGS=+dynamo,+inductor,+recompiles,+aot, and it seems like most of the time is spent tracing the code and running shape solving, even though the model code is unchanged.

Files are written to the disk cache, and the disk cache does speed up compile times, but I'm not sure why it still takes so long to trace and run the shape solver when the model code is unchanged, are the traced graphs not cached?","I've noticed that when I use torch.compile on my transformer model, the first run of the script compiles the model without any recompiles. However, when I run the script again, the warmup process is quite slow, taking around 10 seconds per batch size. I've enabled detailed logging, and it shows that most of the time is spent on tracing and shape solving, even though the model code remains the same. I'm unsure why the traced graphs aren't being cached more efficiently.",Unknown,29,29,pytorch_pytorch,154456,issue,,1,3,3,1,"I've noticed that when I use torch.compile on my transformer model, the first run of the script compiles the model without any recompiles. However, when I run the script again, the warmup process is quite slow, taking around 10 seconds per batch size. I've enabled detailed logging, and it shows that most of the time is spent on tracing and shape solving, even though the model code remains the same. I'm unsure why the traced graphs aren't being cached more efficiently."
266,pytorch_pytorch,issue,154456,"I'm running torch.compile on a transformer-based model. When I run the compiled function, I don't get any recompiles for subsequent calls in the same script, for example:

m = torch.compile(self.transformer.forward, fullgraph=True, dynamic=True, mode=""max-autotune"", disable=False)
m(...)
m(...)

works fine, with no recompiles. But when I run the script again, the torch.compile warmup takes a reasonably long time (running twice each for batch sizes 1, 2, and 4) -- about 10 seconds per input batch size. I ran the script with TORCH_LOGS=+dynamo,+inductor,+recompiles,+aot, and it seems like most of the time is spent tracing the code and running shape solving, even though the model code is unchanged.

Files are written to the disk cache, and the disk cache does speed up compile times, but I'm not sure why it still takes so long to trace and run the shape solver when the model code is unchanged, are the traced graphs not cached?","I'm experiencing long warmup times when using torch.compile on a transformer model, even though the model code hasn't changed between runs. The first run of the script compiles the model without recompiles, but subsequent runs take a long time, with most of the time spent on tracing and shape solving. I've checked the logs and confirmed that the disk cache is being used, but I'm not sure why the traced graphs aren't being reused.",Unknown,29,29,pytorch_pytorch,154456,issue,,1,3,3,1,"I'm experiencing long warmup times when using torch.compile on a transformer model, even though the model code hasn't changed between runs. The first run of the script compiles the model without recompiles, but subsequent runs take a long time, with most of the time spent on tracing and shape solving. I've checked the logs and confirmed that the disk cache is being used, but I'm not sure why the traced graphs aren't being reused."
267,pytorch_pytorch,issue,154456,"I'm running torch.compile on a transformer-based model. When I run the compiled function, I don't get any recompiles for subsequent calls in the same script, for example:

m = torch.compile(self.transformer.forward, fullgraph=True, dynamic=True, mode=""max-autotune"", disable=False)
m(...)
m(...)

works fine, with no recompiles. But when I run the script again, the torch.compile warmup takes a reasonably long time (running twice each for batch sizes 1, 2, and 4) -- about 10 seconds per input batch size. I ran the script with TORCH_LOGS=+dynamo,+inductor,+recompiles,+aot, and it seems like most of the time is spent tracing the code and running shape solving, even though the model code is unchanged.

Files are written to the disk cache, and the disk cache does speed up compile times, but I'm not sure why it still takes so long to trace and run the shape solver when the model code is unchanged, are the traced graphs not cached?","I'm using torch.compile on a transformer model, and while the initial compilation works without recompiles in the same script, the warmup time for subsequent runs is quite long. Despite the disk cache being utilized, the tracing and shape solving processes still take a significant amount of time. I'm not sure if the traced graphs are being cached properly, given that the model code is unchanged.",Unknown,29,29,pytorch_pytorch,154456,issue,,1,3,3,1,"I'm using torch.compile on a transformer model, and while the initial compilation works without recompiles in the same script, the warmup time for subsequent runs is quite long. Despite the disk cache being utilized, the tracing and shape solving processes still take a significant amount of time. I'm not sure if the traced graphs are being cached properly, given that the model code is unchanged."
268,pytorch_pytorch,issue,154456,"I'm running torch.compile on a transformer-based model. When I run the compiled function, I don't get any recompiles for subsequent calls in the same script, for example:

m = torch.compile(self.transformer.forward, fullgraph=True, dynamic=True, mode=""max-autotune"", disable=False)
m(...)
m(...)

works fine, with no recompiles. But when I run the script again, the torch.compile warmup takes a reasonably long time (running twice each for batch sizes 1, 2, and 4) -- about 10 seconds per input batch size. I ran the script with TORCH_LOGS=+dynamo,+inductor,+recompiles,+aot, and it seems like most of the time is spent tracing the code and running shape solving, even though the model code is unchanged.

Files are written to the disk cache, and the disk cache does speed up compile times, but I'm not sure why it still takes so long to trace and run the shape solver when the model code is unchanged, are the traced graphs not cached?","I've observed that torch.compile on my transformer model works well in the first run of the script, with no recompiles. However, when I run the script again, the warmup process is slow, taking about 10 seconds per batch size. The logs indicate that most of the time is spent on tracing and shape solving, even though the model code hasn't changed. I'm unsure why the traced graphs aren't being cached more effectively.",Unknown,29,29,pytorch_pytorch,154456,issue,,1,3,3,1,"I've observed that torch.compile on my transformer model works well in the first run of the script, with no recompiles. However, when I run the script again, the warmup process is slow, taking about 10 seconds per batch size. The logs indicate that most of the time is spent on tracing and shape solving, even though the model code hasn't changed. I'm unsure why the traced graphs aren't being cached more effectively."
269,pytorch_pytorch,issue,154456,"I'm running torch.compile on a transformer-based model. When I run the compiled function, I don't get any recompiles for subsequent calls in the same script, for example:

m = torch.compile(self.transformer.forward, fullgraph=True, dynamic=True, mode=""max-autotune"", disable=False)
m(...)
m(...)

works fine, with no recompiles. But when I run the script again, the torch.compile warmup takes a reasonably long time (running twice each for batch sizes 1, 2, and 4) -- about 10 seconds per input batch size. I ran the script with TORCH_LOGS=+dynamo,+inductor,+recompiles,+aot, and it seems like most of the time is spent tracing the code and running shape solving, even though the model code is unchanged.

Files are written to the disk cache, and the disk cache does speed up compile times, but I'm not sure why it still takes so long to trace and run the shape solver when the model code is unchanged, are the traced graphs not cached?","I'm using torch.compile on a transformer model, and while the initial compilation works fine without recompiles in the same script, subsequent runs of the script take a long time to warm up. The logs show that most of the time is spent on tracing and shape solving, even though the model code is unchanged. I'm not sure if the traced graphs are being cached properly, despite the disk cache being used.",Unknown,29,29,pytorch_pytorch,154456,issue,,1,3,3,1,"I'm using torch.compile on a transformer model, and while the initial compilation works fine without recompiles in the same script, subsequent runs of the script take a long time to warm up. The logs show that most of the time is spent on tracing and shape solving, even though the model code is unchanged. I'm not sure if the traced graphs are being cached properly, despite the disk cache being used."
270,pytorch_pytorch,issue,154456,"I'm running torch.compile on a transformer-based model. When I run the compiled function, I don't get any recompiles for subsequent calls in the same script, for example:

m = torch.compile(self.transformer.forward, fullgraph=True, dynamic=True, mode=""max-autotune"", disable=False)
m(...)
m(...)

works fine, with no recompiles. But when I run the script again, the torch.compile warmup takes a reasonably long time (running twice each for batch sizes 1, 2, and 4) -- about 10 seconds per input batch size. I ran the script with TORCH_LOGS=+dynamo,+inductor,+recompiles,+aot, and it seems like most of the time is spent tracing the code and running shape solving, even though the model code is unchanged.

Files are written to the disk cache, and the disk cache does speed up compile times, but I'm not sure why it still takes so long to trace and run the shape solver when the model code is unchanged, are the traced graphs not cached?","I'm running torch.compile on a transformer model, and while the first run of the script compiles the model without recompiles, subsequent runs take a long time to warm up. The logs indicate that most of the time is spent on tracing and shape solving, even though the model code hasn't changed. I'm unsure why the traced graphs aren't being cached more efficiently, despite the disk cache being used.",Unknown,29,29,pytorch_pytorch,154456,issue,,1,3,3,1,"I'm running torch.compile on a transformer model, and while the first run of the script compiles the model without recompiles, subsequent runs take a long time to warm up. The logs indicate that most of the time is spent on tracing and shape solving, even though the model code hasn't changed. I'm unsure why the traced graphs aren't being cached more efficiently, despite the disk cache being used."
271,pytorch_pytorch,issue,154456,"I'm running torch.compile on a transformer-based model. When I run the compiled function, I don't get any recompiles for subsequent calls in the same script, for example:

m = torch.compile(self.transformer.forward, fullgraph=True, dynamic=True, mode=""max-autotune"", disable=False)
m(...)
m(...)

works fine, with no recompiles. But when I run the script again, the torch.compile warmup takes a reasonably long time (running twice each for batch sizes 1, 2, and 4) -- about 10 seconds per input batch size. I ran the script with TORCH_LOGS=+dynamo,+inductor,+recompiles,+aot, and it seems like most of the time is spent tracing the code and running shape solving, even though the model code is unchanged.

Files are written to the disk cache, and the disk cache does speed up compile times, but I'm not sure why it still takes so long to trace and run the shape solver when the model code is unchanged, are the traced graphs not cached?","I'm using torch.compile on a transformer model, and while the initial compilation works without recompiles in the same script, subsequent runs of the script take a long time to warm up. The logs show that most of the time is spent on tracing and shape solving, even though the model code is unchanged. I'm not sure if the traced graphs are being cached properly, despite the disk cache being used.",Unknown,29,29,pytorch_pytorch,154456,issue,,1,3,3,1,"I'm using torch.compile on a transformer model, and while the initial compilation works without recompiles in the same script, subsequent runs of the script take a long time to warm up. The logs show that most of the time is spent on tracing and shape solving, even though the model code is unchanged. I'm not sure if the traced graphs are being cached properly, despite the disk cache being used."
272,pytorch_pytorch,issue,154456,"I'm running torch.compile on a transformer-based model. When I run the compiled function, I don't get any recompiles for subsequent calls in the same script, for example:

m = torch.compile(self.transformer.forward, fullgraph=True, dynamic=True, mode=""max-autotune"", disable=False)
m(...)
m(...)

works fine, with no recompiles. But when I run the script again, the torch.compile warmup takes a reasonably long time (running twice each for batch sizes 1, 2, and 4) -- about 10 seconds per input batch size. I ran the script with TORCH_LOGS=+dynamo,+inductor,+recompiles,+aot, and it seems like most of the time is spent tracing the code and running shape solving, even though the model code is unchanged.

Files are written to the disk cache, and the disk cache does speed up compile times, but I'm not sure why it still takes so long to trace and run the shape solver when the model code is unchanged, are the traced graphs not cached?","I've noticed that when I use torch.compile on my transformer model, the first run of the script compiles the model without recompiles, but subsequent runs take a long time to warm up. The logs indicate that most of the time is spent on tracing and shape solving, even though the model code hasn't changed. I'm unsure why the traced graphs aren't being cached more effectively, despite the disk cache being used.",Unknown,29,29,pytorch_pytorch,154456,issue,,1,3,3,1,"I've noticed that when I use torch.compile on my transformer model, the first run of the script compiles the model without recompiles, but subsequent runs take a long time to warm up. The logs indicate that most of the time is spent on tracing and shape solving, even though the model code hasn't changed. I'm unsure why the traced graphs aren't being cached more effectively, despite the disk cache being used."
273,pytorch_pytorch,issue,154456,"I was expecting the cache to write to TORCHINDUCTOR_CACHE_DIR automatically (or i guess in my case it was writing to /tmp/torchinductor_user) , is that not the case?","I thought the cache would automatically write to the directory specified by TORCHINDUCTOR_CACHE_DIR, but it seems to be writing to /tmp/torchinductor_user instead. Is this the expected behavior, or am I missing something in the configuration?",Unknown,30,30,pytorch_pytorch,154456,issue,,1,3,3,1,"I thought the cache would automatically write to the directory specified by TORCHINDUCTOR_CACHE_DIR, but it seems to be writing to /tmp/torchinductor_user instead. Is this the expected behavior, or am I missing something in the configuration?"
274,pytorch_pytorch,issue,154456,"I was expecting the cache to write to TORCHINDUCTOR_CACHE_DIR automatically (or i guess in my case it was writing to /tmp/torchinductor_user) , is that not the case?","I expected the cache to be stored in the directory set by TORCHINDUCTOR_CACHE_DIR, but it appears to be using /tmp/torchinductor_user. Is this a known issue, or should I be setting up the environment differently?",Unknown,30,30,pytorch_pytorch,154456,issue,,1,3,3,1,"I expected the cache to be stored in the directory set by TORCHINDUCTOR_CACHE_DIR, but it appears to be using /tmp/torchinductor_user. Is this a known issue, or should I be setting up the environment differently?"
275,pytorch_pytorch,issue,154456,"I was expecting the cache to write to TORCHINDUCTOR_CACHE_DIR automatically (or i guess in my case it was writing to /tmp/torchinductor_user) , is that not the case?","I'm not sure if I'm misunderstanding the cache behavior, but I expected it to write to the directory specified by TORCHINDUCTOR_CACHE_DIR. Instead, it's writing to /tmp/torchinductor_user. Is this the intended behavior, or is there a configuration step I'm missing?",Unknown,30,30,pytorch_pytorch,154456,issue,,1,3,3,1,"I'm not sure if I'm misunderstanding the cache behavior, but I expected it to write to the directory specified by TORCHINDUCTOR_CACHE_DIR. Instead, it's writing to /tmp/torchinductor_user. Is this the intended behavior, or is there a configuration step I'm missing?"
276,pytorch_pytorch,issue,154456,"I was expecting the cache to write to TORCHINDUCTOR_CACHE_DIR automatically (or i guess in my case it was writing to /tmp/torchinductor_user) , is that not the case?","I was under the impression that the cache would be written to the directory specified by TORCHINDUCTOR_CACHE_DIR, but it's actually being stored in /tmp/torchinductor_user. Is this a bug, or is there a specific reason for this behavior?",Unknown,30,30,pytorch_pytorch,154456,issue,,1,3,3,1,"I was under the impression that the cache would be written to the directory specified by TORCHINDUCTOR_CACHE_DIR, but it's actually being stored in /tmp/torchinductor_user. Is this a bug, or is there a specific reason for this behavior?"
277,pytorch_pytorch,issue,154456,"I was expecting the cache to write to TORCHINDUCTOR_CACHE_DIR automatically (or i guess in my case it was writing to /tmp/torchinductor_user) , is that not the case?","I set up the environment variable TORCHINDUCTOR_CACHE_DIR to point to a specific directory, but the cache is still being written to /tmp/torchinductor_user. Is this the expected behavior, or should I be checking my setup for any issues?",Unknown,30,30,pytorch_pytorch,154456,issue,,1,3,3,1,"I set up the environment variable TORCHINDUCTOR_CACHE_DIR to point to a specific directory, but the cache is still being written to /tmp/torchinductor_user. Is this the expected behavior, or should I be checking my setup for any issues?"
278,pytorch_pytorch,issue,154456,"I was expecting the cache to write to TORCHINDUCTOR_CACHE_DIR automatically (or i guess in my case it was writing to /tmp/torchinductor_user) , is that not the case?","I expected the cache to be stored in the directory defined by TORCHINDUCTOR_CACHE_DIR, but it's being written to /tmp/torchinductor_user. Is this a configuration issue, or is this the intended behavior?",Unknown,30,30,pytorch_pytorch,154456,issue,,1,3,3,1,"I expected the cache to be stored in the directory defined by TORCHINDUCTOR_CACHE_DIR, but it's being written to /tmp/torchinductor_user. Is this a configuration issue, or is this the intended behavior?"
279,pytorch_pytorch,issue,154456,"I was expecting the cache to write to TORCHINDUCTOR_CACHE_DIR automatically (or i guess in my case it was writing to /tmp/torchinductor_user) , is that not the case?","I thought the cache would be written to the directory specified by TORCHINDUCTOR_CACHE_DIR, but it's being stored in /tmp/torchinductor_user. Is this a known issue, or am I misunderstanding the cache behavior?",Unknown,30,30,pytorch_pytorch,154456,issue,,1,3,3,1,"I thought the cache would be written to the directory specified by TORCHINDUCTOR_CACHE_DIR, but it's being stored in /tmp/torchinductor_user. Is this a known issue, or am I misunderstanding the cache behavior?"
280,pytorch_pytorch,issue,154456,"I was expecting the cache to write to TORCHINDUCTOR_CACHE_DIR automatically (or i guess in my case it was writing to /tmp/torchinductor_user) , is that not the case?","I set the environment variable TORCHINDUCTOR_CACHE_DIR to a specific directory, but the cache is still being written to /tmp/torchinductor_user. Is this the expected behavior, or is there a configuration step I'm missing?",Unknown,30,30,pytorch_pytorch,154456,issue,,1,3,3,1,"I set the environment variable TORCHINDUCTOR_CACHE_DIR to a specific directory, but the cache is still being written to /tmp/torchinductor_user. Is this the expected behavior, or is there a configuration step I'm missing?"
281,pytorch_pytorch,issue,154456,"I was expecting the cache to write to TORCHINDUCTOR_CACHE_DIR automatically (or i guess in my case it was writing to /tmp/torchinductor_user) , is that not the case?","I expected the cache to be written to the directory specified by TORCHINDUCTOR_CACHE_DIR, but it's being stored in /tmp/torchinductor_user. Is this a bug, or is there a specific reason for this behavior?",Unknown,30,30,pytorch_pytorch,154456,issue,,1,3,3,1,"I expected the cache to be written to the directory specified by TORCHINDUCTOR_CACHE_DIR, but it's being stored in /tmp/torchinductor_user. Is this a bug, or is there a specific reason for this behavior?"
282,pytorch_pytorch,issue,154456,"I was expecting the cache to write to TORCHINDUCTOR_CACHE_DIR automatically (or i guess in my case it was writing to /tmp/torchinductor_user) , is that not the case?","I was expecting the cache to be written to the directory specified by TORCHINDUCTOR_CACHE_DIR, but it's being stored in /tmp/torchinductor_user. Is this the intended behavior, or should I be checking my setup for any issues?",Unknown,30,30,pytorch_pytorch,154456,issue,,1,3,3,1,"I was expecting the cache to be written to the directory specified by TORCHINDUCTOR_CACHE_DIR, but it's being stored in /tmp/torchinductor_user. Is this the intended behavior, or should I be checking my setup for any issues?"
283,pytorch_pytorch,issue,154556,"The tracing component (Dynamo) is not fully cached today, but we are working on this.","Currently, the tracing component (Dynamo) is not fully cached, but our team is actively working on implementing this feature.",Knowledge,31,31,pytorch_pytorch,154556,issue,,0,0,0,0,"Currently, the tracing component (Dynamo) is not fully cached, but our team is actively working on implementing this feature."
284,pytorch_pytorch,issue,154556,"The tracing component (Dynamo) is not fully cached today, but we are working on this.","Dynamo, the tracing component, is not yet fully cached, but we are in the process of adding this functionality.",Knowledge,31,31,pytorch_pytorch,154556,issue,,0,0,0,0,"Dynamo, the tracing component, is not yet fully cached, but we are in the process of adding this functionality."
285,pytorch_pytorch,issue,154556,"The tracing component (Dynamo) is not fully cached today, but we are working on this.","We are aware that the tracing component (Dynamo) is not fully cached at the moment, and we are actively developing a solution to address this.",Knowledge,31,31,pytorch_pytorch,154556,issue,,0,0,0,0,"We are aware that the tracing component (Dynamo) is not fully cached at the moment, and we are actively developing a solution to address this."
286,pytorch_pytorch,issue,154556,"The tracing component (Dynamo) is not fully cached today, but we are working on this.","The caching mechanism for the tracing component (Dynamo) is not yet complete, but we are making progress on this front.",Knowledge,31,31,pytorch_pytorch,154556,issue,,0,0,0,0,"The caching mechanism for the tracing component (Dynamo) is not yet complete, but we are making progress on this front."
287,pytorch_pytorch,issue,154556,"The tracing component (Dynamo) is not fully cached today, but we are working on this.","Although the tracing component (Dynamo) is not fully cached today, our development team is working diligently to implement this feature.",Knowledge,31,31,pytorch_pytorch,154556,issue,,0,0,0,0,"Although the tracing component (Dynamo) is not fully cached today, our development team is working diligently to implement this feature."
288,pytorch_pytorch,issue,154556,"The tracing component (Dynamo) is not fully cached today, but we are working on this.","Dynamo, which handles tracing, is not currently fully cached, but we are working on it to improve performance.",Knowledge,31,31,pytorch_pytorch,154556,issue,,0,0,0,0,"Dynamo, which handles tracing, is not currently fully cached, but we are working on it to improve performance."
289,pytorch_pytorch,issue,154556,"The tracing component (Dynamo) is not fully cached today, but we are working on this.","The tracing component (Dynamo) lacks full caching support at present, but we are actively working to add this capability.",Knowledge,31,31,pytorch_pytorch,154556,issue,,0,0,0,0,"The tracing component (Dynamo) lacks full caching support at present, but we are actively working to add this capability."
290,pytorch_pytorch,issue,154556,"The tracing component (Dynamo) is not fully cached today, but we are working on this.","We recognize that the tracing component (Dynamo) is not fully cached, and our team is focused on completing this feature.",Knowledge,31,31,pytorch_pytorch,154556,issue,,0,0,0,0,"We recognize that the tracing component (Dynamo) is not fully cached, and our team is focused on completing this feature."
291,pytorch_pytorch,issue,154556,"The tracing component (Dynamo) is not fully cached today, but we are working on this.","The caching of the tracing component (Dynamo) is not yet fully implemented, but we are actively working to complete it.",Knowledge,31,31,pytorch_pytorch,154556,issue,,0,0,0,0,"The caching of the tracing component (Dynamo) is not yet fully implemented, but we are actively working to complete it."
292,pytorch_pytorch,issue,154556,"The tracing component (Dynamo) is not fully cached today, but we are working on this.","Dynamo, the component responsible for tracing, is not fully cached at the moment, but we are working on it to enhance its performance.",Knowledge,31,31,pytorch_pytorch,154556,issue,,0,0,0,0,"Dynamo, the component responsible for tracing, is not fully cached at the moment, but we are working on it to enhance its performance."
293,pytorch_pytorch,issue,132332,Under certain circumstances nn.Linear will have memory leaks on MPS. The exact failure mode and condition that leads to leakage is unclear at this moment. Need to investigate if the memory leak is in PyTorch or MPS,"I've noticed that nn.Linear can cause memory leaks on the MPS backend, but the specific conditions and failure modes are not yet clear. It's unclear whether the issue lies within PyTorch or the MPS implementation itself. Further investigation is needed to pinpoint the root cause.",Known,32,32,pytorch_pytorch,132332,issue,,1,1,1,1,"I've noticed that nn.Linear can cause memory leaks on the MPS backend, but the specific conditions and failure modes are not yet clear. It's unclear whether the issue lies within PyTorch or the MPS implementation itself. Further investigation is needed to pinpoint the root cause."
294,pytorch_pytorch,issue,132332,Under certain circumstances nn.Linear will have memory leaks on MPS. The exact failure mode and condition that leads to leakage is unclear at this moment. Need to investigate if the memory leak is in PyTorch or MPS,"There seem to be memory leaks when using nn.Linear on the MPS device, but the exact conditions that trigger this issue are still unknown. I need to determine if the problem is with PyTorch's handling of the MPS backend or if it's an issue with the MPS device itself.",Known,32,32,pytorch_pytorch,132332,issue,,1,1,1,1,"There seem to be memory leaks when using nn.Linear on the MPS device, but the exact conditions that trigger this issue are still unknown. I need to determine if the problem is with PyTorch's handling of the MPS backend or if it's an issue with the MPS device itself."
295,pytorch_pytorch,issue,132332,Under certain circumstances nn.Linear will have memory leaks on MPS. The exact failure mode and condition that leads to leakage is unclear at this moment. Need to investigate if the memory leak is in PyTorch or MPS,"I've encountered memory leaks with nn.Linear on the MPS backend, but the precise conditions and failure modes are not well understood. It's unclear whether the issue is within PyTorch's implementation or if it's a problem with the MPS device. More debugging is required to identify the source of the leak.",Known,32,32,pytorch_pytorch,132332,issue,,1,1,1,1,"I've encountered memory leaks with nn.Linear on the MPS backend, but the precise conditions and failure modes are not well understood. It's unclear whether the issue is within PyTorch's implementation or if it's a problem with the MPS device. More debugging is required to identify the source of the leak."
296,pytorch_pytorch,issue,132332,Under certain circumstances nn.Linear will have memory leaks on MPS. The exact failure mode and condition that leads to leakage is unclear at this moment. Need to investigate if the memory leak is in PyTorch or MPS,"Under certain conditions, nn.Linear appears to cause memory leaks on the MPS device, but the exact failure mode and conditions are not yet clear. I need to investigate whether the issue is with PyTorch's integration with MPS or if it's a problem with the MPS backend itself.",Known,32,32,pytorch_pytorch,132332,issue,,1,1,1,1,"Under certain conditions, nn.Linear appears to cause memory leaks on the MPS device, but the exact failure mode and conditions are not yet clear. I need to investigate whether the issue is with PyTorch's integration with MPS or if it's a problem with the MPS backend itself."
297,pytorch_pytorch,issue,132332,Under certain circumstances nn.Linear will have memory leaks on MPS. The exact failure mode and condition that leads to leakage is unclear at this moment. Need to investigate if the memory leak is in PyTorch or MPS,"I'm seeing memory leaks when using nn.Linear on the MPS device, but the specific conditions and failure modes are not well understood. It's unclear whether the issue is with PyTorch's handling of the MPS backend or if it's a problem with the MPS device. Further investigation is necessary to determine the root cause.",Known,32,32,pytorch_pytorch,132332,issue,,1,1,1,1,"I'm seeing memory leaks when using nn.Linear on the MPS device, but the specific conditions and failure modes are not well understood. It's unclear whether the issue is with PyTorch's handling of the MPS backend or if it's a problem with the MPS device. Further investigation is necessary to determine the root cause."
298,pytorch_pytorch,issue,132332,Under certain circumstances nn.Linear will have memory leaks on MPS. The exact failure mode and condition that leads to leakage is unclear at this moment. Need to investigate if the memory leak is in PyTorch or MPS,"nn.Linear seems to be causing memory leaks on the MPS backend, but the exact conditions and failure modes are not yet clear. I need to determine if the issue is with PyTorch's implementation or if it's a problem with the MPS device. More testing is required to identify the root cause.",Known,32,32,pytorch_pytorch,132332,issue,,1,1,1,1,"nn.Linear seems to be causing memory leaks on the MPS backend, but the exact conditions and failure modes are not yet clear. I need to determine if the issue is with PyTorch's implementation or if it's a problem with the MPS device. More testing is required to identify the root cause."
299,pytorch_pytorch,issue,132332,Under certain circumstances nn.Linear will have memory leaks on MPS. The exact failure mode and condition that leads to leakage is unclear at this moment. Need to investigate if the memory leak is in PyTorch or MPS,"I've observed memory leaks with nn.Linear on the MPS device, but the specific conditions and failure modes are not well understood. It's unclear whether the issue is with PyTorch's integration with the MPS backend or if it's a problem with the MPS device itself. Further investigation is needed to identify the root cause.",Known,32,32,pytorch_pytorch,132332,issue,,1,1,1,1,"I've observed memory leaks with nn.Linear on the MPS device, but the specific conditions and failure modes are not well understood. It's unclear whether the issue is with PyTorch's integration with the MPS backend or if it's a problem with the MPS device itself. Further investigation is needed to identify the root cause."
300,pytorch_pytorch,issue,132332,Under certain circumstances nn.Linear will have memory leaks on MPS. The exact failure mode and condition that leads to leakage is unclear at this moment. Need to investigate if the memory leak is in PyTorch or MPS,"Under certain circumstances, nn.Linear appears to cause memory leaks on the MPS device, but the exact conditions and failure modes are not yet clear. I need to determine if the issue is with PyTorch's handling of the MPS backend or if it's a problem with the MPS device. More debugging is required to identify the root cause.",Known,32,32,pytorch_pytorch,132332,issue,,1,1,1,1,"Under certain circumstances, nn.Linear appears to cause memory leaks on the MPS device, but the exact conditions and failure modes are not yet clear. I need to determine if the issue is with PyTorch's handling of the MPS backend or if it's a problem with the MPS device. More debugging is required to identify the root cause."
301,pytorch_pytorch,issue,132332,Under certain circumstances nn.Linear will have memory leaks on MPS. The exact failure mode and condition that leads to leakage is unclear at this moment. Need to investigate if the memory leak is in PyTorch or MPS,"I've noticed that nn.Linear can cause memory leaks on the MPS backend, but the specific conditions and failure modes are not well understood. It's unclear whether the issue is with PyTorch's implementation or if it's a problem with the MPS device. Further investigation is necessary to determine the root cause.",Known,32,32,pytorch_pytorch,132332,issue,,1,1,1,1,"I've noticed that nn.Linear can cause memory leaks on the MPS backend, but the specific conditions and failure modes are not well understood. It's unclear whether the issue is with PyTorch's implementation or if it's a problem with the MPS device. Further investigation is necessary to determine the root cause."
302,pytorch_pytorch,issue,132332,"Ok @hvaara thanks for your patience! Here's the results from my deep dive to the traces of metal resources getting generated during the execution of the code snippet and my current understanding on what's going on:

    The memory grows by the size of the output object of linear layer for each iteration run so by 4 * N * C * H * W bytes, making sense since this is the output object we allocate on L50 in the linear op 1
    When the executeMPSGraph is called using COMMIT_ADAPTIVE as the SyncMode, the operation is encoded to the active command buffer in MPSStream but unless the memory pressure is too high, the command buffer is not committed but instead we keep on encoding operations to the same command buffer 2
    Since each iteration with the nn.Linear needs to maintain its own output tensor until the computation is completed and the python object goes out of scope to be garbage collected, we see the memory increase while the command buffer keeps accumulating more encoded operations. In case we change to COMMIT_AND_WAIT as our SyncMode the command buffers get committed after each encode and the memory buffers required for the outputs are also released once the computation returns. This probably lets the garbage collector sees that the object is no longer needed since we don't assign the outputs of the computation to anything in the python script.
    To validate this I tried to adjust the original script slightly to allow for the adaptive commit to run its course and the computations to return so that the underlying objects can be released and we can see the memory get freed up. It seems like on my local setup it takes about 5-6s of waiting for the objects to get cleaned up. In actual code I assume this would happen more often since there can be other operations calling the command buffer to get synchronized in between in order to perform updates based on the results.

So in summary to me it seems like the memory is managed as intended in this case. In its current form the COMMIT_ADAPTIVE is a bit opaque to the user since it might make the memory seem like its behaving erratically as the commit is automated to only happen once the local device hits the low memory watermark, which depends on the device you are running on. Additionally there's the interplay of when will the underlying python objects get garbage collected that should finally release the memory buffers assigned to them. Let me know if this sounds reasonable or if you think there's still something we missed here. Or if this is causing a concrete issue on your side that would warrant us to do some changes on how the adaptive commit works at the moment.","Thank you for your patience, @hvaara! After a deep dive into the memory traces during the execution of the code snippet, here's my understanding: The memory usage increases by the size of the output object from the linear layer (4 * N * C * H * W bytes) for each iteration, which makes sense as this is the output object allocated in the linear operation. When using COMMIT_ADAPTIVE as the SyncMode, the operations are encoded to the active command buffer in MPSStream, but the buffer is not committed unless memory pressure is high. Each iteration of nn.Linear maintains its own output tensor until the computation completes and the Python object is garbage collected, leading to a memory increase. Changing to COMMIT_AND_WAIT ensures the command buffers are committed and memory is released after each operation. I validated this by adjusting the script to allow for adaptive commit to complete, and it took about 5-6 seconds for the objects to be cleaned up. In actual code, this would happen more frequently due to other operations. In summary, the memory management seems to be working as intended, but COMMIT_ADAPTIVE can make memory usage appear erratic. Let me know if this aligns with your understanding or if there are any issues we need to address.",Knowledge,33,33,pytorch_pytorch,132332,issue,,0,0,0,0,"Thank you for your patience, @hvaara! After a deep dive into the memory traces during the execution of the code snippet, here's my understanding: The memory usage increases by the size of the output object from the linear layer (4 * N * C * H * W bytes) for each iteration, which makes sense as this is the output object allocated in the linear operation. When using COMMIT_ADAPTIVE as the SyncMode, the operations are encoded to the active command buffer in MPSStream, but the buffer is not committed unless memory pressure is high. Each iteration of nn.Linear maintains its own output tensor until the computation completes and the Python object is garbage collected, leading to a memory increase. Changing to COMMIT_AND_WAIT ensures the command buffers are committed and memory is released after each operation. I validated this by adjusting the script to allow for adaptive commit to complete, and it took about 5-6 seconds for the objects to be cleaned up. In actual code, this would happen more frequently due to other operations. In summary, the memory management seems to be working as intended, but COMMIT_ADAPTIVE can make memory usage appear erratic. Let me know if this aligns with your understanding or if there are any issues we need to address."
303,pytorch_pytorch,issue,132332,"Ok @hvaara thanks for your patience! Here's the results from my deep dive to the traces of metal resources getting generated during the execution of the code snippet and my current understanding on what's going on:

    The memory grows by the size of the output object of linear layer for each iteration run so by 4 * N * C * H * W bytes, making sense since this is the output object we allocate on L50 in the linear op 1
    When the executeMPSGraph is called using COMMIT_ADAPTIVE as the SyncMode, the operation is encoded to the active command buffer in MPSStream but unless the memory pressure is too high, the command buffer is not committed but instead we keep on encoding operations to the same command buffer 2
    Since each iteration with the nn.Linear needs to maintain its own output tensor until the computation is completed and the python object goes out of scope to be garbage collected, we see the memory increase while the command buffer keeps accumulating more encoded operations. In case we change to COMMIT_AND_WAIT as our SyncMode the command buffers get committed after each encode and the memory buffers required for the outputs are also released once the computation returns. This probably lets the garbage collector sees that the object is no longer needed since we don't assign the outputs of the computation to anything in the python script.
    To validate this I tried to adjust the original script slightly to allow for the adaptive commit to run its course and the computations to return so that the underlying objects can be released and we can see the memory get freed up. It seems like on my local setup it takes about 5-6s of waiting for the objects to get cleaned up. In actual code I assume this would happen more often since there can be other operations calling the command buffer to get synchronized in between in order to perform updates based on the results.

So in summary to me it seems like the memory is managed as intended in this case. In its current form the COMMIT_ADAPTIVE is a bit opaque to the user since it might make the memory seem like its behaving erratically as the commit is automated to only happen once the local device hits the low memory watermark, which depends on the device you are running on. Additionally there's the interplay of when will the underlying python objects get garbage collected that should finally release the memory buffers assigned to them. Let me know if this sounds reasonable or if you think there's still something we missed here. Or if this is causing a concrete issue on your side that would warrant us to do some changes on how the adaptive commit works at the moment.","Thanks for your patience, @hvaara! Here's what I found after analyzing the memory traces: The memory grows by the size of the linear layer's output object (4 * N * C * H * W bytes) for each iteration, which is expected. When using COMMIT_ADAPTIVE, the operations are encoded to the active command buffer, but the buffer is not committed unless memory pressure is high. Each nn.Linear iteration maintains its output tensor until the computation is completed and the Python object is garbage collected, leading to a memory increase. Switching to COMMIT_AND_WAIT ensures the command buffers are committed and memory is released after each operation. I tested this by slightly adjusting the script, and it took about 5-6 seconds for the objects to be cleaned up. In actual code, this would happen more frequently due to other operations. In summary, the memory management seems to be working as intended, but COMMIT_ADAPTIVE can make memory usage appear erratic. Please let me know if this makes sense or if there are any issues we need to address.",Knowledge,33,33,pytorch_pytorch,132332,issue,,0,0,0,0,"Thanks for your patience, @hvaara! Here's what I found after analyzing the memory traces: The memory grows by the size of the linear layer's output object (4 * N * C * H * W bytes) for each iteration, which is expected. When using COMMIT_ADAPTIVE, the operations are encoded to the active command buffer, but the buffer is not committed unless memory pressure is high. Each nn.Linear iteration maintains its output tensor until the computation is completed and the Python object is garbage collected, leading to a memory increase. Switching to COMMIT_AND_WAIT ensures the command buffers are committed and memory is released after each operation. I tested this by slightly adjusting the script, and it took about 5-6 seconds for the objects to be cleaned up. In actual code, this would happen more frequently due to other operations. In summary, the memory management seems to be working as intended, but COMMIT_ADAPTIVE can make memory usage appear erratic. Please let me know if this makes sense or if there are any issues we need to address."
304,pytorch_pytorch,issue,132332,"Ok @hvaara thanks for your patience! Here's the results from my deep dive to the traces of metal resources getting generated during the execution of the code snippet and my current understanding on what's going on:

    The memory grows by the size of the output object of linear layer for each iteration run so by 4 * N * C * H * W bytes, making sense since this is the output object we allocate on L50 in the linear op 1
    When the executeMPSGraph is called using COMMIT_ADAPTIVE as the SyncMode, the operation is encoded to the active command buffer in MPSStream but unless the memory pressure is too high, the command buffer is not committed but instead we keep on encoding operations to the same command buffer 2
    Since each iteration with the nn.Linear needs to maintain its own output tensor until the computation is completed and the python object goes out of scope to be garbage collected, we see the memory increase while the command buffer keeps accumulating more encoded operations. In case we change to COMMIT_AND_WAIT as our SyncMode the command buffers get committed after each encode and the memory buffers required for the outputs are also released once the computation returns. This probably lets the garbage collector sees that the object is no longer needed since we don't assign the outputs of the computation to anything in the python script.
    To validate this I tried to adjust the original script slightly to allow for the adaptive commit to run its course and the computations to return so that the underlying objects can be released and we can see the memory get freed up. It seems like on my local setup it takes about 5-6s of waiting for the objects to get cleaned up. In actual code I assume this would happen more often since there can be other operations calling the command buffer to get synchronized in between in order to perform updates based on the results.

So in summary to me it seems like the memory is managed as intended in this case. In its current form the COMMIT_ADAPTIVE is a bit opaque to the user since it might make the memory seem like its behaving erratically as the commit is automated to only happen once the local device hits the low memory watermark, which depends on the device you are running on. Additionally there's the interplay of when will the underlying python objects get garbage collected that should finally release the memory buffers assigned to them. Let me know if this sounds reasonable or if you think there's still something we missed here. Or if this is causing a concrete issue on your side that would warrant us to do some changes on how the adaptive commit works at the moment.","Thank you for your patience, @hvaara! After a detailed analysis of the memory traces, here's my current understanding: The memory usage increases by the size of the linear layer's output object (4 * N * C * H * W bytes) for each iteration, which is expected. When using COMMIT_ADAPTIVE, the operations are encoded to the active command buffer, but the buffer is not committed unless memory pressure is high. Each nn.Linear iteration maintains its output tensor until the computation is completed and the Python object is garbage collected, leading to a memory increase. Changing to COMMIT_AND_WAIT ensures the command buffers are committed and memory is released after each operation. I validated this by adjusting the script, and it took about 5-6 seconds for the objects to be cleaned up. In actual code, this would happen more frequently due to other operations. In summary, the memory management seems to be working as intended, but COMMIT_ADAPTIVE can make memory usage appear erratic. Please let me know if this aligns with your understanding or if there are any issues we need to address.",Knowledge,33,33,pytorch_pytorch,132332,issue,,0,0,0,0,"Thank you for your patience, @hvaara! After a detailed analysis of the memory traces, here's my current understanding: The memory usage increases by the size of the linear layer's output object (4 * N * C * H * W bytes) for each iteration, which is expected. When using COMMIT_ADAPTIVE, the operations are encoded to the active command buffer, but the buffer is not committed unless memory pressure is high. Each nn.Linear iteration maintains its output tensor until the computation is completed and the Python object is garbage collected, leading to a memory increase. Changing to COMMIT_AND_WAIT ensures the command buffers are committed and memory is released after each operation. I validated this by adjusting the script, and it took about 5-6 seconds for the objects to be cleaned up. In actual code, this would happen more frequently due to other operations. In summary, the memory management seems to be working as intended, but COMMIT_ADAPTIVE can make memory usage appear erratic. Please let me know if this aligns with your understanding or if there are any issues we need to address."
305,pytorch_pytorch,issue,132332,"Ok @hvaara thanks for your patience! Here's the results from my deep dive to the traces of metal resources getting generated during the execution of the code snippet and my current understanding on what's going on:

    The memory grows by the size of the output object of linear layer for each iteration run so by 4 * N * C * H * W bytes, making sense since this is the output object we allocate on L50 in the linear op 1
    When the executeMPSGraph is called using COMMIT_ADAPTIVE as the SyncMode, the operation is encoded to the active command buffer in MPSStream but unless the memory pressure is too high, the command buffer is not committed but instead we keep on encoding operations to the same command buffer 2
    Since each iteration with the nn.Linear needs to maintain its own output tensor until the computation is completed and the python object goes out of scope to be garbage collected, we see the memory increase while the command buffer keeps accumulating more encoded operations. In case we change to COMMIT_AND_WAIT as our SyncMode the command buffers get committed after each encode and the memory buffers required for the outputs are also released once the computation returns. This probably lets the garbage collector sees that the object is no longer needed since we don't assign the outputs of the computation to anything in the python script.
    To validate this I tried to adjust the original script slightly to allow for the adaptive commit to run its course and the computations to return so that the underlying objects can be released and we can see the memory get freed up. It seems like on my local setup it takes about 5-6s of waiting for the objects to get cleaned up. In actual code I assume this would happen more often since there can be other operations calling the command buffer to get synchronized in between in order to perform updates based on the results.

So in summary to me it seems like the memory is managed as intended in this case. In its current form the COMMIT_ADAPTIVE is a bit opaque to the user since it might make the memory seem like its behaving erratically as the commit is automated to only happen once the local device hits the low memory watermark, which depends on the device you are running on. Additionally there's the interplay of when will the underlying python objects get garbage collected that should finally release the memory buffers assigned to them. Let me know if this sounds reasonable or if you think there's still something we missed here. Or if this is causing a concrete issue on your side that would warrant us to do some changes on how the adaptive commit works at the moment.","Thanks for your patience, @hvaara! Here's my analysis of the memory traces: The memory grows by the size of the linear layer's output object (4 * N * C * H * W bytes) for each iteration, which is expected. When using COMMIT_ADAPTIVE, the operations are encoded to the active command buffer, but the buffer is not committed unless memory pressure is high. Each nn.Linear iteration maintains its output tensor until the computation is completed and the Python object is garbage collected, leading to a memory increase. Switching to COMMIT_AND_WAIT ensures the command buffers are committed and memory is released after each operation. I tested this by slightly adjusting the script, and it took about 5-6 seconds for the objects to be cleaned up. In actual code, this would happen more frequently due to other operations. In summary, the memory management seems to be working as intended, but COMMIT_ADAPTIVE can make memory usage appear erratic. Please let me know if this makes sense or if there are any issues we need to address.",Knowledge,33,33,pytorch_pytorch,132332,issue,,0,0,0,0,"Thanks for your patience, @hvaara! Here's my analysis of the memory traces: The memory grows by the size of the linear layer's output object (4 * N * C * H * W bytes) for each iteration, which is expected. When using COMMIT_ADAPTIVE, the operations are encoded to the active command buffer, but the buffer is not committed unless memory pressure is high. Each nn.Linear iteration maintains its output tensor until the computation is completed and the Python object is garbage collected, leading to a memory increase. Switching to COMMIT_AND_WAIT ensures the command buffers are committed and memory is released after each operation. I tested this by slightly adjusting the script, and it took about 5-6 seconds for the objects to be cleaned up. In actual code, this would happen more frequently due to other operations. In summary, the memory management seems to be working as intended, but COMMIT_ADAPTIVE can make memory usage appear erratic. Please let me know if this makes sense or if there are any issues we need to address."
306,tensorflow_tensorflow,issue,83379,"You're absolutely right to point out that the CUDA plugin registration error #62075 has been a persistent issue for many users in the TensorFlow community, especially with newer CUDA versions like 12.x. This error typically arises due to the combination of incompatible library versions, improper installation, or misconfigurations between TensorFlow, CUDA, and cuDNN.","You're correct in highlighting that the CUDA plugin registration error #62075 has been a recurring problem for TensorFlow users, particularly with the latest CUDA 12.x versions. This issue often stems from mismatches between TensorFlow, CUDA, and cuDNN, or from incorrect setup procedures.",Knowledge,34,34,tensorflow_tensorflow,83379,issue,,0,0,0,0,"You're correct in highlighting that the CUDA plugin registration error #62075 has been a recurring problem for TensorFlow users, particularly with the latest CUDA 12.x versions. This issue often stems from mismatches between TensorFlow, CUDA, and cuDNN, or from incorrect setup procedures."
307,tensorflow_tensorflow,issue,83379,"You're absolutely right to point out that the CUDA plugin registration error #62075 has been a persistent issue for many users in the TensorFlow community, especially with newer CUDA versions like 12.x. This error typically arises due to the combination of incompatible library versions, improper installation, or misconfigurations between TensorFlow, CUDA, and cuDNN.","Thank you for bringing up the CUDA plugin registration error #62075, which has affected many TensorFlow users, especially those using CUDA 12.x. The root cause is usually a combination of incompatible library versions, flawed installations, or misconfigurations between TensorFlow, CUDA, and cuDNN.",Knowledge,34,34,tensorflow_tensorflow,83379,issue,,0,0,0,0,"Thank you for bringing up the CUDA plugin registration error #62075, which has affected many TensorFlow users, especially those using CUDA 12.x. The root cause is usually a combination of incompatible library versions, flawed installations, or misconfigurations between TensorFlow, CUDA, and cuDNN."
308,tensorflow_tensorflow,issue,83379,"You're absolutely right to point out that the CUDA plugin registration error #62075 has been a persistent issue for many users in the TensorFlow community, especially with newer CUDA versions like 12.x. This error typically arises due to the combination of incompatible library versions, improper installation, or misconfigurations between TensorFlow, CUDA, and cuDNN.","I agree that the CUDA plugin registration error #62075 has been a significant pain point for TensorFlow users, particularly with the recent CUDA 12.x releases. This error is commonly due to version incompatibilities, improper installations, or misconfigurations involving TensorFlow, CUDA, and cuDNN.",Knowledge,34,34,tensorflow_tensorflow,83379,issue,,0,0,0,0,"I agree that the CUDA plugin registration error #62075 has been a significant pain point for TensorFlow users, particularly with the recent CUDA 12.x releases. This error is commonly due to version incompatibilities, improper installations, or misconfigurations involving TensorFlow, CUDA, and cuDNN."
309,tensorflow_tensorflow,issue,83379,"You're absolutely right to point out that the CUDA plugin registration error #62075 has been a persistent issue for many users in the TensorFlow community, especially with newer CUDA versions like 12.x. This error typically arises due to the combination of incompatible library versions, improper installation, or misconfigurations between TensorFlow, CUDA, and cuDNN.","You're right to mention the persistent CUDA plugin registration error #62075, which has been a common issue for TensorFlow users, especially those working with CUDA 12.x. The problem often arises from mismatches in library versions, incorrect installations, or misconfigurations between TensorFlow, CUDA, and cuDNN.",Knowledge,34,34,tensorflow_tensorflow,83379,issue,,0,0,0,0,"You're right to mention the persistent CUDA plugin registration error #62075, which has been a common issue for TensorFlow users, especially those working with CUDA 12.x. The problem often arises from mismatches in library versions, incorrect installations, or misconfigurations between TensorFlow, CUDA, and cuDNN."
310,tensorflow_tensorflow,issue,83379,"You're absolutely right to point out that the CUDA plugin registration error #62075 has been a persistent issue for many users in the TensorFlow community, especially with newer CUDA versions like 12.x. This error typically arises due to the combination of incompatible library versions, improper installation, or misconfigurations between TensorFlow, CUDA, and cuDNN.","I concur that the CUDA plugin registration error #62075 has been a frequent problem for TensorFlow users, particularly with the newer CUDA 12.x versions. This error is typically caused by version incompatibilities, improper installations, or misconfigurations between TensorFlow, CUDA, and cuDNN.",Knowledge,34,34,tensorflow_tensorflow,83379,issue,,0,0,0,0,"I concur that the CUDA plugin registration error #62075 has been a frequent problem for TensorFlow users, particularly with the newer CUDA 12.x versions. This error is typically caused by version incompatibilities, improper installations, or misconfigurations between TensorFlow, CUDA, and cuDNN."
311,tensorflow_tensorflow,issue,83379,"You're absolutely right to point out that the CUDA plugin registration error #62075 has been a persistent issue for many users in the TensorFlow community, especially with newer CUDA versions like 12.x. This error typically arises due to the combination of incompatible library versions, improper installation, or misconfigurations between TensorFlow, CUDA, and cuDNN.","You're absolutely correct in pointing out that the CUDA plugin registration error #62075 has been a recurring issue for many TensorFlow users, especially those using CUDA 12.x. The error is often due to version mismatches, incorrect installations, or misconfigurations between TensorFlow, CUDA, and cuDNN.",Knowledge,34,34,tensorflow_tensorflow,83379,issue,,0,0,0,0,"You're absolutely correct in pointing out that the CUDA plugin registration error #62075 has been a recurring issue for many TensorFlow users, especially those using CUDA 12.x. The error is often due to version mismatches, incorrect installations, or misconfigurations between TensorFlow, CUDA, and cuDNN."
312,tensorflow_tensorflow,issue,83379,"You're absolutely right to point out that the CUDA plugin registration error #62075 has been a persistent issue for many users in the TensorFlow community, especially with newer CUDA versions like 12.x. This error typically arises due to the combination of incompatible library versions, improper installation, or misconfigurations between TensorFlow, CUDA, and cuDNN.","Thank you for noting that the CUDA plugin registration error #62075 has been a persistent problem for TensorFlow users, particularly with the latest CUDA 12.x versions. This issue is usually caused by incompatible library versions, improper installations, or misconfigurations between TensorFlow, CUDA, and cuDNN.",Knowledge,34,34,tensorflow_tensorflow,83379,issue,,0,0,0,0,"Thank you for noting that the CUDA plugin registration error #62075 has been a persistent problem for TensorFlow users, particularly with the latest CUDA 12.x versions. This issue is usually caused by incompatible library versions, improper installations, or misconfigurations between TensorFlow, CUDA, and cuDNN."
313,tensorflow_tensorflow,issue,83379,"You're absolutely right to point out that the CUDA plugin registration error #62075 has been a persistent issue for many users in the TensorFlow community, especially with newer CUDA versions like 12.x. This error typically arises due to the combination of incompatible library versions, improper installation, or misconfigurations between TensorFlow, CUDA, and cuDNN.","I agree with your observation that the CUDA plugin registration error #62075 has been a significant issue for TensorFlow users, especially those using CUDA 12.x. The error typically results from version incompatibilities, incorrect installations, or misconfigurations between TensorFlow, CUDA, and cuDNN.",Knowledge,34,34,tensorflow_tensorflow,83379,issue,,0,0,0,0,"I agree with your observation that the CUDA plugin registration error #62075 has been a significant issue for TensorFlow users, especially those using CUDA 12.x. The error typically results from version incompatibilities, incorrect installations, or misconfigurations between TensorFlow, CUDA, and cuDNN."
314,tensorflow_tensorflow,issue,83379,"You're absolutely right to point out that the CUDA plugin registration error #62075 has been a persistent issue for many users in the TensorFlow community, especially with newer CUDA versions like 12.x. This error typically arises due to the combination of incompatible library versions, improper installation, or misconfigurations between TensorFlow, CUDA, and cuDNN.","You're right to highlight the CUDA plugin registration error #62075, which has been a common problem for TensorFlow users, particularly with the recent CUDA 12.x releases. This issue is often due to version mismatches, improper installations, or misconfigurations involving TensorFlow, CUDA, and cuDNN.",Knowledge,34,34,tensorflow_tensorflow,83379,issue,,0,0,0,0,"You're right to highlight the CUDA plugin registration error #62075, which has been a common problem for TensorFlow users, particularly with the recent CUDA 12.x releases. This issue is often due to version mismatches, improper installations, or misconfigurations involving TensorFlow, CUDA, and cuDNN."
315,tensorflow_tensorflow,issue,83379,"You're absolutely right to point out that the CUDA plugin registration error #62075 has been a persistent issue for many users in the TensorFlow community, especially with newer CUDA versions like 12.x. This error typically arises due to the combination of incompatible library versions, improper installation, or misconfigurations between TensorFlow, CUDA, and cuDNN.","I concur with your point that the CUDA plugin registration error #62075 has been a recurring issue for TensorFlow users, especially those working with CUDA 12.x. The error is typically caused by version incompatibilities, incorrect installations, or misconfigurations between TensorFlow, CUDA, and cuDNN.",Knowledge,34,34,tensorflow_tensorflow,83379,issue,,0,0,0,0,"I concur with your point that the CUDA plugin registration error #62075 has been a recurring issue for TensorFlow users, especially those working with CUDA 12.x. The error is typically caused by version incompatibilities, incorrect installations, or misconfigurations between TensorFlow, CUDA, and cuDNN."
316,tensorflow_tensorflow,issue,84102,"I think TensorFlow on Windows is only supported on Intel, not ARM.",I believe TensorFlow on Windows is only compatible with Intel processors and not ARM architecture. Can someone confirm if this is still the case or if there have been any updates?,Irrelevant,35,35,tensorflow_tensorflow,84102,issue,,1,2,2,1,I believe TensorFlow on Windows is only compatible with Intel processors and not ARM architecture. Can someone confirm if this is still the case or if there have been any updates?
317,tensorflow_tensorflow,issue,84102,"I think TensorFlow on Windows is only supported on Intel, not ARM.","I'm under the impression that TensorFlow on Windows is limited to Intel processors and doesn't support ARM. Is this correct, or has there been any progress in ARM support?",Irrelevant,35,35,tensorflow_tensorflow,84102,issue,,1,2,2,1,"I'm under the impression that TensorFlow on Windows is limited to Intel processors and doesn't support ARM. Is this correct, or has there been any progress in ARM support?"
318,tensorflow_tensorflow,issue,84102,"I think TensorFlow on Windows is only supported on Intel, not ARM.","I've read that TensorFlow on Windows is only supported on Intel processors and not on ARM. Is this still true, or are there any plans to add ARM support in the future?",Irrelevant,35,35,tensorflow_tensorflow,84102,issue,,1,2,2,1,"I've read that TensorFlow on Windows is only supported on Intel processors and not on ARM. Is this still true, or are there any plans to add ARM support in the future?"
319,tensorflow_tensorflow,issue,84102,"I think TensorFlow on Windows is only supported on Intel, not ARM.","I'm trying to run TensorFlow on a Windows machine with an ARM processor, but I think it's only supported on Intel. Can anyone verify this and let me know if there are any workarounds?",Irrelevant,35,35,tensorflow_tensorflow,84102,issue,,1,2,2,1,"I'm trying to run TensorFlow on a Windows machine with an ARM processor, but I think it's only supported on Intel. Can anyone verify this and let me know if there are any workarounds?"
320,tensorflow_tensorflow,issue,84102,"I think TensorFlow on Windows is only supported on Intel, not ARM.","I'm not sure if TensorFlow on Windows supports ARM processors. From what I've seen, it seems to be limited to Intel. Can someone clarify this for me?",Irrelevant,35,35,tensorflow_tensorflow,84102,issue,,1,2,2,1,"I'm not sure if TensorFlow on Windows supports ARM processors. From what I've seen, it seems to be limited to Intel. Can someone clarify this for me?"
321,tensorflow_tensorflow,issue,84102,"I think TensorFlow on Windows is only supported on Intel, not ARM.","I'm considering using TensorFlow on a Windows device with an ARM processor, but I think it's only supported on Intel. Is this accurate, or have there been any recent changes?",Irrelevant,35,35,tensorflow_tensorflow,84102,issue,,1,2,2,1,"I'm considering using TensorFlow on a Windows device with an ARM processor, but I think it's only supported on Intel. Is this accurate, or have there been any recent changes?"
322,tensorflow_tensorflow,issue,84102,"I think TensorFlow on Windows is only supported on Intel, not ARM.",I'm curious about TensorFlow's support on Windows. I believe it only works with Intel processors and not ARM. Can anyone confirm this or provide more details?,Irrelevant,35,35,tensorflow_tensorflow,84102,issue,,1,2,2,1,I'm curious about TensorFlow's support on Windows. I believe it only works with Intel processors and not ARM. Can anyone confirm this or provide more details?
323,tensorflow_tensorflow,issue,84102,"I think TensorFlow on Windows is only supported on Intel, not ARM.","I've heard that TensorFlow on Windows is only supported on Intel processors and not on ARM. Is this still the case, or are there any updates on ARM support?",Irrelevant,35,35,tensorflow_tensorflow,84102,issue,,1,2,2,1,"I've heard that TensorFlow on Windows is only supported on Intel processors and not on ARM. Is this still the case, or are there any updates on ARM support?"
324,tensorflow_tensorflow,issue,84102,"I think TensorFlow on Windows is only supported on Intel, not ARM.","I'm trying to set up TensorFlow on a Windows machine with an ARM processor, but I think it's only supported on Intel. Can someone confirm this and suggest any alternatives if it's true?",Irrelevant,35,35,tensorflow_tensorflow,84102,issue,,1,2,2,1,"I'm trying to set up TensorFlow on a Windows machine with an ARM processor, but I think it's only supported on Intel. Can someone confirm this and suggest any alternatives if it's true?"
325,tensorflow_tensorflow,issue,84102,"Confirmed that Windows support only exists for Intel CPUs. On https://pypi.org/project/tensorflow/#files there is no Windows + ARM wheel.

On https://pypi.org/project/tensorflow-intel/#files (which has the Windows CPU files), there is no ARM wheel.",I've verified that TensorFlow only supports Intel CPUs on Windows. There are no ARM wheels available for Windows on the official PyPI page for TensorFlow.,Knowledge,36,36,tensorflow_tensorflow,84102,issue,,0,0,0,0,I've verified that TensorFlow only supports Intel CPUs on Windows. There are no ARM wheels available for Windows on the official PyPI page for TensorFlow.
326,tensorflow_tensorflow,issue,84102,"Confirmed that Windows support only exists for Intel CPUs. On https://pypi.org/project/tensorflow/#files there is no Windows + ARM wheel.

On https://pypi.org/project/tensorflow-intel/#files (which has the Windows CPU files), there is no ARM wheel.",It appears that TensorFlow does not provide ARM support for Windows. The PyPI page for TensorFlow lists only Intel CPU wheels for Windows.,Knowledge,36,36,tensorflow_tensorflow,84102,issue,,0,0,0,0,It appears that TensorFlow does not provide ARM support for Windows. The PyPI page for TensorFlow lists only Intel CPU wheels for Windows.
327,tensorflow_tensorflow,issue,84102,"Confirmed that Windows support only exists for Intel CPUs. On https://pypi.org/project/tensorflow/#files there is no Windows + ARM wheel.

On https://pypi.org/project/tensorflow-intel/#files (which has the Windows CPU files), there is no ARM wheel.",I checked the TensorFlow PyPI page and confirmed that there are no ARM wheels available for Windows. The only Windows wheels provided are for Intel CPUs.,Knowledge,36,36,tensorflow_tensorflow,84102,issue,,0,0,0,0,I checked the TensorFlow PyPI page and confirmed that there are no ARM wheels available for Windows. The only Windows wheels provided are for Intel CPUs.
328,tensorflow_tensorflow,issue,84102,"Confirmed that Windows support only exists for Intel CPUs. On https://pypi.org/project/tensorflow/#files there is no Windows + ARM wheel.

On https://pypi.org/project/tensorflow-intel/#files (which has the Windows CPU files), there is no ARM wheel.",I noticed that TensorFlow's PyPI page does not include any ARM wheels for Windows. This means that ARM-based Windows systems are not supported.,Knowledge,36,36,tensorflow_tensorflow,84102,issue,,0,0,0,0,I noticed that TensorFlow's PyPI page does not include any ARM wheels for Windows. This means that ARM-based Windows systems are not supported.
329,tensorflow_tensorflow,issue,84102,"Confirmed that Windows support only exists for Intel CPUs. On https://pypi.org/project/tensorflow/#files there is no Windows + ARM wheel.

On https://pypi.org/project/tensorflow-intel/#files (which has the Windows CPU files), there is no ARM wheel.","After reviewing the TensorFlow PyPI page, I can confirm that Windows support is limited to Intel CPUs. There are no ARM wheels available for Windows users.",Knowledge,36,36,tensorflow_tensorflow,84102,issue,,0,0,0,0,"After reviewing the TensorFlow PyPI page, I can confirm that Windows support is limited to Intel CPUs. There are no ARM wheels available for Windows users."
330,tensorflow_tensorflow,issue,84102,"Confirmed that Windows support only exists for Intel CPUs. On https://pypi.org/project/tensorflow/#files there is no Windows + ARM wheel.

On https://pypi.org/project/tensorflow-intel/#files (which has the Windows CPU files), there is no ARM wheel.",I've confirmed that TensorFlow does not offer ARM support on Windows. The PyPI page for TensorFlow only lists wheels for Intel CPUs.,Knowledge,36,36,tensorflow_tensorflow,84102,issue,,0,0,0,0,I've confirmed that TensorFlow does not offer ARM support on Windows. The PyPI page for TensorFlow only lists wheels for Intel CPUs.
331,tensorflow_tensorflow,issue,84102,"Confirmed that Windows support only exists for Intel CPUs. On https://pypi.org/project/tensorflow/#files there is no Windows + ARM wheel.

On https://pypi.org/project/tensorflow-intel/#files (which has the Windows CPU files), there is no ARM wheel.",I've found that TensorFlow's Windows support is restricted to Intel CPUs. The PyPI page does not provide any ARM wheels for Windows.,Knowledge,36,36,tensorflow_tensorflow,84102,issue,,0,0,0,0,I've found that TensorFlow's Windows support is restricted to Intel CPUs. The PyPI page does not provide any ARM wheels for Windows.
332,tensorflow_tensorflow,issue,84102,"Confirmed that Windows support only exists for Intel CPUs. On https://pypi.org/project/tensorflow/#files there is no Windows + ARM wheel.

On https://pypi.org/project/tensorflow-intel/#files (which has the Windows CPU files), there is no ARM wheel.",I've verified that TensorFlow's Windows support is limited to Intel CPUs. The PyPI page for TensorFlow does not include any ARM wheels for Windows.,Knowledge,36,36,tensorflow_tensorflow,84102,issue,,0,0,0,0,I've verified that TensorFlow's Windows support is limited to Intel CPUs. The PyPI page for TensorFlow does not include any ARM wheels for Windows.
333,tensorflow_tensorflow,issue,84102,"Confirmed that Windows support only exists for Intel CPUs. On https://pypi.org/project/tensorflow/#files there is no Windows + ARM wheel.

On https://pypi.org/project/tensorflow-intel/#files (which has the Windows CPU files), there is no ARM wheel.",I checked the TensorFlow PyPI page and found that there are no ARM wheels available for Windows. This means that ARM-based Windows systems are not supported by TensorFlow.,Knowledge,36,36,tensorflow_tensorflow,84102,issue,,0,0,0,0,I checked the TensorFlow PyPI page and found that there are no ARM wheels available for Windows. This means that ARM-based Windows systems are not supported by TensorFlow.
334,tensorflow_tensorflow,issue,84102,"Confirmed that Windows support only exists for Intel CPUs. On https://pypi.org/project/tensorflow/#files there is no Windows + ARM wheel.

On https://pypi.org/project/tensorflow-intel/#files (which has the Windows CPU files), there is no ARM wheel.",I've confirmed that TensorFlow only supports Intel CPUs on Windows. The PyPI page for TensorFlow does not list any ARM wheels for Windows.,Knowledge,36,36,tensorflow_tensorflow,84102,issue,,0,0,0,0,I've confirmed that TensorFlow only supports Intel CPUs on Windows. The PyPI page for TensorFlow does not list any ARM wheels for Windows.
335,tensorflow_tensorflow,issue,84102,"This is also weird, because the pip installer should not have proceeded due to missing wheels.",It's strange that the pip installer continued despite the missing wheels. I expected it to halt the installation process.,Unknown,37,37,tensorflow_tensorflow,84102,issue,,1,3,3,1,It's strange that the pip installer continued despite the missing wheels. I expected it to halt the installation process.
336,tensorflow_tensorflow,issue,84102,"This is also weird, because the pip installer should not have proceeded due to missing wheels.","I'm confused because the pip installer should have stopped when it couldn't find the necessary wheels. Instead, it proceeded and now my environment is in an inconsistent state.",Unknown,37,37,tensorflow_tensorflow,84102,issue,,1,3,3,1,"I'm confused because the pip installer should have stopped when it couldn't find the necessary wheels. Instead, it proceeded and now my environment is in an inconsistent state."
337,tensorflow_tensorflow,issue,84102,"This is also weird, because the pip installer should not have proceeded due to missing wheels.",The pip installer is behaving unexpectedly by continuing the installation even though the required wheels are missing. This shouldn't happen according to the documentation.,Unknown,37,37,tensorflow_tensorflow,84102,issue,,1,3,3,1,The pip installer is behaving unexpectedly by continuing the installation even though the required wheels are missing. This shouldn't happen according to the documentation.
338,tensorflow_tensorflow,issue,84102,"This is also weird, because the pip installer should not have proceeded due to missing wheels.",I don't understand why the pip installer didn't fail when it encountered missing wheels. It should have stopped the installation to prevent issues.,Unknown,37,37,tensorflow_tensorflow,84102,issue,,1,3,3,1,I don't understand why the pip installer didn't fail when it encountered missing wheels. It should have stopped the installation to prevent issues.
339,tensorflow_tensorflow,issue,84102,"This is also weird, because the pip installer should not have proceeded due to missing wheels.",It's odd that the pip installer didn't raise an error when the wheels were missing. I expected it to prevent the installation from proceeding.,Unknown,37,37,tensorflow_tensorflow,84102,issue,,1,3,3,1,It's odd that the pip installer didn't raise an error when the wheels were missing. I expected it to prevent the installation from proceeding.
340,tensorflow_tensorflow,issue,84102,"This is also weird, because the pip installer should not have proceeded due to missing wheels.","The pip installer is not behaving as expected. It should have stopped the installation process when it couldn't find the required wheels, but it continued anyway.",Unknown,37,37,tensorflow_tensorflow,84102,issue,,1,3,3,1,"The pip installer is not behaving as expected. It should have stopped the installation process when it couldn't find the required wheels, but it continued anyway."
341,tensorflow_tensorflow,issue,84102,"This is also weird, because the pip installer should not have proceeded due to missing wheels.","I'm puzzled by the pip installer's behavior. It should have halted the installation due to missing wheels, but it didn't and now I'm facing issues.",Unknown,37,37,tensorflow_tensorflow,84102,issue,,1,3,3,1,"I'm puzzled by the pip installer's behavior. It should have halted the installation due to missing wheels, but it didn't and now I'm facing issues."
342,tensorflow_tensorflow,issue,84102,"This is also weird, because the pip installer should not have proceeded due to missing wheels.",It's surprising that the pip installer didn't stop when it couldn't find the necessary wheels. This is not the expected behavior and has caused problems in my environment.,Unknown,37,37,tensorflow_tensorflow,84102,issue,,1,3,3,1,It's surprising that the pip installer didn't stop when it couldn't find the necessary wheels. This is not the expected behavior and has caused problems in my environment.
343,tensorflow_tensorflow,issue,84102,"This is also weird, because the pip installer should not have proceeded due to missing wheels.","The pip installer should have failed when it couldn't locate the required wheels, but it continued the installation. This is causing unexpected issues.",Unknown,37,37,tensorflow_tensorflow,84102,issue,,1,3,3,1,"The pip installer should have failed when it couldn't locate the required wheels, but it continued the installation. This is causing unexpected issues."
344,tensorflow_tensorflow,issue,84102,"This is also weird, because the pip installer should not have proceeded due to missing wheels.",I'm not sure why the pip installer didn't stop when it encountered missing wheels. It should have prevented the installation to avoid inconsistencies.,Unknown,37,37,tensorflow_tensorflow,84102,issue,,1,3,3,1,I'm not sure why the pip installer didn't stop when it encountered missing wheels. It should have prevented the installation to avoid inconsistencies.
345,tensorflow_tensorflow,issue,82736,"I have a simple program that builds graphs through the c_api and executes sessions. It works perfectly as long as it is run only on CPU. If GPU is involved then at random places the program generates a Div0 error. The very same program run twice within one minute on the same hardware, etc. behave differently.
The error is somewhere deep in TF/Cuda because in the error dump I see 15 program jumps outside my code (what I can still debug).
I tried it on the different partition of a HPC, but both behave the same way. Card is Quadro RTX 6000, driver 565.57.01 Cuda 12.7 as seen in nvidia-smi, but 12.3 is available as libraries.
I tried with many different settings, etc., but cannot identify any rootcause. I am not even sure if the bug is in the TF binary or in one of the Cuda libraries (or elsewhere).","I have a program that constructs and runs graphs using the TensorFlow c_api. It runs flawlessly on CPU, but when I enable GPU, it randomly throws a Div0 error. The same program, executed within a minute on the same hardware, produces different results. The error seems to originate deep within TensorFlow or CUDA, as the stack trace shows multiple jumps outside my code. I've tested this on different partitions of an HPC cluster, and the behavior is consistent. The GPU card is a Quadro RTX 6000 with driver 565.57.01 and CUDA 12.7, but CUDA 12.3 libraries are also available. Despite trying various settings, I can't pinpoint the root cause and am unsure if the issue lies in TensorFlow or CUDA.",Unknown,38,38,tensorflow_tensorflow,82736,issue,,1,3,3,1,"I have a program that constructs and runs graphs using the TensorFlow c_api. It runs flawlessly on CPU, but when I enable GPU, it randomly throws a Div0 error. The same program, executed within a minute on the same hardware, produces different results. The error seems to originate deep within TensorFlow or CUDA, as the stack trace shows multiple jumps outside my code. I've tested this on different partitions of an HPC cluster, and the behavior is consistent. The GPU card is a Quadro RTX 6000 with driver 565.57.01 and CUDA 12.7, but CUDA 12.3 libraries are also available. Despite trying various settings, I can't pinpoint the root cause and am unsure if the issue lies in TensorFlow or CUDA."
346,tensorflow_tensorflow,issue,82736,"I have a simple program that builds graphs through the c_api and executes sessions. It works perfectly as long as it is run only on CPU. If GPU is involved then at random places the program generates a Div0 error. The very same program run twice within one minute on the same hardware, etc. behave differently.
The error is somewhere deep in TF/Cuda because in the error dump I see 15 program jumps outside my code (what I can still debug).
I tried it on the different partition of a HPC, but both behave the same way. Card is Quadro RTX 6000, driver 565.57.01 Cuda 12.7 as seen in nvidia-smi, but 12.3 is available as libraries.
I tried with many different settings, etc., but cannot identify any rootcause. I am not even sure if the bug is in the TF binary or in one of the Cuda libraries (or elsewhere).","I've developed a program that builds and executes TensorFlow graphs using the c_api. It works perfectly on CPU, but when I switch to GPU, it intermittently generates a Div0 error. The same program, run multiple times in quick succession, behaves differently each time. The error trace indicates that the issue is deep within TensorFlow or CUDA, with many jumps outside my code. I've tested this on different HPC partitions with the same results. The GPU is a Quadro RTX 6000, and I'm using driver 565.57.01 with CUDA 12.7, though CUDA 12.3 libraries are also installed. I've tried various configurations but can't determine the root cause. Is the bug in TensorFlow or CUDA?",Unknown,38,38,tensorflow_tensorflow,82736,issue,,1,3,3,1,"I've developed a program that builds and executes TensorFlow graphs using the c_api. It works perfectly on CPU, but when I switch to GPU, it intermittently generates a Div0 error. The same program, run multiple times in quick succession, behaves differently each time. The error trace indicates that the issue is deep within TensorFlow or CUDA, with many jumps outside my code. I've tested this on different HPC partitions with the same results. The GPU is a Quadro RTX 6000, and I'm using driver 565.57.01 with CUDA 12.7, though CUDA 12.3 libraries are also installed. I've tried various configurations but can't determine the root cause. Is the bug in TensorFlow or CUDA?"
347,tensorflow_tensorflow,issue,82736,"I have a simple program that builds graphs through the c_api and executes sessions. It works perfectly as long as it is run only on CPU. If GPU is involved then at random places the program generates a Div0 error. The very same program run twice within one minute on the same hardware, etc. behave differently.
The error is somewhere deep in TF/Cuda because in the error dump I see 15 program jumps outside my code (what I can still debug).
I tried it on the different partition of a HPC, but both behave the same way. Card is Quadro RTX 6000, driver 565.57.01 Cuda 12.7 as seen in nvidia-smi, but 12.3 is available as libraries.
I tried with many different settings, etc., but cannot identify any rootcause. I am not even sure if the bug is in the TF binary or in one of the Cuda libraries (or elsewhere).","I have a TensorFlow program that constructs and runs graphs using the c_api. It runs smoothly on CPU, but when I enable GPU, it randomly throws a Div0 error. The same program, executed within a minute on the same hardware, produces inconsistent results. The error seems to be deep within TensorFlow or CUDA, as the stack trace shows multiple jumps outside my code. I've tested this on different HPC partitions, and the behavior is the same. The GPU is a Quadro RTX 6000 with driver 565.57.01 and CUDA 12.7, but CUDA 12.3 libraries are also available. Despite trying various settings, I can't identify the root cause and am unsure if the issue is in TensorFlow or CUDA.",Unknown,38,38,tensorflow_tensorflow,82736,issue,,1,3,3,1,"I have a TensorFlow program that constructs and runs graphs using the c_api. It runs smoothly on CPU, but when I enable GPU, it randomly throws a Div0 error. The same program, executed within a minute on the same hardware, produces inconsistent results. The error seems to be deep within TensorFlow or CUDA, as the stack trace shows multiple jumps outside my code. I've tested this on different HPC partitions, and the behavior is the same. The GPU is a Quadro RTX 6000 with driver 565.57.01 and CUDA 12.7, but CUDA 12.3 libraries are also available. Despite trying various settings, I can't identify the root cause and am unsure if the issue is in TensorFlow or CUDA."
348,tensorflow_tensorflow,issue,82736,"I have a simple program that builds graphs through the c_api and executes sessions. It works perfectly as long as it is run only on CPU. If GPU is involved then at random places the program generates a Div0 error. The very same program run twice within one minute on the same hardware, etc. behave differently.
The error is somewhere deep in TF/Cuda because in the error dump I see 15 program jumps outside my code (what I can still debug).
I tried it on the different partition of a HPC, but both behave the same way. Card is Quadro RTX 6000, driver 565.57.01 Cuda 12.7 as seen in nvidia-smi, but 12.3 is available as libraries.
I tried with many different settings, etc., but cannot identify any rootcause. I am not even sure if the bug is in the TF binary or in one of the Cuda libraries (or elsewhere).","I have a TensorFlow program that builds and runs graphs using the c_api. It works fine on CPU, but when I enable GPU, it randomly generates a Div0 error. The same program, run multiple times in quick succession, behaves differently each time. The error trace suggests the issue is deep within TensorFlow or CUDA, with many jumps outside my code. I've tested this on different HPC partitions with the same results. The GPU is a Quadro RTX 6000, and I'm using driver 565.57.01 with CUDA 12.7, though CUDA 12.3 libraries are also installed. I've tried various configurations but can't determine the root cause. Is the bug in TensorFlow or CUDA?",Unknown,38,38,tensorflow_tensorflow,82736,issue,,1,3,3,1,"I have a TensorFlow program that builds and runs graphs using the c_api. It works fine on CPU, but when I enable GPU, it randomly generates a Div0 error. The same program, run multiple times in quick succession, behaves differently each time. The error trace suggests the issue is deep within TensorFlow or CUDA, with many jumps outside my code. I've tested this on different HPC partitions with the same results. The GPU is a Quadro RTX 6000, and I'm using driver 565.57.01 with CUDA 12.7, though CUDA 12.3 libraries are also installed. I've tried various configurations but can't determine the root cause. Is the bug in TensorFlow or CUDA?"
349,tensorflow_tensorflow,issue,82736,"I have a simple program that builds graphs through the c_api and executes sessions. It works perfectly as long as it is run only on CPU. If GPU is involved then at random places the program generates a Div0 error. The very same program run twice within one minute on the same hardware, etc. behave differently.
The error is somewhere deep in TF/Cuda because in the error dump I see 15 program jumps outside my code (what I can still debug).
I tried it on the different partition of a HPC, but both behave the same way. Card is Quadro RTX 6000, driver 565.57.01 Cuda 12.7 as seen in nvidia-smi, but 12.3 is available as libraries.
I tried with many different settings, etc., but cannot identify any rootcause. I am not even sure if the bug is in the TF binary or in one of the Cuda libraries (or elsewhere).","I have a TensorFlow program that constructs and runs graphs using the c_api. It works perfectly on CPU, but when I enable GPU, it randomly throws a Div0 error. The same program, executed within a minute on the same hardware, produces different results. The error seems to be deep within TensorFlow or CUDA, as the stack trace shows multiple jumps outside my code. I've tested this on different HPC partitions, and the behavior is consistent. The GPU is a Quadro RTX 6000 with driver 565.57.01 and CUDA 12.7, but CUDA 12.3 libraries are also available. Despite trying various settings, I can't pinpoint the root cause and am unsure if the issue lies in TensorFlow or CUDA.",Unknown,38,38,tensorflow_tensorflow,82736,issue,,1,3,3,1,"I have a TensorFlow program that constructs and runs graphs using the c_api. It works perfectly on CPU, but when I enable GPU, it randomly throws a Div0 error. The same program, executed within a minute on the same hardware, produces different results. The error seems to be deep within TensorFlow or CUDA, as the stack trace shows multiple jumps outside my code. I've tested this on different HPC partitions, and the behavior is consistent. The GPU is a Quadro RTX 6000 with driver 565.57.01 and CUDA 12.7, but CUDA 12.3 libraries are also available. Despite trying various settings, I can't pinpoint the root cause and am unsure if the issue lies in TensorFlow or CUDA."
350,tensorflow_tensorflow,issue,24828,"I tried installting tensorflow 1.12 using both pip install and building from source.However when I am trying to run faster rcnn model i get following error message:
Failed to get convolution algorithm. This is probably because cuDNN failed to initialize, so try looking to see if a warning log message was printed above.

I only get this with tf 1.12 and python 3.6 ,it works fine with python 3.6","I'm having trouble running the Faster R-CNN model with TensorFlow 1.12. I installed it using both pip and building from source, but I keep getting an error about cuDNN failing to initialize. The error message says 'Failed to get convolution algorithm.' This issue only occurs with TensorFlow 1.12 and Python 3.6, but it works fine with other versions.",Unknown,39,39,tensorflow_tensorflow,24828,issue,,1,3,3,1,"I'm having trouble running the Faster R-CNN model with TensorFlow 1.12. I installed it using both pip and building from source, but I keep getting an error about cuDNN failing to initialize. The error message says 'Failed to get convolution algorithm.' This issue only occurs with TensorFlow 1.12 and Python 3.6, but it works fine with other versions."
351,tensorflow_tensorflow,issue,24828,"I tried installting tensorflow 1.12 using both pip install and building from source.However when I am trying to run faster rcnn model i get following error message:
Failed to get convolution algorithm. This is probably because cuDNN failed to initialize, so try looking to see if a warning log message was printed above.

I only get this with tf 1.12 and python 3.6 ,it works fine with python 3.6","I've installed TensorFlow 1.12 using both pip and from source, but I can't get the Faster R-CNN model to run. The error message I receive is 'Failed to get convolution algorithm,' which suggests a cuDNN initialization problem. This only happens with TensorFlow 1.12 and Python 3.6. I don't encounter this issue with other versions of TensorFlow.",Unknown,39,39,tensorflow_tensorflow,24828,issue,,1,3,3,1,"I've installed TensorFlow 1.12 using both pip and from source, but I can't get the Faster R-CNN model to run. The error message I receive is 'Failed to get convolution algorithm,' which suggests a cuDNN initialization problem. This only happens with TensorFlow 1.12 and Python 3.6. I don't encounter this issue with other versions of TensorFlow."
352,tensorflow_tensorflow,issue,24828,"I tried installting tensorflow 1.12 using both pip install and building from source.However when I am trying to run faster rcnn model i get following error message:
Failed to get convolution algorithm. This is probably because cuDNN failed to initialize, so try looking to see if a warning log message was printed above.

I only get this with tf 1.12 and python 3.6 ,it works fine with python 3.6","I'm encountering an error when trying to run the Faster R-CNN model with TensorFlow 1.12. The error message indicates that cuDNN failed to initialize, and it says 'Failed to get convolution algorithm.' I've tried installing TensorFlow 1.12 using both pip and building from source, but the issue persists. It only occurs with TensorFlow 1.12 and Python 3.6, and it works fine with other versions.",Unknown,39,39,tensorflow_tensorflow,24828,issue,,1,3,3,1,"I'm encountering an error when trying to run the Faster R-CNN model with TensorFlow 1.12. The error message indicates that cuDNN failed to initialize, and it says 'Failed to get convolution algorithm.' I've tried installing TensorFlow 1.12 using both pip and building from source, but the issue persists. It only occurs with TensorFlow 1.12 and Python 3.6, and it works fine with other versions."
353,tensorflow_tensorflow,issue,24828,"I tried installting tensorflow 1.12 using both pip install and building from source.However when I am trying to run faster rcnn model i get following error message:
Failed to get convolution algorithm. This is probably because cuDNN failed to initialize, so try looking to see if a warning log message was printed above.

I only get this with tf 1.12 and python 3.6 ,it works fine with python 3.6","I'm facing a problem with TensorFlow 1.12 when running the Faster R-CNN model. The error message I get is 'Failed to get convolution algorithm,' which seems to be related to cuDNN initialization. I've installed TensorFlow 1.12 using both pip and from source, but the issue only appears with TensorFlow 1.12 and Python 3.6. It works fine with other versions of TensorFlow.",Unknown,39,39,tensorflow_tensorflow,24828,issue,,1,3,3,1,"I'm facing a problem with TensorFlow 1.12 when running the Faster R-CNN model. The error message I get is 'Failed to get convolution algorithm,' which seems to be related to cuDNN initialization. I've installed TensorFlow 1.12 using both pip and from source, but the issue only appears with TensorFlow 1.12 and Python 3.6. It works fine with other versions of TensorFlow."
354,tensorflow_tensorflow,issue,24828,"I tried installting tensorflow 1.12 using both pip install and building from source.However when I am trying to run faster rcnn model i get following error message:
Failed to get convolution algorithm. This is probably because cuDNN failed to initialize, so try looking to see if a warning log message was printed above.

I only get this with tf 1.12 and python 3.6 ,it works fine with python 3.6","I've installed TensorFlow 1.12 using both pip and building from source, but I can't run the Faster R-CNN model. The error message I receive is 'Failed to get convolution algorithm,' which suggests a cuDNN initialization problem. This issue only occurs with TensorFlow 1.12 and Python 3.6, but it works fine with other versions of TensorFlow and Python.",Unknown,39,39,tensorflow_tensorflow,24828,issue,,1,3,3,1,"I've installed TensorFlow 1.12 using both pip and building from source, but I can't run the Faster R-CNN model. The error message I receive is 'Failed to get convolution algorithm,' which suggests a cuDNN initialization problem. This issue only occurs with TensorFlow 1.12 and Python 3.6, but it works fine with other versions of TensorFlow and Python."
355,tensorflow_tensorflow,issue,24828,"I tried installting tensorflow 1.12 using both pip install and building from source.However when I am trying to run faster rcnn model i get following error message:
Failed to get convolution algorithm. This is probably because cuDNN failed to initialize, so try looking to see if a warning log message was printed above.

I only get this with tf 1.12 and python 3.6 ,it works fine with python 3.6","I'm running into an issue with TensorFlow 1.12 when trying to use the Faster R-CNN model. The error message is 'Failed to get convolution algorithm,' which indicates a problem with cuDNN initialization. I've tried installing TensorFlow 1.12 using both pip and from source, but the issue only appears with TensorFlow 1.12 and Python 3.6. It works fine with other versions of TensorFlow and Python.",Unknown,39,39,tensorflow_tensorflow,24828,issue,,1,3,3,1,"I'm running into an issue with TensorFlow 1.12 when trying to use the Faster R-CNN model. The error message is 'Failed to get convolution algorithm,' which indicates a problem with cuDNN initialization. I've tried installing TensorFlow 1.12 using both pip and from source, but the issue only appears with TensorFlow 1.12 and Python 3.6. It works fine with other versions of TensorFlow and Python."
356,tensorflow_tensorflow,issue,24828,"I tried installting tensorflow 1.12 using both pip install and building from source.However when I am trying to run faster rcnn model i get following error message:
Failed to get convolution algorithm. This is probably because cuDNN failed to initialize, so try looking to see if a warning log message was printed above.

I only get this with tf 1.12 and python 3.6 ,it works fine with python 3.6","I'm having trouble with TensorFlow 1.12 when running the Faster R-CNN model. The error message I get is 'Failed to get convolution algorithm,' which seems to be related to cuDNN initialization. I've installed TensorFlow 1.12 using both pip and building from source, but the issue only occurs with TensorFlow 1.12 and Python 3.6. It works fine with other versions of TensorFlow and Python.",Unknown,39,39,tensorflow_tensorflow,24828,issue,,1,3,3,1,"I'm having trouble with TensorFlow 1.12 when running the Faster R-CNN model. The error message I get is 'Failed to get convolution algorithm,' which seems to be related to cuDNN initialization. I've installed TensorFlow 1.12 using both pip and building from source, but the issue only occurs with TensorFlow 1.12 and Python 3.6. It works fine with other versions of TensorFlow and Python."
357,tensorflow_tensorflow,issue,24828,"I tried installting tensorflow 1.12 using both pip install and building from source.However when I am trying to run faster rcnn model i get following error message:
Failed to get convolution algorithm. This is probably because cuDNN failed to initialize, so try looking to see if a warning log message was printed above.

I only get this with tf 1.12 and python 3.6 ,it works fine with python 3.6","I'm encountering an error when trying to run the Faster R-CNN model with TensorFlow 1.12. The error message is 'Failed to get convolution algorithm,' which suggests a cuDNN initialization problem. I've installed TensorFlow 1.12 using both pip and building from source, but the issue only appears with TensorFlow 1.12 and Python 3.6. It works fine with other versions of TensorFlow and Python.",Unknown,39,39,tensorflow_tensorflow,24828,issue,,1,3,3,1,"I'm encountering an error when trying to run the Faster R-CNN model with TensorFlow 1.12. The error message is 'Failed to get convolution algorithm,' which suggests a cuDNN initialization problem. I've installed TensorFlow 1.12 using both pip and building from source, but the issue only appears with TensorFlow 1.12 and Python 3.6. It works fine with other versions of TensorFlow and Python."
358,tensorflow_tensorflow,issue,24828,"It is just a problem with cuDNN version incompatibility. I also downgrade to 1.8, although solving the problem, but no need when there is a much higher version which is definitely better than the old version. So I found that I was using conda installing TF, which conda was also installed everything even Cuda and cudnn, so the python was not detecting my installed Cuda and cudnn, it was using which was installed by conda which was very old version of cudnn, so I deleted the conda installed Cuda and cudnn, and then installed TF with pip, and it was OK.","The issue was due to a cuDNN version mismatch. I downgraded to 1.8, which fixed the problem, but it wasn't necessary since a much newer version is available and better. I realized that conda was managing the installation of TensorFlow, including Cuda and cuDNN, which led to the old cuDNN version being used. After removing the conda-installed Cuda and cuDNN, I installed TensorFlow with pip, and everything worked fine.",Knowledge,40,40,tensorflow_tensorflow,24828,issue,,0,0,0,0,"The issue was due to a cuDNN version mismatch. I downgraded to 1.8, which fixed the problem, but it wasn't necessary since a much newer version is available and better. I realized that conda was managing the installation of TensorFlow, including Cuda and cuDNN, which led to the old cuDNN version being used. After removing the conda-installed Cuda and cuDNN, I installed TensorFlow with pip, and everything worked fine."
359,tensorflow_tensorflow,issue,24828,"It is just a problem with cuDNN version incompatibility. I also downgrade to 1.8, although solving the problem, but no need when there is a much higher version which is definitely better than the old version. So I found that I was using conda installing TF, which conda was also installed everything even Cuda and cudnn, so the python was not detecting my installed Cuda and cudnn, it was using which was installed by conda which was very old version of cudnn, so I deleted the conda installed Cuda and cudnn, and then installed TF with pip, and it was OK.","I encountered a problem with cuDNN version incompatibility. Downgrading to 1.8 resolved it, but it wasn't ideal since a newer version is available. The root cause was that conda was handling the installation of TensorFlow, Cuda, and cuDNN, leading to an outdated cuDNN version. I uninstalled the conda-managed Cuda and cuDNN, then installed TensorFlow with pip, and the issue was resolved.",Knowledge,40,40,tensorflow_tensorflow,24828,issue,,0,0,0,0,"I encountered a problem with cuDNN version incompatibility. Downgrading to 1.8 resolved it, but it wasn't ideal since a newer version is available. The root cause was that conda was handling the installation of TensorFlow, Cuda, and cuDNN, leading to an outdated cuDNN version. I uninstalled the conda-managed Cuda and cuDNN, then installed TensorFlow with pip, and the issue was resolved."
360,tensorflow_tensorflow,issue,24828,"It is just a problem with cuDNN version incompatibility. I also downgrade to 1.8, although solving the problem, but no need when there is a much higher version which is definitely better than the old version. So I found that I was using conda installing TF, which conda was also installed everything even Cuda and cudnn, so the python was not detecting my installed Cuda and cudnn, it was using which was installed by conda which was very old version of cudnn, so I deleted the conda installed Cuda and cudnn, and then installed TF with pip, and it was OK.","The problem was caused by an outdated cuDNN version. I initially downgraded to 1.8, which fixed the issue, but it wasn't necessary given the availability of a much newer version. I discovered that conda was managing the installation of TensorFlow, including Cuda and cuDNN, which led to the old cuDNN version being used. After removing the conda-installed Cuda and cuDNN, I installed TensorFlow with pip, and it worked as expected.",Knowledge,40,40,tensorflow_tensorflow,24828,issue,,0,0,0,0,"The problem was caused by an outdated cuDNN version. I initially downgraded to 1.8, which fixed the issue, but it wasn't necessary given the availability of a much newer version. I discovered that conda was managing the installation of TensorFlow, including Cuda and cuDNN, which led to the old cuDNN version being used. After removing the conda-installed Cuda and cuDNN, I installed TensorFlow with pip, and it worked as expected."
361,tensorflow_tensorflow,issue,24828,"It is just a problem with cuDNN version incompatibility. I also downgrade to 1.8, although solving the problem, but no need when there is a much higher version which is definitely better than the old version. So I found that I was using conda installing TF, which conda was also installed everything even Cuda and cudnn, so the python was not detecting my installed Cuda and cudnn, it was using which was installed by conda which was very old version of cudnn, so I deleted the conda installed Cuda and cudnn, and then installed TF with pip, and it was OK.","I faced a cuDNN version incompatibility issue. Downgrading to 1.8 solved the problem, but it wasn't the best solution since a newer version is available. The issue was that conda was handling the installation of TensorFlow, Cuda, and cuDNN, which resulted in an outdated cuDNN version. I uninstalled the conda-managed Cuda and cuDNN, then installed TensorFlow with pip, and the problem was resolved.",Knowledge,40,40,tensorflow_tensorflow,24828,issue,,0,0,0,0,"I faced a cuDNN version incompatibility issue. Downgrading to 1.8 solved the problem, but it wasn't the best solution since a newer version is available. The issue was that conda was handling the installation of TensorFlow, Cuda, and cuDNN, which resulted in an outdated cuDNN version. I uninstalled the conda-managed Cuda and cuDNN, then installed TensorFlow with pip, and the problem was resolved."
362,tensorflow_tensorflow,issue,24828,"It is just a problem with cuDNN version incompatibility. I also downgrade to 1.8, although solving the problem, but no need when there is a much higher version which is definitely better than the old version. So I found that I was using conda installing TF, which conda was also installed everything even Cuda and cudnn, so the python was not detecting my installed Cuda and cudnn, it was using which was installed by conda which was very old version of cudnn, so I deleted the conda installed Cuda and cudnn, and then installed TF with pip, and it was OK.","The issue was due to a cuDNN version mismatch. I downgraded to 1.8, which fixed the problem, but it wasn't necessary since a much newer version is available. I found that conda was managing the installation of TensorFlow, including Cuda and cuDNN, which led to the old cuDNN version being used. After removing the conda-installed Cuda and cuDNN, I installed TensorFlow with pip, and everything worked correctly.",Knowledge,40,40,tensorflow_tensorflow,24828,issue,,0,0,0,0,"The issue was due to a cuDNN version mismatch. I downgraded to 1.8, which fixed the problem, but it wasn't necessary since a much newer version is available. I found that conda was managing the installation of TensorFlow, including Cuda and cuDNN, which led to the old cuDNN version being used. After removing the conda-installed Cuda and cuDNN, I installed TensorFlow with pip, and everything worked correctly."
363,tensorflow_tensorflow,issue,24828,Had same problem with cuda 9.0 and cuDNN 7.0.5.15-1 on Ubuntu 16.04 with Tensorflow 1.12. Updating to cuDNN 7.4.2.24 fixed this for me!,I faced the same issue with CUDA 9.0 and cuDNN 7.0.5.15 on Ubuntu 16.04 while using Tensorflow 1.12. Upgrading to cuDNN 7.4.2.24 resolved the problem for me.,Knowledge,41,41,tensorflow_tensorflow,24828,issue,,0,0,0,0,I faced the same issue with CUDA 9.0 and cuDNN 7.0.5.15 on Ubuntu 16.04 while using Tensorflow 1.12. Upgrading to cuDNN 7.4.2.24 resolved the problem for me.
364,tensorflow_tensorflow,issue,24828,Had same problem with cuda 9.0 and cuDNN 7.0.5.15-1 on Ubuntu 16.04 with Tensorflow 1.12. Updating to cuDNN 7.4.2.24 fixed this for me!,I encountered the same problem using CUDA 9.0 and cuDNN 7.0.5.15 on Ubuntu 16.04 with Tensorflow 1.12. Switching to cuDNN 7.4.2.24 fixed the issue.,Knowledge,41,41,tensorflow_tensorflow,24828,issue,,0,0,0,0,I encountered the same problem using CUDA 9.0 and cuDNN 7.0.5.15 on Ubuntu 16.04 with Tensorflow 1.12. Switching to cuDNN 7.4.2.24 fixed the issue.
365,tensorflow_tensorflow,issue,24828,Had same problem with cuda 9.0 and cuDNN 7.0.5.15-1 on Ubuntu 16.04 with Tensorflow 1.12. Updating to cuDNN 7.4.2.24 fixed this for me!,I had the same problem with CUDA 9.0 and cuDNN 7.0.5.15 on Ubuntu 16.04 when running Tensorflow 1.12. Updating to cuDNN 7.4.2.24 solved the issue for me.,Knowledge,41,41,tensorflow_tensorflow,24828,issue,,0,0,0,0,I had the same problem with CUDA 9.0 and cuDNN 7.0.5.15 on Ubuntu 16.04 when running Tensorflow 1.12. Updating to cuDNN 7.4.2.24 solved the issue for me.
366,tensorflow_tensorflow,issue,24828,Had same problem with cuda 9.0 and cuDNN 7.0.5.15-1 on Ubuntu 16.04 with Tensorflow 1.12. Updating to cuDNN 7.4.2.24 fixed this for me!,I ran into the same issue with CUDA 9.0 and cuDNN 7.0.5.15 on Ubuntu 16.04 while using Tensorflow 1.12. The problem was resolved by upgrading to cuDNN 7.4.2.24.,Knowledge,41,41,tensorflow_tensorflow,24828,issue,,0,0,0,0,I ran into the same issue with CUDA 9.0 and cuDNN 7.0.5.15 on Ubuntu 16.04 while using Tensorflow 1.12. The problem was resolved by upgrading to cuDNN 7.4.2.24.
367,tensorflow_tensorflow,issue,24828,Had same problem with cuda 9.0 and cuDNN 7.0.5.15-1 on Ubuntu 16.04 with Tensorflow 1.12. Updating to cuDNN 7.4.2.24 fixed this for me!,I had the same problem with CUDA 9.0 and cuDNN 7.0.5.15 on Ubuntu 16.04 with Tensorflow 1.12. Updating to cuDNN 7.4.2.24 fixed it for me.,Knowledge,41,41,tensorflow_tensorflow,24828,issue,,0,0,0,0,I had the same problem with CUDA 9.0 and cuDNN 7.0.5.15 on Ubuntu 16.04 with Tensorflow 1.12. Updating to cuDNN 7.4.2.24 fixed it for me.
368,tensorflow_tensorflow,issue,24828,Had same problem with cuda 9.0 and cuDNN 7.0.5.15-1 on Ubuntu 16.04 with Tensorflow 1.12. Updating to cuDNN 7.4.2.24 fixed this for me!,I experienced the same issue with CUDA 9.0 and cuDNN 7.0.5.15 on Ubuntu 16.04 while using Tensorflow 1.12. The problem was resolved by upgrading to cuDNN 7.4.2.24.,Knowledge,41,41,tensorflow_tensorflow,24828,issue,,0,0,0,0,I experienced the same issue with CUDA 9.0 and cuDNN 7.0.5.15 on Ubuntu 16.04 while using Tensorflow 1.12. The problem was resolved by upgrading to cuDNN 7.4.2.24.
369,tensorflow_tensorflow,issue,24828,Had same problem with cuda 9.0 and cuDNN 7.0.5.15-1 on Ubuntu 16.04 with Tensorflow 1.12. Updating to cuDNN 7.4.2.24 fixed this for me!,I had the same problem with CUDA 9.0 and cuDNN 7.0.5.15 on Ubuntu 16.04 with Tensorflow 1.12. The issue was fixed by updating to cuDNN 7.4.2.24.,Knowledge,41,41,tensorflow_tensorflow,24828,issue,,0,0,0,0,I had the same problem with CUDA 9.0 and cuDNN 7.0.5.15 on Ubuntu 16.04 with Tensorflow 1.12. The issue was fixed by updating to cuDNN 7.4.2.24.
370,tensorflow_tensorflow,issue,24828,Had same problem with cuda 9.0 and cuDNN 7.0.5.15-1 on Ubuntu 16.04 with Tensorflow 1.12. Updating to cuDNN 7.4.2.24 fixed this for me!,I faced the same issue with CUDA 9.0 and cuDNN 7.0.5.15 on Ubuntu 16.04 while using Tensorflow 1.12. The problem was resolved by upgrading to cuDNN 7.4.2.24.,Knowledge,41,41,tensorflow_tensorflow,24828,issue,,0,0,0,0,I faced the same issue with CUDA 9.0 and cuDNN 7.0.5.15 on Ubuntu 16.04 while using Tensorflow 1.12. The problem was resolved by upgrading to cuDNN 7.4.2.24.
371,tensorflow_tensorflow,issue,24828,Had same problem with cuda 9.0 and cuDNN 7.0.5.15-1 on Ubuntu 16.04 with Tensorflow 1.12. Updating to cuDNN 7.4.2.24 fixed this for me!,I encountered the same problem with CUDA 9.0 and cuDNN 7.0.5.15 on Ubuntu 16.04 with Tensorflow 1.12. Updating to cuDNN 7.4.2.24 fixed the issue for me.,Knowledge,41,41,tensorflow_tensorflow,24828,issue,,0,0,0,0,I encountered the same problem with CUDA 9.0 and cuDNN 7.0.5.15 on Ubuntu 16.04 with Tensorflow 1.12. Updating to cuDNN 7.4.2.24 fixed the issue for me.
372,tensorflow_tensorflow,issue,24828,Had same problem with cuda 9.0 and cuDNN 7.0.5.15-1 on Ubuntu 16.04 with Tensorflow 1.12. Updating to cuDNN 7.4.2.24 fixed this for me!,I had the same issue with CUDA 9.0 and cuDNN 7.0.5.15 on Ubuntu 16.04 while using Tensorflow 1.12. The problem was resolved by upgrading to cuDNN 7.4.2.24.,Knowledge,41,41,tensorflow_tensorflow,24828,issue,,0,0,0,0,I had the same issue with CUDA 9.0 and cuDNN 7.0.5.15 on Ubuntu 16.04 while using Tensorflow 1.12. The problem was resolved by upgrading to cuDNN 7.4.2.24.
373,tensorflow_tensorflow,issue,24828,"OK, I was able to execute my CNN. I'm using tensorflow tf-nightly-gpu-2.0-preview, and running on a ipython notebook. I had to add this to my notebook:

from tensorflow.compat.v1 import ConfigProto
from tensorflow.compat.v1 import InteractiveSession

config = ConfigProto()
config.gpu_options.allow_growth = True
session = InteractiveSession(config=config)","I managed to run my CNN using tensorflow tf-nightly-gpu-2.0-preview in an IPython notebook. To get it working, I had to include the following code to configure GPU memory growth: from tensorflow.compat.v1 import ConfigProto, InteractiveSession; config = ConfigProto(); config.gpu_options.allow_growth = True; session = InteractiveSession(config=config).",Knowledge,42,42,tensorflow_tensorflow,24828,issue,,0,0,0,0,"I managed to run my CNN using tensorflow tf-nightly-gpu-2.0-preview in an IPython notebook. To get it working, I had to include the following code to configure GPU memory growth: from tensorflow.compat.v1 import ConfigProto, InteractiveSession; config = ConfigProto(); config.gpu_options.allow_growth = True; session = InteractiveSession(config=config)."
374,tensorflow_tensorflow,issue,24828,"OK, I was able to execute my CNN. I'm using tensorflow tf-nightly-gpu-2.0-preview, and running on a ipython notebook. I had to add this to my notebook:

from tensorflow.compat.v1 import ConfigProto
from tensorflow.compat.v1 import InteractiveSession

config = ConfigProto()
config.gpu_options.allow_growth = True
session = InteractiveSession(config=config)","I successfully executed my CNN in an IPython notebook with tensorflow tf-nightly-gpu-2.0-preview. The key was to set up GPU memory management by adding these lines: from tensorflow.compat.v1 import ConfigProto, InteractiveSession; config = ConfigProto(); config.gpu_options.allow_growth = True; session = InteractiveSession(config=config).",Knowledge,42,42,tensorflow_tensorflow,24828,issue,,0,0,0,0,"I successfully executed my CNN in an IPython notebook with tensorflow tf-nightly-gpu-2.0-preview. The key was to set up GPU memory management by adding these lines: from tensorflow.compat.v1 import ConfigProto, InteractiveSession; config = ConfigProto(); config.gpu_options.allow_growth = True; session = InteractiveSession(config=config)."
375,tensorflow_tensorflow,issue,24828,"OK, I was able to execute my CNN. I'm using tensorflow tf-nightly-gpu-2.0-preview, and running on a ipython notebook. I had to add this to my notebook:

from tensorflow.compat.v1 import ConfigProto
from tensorflow.compat.v1 import InteractiveSession

config = ConfigProto()
config.gpu_options.allow_growth = True
session = InteractiveSession(config=config)","I got my CNN running in an IPython notebook using tensorflow tf-nightly-gpu-2.0-preview. The trick was to configure the GPU to allow memory growth with the following code: from tensorflow.compat.v1 import ConfigProto, InteractiveSession; config = ConfigProto(); config.gpu_options.allow_growth = True; session = InteractiveSession(config=config).",Knowledge,42,42,tensorflow_tensorflow,24828,issue,,0,0,0,0,"I got my CNN running in an IPython notebook using tensorflow tf-nightly-gpu-2.0-preview. The trick was to configure the GPU to allow memory growth with the following code: from tensorflow.compat.v1 import ConfigProto, InteractiveSession; config = ConfigProto(); config.gpu_options.allow_growth = True; session = InteractiveSession(config=config)."
376,tensorflow_tensorflow,issue,24828,"OK, I was able to execute my CNN. I'm using tensorflow tf-nightly-gpu-2.0-preview, and running on a ipython notebook. I had to add this to my notebook:

from tensorflow.compat.v1 import ConfigProto
from tensorflow.compat.v1 import InteractiveSession

config = ConfigProto()
config.gpu_options.allow_growth = True
session = InteractiveSession(config=config)","I was able to run my CNN in an IPython notebook with tensorflow tf-nightly-gpu-2.0-preview by configuring the GPU to dynamically allocate memory. Here's the code I used: from tensorflow.compat.v1 import ConfigProto, InteractiveSession; config = ConfigProto(); config.gpu_options.allow_growth = True; session = InteractiveSession(config=config).",Knowledge,42,42,tensorflow_tensorflow,24828,issue,,0,0,0,0,"I was able to run my CNN in an IPython notebook with tensorflow tf-nightly-gpu-2.0-preview by configuring the GPU to dynamically allocate memory. Here's the code I used: from tensorflow.compat.v1 import ConfigProto, InteractiveSession; config = ConfigProto(); config.gpu_options.allow_growth = True; session = InteractiveSession(config=config)."
377,tensorflow_tensorflow,issue,24828,"OK, I was able to execute my CNN. I'm using tensorflow tf-nightly-gpu-2.0-preview, and running on a ipython notebook. I had to add this to my notebook:

from tensorflow.compat.v1 import ConfigProto
from tensorflow.compat.v1 import InteractiveSession

config = ConfigProto()
config.gpu_options.allow_growth = True
session = InteractiveSession(config=config)","I successfully executed my CNN using tensorflow tf-nightly-gpu-2.0-preview in an IPython notebook. The crucial step was to set up the GPU to allow memory growth with this configuration: from tensorflow.compat.v1 import ConfigProto, InteractiveSession; config = ConfigProto(); config.gpu_options.allow_growth = True; session = InteractiveSession(config=config).",Knowledge,42,42,tensorflow_tensorflow,24828,issue,,0,0,0,0,"I successfully executed my CNN using tensorflow tf-nightly-gpu-2.0-preview in an IPython notebook. The crucial step was to set up the GPU to allow memory growth with this configuration: from tensorflow.compat.v1 import ConfigProto, InteractiveSession; config = ConfigProto(); config.gpu_options.allow_growth = True; session = InteractiveSession(config=config)."
378,tensorflow_tensorflow,issue,24828,"OK, I was able to execute my CNN. I'm using tensorflow tf-nightly-gpu-2.0-preview, and running on a ipython notebook. I had to add this to my notebook:

from tensorflow.compat.v1 import ConfigProto
from tensorflow.compat.v1 import InteractiveSession

config = ConfigProto()
config.gpu_options.allow_growth = True
session = InteractiveSession(config=config)","I managed to get my CNN running in an IPython notebook with tensorflow tf-nightly-gpu-2.0-preview by adding the following lines to handle GPU memory: from tensorflow.compat.v1 import ConfigProto, InteractiveSession; config = ConfigProto(); config.gpu_options.allow_growth = True; session = InteractiveSession(config=config).",Knowledge,42,42,tensorflow_tensorflow,24828,issue,,0,0,0,0,"I managed to get my CNN running in an IPython notebook with tensorflow tf-nightly-gpu-2.0-preview by adding the following lines to handle GPU memory: from tensorflow.compat.v1 import ConfigProto, InteractiveSession; config = ConfigProto(); config.gpu_options.allow_growth = True; session = InteractiveSession(config=config)."
379,tensorflow_tensorflow,issue,24828,"OK, I was able to execute my CNN. I'm using tensorflow tf-nightly-gpu-2.0-preview, and running on a ipython notebook. I had to add this to my notebook:

from tensorflow.compat.v1 import ConfigProto
from tensorflow.compat.v1 import InteractiveSession

config = ConfigProto()
config.gpu_options.allow_growth = True
session = InteractiveSession(config=config)","I was able to run my CNN in an IPython notebook using tensorflow tf-nightly-gpu-2.0-preview. The key to success was configuring the GPU to allow memory growth with this code: from tensorflow.compat.v1 import ConfigProto, InteractiveSession; config = ConfigProto(); config.gpu_options.allow_growth = True; session = InteractiveSession(config=config).",Knowledge,42,42,tensorflow_tensorflow,24828,issue,,0,0,0,0,"I was able to run my CNN in an IPython notebook using tensorflow tf-nightly-gpu-2.0-preview. The key to success was configuring the GPU to allow memory growth with this code: from tensorflow.compat.v1 import ConfigProto, InteractiveSession; config = ConfigProto(); config.gpu_options.allow_growth = True; session = InteractiveSession(config=config)."
380,tensorflow_tensorflow,issue,24828,"OK, I was able to execute my CNN. I'm using tensorflow tf-nightly-gpu-2.0-preview, and running on a ipython notebook. I had to add this to my notebook:

from tensorflow.compat.v1 import ConfigProto
from tensorflow.compat.v1 import InteractiveSession

config = ConfigProto()
config.gpu_options.allow_growth = True
session = InteractiveSession(config=config)","I successfully executed my CNN in an IPython notebook with tensorflow tf-nightly-gpu-2.0-preview. The solution was to set up the GPU to dynamically allocate memory using the following configuration: from tensorflow.compat.v1 import ConfigProto, InteractiveSession; config = ConfigProto(); config.gpu_options.allow_growth = True; session = InteractiveSession(config=config).",Knowledge,42,42,tensorflow_tensorflow,24828,issue,,0,0,0,0,"I successfully executed my CNN in an IPython notebook with tensorflow tf-nightly-gpu-2.0-preview. The solution was to set up the GPU to dynamically allocate memory using the following configuration: from tensorflow.compat.v1 import ConfigProto, InteractiveSession; config = ConfigProto(); config.gpu_options.allow_growth = True; session = InteractiveSession(config=config)."
381,tensorflow_tensorflow,issue,24828,"OK, I was able to execute my CNN. I'm using tensorflow tf-nightly-gpu-2.0-preview, and running on a ipython notebook. I had to add this to my notebook:

from tensorflow.compat.v1 import ConfigProto
from tensorflow.compat.v1 import InteractiveSession

config = ConfigProto()
config.gpu_options.allow_growth = True
session = InteractiveSession(config=config)","I got my CNN running in an IPython notebook with tensorflow tf-nightly-gpu-2.0-preview by configuring the GPU to allow memory growth. Here's the code I used: from tensorflow.compat.v1 import ConfigProto, InteractiveSession; config = ConfigProto(); config.gpu_options.allow_growth = True; session = InteractiveSession(config=config).",Knowledge,42,42,tensorflow_tensorflow,24828,issue,,0,0,0,0,"I got my CNN running in an IPython notebook with tensorflow tf-nightly-gpu-2.0-preview by configuring the GPU to allow memory growth. Here's the code I used: from tensorflow.compat.v1 import ConfigProto, InteractiveSession; config = ConfigProto(); config.gpu_options.allow_growth = True; session = InteractiveSession(config=config)."
382,tensorflow_tensorflow,issue,24828,"OK, I was able to execute my CNN. I'm using tensorflow tf-nightly-gpu-2.0-preview, and running on a ipython notebook. I had to add this to my notebook:

from tensorflow.compat.v1 import ConfigProto
from tensorflow.compat.v1 import InteractiveSession

config = ConfigProto()
config.gpu_options.allow_growth = True
session = InteractiveSession(config=config)","I was able to run my CNN in an IPython notebook using tensorflow tf-nightly-gpu-2.0-preview. The crucial step was to set up the GPU to dynamically allocate memory with this configuration: from tensorflow.compat.v1 import ConfigProto, InteractiveSession; config = ConfigProto(); config.gpu_options.allow_growth = True; session = InteractiveSession(config=config).",Knowledge,42,42,tensorflow_tensorflow,24828,issue,,0,0,0,0,"I was able to run my CNN in an IPython notebook using tensorflow tf-nightly-gpu-2.0-preview. The crucial step was to set up the GPU to dynamically allocate memory with this configuration: from tensorflow.compat.v1 import ConfigProto, InteractiveSession; config = ConfigProto(); config.gpu_options.allow_growth = True; session = InteractiveSession(config=config)."
383,tensorflow_tensorflow,issue,24496,"I'm running the CNN model on MNIST. When I'm running with the GPU, I am encountering
2018-12-20 20:09:13.644176: E tensorflow/stream_executor/cuda/cuda_dnn.cc:334] Could not create cudnn handle: CUDNN_STATUS_INTERNAL_ERROR

I did some digging and realized that it is a memory issue (which shouldn't be the case as I have 32GB of RAM and 64GB of swap. I ran htop when running the model and I have 20+GB free, which is more than enough to fit the 8GB vRAM mappings.

Using the gpu_options.allow_growth = True gets the model to work properly, and setting os.environ['CUDA_VISIBLE_DEVICES'] = '-1' also works. This means that I AM facing a memory issue, but I don't see how.","I'm training a CNN on the MNIST dataset using a GPU, but I'm encountering a CUDNN_STATUS_INTERNAL_ERROR. Despite having 32GB of RAM and 64GB of swap, and observing over 20GB of free memory with htop, the error persists. Setting `gpu_options.allow_growth = True` or using `os.environ['CUDA_VISIBLE_DEVICES'] = '-1'` resolves the issue, but I'm confused about why I'm facing a memory problem.",Unknown,43,43,tensorflow_tensorflow,24496,issue,,1,3,3,1,"I'm training a CNN on the MNIST dataset using a GPU, but I'm encountering a CUDNN_STATUS_INTERNAL_ERROR. Despite having 32GB of RAM and 64GB of swap, and observing over 20GB of free memory with htop, the error persists. Setting `gpu_options.allow_growth = True` or using `os.environ['CUDA_VISIBLE_DEVICES'] = '-1'` resolves the issue, but I'm confused about why I'm facing a memory problem."
384,tensorflow_tensorflow,issue,24496,"I'm running the CNN model on MNIST. When I'm running with the GPU, I am encountering
2018-12-20 20:09:13.644176: E tensorflow/stream_executor/cuda/cuda_dnn.cc:334] Could not create cudnn handle: CUDNN_STATUS_INTERNAL_ERROR

I did some digging and realized that it is a memory issue (which shouldn't be the case as I have 32GB of RAM and 64GB of swap. I ran htop when running the model and I have 20+GB free, which is more than enough to fit the 8GB vRAM mappings.

Using the gpu_options.allow_growth = True gets the model to work properly, and setting os.environ['CUDA_VISIBLE_DEVICES'] = '-1' also works. This means that I AM facing a memory issue, but I don't see how.","I'm running a CNN on MNIST with a GPU and keep getting a CUDNN_STATUS_INTERNAL_ERROR. My system has 32GB of RAM and 64GB of swap, and htop shows more than 20GB of free memory. The issue is resolved by setting `gpu_options.allow_growth = True` or disabling the GPU, but I don't understand why I'm encountering a memory issue.",Unknown,43,43,tensorflow_tensorflow,24496,issue,,1,3,3,1,"I'm running a CNN on MNIST with a GPU and keep getting a CUDNN_STATUS_INTERNAL_ERROR. My system has 32GB of RAM and 64GB of swap, and htop shows more than 20GB of free memory. The issue is resolved by setting `gpu_options.allow_growth = True` or disabling the GPU, but I don't understand why I'm encountering a memory issue."
385,tensorflow_tensorflow,issue,24496,"I'm running the CNN model on MNIST. When I'm running with the GPU, I am encountering
2018-12-20 20:09:13.644176: E tensorflow/stream_executor/cuda/cuda_dnn.cc:334] Could not create cudnn handle: CUDNN_STATUS_INTERNAL_ERROR

I did some digging and realized that it is a memory issue (which shouldn't be the case as I have 32GB of RAM and 64GB of swap. I ran htop when running the model and I have 20+GB free, which is more than enough to fit the 8GB vRAM mappings.

Using the gpu_options.allow_growth = True gets the model to work properly, and setting os.environ['CUDA_VISIBLE_DEVICES'] = '-1' also works. This means that I AM facing a memory issue, but I don't see how.","I'm training a CNN on the MNIST dataset and facing a CUDNN_STATUS_INTERNAL_ERROR when using a GPU. My system has 32GB of RAM and 64GB of swap, and htop indicates over 20GB of free memory. The problem is fixed by enabling GPU memory growth or running the model on the CPU, but I'm puzzled by the memory issue.",Unknown,43,43,tensorflow_tensorflow,24496,issue,,1,3,3,1,"I'm training a CNN on the MNIST dataset and facing a CUDNN_STATUS_INTERNAL_ERROR when using a GPU. My system has 32GB of RAM and 64GB of swap, and htop indicates over 20GB of free memory. The problem is fixed by enabling GPU memory growth or running the model on the CPU, but I'm puzzled by the memory issue."
386,tensorflow_tensorflow,issue,24496,"I'm running the CNN model on MNIST. When I'm running with the GPU, I am encountering
2018-12-20 20:09:13.644176: E tensorflow/stream_executor/cuda/cuda_dnn.cc:334] Could not create cudnn handle: CUDNN_STATUS_INTERNAL_ERROR

I did some digging and realized that it is a memory issue (which shouldn't be the case as I have 32GB of RAM and 64GB of swap. I ran htop when running the model and I have 20+GB free, which is more than enough to fit the 8GB vRAM mappings.

Using the gpu_options.allow_growth = True gets the model to work properly, and setting os.environ['CUDA_VISIBLE_DEVICES'] = '-1' also works. This means that I AM facing a memory issue, but I don't see how.","I'm encountering a CUDNN_STATUS_INTERNAL_ERROR while training a CNN on MNIST with a GPU. Despite having 32GB of RAM and 64GB of swap, and observing over 20GB of free memory, the error occurs. Setting `gpu_options.allow_growth = True` or using the CPU resolves the issue, but I don't see why I'm facing a memory problem.",Unknown,43,43,tensorflow_tensorflow,24496,issue,,1,3,3,1,"I'm encountering a CUDNN_STATUS_INTERNAL_ERROR while training a CNN on MNIST with a GPU. Despite having 32GB of RAM and 64GB of swap, and observing over 20GB of free memory, the error occurs. Setting `gpu_options.allow_growth = True` or using the CPU resolves the issue, but I don't see why I'm facing a memory problem."
387,tensorflow_tensorflow,issue,24496,"I'm running the CNN model on MNIST. When I'm running with the GPU, I am encountering
2018-12-20 20:09:13.644176: E tensorflow/stream_executor/cuda/cuda_dnn.cc:334] Could not create cudnn handle: CUDNN_STATUS_INTERNAL_ERROR

I did some digging and realized that it is a memory issue (which shouldn't be the case as I have 32GB of RAM and 64GB of swap. I ran htop when running the model and I have 20+GB free, which is more than enough to fit the 8GB vRAM mappings.

Using the gpu_options.allow_growth = True gets the model to work properly, and setting os.environ['CUDA_VISIBLE_DEVICES'] = '-1' also works. This means that I AM facing a memory issue, but I don't see how.","I'm running a CNN on the MNIST dataset and getting a CUDNN_STATUS_INTERNAL_ERROR with the GPU. My system has 32GB of RAM and 64GB of swap, and htop shows more than 20GB of free memory. The issue is resolved by enabling GPU memory growth or running the model on the CPU, but I'm confused about the memory issue.",Unknown,43,43,tensorflow_tensorflow,24496,issue,,1,3,3,1,"I'm running a CNN on the MNIST dataset and getting a CUDNN_STATUS_INTERNAL_ERROR with the GPU. My system has 32GB of RAM and 64GB of swap, and htop shows more than 20GB of free memory. The issue is resolved by enabling GPU memory growth or running the model on the CPU, but I'm confused about the memory issue."
388,tensorflow_tensorflow,issue,24496,"I'm running the CNN model on MNIST. When I'm running with the GPU, I am encountering
2018-12-20 20:09:13.644176: E tensorflow/stream_executor/cuda/cuda_dnn.cc:334] Could not create cudnn handle: CUDNN_STATUS_INTERNAL_ERROR

I did some digging and realized that it is a memory issue (which shouldn't be the case as I have 32GB of RAM and 64GB of swap. I ran htop when running the model and I have 20+GB free, which is more than enough to fit the 8GB vRAM mappings.

Using the gpu_options.allow_growth = True gets the model to work properly, and setting os.environ['CUDA_VISIBLE_DEVICES'] = '-1' also works. This means that I AM facing a memory issue, but I don't see how.","I'm training a CNN on MNIST and facing a CUDNN_STATUS_INTERNAL_ERROR when using a GPU. Despite having 32GB of RAM and 64GB of swap, and observing over 20GB of free memory, the error persists. Setting `gpu_options.allow_growth = True` or disabling the GPU fixes the issue, but I don't understand why I'm encountering a memory problem.",Unknown,43,43,tensorflow_tensorflow,24496,issue,,1,3,3,1,"I'm training a CNN on MNIST and facing a CUDNN_STATUS_INTERNAL_ERROR when using a GPU. Despite having 32GB of RAM and 64GB of swap, and observing over 20GB of free memory, the error persists. Setting `gpu_options.allow_growth = True` or disabling the GPU fixes the issue, but I don't understand why I'm encountering a memory problem."
389,tensorflow_tensorflow,issue,24496,"I'm running the CNN model on MNIST. When I'm running with the GPU, I am encountering
2018-12-20 20:09:13.644176: E tensorflow/stream_executor/cuda/cuda_dnn.cc:334] Could not create cudnn handle: CUDNN_STATUS_INTERNAL_ERROR

I did some digging and realized that it is a memory issue (which shouldn't be the case as I have 32GB of RAM and 64GB of swap. I ran htop when running the model and I have 20+GB free, which is more than enough to fit the 8GB vRAM mappings.

Using the gpu_options.allow_growth = True gets the model to work properly, and setting os.environ['CUDA_VISIBLE_DEVICES'] = '-1' also works. This means that I AM facing a memory issue, but I don't see how.","I'm running a CNN on the MNIST dataset and encountering a CUDNN_STATUS_INTERNAL_ERROR with the GPU. My system has 32GB of RAM and 64GB of swap, and htop shows more than 20GB of free memory. The problem is resolved by enabling GPU memory growth or running the model on the CPU, but I'm puzzled by the memory issue.",Unknown,43,43,tensorflow_tensorflow,24496,issue,,1,3,3,1,"I'm running a CNN on the MNIST dataset and encountering a CUDNN_STATUS_INTERNAL_ERROR with the GPU. My system has 32GB of RAM and 64GB of swap, and htop shows more than 20GB of free memory. The problem is resolved by enabling GPU memory growth or running the model on the CPU, but I'm puzzled by the memory issue."
390,tensorflow_tensorflow,issue,24496,"I'm running the CNN model on MNIST. When I'm running with the GPU, I am encountering
2018-12-20 20:09:13.644176: E tensorflow/stream_executor/cuda/cuda_dnn.cc:334] Could not create cudnn handle: CUDNN_STATUS_INTERNAL_ERROR

I did some digging and realized that it is a memory issue (which shouldn't be the case as I have 32GB of RAM and 64GB of swap. I ran htop when running the model and I have 20+GB free, which is more than enough to fit the 8GB vRAM mappings.

Using the gpu_options.allow_growth = True gets the model to work properly, and setting os.environ['CUDA_VISIBLE_DEVICES'] = '-1' also works. This means that I AM facing a memory issue, but I don't see how.","I'm training a CNN on MNIST and facing a CUDNN_STATUS_INTERNAL_ERROR when using a GPU. Despite having 32GB of RAM and 64GB of swap, and observing over 20GB of free memory, the error occurs. Setting `gpu_options.allow_growth = True` or using the CPU resolves the issue, but I don't see why I'm encountering a memory problem.",Unknown,43,43,tensorflow_tensorflow,24496,issue,,1,3,3,1,"I'm training a CNN on MNIST and facing a CUDNN_STATUS_INTERNAL_ERROR when using a GPU. Despite having 32GB of RAM and 64GB of swap, and observing over 20GB of free memory, the error occurs. Setting `gpu_options.allow_growth = True` or using the CPU resolves the issue, but I don't see why I'm encountering a memory problem."
391,tensorflow_tensorflow,issue,24496,"I'm running the CNN model on MNIST. When I'm running with the GPU, I am encountering
2018-12-20 20:09:13.644176: E tensorflow/stream_executor/cuda/cuda_dnn.cc:334] Could not create cudnn handle: CUDNN_STATUS_INTERNAL_ERROR

I did some digging and realized that it is a memory issue (which shouldn't be the case as I have 32GB of RAM and 64GB of swap. I ran htop when running the model and I have 20+GB free, which is more than enough to fit the 8GB vRAM mappings.

Using the gpu_options.allow_growth = True gets the model to work properly, and setting os.environ['CUDA_VISIBLE_DEVICES'] = '-1' also works. This means that I AM facing a memory issue, but I don't see how.","I'm running a CNN on the MNIST dataset and getting a CUDNN_STATUS_INTERNAL_ERROR with the GPU. My system has 32GB of RAM and 64GB of swap, and htop indicates over 20GB of free memory. The issue is fixed by enabling GPU memory growth or running the model on the CPU, but I'm confused about the memory issue.",Unknown,43,43,tensorflow_tensorflow,24496,issue,,1,3,3,1,"I'm running a CNN on the MNIST dataset and getting a CUDNN_STATUS_INTERNAL_ERROR with the GPU. My system has 32GB of RAM and 64GB of swap, and htop indicates over 20GB of free memory. The issue is fixed by enabling GPU memory growth or running the model on the CPU, but I'm confused about the memory issue."
392,tensorflow_tensorflow,issue,24496,I was finally able to fix my problem was setting config.gpu_options.allow_growth = True.,I managed to resolve my issue by setting `config.gpu_options.allow_growth = True`. This configuration change allowed my TensorFlow model to run without running out of GPU memory.,Knowledge,44,44,tensorflow_tensorflow,24496,issue,,0,0,0,0,I managed to resolve my issue by setting `config.gpu_options.allow_growth = True`. This configuration change allowed my TensorFlow model to run without running out of GPU memory.
393,tensorflow_tensorflow,issue,24496,I was finally able to fix my problem was setting config.gpu_options.allow_growth = True.,The solution to my problem was to enable GPU memory growth by setting `config.gpu_options.allow_growth = True`. This adjustment prevented the out-of-memory errors I was encountering during training.,Knowledge,44,44,tensorflow_tensorflow,24496,issue,,0,0,0,0,The solution to my problem was to enable GPU memory growth by setting `config.gpu_options.allow_growth = True`. This adjustment prevented the out-of-memory errors I was encountering during training.
394,tensorflow_tensorflow,issue,24496,I was finally able to fix my problem was setting config.gpu_options.allow_growth = True.,I finally got my model to work by configuring TensorFlow to allow GPU memory growth. Setting `config.gpu_options.allow_growth = True` solved the memory allocation issues I was facing.,Knowledge,44,44,tensorflow_tensorflow,24496,issue,,0,0,0,0,I finally got my model to work by configuring TensorFlow to allow GPU memory growth. Setting `config.gpu_options.allow_growth = True` solved the memory allocation issues I was facing.
395,tensorflow_tensorflow,issue,24496,I was finally able to fix my problem was setting config.gpu_options.allow_growth = True.,"After much troubleshooting, I discovered that setting `config.gpu_options.allow_growth = True` fixed the GPU memory issues I was experiencing. This configuration change allowed my model to run smoothly.",Knowledge,44,44,tensorflow_tensorflow,24496,issue,,0,0,0,0,"After much troubleshooting, I discovered that setting `config.gpu_options.allow_growth = True` fixed the GPU memory issues I was experiencing. This configuration change allowed my model to run smoothly."
396,tensorflow_tensorflow,issue,24496,I was finally able to fix my problem was setting config.gpu_options.allow_growth = True.,I was able to fix the GPU memory allocation problem by enabling memory growth in TensorFlow. Setting `config.gpu_options.allow_growth = True` resolved the issue and my model now runs without errors.,Knowledge,44,44,tensorflow_tensorflow,24496,issue,,0,0,0,0,I was able to fix the GPU memory allocation problem by enabling memory growth in TensorFlow. Setting `config.gpu_options.allow_growth = True` resolved the issue and my model now runs without errors.
397,tensorflow_tensorflow,issue,24496,I was finally able to fix my problem was setting config.gpu_options.allow_growth = True.,"The key to resolving my GPU memory issues was to set `config.gpu_options.allow_growth = True`. This configuration change allowed TensorFlow to dynamically allocate memory, preventing out-of-memory errors.",Knowledge,44,44,tensorflow_tensorflow,24496,issue,,0,0,0,0,"The key to resolving my GPU memory issues was to set `config.gpu_options.allow_growth = True`. This configuration change allowed TensorFlow to dynamically allocate memory, preventing out-of-memory errors."
398,tensorflow_tensorflow,issue,24496,I was finally able to fix my problem was setting config.gpu_options.allow_growth = True.,I finally found the solution to my GPU memory problems by enabling memory growth in TensorFlow. Setting `config.gpu_options.allow_growth = True` fixed the issue and my model runs as expected.,Knowledge,44,44,tensorflow_tensorflow,24496,issue,,0,0,0,0,I finally found the solution to my GPU memory problems by enabling memory growth in TensorFlow. Setting `config.gpu_options.allow_growth = True` fixed the issue and my model runs as expected.
399,tensorflow_tensorflow,issue,24496,I was finally able to fix my problem was setting config.gpu_options.allow_growth = True.,"To fix the GPU memory allocation errors, I set `config.gpu_options.allow_growth = True`. This configuration change allowed TensorFlow to manage memory more efficiently, resolving the issue.",Knowledge,44,44,tensorflow_tensorflow,24496,issue,,0,0,0,0,"To fix the GPU memory allocation errors, I set `config.gpu_options.allow_growth = True`. This configuration change allowed TensorFlow to manage memory more efficiently, resolving the issue."
400,tensorflow_tensorflow,issue,24496,I was finally able to fix my problem was setting config.gpu_options.allow_growth = True.,I was able to resolve the GPU memory issues by enabling dynamic memory growth in TensorFlow. Setting `config.gpu_options.allow_growth = True` fixed the problem and my model now runs without any errors.,Knowledge,44,44,tensorflow_tensorflow,24496,issue,,0,0,0,0,I was able to resolve the GPU memory issues by enabling dynamic memory growth in TensorFlow. Setting `config.gpu_options.allow_growth = True` fixed the problem and my model now runs without any errors.
401,tensorflow_tensorflow,issue,24496,I was finally able to fix my problem was setting config.gpu_options.allow_growth = True.,"The solution to my GPU memory allocation problem was to set `config.gpu_options.allow_growth = True`. This configuration change allowed TensorFlow to dynamically manage memory, preventing the out-of-memory errors I was encountering.",Knowledge,44,44,tensorflow_tensorflow,24496,issue,,0,0,0,0,"The solution to my GPU memory allocation problem was to set `config.gpu_options.allow_growth = True`. This configuration change allowed TensorFlow to dynamically manage memory, preventing the out-of-memory errors I was encountering."
402,tensorflow_tensorflow,issue,37942,"my jupyter notebook that is training some LSTMs on the GPU crashes after some time with the following traceback:

InternalError:  [_Derived_]  Failed to call ThenRnnBackward with model config: [rnn_mode, rnn_input_mode, rnn_direction_mode]: 2, 0, 0 , [num_layers, input_size, num_units, dir_count, max_seq_length, batch_size, cell_num_units]: [1, 100, 100, 1, 249, 32, 100] 
         [[{{node gradients/CudnnRNN_grad/CudnnRNNBackprop}}]]
         [[StatefulPartitionedCall_1]] [Op:__inference_distributed_function_7604]

Function call stack:
distributed_function -> distributed_function -> distributed_function

This crash happens after a random amount of epochs (sometimes 6, sometimes 130+ sometimes 300+). It also crashes on different Windows machines with different GPUs. In the stacktrace I can find the following line:
130         # TODO(kaftan): File bug about tf function and errors.OutOfRangeError?

I wonder if this is connected to this issue?","My Jupyter notebook training LSTMs on the GPU crashes intermittently with an InternalError related to CudnnRNNBackward. The error message indicates a failure in the model configuration, but the crash occurs at different epochs and on different machines. I'm not sure if this is a bug in TensorFlow or if there's a configuration issue on my end.",Unknown,45,45,tensorflow_tensorflow,37942,issue,,1,3,3,1,"My Jupyter notebook training LSTMs on the GPU crashes intermittently with an InternalError related to CudnnRNNBackward. The error message indicates a failure in the model configuration, but the crash occurs at different epochs and on different machines. I'm not sure if this is a bug in TensorFlow or if there's a configuration issue on my end."
403,tensorflow_tensorflow,issue,37942,"my jupyter notebook that is training some LSTMs on the GPU crashes after some time with the following traceback:

InternalError:  [_Derived_]  Failed to call ThenRnnBackward with model config: [rnn_mode, rnn_input_mode, rnn_direction_mode]: 2, 0, 0 , [num_layers, input_size, num_units, dir_count, max_seq_length, batch_size, cell_num_units]: [1, 100, 100, 1, 249, 32, 100] 
         [[{{node gradients/CudnnRNN_grad/CudnnRNNBackprop}}]]
         [[StatefulPartitionedCall_1]] [Op:__inference_distributed_function_7604]

Function call stack:
distributed_function -> distributed_function -> distributed_function

This crash happens after a random amount of epochs (sometimes 6, sometimes 130+ sometimes 300+). It also crashes on different Windows machines with different GPUs. In the stacktrace I can find the following line:
130         # TODO(kaftan): File bug about tf function and errors.OutOfRangeError?

I wonder if this is connected to this issue?","I'm experiencing random crashes while training LSTMs on the GPU using a Jupyter notebook. The traceback points to an InternalError in the CudnnRNNBackprop node, suggesting a problem with the RNN configuration. The crash happens at varying epochs and across different Windows machines with different GPUs. Could this be related to a known issue in TensorFlow?",Unknown,45,45,tensorflow_tensorflow,37942,issue,,1,3,3,1,"I'm experiencing random crashes while training LSTMs on the GPU using a Jupyter notebook. The traceback points to an InternalError in the CudnnRNNBackprop node, suggesting a problem with the RNN configuration. The crash happens at varying epochs and across different Windows machines with different GPUs. Could this be related to a known issue in TensorFlow?"
404,tensorflow_tensorflow,issue,37942,"my jupyter notebook that is training some LSTMs on the GPU crashes after some time with the following traceback:

InternalError:  [_Derived_]  Failed to call ThenRnnBackward with model config: [rnn_mode, rnn_input_mode, rnn_direction_mode]: 2, 0, 0 , [num_layers, input_size, num_units, dir_count, max_seq_length, batch_size, cell_num_units]: [1, 100, 100, 1, 249, 32, 100] 
         [[{{node gradients/CudnnRNN_grad/CudnnRNNBackprop}}]]
         [[StatefulPartitionedCall_1]] [Op:__inference_distributed_function_7604]

Function call stack:
distributed_function -> distributed_function -> distributed_function

This crash happens after a random amount of epochs (sometimes 6, sometimes 130+ sometimes 300+). It also crashes on different Windows machines with different GPUs. In the stacktrace I can find the following line:
130         # TODO(kaftan): File bug about tf function and errors.OutOfRangeError?

I wonder if this is connected to this issue?","My LSTM training process in a Jupyter notebook keeps crashing with an InternalError involving CudnnRNNBackward. The error message suggests a problem with the model configuration, but the crash occurs at different epochs and on different machines. I noticed a TODO comment in the stacktrace about a potential bug in TensorFlow. Is this a known issue?",Unknown,45,45,tensorflow_tensorflow,37942,issue,,1,3,3,1,"My LSTM training process in a Jupyter notebook keeps crashing with an InternalError involving CudnnRNNBackward. The error message suggests a problem with the model configuration, but the crash occurs at different epochs and on different machines. I noticed a TODO comment in the stacktrace about a potential bug in TensorFlow. Is this a known issue?"
405,tensorflow_tensorflow,issue,37942,"my jupyter notebook that is training some LSTMs on the GPU crashes after some time with the following traceback:

InternalError:  [_Derived_]  Failed to call ThenRnnBackward with model config: [rnn_mode, rnn_input_mode, rnn_direction_mode]: 2, 0, 0 , [num_layers, input_size, num_units, dir_count, max_seq_length, batch_size, cell_num_units]: [1, 100, 100, 1, 249, 32, 100] 
         [[{{node gradients/CudnnRNN_grad/CudnnRNNBackprop}}]]
         [[StatefulPartitionedCall_1]] [Op:__inference_distributed_function_7604]

Function call stack:
distributed_function -> distributed_function -> distributed_function

This crash happens after a random amount of epochs (sometimes 6, sometimes 130+ sometimes 300+). It also crashes on different Windows machines with different GPUs. In the stacktrace I can find the following line:
130         # TODO(kaftan): File bug about tf function and errors.OutOfRangeError?

I wonder if this is connected to this issue?","I'm running LSTM training on the GPU in a Jupyter notebook, and it crashes with an InternalError after a random number of epochs. The error points to a failure in the CudnnRNNBackprop node, and it happens on different Windows machines with different GPUs. The stacktrace includes a TODO comment about a potential bug. Is this a known issue in TensorFlow?",Unknown,45,45,tensorflow_tensorflow,37942,issue,,1,3,3,1,"I'm running LSTM training on the GPU in a Jupyter notebook, and it crashes with an InternalError after a random number of epochs. The error points to a failure in the CudnnRNNBackprop node, and it happens on different Windows machines with different GPUs. The stacktrace includes a TODO comment about a potential bug. Is this a known issue in TensorFlow?"
406,tensorflow_tensorflow,issue,37942,"my jupyter notebook that is training some LSTMs on the GPU crashes after some time with the following traceback:

InternalError:  [_Derived_]  Failed to call ThenRnnBackward with model config: [rnn_mode, rnn_input_mode, rnn_direction_mode]: 2, 0, 0 , [num_layers, input_size, num_units, dir_count, max_seq_length, batch_size, cell_num_units]: [1, 100, 100, 1, 249, 32, 100] 
         [[{{node gradients/CudnnRNN_grad/CudnnRNNBackprop}}]]
         [[StatefulPartitionedCall_1]] [Op:__inference_distributed_function_7604]

Function call stack:
distributed_function -> distributed_function -> distributed_function

This crash happens after a random amount of epochs (sometimes 6, sometimes 130+ sometimes 300+). It also crashes on different Windows machines with different GPUs. In the stacktrace I can find the following line:
130         # TODO(kaftan): File bug about tf function and errors.OutOfRangeError?

I wonder if this is connected to this issue?","My Jupyter notebook training LSTMs on the GPU crashes with an InternalError related to CudnnRNNBackward. The error message suggests a problem with the RNN configuration, but the crash occurs at different epochs and on different machines. The stacktrace includes a TODO comment about a potential bug. Could this be a known issue in TensorFlow?",Unknown,45,45,tensorflow_tensorflow,37942,issue,,1,3,3,1,"My Jupyter notebook training LSTMs on the GPU crashes with an InternalError related to CudnnRNNBackward. The error message suggests a problem with the RNN configuration, but the crash occurs at different epochs and on different machines. The stacktrace includes a TODO comment about a potential bug. Could this be a known issue in TensorFlow?"
407,tensorflow_tensorflow,issue,37942,"my jupyter notebook that is training some LSTMs on the GPU crashes after some time with the following traceback:

InternalError:  [_Derived_]  Failed to call ThenRnnBackward with model config: [rnn_mode, rnn_input_mode, rnn_direction_mode]: 2, 0, 0 , [num_layers, input_size, num_units, dir_count, max_seq_length, batch_size, cell_num_units]: [1, 100, 100, 1, 249, 32, 100] 
         [[{{node gradients/CudnnRNN_grad/CudnnRNNBackprop}}]]
         [[StatefulPartitionedCall_1]] [Op:__inference_distributed_function_7604]

Function call stack:
distributed_function -> distributed_function -> distributed_function

This crash happens after a random amount of epochs (sometimes 6, sometimes 130+ sometimes 300+). It also crashes on different Windows machines with different GPUs. In the stacktrace I can find the following line:
130         # TODO(kaftan): File bug about tf function and errors.OutOfRangeError?

I wonder if this is connected to this issue?","I'm encountering random crashes while training LSTMs on the GPU in a Jupyter notebook. The error message points to an InternalError in the CudnnRNNBackprop node, indicating a problem with the model configuration. The crash happens at different epochs and on different Windows machines with different GPUs. The stacktrace includes a TODO comment about a potential bug. Is this a known issue in TensorFlow?",Unknown,45,45,tensorflow_tensorflow,37942,issue,,1,3,3,1,"I'm encountering random crashes while training LSTMs on the GPU in a Jupyter notebook. The error message points to an InternalError in the CudnnRNNBackprop node, indicating a problem with the model configuration. The crash happens at different epochs and on different Windows machines with different GPUs. The stacktrace includes a TODO comment about a potential bug. Is this a known issue in TensorFlow?"
408,tensorflow_tensorflow,issue,37492,"To train the LSTM-model on a GPU on my plattform (Windows 10, tf 2.1.0), the parameter batch_input_shape must be specified during the model creation in the very first layer and the parameter input_shape must be omitted.","When training an LSTM model on a GPU using TensorFlow 2.1.0 on Windows 10, I had to specify the `batch_input_shape` parameter in the first layer and omit the `input_shape` parameter to avoid errors.",Knowledge,46,46,tensorflow_tensorflow,37492,issue,,0,0,0,0,"When training an LSTM model on a GPU using TensorFlow 2.1.0 on Windows 10, I had to specify the `batch_input_shape` parameter in the first layer and omit the `input_shape` parameter to avoid errors."
409,tensorflow_tensorflow,issue,37492,"To train the LSTM-model on a GPU on my plattform (Windows 10, tf 2.1.0), the parameter batch_input_shape must be specified during the model creation in the very first layer and the parameter input_shape must be omitted.","To ensure the LSTM model runs smoothly on a GPU with TensorFlow 2.1.0 on Windows 10, I needed to set the `batch_input_shape` in the initial layer and leave out the `input_shape` parameter.",Knowledge,46,46,tensorflow_tensorflow,37492,issue,,0,0,0,0,"To ensure the LSTM model runs smoothly on a GPU with TensorFlow 2.1.0 on Windows 10, I needed to set the `batch_input_shape` in the initial layer and leave out the `input_shape` parameter."
410,tensorflow_tensorflow,issue,37492,"To train the LSTM-model on a GPU on my plattform (Windows 10, tf 2.1.0), the parameter batch_input_shape must be specified during the model creation in the very first layer and the parameter input_shape must be omitted.","I discovered that to train an LSTM model on a GPU with TensorFlow 2.1.0 on Windows 10, the `batch_input_shape` must be defined in the first layer, and the `input_shape` should not be included.",Knowledge,46,46,tensorflow_tensorflow,37492,issue,,0,0,0,0,"I discovered that to train an LSTM model on a GPU with TensorFlow 2.1.0 on Windows 10, the `batch_input_shape` must be defined in the first layer, and the `input_shape` should not be included."
411,tensorflow_tensorflow,issue,37492,"To train the LSTM-model on a GPU on my plattform (Windows 10, tf 2.1.0), the parameter batch_input_shape must be specified during the model creation in the very first layer and the parameter input_shape must be omitted.","For my LSTM model to work correctly on a GPU using TensorFlow 2.1.0 on Windows 10, I had to specify the `batch_input_shape` in the first layer and exclude the `input_shape` parameter.",Knowledge,46,46,tensorflow_tensorflow,37492,issue,,0,0,0,0,"For my LSTM model to work correctly on a GPU using TensorFlow 2.1.0 on Windows 10, I had to specify the `batch_input_shape` in the first layer and exclude the `input_shape` parameter."
412,tensorflow_tensorflow,issue,37492,"To train the LSTM-model on a GPU on my plattform (Windows 10, tf 2.1.0), the parameter batch_input_shape must be specified during the model creation in the very first layer and the parameter input_shape must be omitted.","To get the LSTM model to train on a GPU with TensorFlow 2.1.0 on Windows 10, I found that specifying the `batch_input_shape` in the first layer and omitting the `input_shape` parameter was necessary.",Knowledge,46,46,tensorflow_tensorflow,37492,issue,,0,0,0,0,"To get the LSTM model to train on a GPU with TensorFlow 2.1.0 on Windows 10, I found that specifying the `batch_input_shape` in the first layer and omitting the `input_shape` parameter was necessary."
413,tensorflow_tensorflow,issue,37492,"To train the LSTM-model on a GPU on my plattform (Windows 10, tf 2.1.0), the parameter batch_input_shape must be specified during the model creation in the very first layer and the parameter input_shape must be omitted.",I encountered an issue where the LSTM model wouldn't train on a GPU with TensorFlow 2.1.0 on Windows 10 unless I specified the `batch_input_shape` in the first layer and did not include the `input_shape` parameter.,Knowledge,46,46,tensorflow_tensorflow,37492,issue,,0,0,0,0,I encountered an issue where the LSTM model wouldn't train on a GPU with TensorFlow 2.1.0 on Windows 10 unless I specified the `batch_input_shape` in the first layer and did not include the `input_shape` parameter.
414,tensorflow_tensorflow,issue,37492,"To train the LSTM-model on a GPU on my plattform (Windows 10, tf 2.1.0), the parameter batch_input_shape must be specified during the model creation in the very first layer and the parameter input_shape must be omitted.",Training the LSTM model on a GPU using TensorFlow 2.1.0 on Windows 10 required me to set the `batch_input_shape` in the first layer and avoid using the `input_shape` parameter to prevent errors.,Knowledge,46,46,tensorflow_tensorflow,37492,issue,,0,0,0,0,Training the LSTM model on a GPU using TensorFlow 2.1.0 on Windows 10 required me to set the `batch_input_shape` in the first layer and avoid using the `input_shape` parameter to prevent errors.
415,tensorflow_tensorflow,issue,37492,"To train the LSTM-model on a GPU on my plattform (Windows 10, tf 2.1.0), the parameter batch_input_shape must be specified during the model creation in the very first layer and the parameter input_shape must be omitted.","To successfully train the LSTM model on a GPU with TensorFlow 2.1.0 on Windows 10, I had to specify the `batch_input_shape` in the initial layer and omit the `input_shape` parameter to ensure it ran without issues.",Knowledge,46,46,tensorflow_tensorflow,37492,issue,,0,0,0,0,"To successfully train the LSTM model on a GPU with TensorFlow 2.1.0 on Windows 10, I had to specify the `batch_input_shape` in the initial layer and omit the `input_shape` parameter to ensure it ran without issues."
416,tensorflow_tensorflow,issue,37492,"To train the LSTM-model on a GPU on my plattform (Windows 10, tf 2.1.0), the parameter batch_input_shape must be specified during the model creation in the very first layer and the parameter input_shape must be omitted.","I found that to train the LSTM model on a GPU with TensorFlow 2.1.0 on Windows 10, the `batch_input_shape` needed to be specified in the first layer, and the `input_shape` parameter had to be omitted.",Knowledge,46,46,tensorflow_tensorflow,37492,issue,,0,0,0,0,"I found that to train the LSTM model on a GPU with TensorFlow 2.1.0 on Windows 10, the `batch_input_shape` needed to be specified in the first layer, and the `input_shape` parameter had to be omitted."
417,tensorflow_tensorflow,issue,37492,"To train the LSTM-model on a GPU on my plattform (Windows 10, tf 2.1.0), the parameter batch_input_shape must be specified during the model creation in the very first layer and the parameter input_shape must be omitted.","For the LSTM model to train on a GPU with TensorFlow 2.1.0 on Windows 10, I had to specify the `batch_input_shape` in the first layer and leave out the `input_shape` parameter to avoid any runtime errors.",Knowledge,46,46,tensorflow_tensorflow,37492,issue,,0,0,0,0,"For the LSTM model to train on a GPU with TensorFlow 2.1.0 on Windows 10, I had to specify the `batch_input_shape` in the first layer and leave out the `input_shape` parameter to avoid any runtime errors."
418,tensorflow_tensorflow,issue,37492,"Hey all,

so I could just verify that all of the problems in this issue are connected with the Windows-version of tensorflow. I installed a fresh Ubuntu Bionic and followed the instructions [0] to set it up with CUDA on my homeoffice-workstation with a GTX 1060 6GB and none of the described problems was ever encountered again. :/

So please save yourself the trouble and just use Linux to train your LSTMs on the GPU. Even the validation_split-parameter works there without any problem and I had not to set up any batch_input_shape with divisible sets for training, validation, etc. Now it is just so easy.","I've confirmed that the issues mentioned in this thread are specific to the Windows version of TensorFlow. After setting up a fresh Ubuntu Bionic installation on my home office workstation with a GTX 1060 6GB and following the CUDA setup instructions, I didn't encounter any of the problems described. It seems that using Linux for GPU training with TensorFlow is much smoother, and even the validation_split parameter works without any issues.",Knowledge,47,47,tensorflow_tensorflow,37492,issue,,0,0,0,0,"I've confirmed that the issues mentioned in this thread are specific to the Windows version of TensorFlow. After setting up a fresh Ubuntu Bionic installation on my home office workstation with a GTX 1060 6GB and following the CUDA setup instructions, I didn't encounter any of the problems described. It seems that using Linux for GPU training with TensorFlow is much smoother, and even the validation_split parameter works without any issues."
419,tensorflow_tensorflow,issue,37492,"Hey all,

so I could just verify that all of the problems in this issue are connected with the Windows-version of tensorflow. I installed a fresh Ubuntu Bionic and followed the instructions [0] to set it up with CUDA on my homeoffice-workstation with a GTX 1060 6GB and none of the described problems was ever encountered again. :/

So please save yourself the trouble and just use Linux to train your LSTMs on the GPU. Even the validation_split-parameter works there without any problem and I had not to set up any batch_input_shape with divisible sets for training, validation, etc. Now it is just so easy.","Just wanted to share that the problems discussed here are only present in the Windows version of TensorFlow. I installed a clean Ubuntu Bionic on my home office machine with a GTX 1060 6GB and followed the CUDA setup guide, and everything worked perfectly. The validation_split parameter and other configurations ran smoothly without any of the issues I faced on Windows.",Knowledge,47,47,tensorflow_tensorflow,37492,issue,,0,0,0,0,"Just wanted to share that the problems discussed here are only present in the Windows version of TensorFlow. I installed a clean Ubuntu Bionic on my home office machine with a GTX 1060 6GB and followed the CUDA setup guide, and everything worked perfectly. The validation_split parameter and other configurations ran smoothly without any of the issues I faced on Windows."
420,tensorflow_tensorflow,issue,37492,"Hey all,

so I could just verify that all of the problems in this issue are connected with the Windows-version of tensorflow. I installed a fresh Ubuntu Bionic and followed the instructions [0] to set it up with CUDA on my homeoffice-workstation with a GTX 1060 6GB and none of the described problems was ever encountered again. :/

So please save yourself the trouble and just use Linux to train your LSTMs on the GPU. Even the validation_split-parameter works there without any problem and I had not to set up any batch_input_shape with divisible sets for training, validation, etc. Now it is just so easy.","I can confirm that the issues in this issue thread are Windows-specific. I set up a fresh Ubuntu Bionic environment on my home office workstation with a GTX 1060 6GB and followed the CUDA setup instructions, and none of the problems were present. Training LSTMs on the GPU was much easier on Linux, and the validation_split parameter worked as expected without any additional configuration.",Knowledge,47,47,tensorflow_tensorflow,37492,issue,,0,0,0,0,"I can confirm that the issues in this issue thread are Windows-specific. I set up a fresh Ubuntu Bionic environment on my home office workstation with a GTX 1060 6GB and followed the CUDA setup instructions, and none of the problems were present. Training LSTMs on the GPU was much easier on Linux, and the validation_split parameter worked as expected without any additional configuration."
421,tensorflow_tensorflow,issue,37492,"Hey all,

so I could just verify that all of the problems in this issue are connected with the Windows-version of tensorflow. I installed a fresh Ubuntu Bionic and followed the instructions [0] to set it up with CUDA on my homeoffice-workstation with a GTX 1060 6GB and none of the described problems was ever encountered again. :/

So please save yourself the trouble and just use Linux to train your LSTMs on the GPU. Even the validation_split-parameter works there without any problem and I had not to set up any batch_input_shape with divisible sets for training, validation, etc. Now it is just so easy.","I've verified that the issues mentioned in this thread are only affecting the Windows version of TensorFlow. After setting up a fresh Ubuntu Bionic installation on my home office workstation with a GTX 1060 6GB and following the CUDA setup guide, I didn't encounter any of the described problems. It's much easier to train LSTMs on the GPU using Linux, and the validation_split parameter works without any issues.",Knowledge,47,47,tensorflow_tensorflow,37492,issue,,0,0,0,0,"I've verified that the issues mentioned in this thread are only affecting the Windows version of TensorFlow. After setting up a fresh Ubuntu Bionic installation on my home office workstation with a GTX 1060 6GB and following the CUDA setup guide, I didn't encounter any of the described problems. It's much easier to train LSTMs on the GPU using Linux, and the validation_split parameter works without any issues."
422,tensorflow_tensorflow,issue,37492,"Hey all,

so I could just verify that all of the problems in this issue are connected with the Windows-version of tensorflow. I installed a fresh Ubuntu Bionic and followed the instructions [0] to set it up with CUDA on my homeoffice-workstation with a GTX 1060 6GB and none of the described problems was ever encountered again. :/

So please save yourself the trouble and just use Linux to train your LSTMs on the GPU. Even the validation_split-parameter works there without any problem and I had not to set up any batch_input_shape with divisible sets for training, validation, etc. Now it is just so easy.","I can confirm that the issues discussed in this thread are specific to the Windows version of TensorFlow. I installed a fresh Ubuntu Bionic on my home office workstation with a GTX 1060 6GB and followed the CUDA setup instructions, and everything worked flawlessly. The validation_split parameter and other configurations ran smoothly, and I didn't need to set up any batch_input_shape with divisible sets for training, validation, etc.",Knowledge,47,47,tensorflow_tensorflow,37492,issue,,0,0,0,0,"I can confirm that the issues discussed in this thread are specific to the Windows version of TensorFlow. I installed a fresh Ubuntu Bionic on my home office workstation with a GTX 1060 6GB and followed the CUDA setup instructions, and everything worked flawlessly. The validation_split parameter and other configurations ran smoothly, and I didn't need to set up any batch_input_shape with divisible sets for training, validation, etc."
423,tensorflow_tensorflow,issue,37492,"Hey all,

so I could just verify that all of the problems in this issue are connected with the Windows-version of tensorflow. I installed a fresh Ubuntu Bionic and followed the instructions [0] to set it up with CUDA on my homeoffice-workstation with a GTX 1060 6GB and none of the described problems was ever encountered again. :/

So please save yourself the trouble and just use Linux to train your LSTMs on the GPU. Even the validation_split-parameter works there without any problem and I had not to set up any batch_input_shape with divisible sets for training, validation, etc. Now it is just so easy.","I've found that the issues mentioned in this thread are only present in the Windows version of TensorFlow. After setting up a fresh Ubuntu Bionic environment on my home office workstation with a GTX 1060 6GB and following the CUDA setup guide, I didn't encounter any of the problems described. Training LSTMs on the GPU was much easier on Linux, and the validation_split parameter worked without any issues.",Knowledge,47,47,tensorflow_tensorflow,37492,issue,,0,0,0,0,"I've found that the issues mentioned in this thread are only present in the Windows version of TensorFlow. After setting up a fresh Ubuntu Bionic environment on my home office workstation with a GTX 1060 6GB and following the CUDA setup guide, I didn't encounter any of the problems described. Training LSTMs on the GPU was much easier on Linux, and the validation_split parameter worked without any issues."
424,tensorflow_tensorflow,issue,37492,"Hey all,

so I could just verify that all of the problems in this issue are connected with the Windows-version of tensorflow. I installed a fresh Ubuntu Bionic and followed the instructions [0] to set it up with CUDA on my homeoffice-workstation with a GTX 1060 6GB and none of the described problems was ever encountered again. :/

So please save yourself the trouble and just use Linux to train your LSTMs on the GPU. Even the validation_split-parameter works there without any problem and I had not to set up any batch_input_shape with divisible sets for training, validation, etc. Now it is just so easy.","I can confirm that the issues in this thread are Windows-specific. I installed a fresh Ubuntu Bionic on my home office workstation with a GTX 1060 6GB and followed the CUDA setup instructions, and everything worked perfectly. The validation_split parameter and other configurations ran smoothly, and I didn't need to set up any batch_input_shape with divisible sets for training, validation, etc.",Knowledge,47,47,tensorflow_tensorflow,37492,issue,,0,0,0,0,"I can confirm that the issues in this thread are Windows-specific. I installed a fresh Ubuntu Bionic on my home office workstation with a GTX 1060 6GB and followed the CUDA setup instructions, and everything worked perfectly. The validation_split parameter and other configurations ran smoothly, and I didn't need to set up any batch_input_shape with divisible sets for training, validation, etc."
425,tensorflow_tensorflow,issue,37492,"Hey all,

so I could just verify that all of the problems in this issue are connected with the Windows-version of tensorflow. I installed a fresh Ubuntu Bionic and followed the instructions [0] to set it up with CUDA on my homeoffice-workstation with a GTX 1060 6GB and none of the described problems was ever encountered again. :/

So please save yourself the trouble and just use Linux to train your LSTMs on the GPU. Even the validation_split-parameter works there without any problem and I had not to set up any batch_input_shape with divisible sets for training, validation, etc. Now it is just so easy.","I've verified that the issues mentioned in this thread are only affecting the Windows version of TensorFlow. After setting up a fresh Ubuntu Bionic installation on my home office workstation with a GTX 1060 6GB and following the CUDA setup guide, I didn't encounter any of the described problems. It's much easier to train LSTMs on the GPU using Linux, and the validation_split parameter works without any issues.",Knowledge,47,47,tensorflow_tensorflow,37492,issue,,0,0,0,0,"I've verified that the issues mentioned in this thread are only affecting the Windows version of TensorFlow. After setting up a fresh Ubuntu Bionic installation on my home office workstation with a GTX 1060 6GB and following the CUDA setup guide, I didn't encounter any of the described problems. It's much easier to train LSTMs on the GPU using Linux, and the validation_split parameter works without any issues."
426,tensorflow_tensorflow,issue,37492,"Hey all,

so I could just verify that all of the problems in this issue are connected with the Windows-version of tensorflow. I installed a fresh Ubuntu Bionic and followed the instructions [0] to set it up with CUDA on my homeoffice-workstation with a GTX 1060 6GB and none of the described problems was ever encountered again. :/

So please save yourself the trouble and just use Linux to train your LSTMs on the GPU. Even the validation_split-parameter works there without any problem and I had not to set up any batch_input_shape with divisible sets for training, validation, etc. Now it is just so easy.","I can confirm that the issues discussed in this thread are specific to the Windows version of TensorFlow. I installed a fresh Ubuntu Bionic on my home office workstation with a GTX 1060 6GB and followed the CUDA setup instructions, and everything worked flawlessly. The validation_split parameter and other configurations ran smoothly, and I didn't need to set up any batch_input_shape with divisible sets for training, validation, etc.",Knowledge,47,47,tensorflow_tensorflow,37492,issue,,0,0,0,0,"I can confirm that the issues discussed in this thread are specific to the Windows version of TensorFlow. I installed a fresh Ubuntu Bionic on my home office workstation with a GTX 1060 6GB and followed the CUDA setup instructions, and everything worked flawlessly. The validation_split parameter and other configurations ran smoothly, and I didn't need to set up any batch_input_shape with divisible sets for training, validation, etc."
427,numpy_numpy,issue,28024,"Since there is a well defined interoperability protocol I was assuming that it is safe to call numpy functions on foreign objects, until I discovered this strange behaviour:

>>> import numpy as np
>>> class Kls: pass
...
>>> a = Kls()
>>> a.mean = None
>>> np.mean(a)
Traceback (most recent call last):
  File ""<stdin>"", line 1, in <module>
  File ""/Users/miccoli/work/uvenv/bigdata/.venv/lib/python3.12/site-packages/numpy/_core/fromnumeric.py"", line 3858, in mean
    return mean(axis=axis, dtype=dtype, out=out, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
TypeError: 'NoneType' object is not callable
>>> a.mean = lambda : 0
>>> np.mean(a)
Traceback (most recent call last):
  File ""<stdin>"", line 1, in <module>
  File ""/Users/miccoli/work/uvenv/bigdata/.venv/lib/python3.12/site-packages/numpy/_core/fromnumeric.py"", line 3858, in mean
    return mean(axis=axis, dtype=dtype, out=out, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
TypeError: <lambda>() got an unexpected keyword argument 'axis'

So if np.mean(a) discovers that a has a mean attribute, it blindly assumes that it is a method with the same semantics and signature of np.mean.

This is crazy! numpy does not owns the mean method and in fact other libraries implemented it in different ways:

>>> import polars as pl
>>> a = pl.Series([0, 1, 2])
>>> np.mean(a) #broken
Traceback (most recent call last):
  File ""<stdin>"", line 1, in <module>
  File ""/Users/miccoli/work/uvenv/bigdata/.venv/lib/python3.12/site-packages/numpy/_core/fromnumeric.py"", line 3858, in mean
    return mean(axis=axis, dtype=dtype, out=out, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
TypeError: Series.mean() got an unexpected keyword argument 'axis'
>>> np.mean(np.asarray(a)) #works
np.float64(1.0)","I assumed that calling numpy functions on objects from other libraries would be safe due to the interoperability protocol. However, I encountered an issue where `np.mean` fails when the object has a `mean` attribute. For example, when I set `a.mean = None`, `np.mean(a)` raises a `TypeError` because it tries to call `None`. This behavior seems problematic, especially since other libraries like Polars implement `mean` differently, leading to unexpected errors.",Unknown,48,48,numpy_numpy,28024,issue,,1,3,3,1,"I assumed that calling numpy functions on objects from other libraries would be safe due to the interoperability protocol. However, I encountered an issue where `np.mean` fails when the object has a `mean` attribute. For example, when I set `a.mean = None`, `np.mean(a)` raises a `TypeError` because it tries to call `None`. This behavior seems problematic, especially since other libraries like Polars implement `mean` differently, leading to unexpected errors."
428,numpy_numpy,issue,28024,"Since there is a well defined interoperability protocol I was assuming that it is safe to call numpy functions on foreign objects, until I discovered this strange behaviour:

>>> import numpy as np
>>> class Kls: pass
...
>>> a = Kls()
>>> a.mean = None
>>> np.mean(a)
Traceback (most recent call last):
  File ""<stdin>"", line 1, in <module>
  File ""/Users/miccoli/work/uvenv/bigdata/.venv/lib/python3.12/site-packages/numpy/_core/fromnumeric.py"", line 3858, in mean
    return mean(axis=axis, dtype=dtype, out=out, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
TypeError: 'NoneType' object is not callable
>>> a.mean = lambda : 0
>>> np.mean(a)
Traceback (most recent call last):
  File ""<stdin>"", line 1, in <module>
  File ""/Users/miccoli/work/uvenv/bigdata/.venv/lib/python3.12/site-packages/numpy/_core/fromnumeric.py"", line 3858, in mean
    return mean(axis=axis, dtype=dtype, out=out, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
TypeError: <lambda>() got an unexpected keyword argument 'axis'

So if np.mean(a) discovers that a has a mean attribute, it blindly assumes that it is a method with the same semantics and signature of np.mean.

This is crazy! numpy does not owns the mean method and in fact other libraries implemented it in different ways:

>>> import polars as pl
>>> a = pl.Series([0, 1, 2])
>>> np.mean(a) #broken
Traceback (most recent call last):
  File ""<stdin>"", line 1, in <module>
  File ""/Users/miccoli/work/uvenv/bigdata/.venv/lib/python3.12/site-packages/numpy/_core/fromnumeric.py"", line 3858, in mean
    return mean(axis=axis, dtype=dtype, out=out, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
TypeError: Series.mean() got an unexpected keyword argument 'axis'
>>> np.mean(np.asarray(a)) #works
np.float64(1.0)","I was under the impression that numpy functions would work seamlessly with objects from other libraries, but I found a strange issue with `np.mean`. If an object has a `mean` attribute, numpy assumes it's a method with the same signature as `np.mean`, which can lead to errors. For instance, setting `a.mean = lambda: 0` results in a `TypeError` because the lambda function doesn't accept the `axis` argument. This behavior is concerning, as it can break compatibility with other libraries like Polars.",Unknown,48,48,numpy_numpy,28024,issue,,1,3,3,1,"I was under the impression that numpy functions would work seamlessly with objects from other libraries, but I found a strange issue with `np.mean`. If an object has a `mean` attribute, numpy assumes it's a method with the same signature as `np.mean`, which can lead to errors. For instance, setting `a.mean = lambda: 0` results in a `TypeError` because the lambda function doesn't accept the `axis` argument. This behavior is concerning, as it can break compatibility with other libraries like Polars."
429,numpy_numpy,issue,28024,"Since there is a well defined interoperability protocol I was assuming that it is safe to call numpy functions on foreign objects, until I discovered this strange behaviour:

>>> import numpy as np
>>> class Kls: pass
...
>>> a = Kls()
>>> a.mean = None
>>> np.mean(a)
Traceback (most recent call last):
  File ""<stdin>"", line 1, in <module>
  File ""/Users/miccoli/work/uvenv/bigdata/.venv/lib/python3.12/site-packages/numpy/_core/fromnumeric.py"", line 3858, in mean
    return mean(axis=axis, dtype=dtype, out=out, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
TypeError: 'NoneType' object is not callable
>>> a.mean = lambda : 0
>>> np.mean(a)
Traceback (most recent call last):
  File ""<stdin>"", line 1, in <module>
  File ""/Users/miccoli/work/uvenv/bigdata/.venv/lib/python3.12/site-packages/numpy/_core/fromnumeric.py"", line 3858, in mean
    return mean(axis=axis, dtype=dtype, out=out, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
TypeError: <lambda>() got an unexpected keyword argument 'axis'

So if np.mean(a) discovers that a has a mean attribute, it blindly assumes that it is a method with the same semantics and signature of np.mean.

This is crazy! numpy does not owns the mean method and in fact other libraries implemented it in different ways:

>>> import polars as pl
>>> a = pl.Series([0, 1, 2])
>>> np.mean(a) #broken
Traceback (most recent call last):
  File ""<stdin>"", line 1, in <module>
  File ""/Users/miccoli/work/uvenv/bigdata/.venv/lib/python3.12/site-packages/numpy/_core/fromnumeric.py"", line 3858, in mean
    return mean(axis=axis, dtype=dtype, out=out, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
TypeError: Series.mean() got an unexpected keyword argument 'axis'
>>> np.mean(np.asarray(a)) #works
np.float64(1.0)","I expected numpy to handle objects from other libraries gracefully, but I discovered a surprising issue with `np.mean`. When an object has a `mean` attribute, numpy assumes it's a method and tries to call it with the same arguments as `np.mean`. This can cause errors, such as when `a.mean = None` or when using a lambda function that doesn't accept the `axis` argument. This behavior is particularly problematic when working with libraries like Polars, which have their own `mean` implementations.",Unknown,48,48,numpy_numpy,28024,issue,,1,3,3,1,"I expected numpy to handle objects from other libraries gracefully, but I discovered a surprising issue with `np.mean`. When an object has a `mean` attribute, numpy assumes it's a method and tries to call it with the same arguments as `np.mean`. This can cause errors, such as when `a.mean = None` or when using a lambda function that doesn't accept the `axis` argument. This behavior is particularly problematic when working with libraries like Polars, which have their own `mean` implementations."
430,numpy_numpy,issue,28024,"Since there is a well defined interoperability protocol I was assuming that it is safe to call numpy functions on foreign objects, until I discovered this strange behaviour:

>>> import numpy as np
>>> class Kls: pass
...
>>> a = Kls()
>>> a.mean = None
>>> np.mean(a)
Traceback (most recent call last):
  File ""<stdin>"", line 1, in <module>
  File ""/Users/miccoli/work/uvenv/bigdata/.venv/lib/python3.12/site-packages/numpy/_core/fromnumeric.py"", line 3858, in mean
    return mean(axis=axis, dtype=dtype, out=out, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
TypeError: 'NoneType' object is not callable
>>> a.mean = lambda : 0
>>> np.mean(a)
Traceback (most recent call last):
  File ""<stdin>"", line 1, in <module>
  File ""/Users/miccoli/work/uvenv/bigdata/.venv/lib/python3.12/site-packages/numpy/_core/fromnumeric.py"", line 3858, in mean
    return mean(axis=axis, dtype=dtype, out=out, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
TypeError: <lambda>() got an unexpected keyword argument 'axis'

So if np.mean(a) discovers that a has a mean attribute, it blindly assumes that it is a method with the same semantics and signature of np.mean.

This is crazy! numpy does not owns the mean method and in fact other libraries implemented it in different ways:

>>> import polars as pl
>>> a = pl.Series([0, 1, 2])
>>> np.mean(a) #broken
Traceback (most recent call last):
  File ""<stdin>"", line 1, in <module>
  File ""/Users/miccoli/work/uvenv/bigdata/.venv/lib/python3.12/site-packages/numpy/_core/fromnumeric.py"", line 3858, in mean
    return mean(axis=axis, dtype=dtype, out=out, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
TypeError: Series.mean() got an unexpected keyword argument 'axis'
>>> np.mean(np.asarray(a)) #works
np.float64(1.0)","I was surprised to find that numpy's `np.mean` function can fail when called on objects with a `mean` attribute. For example, if `a.mean` is set to `None`, `np.mean(a)` raises a `TypeError` because it tries to call `None`. Similarly, if `a.mean` is a lambda function, it fails because the lambda doesn't accept the `axis` argument. This behavior is concerning, as it can lead to compatibility issues with other libraries like Polars.",Unknown,48,48,numpy_numpy,28024,issue,,1,3,3,1,"I was surprised to find that numpy's `np.mean` function can fail when called on objects with a `mean` attribute. For example, if `a.mean` is set to `None`, `np.mean(a)` raises a `TypeError` because it tries to call `None`. Similarly, if `a.mean` is a lambda function, it fails because the lambda doesn't accept the `axis` argument. This behavior is concerning, as it can lead to compatibility issues with other libraries like Polars."
431,numpy_numpy,issue,28024,"Since there is a well defined interoperability protocol I was assuming that it is safe to call numpy functions on foreign objects, until I discovered this strange behaviour:

>>> import numpy as np
>>> class Kls: pass
...
>>> a = Kls()
>>> a.mean = None
>>> np.mean(a)
Traceback (most recent call last):
  File ""<stdin>"", line 1, in <module>
  File ""/Users/miccoli/work/uvenv/bigdata/.venv/lib/python3.12/site-packages/numpy/_core/fromnumeric.py"", line 3858, in mean
    return mean(axis=axis, dtype=dtype, out=out, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
TypeError: 'NoneType' object is not callable
>>> a.mean = lambda : 0
>>> np.mean(a)
Traceback (most recent call last):
  File ""<stdin>"", line 1, in <module>
  File ""/Users/miccoli/work/uvenv/bigdata/.venv/lib/python3.12/site-packages/numpy/_core/fromnumeric.py"", line 3858, in mean
    return mean(axis=axis, dtype=dtype, out=out, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
TypeError: <lambda>() got an unexpected keyword argument 'axis'

So if np.mean(a) discovers that a has a mean attribute, it blindly assumes that it is a method with the same semantics and signature of np.mean.

This is crazy! numpy does not owns the mean method and in fact other libraries implemented it in different ways:

>>> import polars as pl
>>> a = pl.Series([0, 1, 2])
>>> np.mean(a) #broken
Traceback (most recent call last):
  File ""<stdin>"", line 1, in <module>
  File ""/Users/miccoli/work/uvenv/bigdata/.venv/lib/python3.12/site-packages/numpy/_core/fromnumeric.py"", line 3858, in mean
    return mean(axis=axis, dtype=dtype, out=out, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
TypeError: Series.mean() got an unexpected keyword argument 'axis'
>>> np.mean(np.asarray(a)) #works
np.float64(1.0)","I encountered an unexpected issue with `np.mean` when working with objects that have a `mean` attribute. If the attribute is `None`, `np.mean` fails with a `TypeError`. If it's a lambda function, it fails because the lambda doesn't accept the `axis` argument. This behavior is problematic, especially since other libraries like Polars have their own `mean` implementations, leading to compatibility issues.",Unknown,48,48,numpy_numpy,28024,issue,,1,3,3,1,"I encountered an unexpected issue with `np.mean` when working with objects that have a `mean` attribute. If the attribute is `None`, `np.mean` fails with a `TypeError`. If it's a lambda function, it fails because the lambda doesn't accept the `axis` argument. This behavior is problematic, especially since other libraries like Polars have their own `mean` implementations, leading to compatibility issues."
432,numpy_numpy,issue,28024,"Since there is a well defined interoperability protocol I was assuming that it is safe to call numpy functions on foreign objects, until I discovered this strange behaviour:

>>> import numpy as np
>>> class Kls: pass
...
>>> a = Kls()
>>> a.mean = None
>>> np.mean(a)
Traceback (most recent call last):
  File ""<stdin>"", line 1, in <module>
  File ""/Users/miccoli/work/uvenv/bigdata/.venv/lib/python3.12/site-packages/numpy/_core/fromnumeric.py"", line 3858, in mean
    return mean(axis=axis, dtype=dtype, out=out, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
TypeError: 'NoneType' object is not callable
>>> a.mean = lambda : 0
>>> np.mean(a)
Traceback (most recent call last):
  File ""<stdin>"", line 1, in <module>
  File ""/Users/miccoli/work/uvenv/bigdata/.venv/lib/python3.12/site-packages/numpy/_core/fromnumeric.py"", line 3858, in mean
    return mean(axis=axis, dtype=dtype, out=out, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
TypeError: <lambda>() got an unexpected keyword argument 'axis'

So if np.mean(a) discovers that a has a mean attribute, it blindly assumes that it is a method with the same semantics and signature of np.mean.

This is crazy! numpy does not owns the mean method and in fact other libraries implemented it in different ways:

>>> import polars as pl
>>> a = pl.Series([0, 1, 2])
>>> np.mean(a) #broken
Traceback (most recent call last):
  File ""<stdin>"", line 1, in <module>
  File ""/Users/miccoli/work/uvenv/bigdata/.venv/lib/python3.12/site-packages/numpy/_core/fromnumeric.py"", line 3858, in mean
    return mean(axis=axis, dtype=dtype, out=out, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
TypeError: Series.mean() got an unexpected keyword argument 'axis'
>>> np.mean(np.asarray(a)) #works
np.float64(1.0)","I assumed that numpy's `np.mean` function would work seamlessly with objects from other libraries, but I found a strange issue. If an object has a `mean` attribute, numpy assumes it's a method and tries to call it with the same arguments as `np.mean`. This can lead to errors, such as when `a.mean` is `None` or a lambda function that doesn't accept the `axis` argument. This behavior is concerning, as it can break compatibility with other libraries like Polars.",Unknown,48,48,numpy_numpy,28024,issue,,1,3,3,1,"I assumed that numpy's `np.mean` function would work seamlessly with objects from other libraries, but I found a strange issue. If an object has a `mean` attribute, numpy assumes it's a method and tries to call it with the same arguments as `np.mean`. This can lead to errors, such as when `a.mean` is `None` or a lambda function that doesn't accept the `axis` argument. This behavior is concerning, as it can break compatibility with other libraries like Polars."
433,numpy_numpy,issue,28024,"Since there is a well defined interoperability protocol I was assuming that it is safe to call numpy functions on foreign objects, until I discovered this strange behaviour:

>>> import numpy as np
>>> class Kls: pass
...
>>> a = Kls()
>>> a.mean = None
>>> np.mean(a)
Traceback (most recent call last):
  File ""<stdin>"", line 1, in <module>
  File ""/Users/miccoli/work/uvenv/bigdata/.venv/lib/python3.12/site-packages/numpy/_core/fromnumeric.py"", line 3858, in mean
    return mean(axis=axis, dtype=dtype, out=out, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
TypeError: 'NoneType' object is not callable
>>> a.mean = lambda : 0
>>> np.mean(a)
Traceback (most recent call last):
  File ""<stdin>"", line 1, in <module>
  File ""/Users/miccoli/work/uvenv/bigdata/.venv/lib/python3.12/site-packages/numpy/_core/fromnumeric.py"", line 3858, in mean
    return mean(axis=axis, dtype=dtype, out=out, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
TypeError: <lambda>() got an unexpected keyword argument 'axis'

So if np.mean(a) discovers that a has a mean attribute, it blindly assumes that it is a method with the same semantics and signature of np.mean.

This is crazy! numpy does not owns the mean method and in fact other libraries implemented it in different ways:

>>> import polars as pl
>>> a = pl.Series([0, 1, 2])
>>> np.mean(a) #broken
Traceback (most recent call last):
  File ""<stdin>"", line 1, in <module>
  File ""/Users/miccoli/work/uvenv/bigdata/.venv/lib/python3.12/site-packages/numpy/_core/fromnumeric.py"", line 3858, in mean
    return mean(axis=axis, dtype=dtype, out=out, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
TypeError: Series.mean() got an unexpected keyword argument 'axis'
>>> np.mean(np.asarray(a)) #works
np.float64(1.0)","I was surprised to find that `np.mean` can fail when called on objects with a `mean` attribute. For example, if `a.mean` is `None`, `np.mean(a)` raises a `TypeError`. If `a.mean` is a lambda function, it fails because the lambda doesn't accept the `axis` argument. This behavior is problematic, especially since other libraries like Polars have their own `mean` implementations, leading to compatibility issues.",Unknown,48,48,numpy_numpy,28024,issue,,1,3,3,1,"I was surprised to find that `np.mean` can fail when called on objects with a `mean` attribute. For example, if `a.mean` is `None`, `np.mean(a)` raises a `TypeError`. If `a.mean` is a lambda function, it fails because the lambda doesn't accept the `axis` argument. This behavior is problematic, especially since other libraries like Polars have their own `mean` implementations, leading to compatibility issues."
434,numpy_numpy,issue,28024,"Since there is a well defined interoperability protocol I was assuming that it is safe to call numpy functions on foreign objects, until I discovered this strange behaviour:

>>> import numpy as np
>>> class Kls: pass
...
>>> a = Kls()
>>> a.mean = None
>>> np.mean(a)
Traceback (most recent call last):
  File ""<stdin>"", line 1, in <module>
  File ""/Users/miccoli/work/uvenv/bigdata/.venv/lib/python3.12/site-packages/numpy/_core/fromnumeric.py"", line 3858, in mean
    return mean(axis=axis, dtype=dtype, out=out, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
TypeError: 'NoneType' object is not callable
>>> a.mean = lambda : 0
>>> np.mean(a)
Traceback (most recent call last):
  File ""<stdin>"", line 1, in <module>
  File ""/Users/miccoli/work/uvenv/bigdata/.venv/lib/python3.12/site-packages/numpy/_core/fromnumeric.py"", line 3858, in mean
    return mean(axis=axis, dtype=dtype, out=out, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
TypeError: <lambda>() got an unexpected keyword argument 'axis'

So if np.mean(a) discovers that a has a mean attribute, it blindly assumes that it is a method with the same semantics and signature of np.mean.

This is crazy! numpy does not owns the mean method and in fact other libraries implemented it in different ways:

>>> import polars as pl
>>> a = pl.Series([0, 1, 2])
>>> np.mean(a) #broken
Traceback (most recent call last):
  File ""<stdin>"", line 1, in <module>
  File ""/Users/miccoli/work/uvenv/bigdata/.venv/lib/python3.12/site-packages/numpy/_core/fromnumeric.py"", line 3858, in mean
    return mean(axis=axis, dtype=dtype, out=out, **kwargs)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
TypeError: Series.mean() got an unexpected keyword argument 'axis'
>>> np.mean(np.asarray(a)) #works
np.float64(1.0)","I expected numpy's `np.mean` function to handle objects from other libraries gracefully, but I discovered a strange issue. If an object has a `mean` attribute, numpy assumes it's a method and tries to call it with the same arguments as `np.mean`. This can cause errors, such as when `a.mean` is `None` or a lambda function that doesn't accept the `axis` argument. This behavior is concerning, as it can lead to compatibility issues with other libraries like Polars.",Unknown,48,48,numpy_numpy,28024,issue,,1,3,3,1,"I expected numpy's `np.mean` function to handle objects from other libraries gracefully, but I discovered a strange issue. If an object has a `mean` attribute, numpy assumes it's a method and tries to call it with the same arguments as `np.mean`. This can cause errors, such as when `a.mean` is `None` or a lambda function that doesn't accept the `axis` argument. This behavior is concerning, as it can lead to compatibility issues with other libraries like Polars."
435,numpy_numpy,issue,28024,"The behaviour was added a long time ago, commit 67940c3 in 2006.","This behavior has been part of the codebase since 2006, specifically in commit 67940c3.",Knowledge,49,49,numpy_numpy,28024,issue,,0,0,0,0,"This behavior has been part of the codebase since 2006, specifically in commit 67940c3."
436,numpy_numpy,issue,28024,"The behaviour was added a long time ago, commit 67940c3 in 2006.",The feature was introduced way back in 2006 with commit 67940c3.,Knowledge,49,49,numpy_numpy,28024,issue,,0,0,0,0,The feature was introduced way back in 2006 with commit 67940c3.
437,numpy_numpy,issue,28024,"The behaviour was added a long time ago, commit 67940c3 in 2006.","Commit 67940c3, made in 2006, added this behavior to the project.",Knowledge,49,49,numpy_numpy,28024,issue,,0,0,0,0,"Commit 67940c3, made in 2006, added this behavior to the project."
438,numpy_numpy,issue,28024,"The behaviour was added a long time ago, commit 67940c3 in 2006.","This functionality was added to the codebase in 2006, as seen in commit 67940c3.",Knowledge,49,49,numpy_numpy,28024,issue,,0,0,0,0,"This functionality was added to the codebase in 2006, as seen in commit 67940c3."
439,numpy_numpy,issue,28024,"The behaviour was added a long time ago, commit 67940c3 in 2006.","The behavior you're referring to was implemented in 2006, specifically in commit 67940c3.",Knowledge,49,49,numpy_numpy,28024,issue,,0,0,0,0,"The behavior you're referring to was implemented in 2006, specifically in commit 67940c3."
440,numpy_numpy,issue,28024,"The behaviour was added a long time ago, commit 67940c3 in 2006.",The commit 67940c3 from 2006 introduced this behavior into the project.,Knowledge,49,49,numpy_numpy,28024,issue,,0,0,0,0,The commit 67940c3 from 2006 introduced this behavior into the project.
441,numpy_numpy,issue,28024,"The behaviour was added a long time ago, commit 67940c3 in 2006.","This behavior has been in the code since 2006, as documented in commit 67940c3.",Knowledge,49,49,numpy_numpy,28024,issue,,0,0,0,0,"This behavior has been in the code since 2006, as documented in commit 67940c3."
442,numpy_numpy,issue,28024,"The behaviour was added a long time ago, commit 67940c3 in 2006.","The feature was added to the codebase a long time ago, back in 2006 with commit 67940c3.",Knowledge,49,49,numpy_numpy,28024,issue,,0,0,0,0,"The feature was added to the codebase a long time ago, back in 2006 with commit 67940c3."
443,numpy_numpy,issue,28024,"The behaviour was added a long time ago, commit 67940c3 in 2006.","Commit 67940c3, which dates back to 2006, added this behavior to the project.",Knowledge,49,49,numpy_numpy,28024,issue,,0,0,0,0,"Commit 67940c3, which dates back to 2006, added this behavior to the project."
444,numpy_numpy,issue,28024,"The behaviour was added a long time ago, commit 67940c3 in 2006.",This behavior was first introduced in 2006 with the commit 67940c3.,Knowledge,49,49,numpy_numpy,28024,issue,,0,0,0,0,This behavior was first introduced in 2006 with the commit 67940c3.
445,numpy_numpy,issue,28024,"The behaviour was added a long time ago, commit 67940c3 in 2006. That probably means that for backwards compatibility reasons we cannot change this behaviour.

Maybe we could change the implementation so that if an object has __array_function__ or __array_ufunc__ those are tried first. That still leaves the issue that numpy will call a.mean() for a=Kls() in the example above.","This behavior has been in place since 2006, specifically in commit 67940c3. Due to backwards compatibility concerns, we can't modify it. However, we might consider updating the implementation to prioritize __array_function__ or __array_ufunc__ methods if they are present. This would still leave the issue of numpy calling a.mean() for custom objects like Kls().",Irrelevant,50,50,numpy_numpy,28024,issue,,1,2,2,1,"This behavior has been in place since 2006, specifically in commit 67940c3. Due to backwards compatibility concerns, we can't modify it. However, we might consider updating the implementation to prioritize __array_function__ or __array_ufunc__ methods if they are present. This would still leave the issue of numpy calling a.mean() for custom objects like Kls()."
446,numpy_numpy,issue,28024,"The behaviour was added a long time ago, commit 67940c3 in 2006. That probably means that for backwards compatibility reasons we cannot change this behaviour.

Maybe we could change the implementation so that if an object has __array_function__ or __array_ufunc__ those are tried first. That still leaves the issue that numpy will call a.mean() for a=Kls() in the example above.","The behavior was introduced in 2006 with commit 67940c3, and changing it now could break backwards compatibility. One potential solution is to modify the implementation to check for and use __array_function__ or __array_ufunc__ methods first. However, this doesn't address the problem of numpy calling a.mean() on custom objects like Kls().",Irrelevant,50,50,numpy_numpy,28024,issue,,1,2,2,1,"The behavior was introduced in 2006 with commit 67940c3, and changing it now could break backwards compatibility. One potential solution is to modify the implementation to check for and use __array_function__ or __array_ufunc__ methods first. However, this doesn't address the problem of numpy calling a.mean() on custom objects like Kls()."
447,numpy_numpy,issue,28024,"The behaviour was added a long time ago, commit 67940c3 in 2006. That probably means that for backwards compatibility reasons we cannot change this behaviour.

Maybe we could change the implementation so that if an object has __array_function__ or __array_ufunc__ those are tried first. That still leaves the issue that numpy will call a.mean() for a=Kls() in the example above.","This behavior has been part of the codebase since 2006, as seen in commit 67940c3. Changing it now would likely cause issues with backwards compatibility. We could consider an alternative approach where we first try __array_function__ or __array_ufunc__ methods if they exist. This would still leave the issue of numpy calling a.mean() on objects like Kls().",Irrelevant,50,50,numpy_numpy,28024,issue,,1,2,2,1,"This behavior has been part of the codebase since 2006, as seen in commit 67940c3. Changing it now would likely cause issues with backwards compatibility. We could consider an alternative approach where we first try __array_function__ or __array_ufunc__ methods if they exist. This would still leave the issue of numpy calling a.mean() on objects like Kls()."
448,numpy_numpy,issue,28024,"The behaviour was added a long time ago, commit 67940c3 in 2006. That probably means that for backwards compatibility reasons we cannot change this behaviour.

Maybe we could change the implementation so that if an object has __array_function__ or __array_ufunc__ those are tried first. That still leaves the issue that numpy will call a.mean() for a=Kls() in the example above.","The behavior was added in 2006 with commit 67940c3, and altering it now could break existing code. One possible solution is to update the implementation to use __array_function__ or __array_ufunc__ methods if they are available. However, this doesn't solve the problem of numpy calling a.mean() on custom objects like Kls().",Irrelevant,50,50,numpy_numpy,28024,issue,,1,2,2,1,"The behavior was added in 2006 with commit 67940c3, and altering it now could break existing code. One possible solution is to update the implementation to use __array_function__ or __array_ufunc__ methods if they are available. However, this doesn't solve the problem of numpy calling a.mean() on custom objects like Kls()."
449,numpy_numpy,issue,28024,"The behaviour was added a long time ago, commit 67940c3 in 2006. That probably means that for backwards compatibility reasons we cannot change this behaviour.

Maybe we could change the implementation so that if an object has __array_function__ or __array_ufunc__ those are tried first. That still leaves the issue that numpy will call a.mean() for a=Kls() in the example above.","This behavior has been around since 2006, as documented in commit 67940c3. Changing it now would risk breaking backwards compatibility. We could modify the implementation to prioritize __array_function__ or __array_ufunc__ methods if they are defined. However, this wouldn't resolve the issue of numpy calling a.mean() on objects like Kls().",Irrelevant,50,50,numpy_numpy,28024,issue,,1,2,2,1,"This behavior has been around since 2006, as documented in commit 67940c3. Changing it now would risk breaking backwards compatibility. We could modify the implementation to prioritize __array_function__ or __array_ufunc__ methods if they are defined. However, this wouldn't resolve the issue of numpy calling a.mean() on objects like Kls()."
450,numpy_numpy,issue,28024,"The behaviour was added a long time ago, commit 67940c3 in 2006. That probably means that for backwards compatibility reasons we cannot change this behaviour.

Maybe we could change the implementation so that if an object has __array_function__ or __array_ufunc__ those are tried first. That still leaves the issue that numpy will call a.mean() for a=Kls() in the example above.","The behavior was introduced in 2006 with commit 67940c3, and changing it now could have significant backwards compatibility implications. One potential solution is to update the implementation to try __array_function__ or __array_ufunc__ methods first. This would still leave the issue of numpy calling a.mean() on custom objects like Kls().",Irrelevant,50,50,numpy_numpy,28024,issue,,1,2,2,1,"The behavior was introduced in 2006 with commit 67940c3, and changing it now could have significant backwards compatibility implications. One potential solution is to update the implementation to try __array_function__ or __array_ufunc__ methods first. This would still leave the issue of numpy calling a.mean() on custom objects like Kls()."
451,numpy_numpy,issue,28024,"The behaviour was added a long time ago, commit 67940c3 in 2006. That probably means that for backwards compatibility reasons we cannot change this behaviour.

Maybe we could change the implementation so that if an object has __array_function__ or __array_ufunc__ those are tried first. That still leaves the issue that numpy will call a.mean() for a=Kls() in the example above.","This behavior has been in the codebase since 2006, as seen in commit 67940c3. Modifying it now could cause compatibility issues. We could consider an approach where we first check for and use __array_function__ or __array_ufunc__ methods. However, this doesn't address the problem of numpy calling a.mean() on objects like Kls().",Irrelevant,50,50,numpy_numpy,28024,issue,,1,2,2,1,"This behavior has been in the codebase since 2006, as seen in commit 67940c3. Modifying it now could cause compatibility issues. We could consider an approach where we first check for and use __array_function__ or __array_ufunc__ methods. However, this doesn't address the problem of numpy calling a.mean() on objects like Kls()."
452,numpy_numpy,issue,28024,"The behaviour was added a long time ago, commit 67940c3 in 2006. That probably means that for backwards compatibility reasons we cannot change this behaviour.

Maybe we could change the implementation so that if an object has __array_function__ or __array_ufunc__ those are tried first. That still leaves the issue that numpy will call a.mean() for a=Kls() in the example above.","The behavior was added in 2006 with commit 67940c3, and changing it now could break backwards compatibility. One possible solution is to update the implementation to use __array_function__ or __array_ufunc__ methods if they are available. This would still leave the issue of numpy calling a.mean() on custom objects like Kls().",Irrelevant,50,50,numpy_numpy,28024,issue,,1,2,2,1,"The behavior was added in 2006 with commit 67940c3, and changing it now could break backwards compatibility. One possible solution is to update the implementation to use __array_function__ or __array_ufunc__ methods if they are available. This would still leave the issue of numpy calling a.mean() on custom objects like Kls()."
453,numpy_numpy,issue,28024,"The behaviour was added a long time ago, commit 67940c3 in 2006. That probably means that for backwards compatibility reasons we cannot change this behaviour.

Maybe we could change the implementation so that if an object has __array_function__ or __array_ufunc__ those are tried first. That still leaves the issue that numpy will call a.mean() for a=Kls() in the example above.","This behavior has been part of the codebase since 2006, as documented in commit 67940c3. Changing it now would risk breaking backwards compatibility. We could consider an alternative approach where we first try __array_function__ or __array_ufunc__ methods if they exist. However, this doesn't solve the problem of numpy calling a.mean() on objects like Kls().",Irrelevant,50,50,numpy_numpy,28024,issue,,1,2,2,1,"This behavior has been part of the codebase since 2006, as documented in commit 67940c3. Changing it now would risk breaking backwards compatibility. We could consider an alternative approach where we first try __array_function__ or __array_ufunc__ methods if they exist. However, this doesn't solve the problem of numpy calling a.mean() on objects like Kls()."
454,numpy_numpy,issue,28024,"The behaviour was added a long time ago, commit 67940c3 in 2006. That probably means that for backwards compatibility reasons we cannot change this behaviour.

Maybe we could change the implementation so that if an object has __array_function__ or __array_ufunc__ those are tried first. That still leaves the issue that numpy will call a.mean() for a=Kls() in the example above.","The behavior was introduced in 2006 with commit 67940c3, and altering it now could have significant backwards compatibility implications. One potential solution is to update the implementation to prioritize __array_function__ or __array_ufunc__ methods if they are defined. This would still leave the issue of numpy calling a.mean() on custom objects like Kls().",Irrelevant,50,50,numpy_numpy,28024,issue,,1,2,2,1,"The behavior was introduced in 2006 with commit 67940c3, and altering it now could have significant backwards compatibility implications. One potential solution is to update the implementation to prioritize __array_function__ or __array_ufunc__ methods if they are defined. This would still leave the issue of numpy calling a.mean() on custom objects like Kls()."
455,numpy_numpy,issue,28024,"As @eendebakpt says, the problem here is that it is not clear what would happen if you changed it now that alternatives exist.
The main issue here I would expect to be pandas code, since dirty mixing of pandas and NumPy is pretty common, and users may be getting away with things there that they should not be getting away with.

So can we change it? Maybe, but it does require carefully thinking about how impactful the change is (it would make things error, which isn't so bad... but if there is a huge amount of code out there that starts failing that wouldn't be great).
","As @eendebakpt mentioned, the ambiguity lies in the potential consequences of making changes now that alternatives are available. The primary concern is the impact on pandas code, where mixing pandas and NumPy is widespread and often tolerated. While changing it could introduce errors, the real issue is the extent of existing code that might break. We need to carefully assess the impact before proceeding.",Known,51,51,numpy_numpy,28024,issue,,1,1,1,1,"As @eendebakpt mentioned, the ambiguity lies in the potential consequences of making changes now that alternatives are available. The primary concern is the impact on pandas code, where mixing pandas and NumPy is widespread and often tolerated. While changing it could introduce errors, the real issue is the extent of existing code that might break. We need to carefully assess the impact before proceeding."
456,numpy_numpy,issue,28024,"As @eendebakpt says, the problem here is that it is not clear what would happen if you changed it now that alternatives exist.
The main issue here I would expect to be pandas code, since dirty mixing of pandas and NumPy is pretty common, and users may be getting away with things there that they should not be getting away with.

So can we change it? Maybe, but it does require carefully thinking about how impactful the change is (it would make things error, which isn't so bad... but if there is a huge amount of code out there that starts failing that wouldn't be great).
","According to @eendebakpt, the challenge is understanding the implications of altering the behavior given the availability of alternatives. The main issue is the potential disruption to pandas code, where the combination of pandas and NumPy is common and sometimes leads to unintended results. While introducing errors isn't ideal, the bigger concern is the volume of code that could be affected. A thorough evaluation is necessary to gauge the impact.",Known,51,51,numpy_numpy,28024,issue,,1,1,1,1,"According to @eendebakpt, the challenge is understanding the implications of altering the behavior given the availability of alternatives. The main issue is the potential disruption to pandas code, where the combination of pandas and NumPy is common and sometimes leads to unintended results. While introducing errors isn't ideal, the bigger concern is the volume of code that could be affected. A thorough evaluation is necessary to gauge the impact."
457,numpy_numpy,issue,28024,"As @eendebakpt says, the problem here is that it is not clear what would happen if you changed it now that alternatives exist.
The main issue here I would expect to be pandas code, since dirty mixing of pandas and NumPy is pretty common, and users may be getting away with things there that they should not be getting away with.

So can we change it? Maybe, but it does require carefully thinking about how impactful the change is (it would make things error, which isn't so bad... but if there is a huge amount of code out there that starts failing that wouldn't be great).
","As @eendebakpt points out, the uncertainty revolves around the effects of making changes now that other options are available. The primary worry is the impact on pandas code, where the intermingling of pandas and NumPy is frequent and often goes unnoticed. While errors might occur, the real concern is the amount of existing code that could break. We need to carefully consider the potential impact before making any changes.",Known,51,51,numpy_numpy,28024,issue,,1,1,1,1,"As @eendebakpt points out, the uncertainty revolves around the effects of making changes now that other options are available. The primary worry is the impact on pandas code, where the intermingling of pandas and NumPy is frequent and often goes unnoticed. While errors might occur, the real concern is the amount of existing code that could break. We need to carefully consider the potential impact before making any changes."
458,numpy_numpy,issue,28024,"As @eendebakpt says, the problem here is that it is not clear what would happen if you changed it now that alternatives exist.
The main issue here I would expect to be pandas code, since dirty mixing of pandas and NumPy is pretty common, and users may be getting away with things there that they should not be getting away with.

So can we change it? Maybe, but it does require carefully thinking about how impactful the change is (it would make things error, which isn't so bad... but if there is a huge amount of code out there that starts failing that wouldn't be great).
","As @eendebakpt suggests, the main issue is the lack of clarity on what would happen if we made changes now that alternatives are available. The primary concern is the impact on pandas code, where the mixing of pandas and NumPy is common and often tolerated. While introducing errors isn't ideal, the bigger issue is the potential for a large amount of existing code to break. A detailed assessment is needed to understand the impact.",Known,51,51,numpy_numpy,28024,issue,,1,1,1,1,"As @eendebakpt suggests, the main issue is the lack of clarity on what would happen if we made changes now that alternatives are available. The primary concern is the impact on pandas code, where the mixing of pandas and NumPy is common and often tolerated. While introducing errors isn't ideal, the bigger issue is the potential for a large amount of existing code to break. A detailed assessment is needed to understand the impact."
459,numpy_numpy,issue,28024,"As @eendebakpt says, the problem here is that it is not clear what would happen if you changed it now that alternatives exist.
The main issue here I would expect to be pandas code, since dirty mixing of pandas and NumPy is pretty common, and users may be getting away with things there that they should not be getting away with.

So can we change it? Maybe, but it does require carefully thinking about how impactful the change is (it would make things error, which isn't so bad... but if there is a huge amount of code out there that starts failing that wouldn't be great).
","According to @eendebakpt, the ambiguity lies in the potential consequences of altering the behavior now that alternatives exist. The main concern is the impact on pandas code, where the combination of pandas and NumPy is widespread and often leads to unintended results. While errors might occur, the real issue is the volume of existing code that could be affected. We need to carefully evaluate the impact before making any changes.",Known,51,51,numpy_numpy,28024,issue,,1,1,1,1,"According to @eendebakpt, the ambiguity lies in the potential consequences of altering the behavior now that alternatives exist. The main concern is the impact on pandas code, where the combination of pandas and NumPy is widespread and often leads to unintended results. While errors might occur, the real issue is the volume of existing code that could be affected. We need to carefully evaluate the impact before making any changes."
460,numpy_numpy,issue,28024,"As @eendebakpt says, the problem here is that it is not clear what would happen if you changed it now that alternatives exist.
The main issue here I would expect to be pandas code, since dirty mixing of pandas and NumPy is pretty common, and users may be getting away with things there that they should not be getting away with.

So can we change it? Maybe, but it does require carefully thinking about how impactful the change is (it would make things error, which isn't so bad... but if there is a huge amount of code out there that starts failing that wouldn't be great).
","As @eendebakpt notes, the challenge is understanding the implications of making changes now that other options are available. The primary concern is the impact on pandas code, where the mixing of pandas and NumPy is common and often tolerated. While introducing errors isn't ideal, the bigger concern is the potential for a large amount of existing code to break. A thorough evaluation is necessary to gauge the impact.",Known,51,51,numpy_numpy,28024,issue,,1,1,1,1,"As @eendebakpt notes, the challenge is understanding the implications of making changes now that other options are available. The primary concern is the impact on pandas code, where the mixing of pandas and NumPy is common and often tolerated. While introducing errors isn't ideal, the bigger concern is the potential for a large amount of existing code to break. A thorough evaluation is necessary to gauge the impact."
461,numpy_numpy,issue,28024,"Hmmm, I doubt that this changes the situation much (w.r.t how disruptive the change would be).
In the end, won't many/most of the problematic objects (e.g. pandas dataframe, or even torch tensors) already have an __array__ attribute, but still return their own type for np.mean() right now?
(IIRC, np.asarray(torch) raises an error, which is also disruptive, but maybe more desirable.)

I wonder if we could nudge libraries towards implementing __array_function__. Although, I am not sure that is quite convenient enough for having partial support.

Could there be a (very slow) deprecation path for this? Right now, I am not sure...","I'm skeptical that this change will significantly alter the current situation. After all, many problematic objects, such as pandas DataFrames and torch tensors, already have an __array__ attribute but still return their own types when using np.mean(). I recall that np.asarray(torch) raises an error, which is disruptive but perhaps more desirable. Is there a way to encourage libraries to implement __array_function__? I'm not sure if partial support would be convenient enough. Could a slow deprecation path be a viable solution?",Irrelevant,52,52,numpy_numpy,28024,issue,,1,2,2,1,"I'm skeptical that this change will significantly alter the current situation. After all, many problematic objects, such as pandas DataFrames and torch tensors, already have an __array__ attribute but still return their own types when using np.mean(). I recall that np.asarray(torch) raises an error, which is disruptive but perhaps more desirable. Is there a way to encourage libraries to implement __array_function__? I'm not sure if partial support would be convenient enough. Could a slow deprecation path be a viable solution?"
462,numpy_numpy,issue,28024,"Hmmm, I doubt that this changes the situation much (w.r.t how disruptive the change would be).
In the end, won't many/most of the problematic objects (e.g. pandas dataframe, or even torch tensors) already have an __array__ attribute, but still return their own type for np.mean() right now?
(IIRC, np.asarray(torch) raises an error, which is also disruptive, but maybe more desirable.)

I wonder if we could nudge libraries towards implementing __array_function__. Although, I am not sure that is quite convenient enough for having partial support.

Could there be a (very slow) deprecation path for this? Right now, I am not sure...","I doubt this change will make much of a difference in terms of disruption. Most problematic objects, like pandas DataFrames and torch tensors, already have an __array__ attribute but still return their own types when using np.mean(). I remember that np.asarray(torch) results in an error, which is disruptive but might be more appropriate. Could we push libraries to implement __array_function__? I'm uncertain if partial support would be practical. Is a gradual deprecation path a possibility?",Irrelevant,52,52,numpy_numpy,28024,issue,,1,2,2,1,"I doubt this change will make much of a difference in terms of disruption. Most problematic objects, like pandas DataFrames and torch tensors, already have an __array__ attribute but still return their own types when using np.mean(). I remember that np.asarray(torch) results in an error, which is disruptive but might be more appropriate. Could we push libraries to implement __array_function__? I'm uncertain if partial support would be practical. Is a gradual deprecation path a possibility?"
463,numpy_numpy,issue,28024,"Hmmm, I doubt that this changes the situation much (w.r.t how disruptive the change would be).
In the end, won't many/most of the problematic objects (e.g. pandas dataframe, or even torch tensors) already have an __array__ attribute, but still return their own type for np.mean() right now?
(IIRC, np.asarray(torch) raises an error, which is also disruptive, but maybe more desirable.)

I wonder if we could nudge libraries towards implementing __array_function__. Although, I am not sure that is quite convenient enough for having partial support.

Could there be a (very slow) deprecation path for this? Right now, I am not sure...","I'm not convinced that this change will significantly reduce the disruption. Many problematic objects, such as pandas DataFrames and torch tensors, already have an __array__ attribute but still return their own types when using np.mean(). I recall that np.asarray(torch) raises an error, which is disruptive but might be more desirable. Could we nudge libraries towards implementing __array_function__? I'm not sure if partial support would be convenient. Is a slow deprecation path a feasible option?",Irrelevant,52,52,numpy_numpy,28024,issue,,1,2,2,1,"I'm not convinced that this change will significantly reduce the disruption. Many problematic objects, such as pandas DataFrames and torch tensors, already have an __array__ attribute but still return their own types when using np.mean(). I recall that np.asarray(torch) raises an error, which is disruptive but might be more desirable. Could we nudge libraries towards implementing __array_function__? I'm not sure if partial support would be convenient. Is a slow deprecation path a feasible option?"
464,numpy_numpy,issue,28024,"Hmmm, I doubt that this changes the situation much (w.r.t how disruptive the change would be).
In the end, won't many/most of the problematic objects (e.g. pandas dataframe, or even torch tensors) already have an __array__ attribute, but still return their own type for np.mean() right now?
(IIRC, np.asarray(torch) raises an error, which is also disruptive, but maybe more desirable.)

I wonder if we could nudge libraries towards implementing __array_function__. Although, I am not sure that is quite convenient enough for having partial support.

Could there be a (very slow) deprecation path for this? Right now, I am not sure...","I'm uncertain if this change will make a significant impact on the disruption. Many problematic objects, like pandas DataFrames and torch tensors, already have an __array__ attribute but still return their own types when using np.mean(). I remember that np.asarray(torch) raises an error, which is disruptive but might be more appropriate. Could we encourage libraries to implement __array_function__? I'm not sure if partial support would be practical. Is a gradual deprecation path a viable solution?",Irrelevant,52,52,numpy_numpy,28024,issue,,1,2,2,1,"I'm uncertain if this change will make a significant impact on the disruption. Many problematic objects, like pandas DataFrames and torch tensors, already have an __array__ attribute but still return their own types when using np.mean(). I remember that np.asarray(torch) raises an error, which is disruptive but might be more appropriate. Could we encourage libraries to implement __array_function__? I'm not sure if partial support would be practical. Is a gradual deprecation path a viable solution?"
465,numpy_numpy,issue,28024,"Hmmm, I doubt that this changes the situation much (w.r.t how disruptive the change would be).
In the end, won't many/most of the problematic objects (e.g. pandas dataframe, or even torch tensors) already have an __array__ attribute, but still return their own type for np.mean() right now?
(IIRC, np.asarray(torch) raises an error, which is also disruptive, but maybe more desirable.)

I wonder if we could nudge libraries towards implementing __array_function__. Although, I am not sure that is quite convenient enough for having partial support.

Could there be a (very slow) deprecation path for this? Right now, I am not sure...","I'm not sure if this change will significantly reduce the disruption. Many problematic objects, such as pandas DataFrames and torch tensors, already have an __array__ attribute but still return their own types when using np.mean(). I recall that np.asarray(torch) raises an error, which is disruptive but might be more desirable. Could we push libraries to implement __array_function__? I'm uncertain if partial support would be convenient. Is a slow deprecation path a possibility?",Irrelevant,52,52,numpy_numpy,28024,issue,,1,2,2,1,"I'm not sure if this change will significantly reduce the disruption. Many problematic objects, such as pandas DataFrames and torch tensors, already have an __array__ attribute but still return their own types when using np.mean(). I recall that np.asarray(torch) raises an error, which is disruptive but might be more desirable. Could we push libraries to implement __array_function__? I'm uncertain if partial support would be convenient. Is a slow deprecation path a possibility?"
466,numpy_numpy,issue,18004,"It seems odd that this doesn't work, and it feels like it should... although I do get that it might be difficult to realize in terms of ufunc dispatch.","I find it surprising that this functionality isn't supported, as it seems like it should be. I understand that implementing it might be challenging due to the intricacies of ufunc dispatch, but it feels like a natural feature to have.",Unknown,53,53,numpy_numpy,18004,issue,,1,3,3,1,"I find it surprising that this functionality isn't supported, as it seems like it should be. I understand that implementing it might be challenging due to the intricacies of ufunc dispatch, but it feels like a natural feature to have."
467,numpy_numpy,issue,18004,"It seems odd that this doesn't work, and it feels like it should... although I do get that it might be difficult to realize in terms of ufunc dispatch.","It's a bit puzzling that this doesn't work, especially since it seems like it should. I can appreciate that there might be technical hurdles with ufunc dispatch, but it would be great if it could be addressed.",Unknown,53,53,numpy_numpy,18004,issue,,1,3,3,1,"It's a bit puzzling that this doesn't work, especially since it seems like it should. I can appreciate that there might be technical hurdles with ufunc dispatch, but it would be great if it could be addressed."
468,numpy_numpy,issue,18004,"It seems odd that this doesn't work, and it feels like it should... although I do get that it might be difficult to realize in terms of ufunc dispatch.","I'm a bit confused about why this isn't working, as it seems like a logical feature. I realize that there might be complexities with ufunc dispatch that make it difficult to implement, but it would be helpful to understand the reasoning behind it.",Unknown,53,53,numpy_numpy,18004,issue,,1,3,3,1,"I'm a bit confused about why this isn't working, as it seems like a logical feature. I realize that there might be complexities with ufunc dispatch that make it difficult to implement, but it would be helpful to understand the reasoning behind it."
469,numpy_numpy,issue,18004,"It seems odd that this doesn't work, and it feels like it should... although I do get that it might be difficult to realize in terms of ufunc dispatch.","It feels counterintuitive that this doesn't work, given how it seems like it should. I understand that there might be technical limitations with ufunc dispatch, but it would be useful to have more clarity on the issue.",Unknown,53,53,numpy_numpy,18004,issue,,1,3,3,1,"It feels counterintuitive that this doesn't work, given how it seems like it should. I understand that there might be technical limitations with ufunc dispatch, but it would be useful to have more clarity on the issue."
470,numpy_numpy,issue,18004,"It seems odd that this doesn't work, and it feels like it should... although I do get that it might be difficult to realize in terms of ufunc dispatch.","I'm surprised that this isn't supported, as it seems like a natural fit. I get that there might be challenges with ufunc dispatch, but it would be great to have this functionality.",Unknown,53,53,numpy_numpy,18004,issue,,1,3,3,1,"I'm surprised that this isn't supported, as it seems like a natural fit. I get that there might be challenges with ufunc dispatch, but it would be great to have this functionality."
471,numpy_numpy,issue,18004,"It seems odd that this doesn't work, and it feels like it should... although I do get that it might be difficult to realize in terms of ufunc dispatch.","It's odd that this doesn't work, especially since it seems like it should. I can see that there might be difficulties with ufunc dispatch, but it would be helpful to know more about the technical constraints.",Unknown,53,53,numpy_numpy,18004,issue,,1,3,3,1,"It's odd that this doesn't work, especially since it seems like it should. I can see that there might be difficulties with ufunc dispatch, but it would be helpful to know more about the technical constraints."
472,numpy_numpy,issue,18004,"It seems odd that this doesn't work, and it feels like it should... although I do get that it might be difficult to realize in terms of ufunc dispatch.","I find it strange that this isn't working, as it seems like it should. I understand that there might be issues with ufunc dispatch, but it would be useful to have more information on why it's not supported.",Unknown,53,53,numpy_numpy,18004,issue,,1,3,3,1,"I find it strange that this isn't working, as it seems like it should. I understand that there might be issues with ufunc dispatch, but it would be useful to have more information on why it's not supported."
473,numpy_numpy,issue,18004,"It seems odd that this doesn't work, and it feels like it should... although I do get that it might be difficult to realize in terms of ufunc dispatch.","It's a bit unexpected that this doesn't work, given how it seems like it should. I realize that there might be technical challenges with ufunc dispatch, but it would be great to have a clearer explanation.",Unknown,53,53,numpy_numpy,18004,issue,,1,3,3,1,"It's a bit unexpected that this doesn't work, given how it seems like it should. I realize that there might be technical challenges with ufunc dispatch, but it would be great to have a clearer explanation."
474,numpy_numpy,issue,18004,"It seems odd that this doesn't work, and it feels like it should... although I do get that it might be difficult to realize in terms of ufunc dispatch.","I'm a bit taken aback that this isn't supported, as it seems like a logical feature. I understand that there might be complexities with ufunc dispatch, but it would be helpful to have more details on the reasoning behind it.",Unknown,53,53,numpy_numpy,18004,issue,,1,3,3,1,"I'm a bit taken aback that this isn't supported, as it seems like a logical feature. I understand that there might be complexities with ufunc dispatch, but it would be helpful to have more details on the reasoning behind it."
475,numpy_numpy,issue,18004,"It seems odd that this doesn't work, and it feels like it should... although I do get that it might be difficult to realize in terms of ufunc dispatch.","It's surprising that this doesn't work, especially since it seems like it should. I can appreciate that there might be technical hurdles with ufunc dispatch, but it would be great to have more insight into the issue.",Unknown,53,53,numpy_numpy,18004,issue,,1,3,3,1,"It's surprising that this doesn't work, especially since it seems like it should. I can appreciate that there might be technical hurdles with ufunc dispatch, but it would be great to have more insight into the issue."
476,numpy_numpy,issue,18004,Not sure how you are getting an object array here.,I'm confused about how you're ending up with an object array in this context. Could you provide more details or a code snippet to help me understand?,Unknown,54,54,numpy_numpy,18004,issue,,1,3,3,1,I'm confused about how you're ending up with an object array in this context. Could you provide more details or a code snippet to help me understand?
477,numpy_numpy,issue,18004,Not sure how you are getting an object array here.,I don't see how an object array is being generated here. Can you explain the steps or share the relevant code that leads to this result?,Unknown,54,54,numpy_numpy,18004,issue,,1,3,3,1,I don't see how an object array is being generated here. Can you explain the steps or share the relevant code that leads to this result?
478,numpy_numpy,issue,18004,Not sure how you are getting an object array here.,I'm not sure how you're getting an object array. Could there be an implicit conversion or a specific configuration causing this?,Unknown,54,54,numpy_numpy,18004,issue,,1,3,3,1,I'm not sure how you're getting an object array. Could there be an implicit conversion or a specific configuration causing this?
479,numpy_numpy,issue,18004,Not sure how you are getting an object array here.,I'm unsure how an object array is being produced in this scenario. Could you clarify the conditions or the code that results in this output?,Unknown,54,54,numpy_numpy,18004,issue,,1,3,3,1,I'm unsure how an object array is being produced in this scenario. Could you clarify the conditions or the code that results in this output?
480,numpy_numpy,issue,18004,Not sure how you are getting an object array here.,I'm puzzled by the object array you mentioned. Could you elaborate on the process or the code that leads to this array type?,Unknown,54,54,numpy_numpy,18004,issue,,1,3,3,1,I'm puzzled by the object array you mentioned. Could you elaborate on the process or the code that leads to this array type?
481,numpy_numpy,issue,18004,Not sure how you are getting an object array here.,I don't understand how you're ending up with an object array. Is there a particular function or method call that might be causing this?,Unknown,54,54,numpy_numpy,18004,issue,,1,3,3,1,I don't understand how you're ending up with an object array. Is there a particular function or method call that might be causing this?
482,numpy_numpy,issue,18004,Not sure how you are getting an object array here.,I'm not clear on how an object array is being created here. Could you provide more context or a code example to help me figure it out?,Unknown,54,54,numpy_numpy,18004,issue,,1,3,3,1,I'm not clear on how an object array is being created here. Could you provide more context or a code example to help me figure it out?
483,numpy_numpy,issue,18004,Not sure how you are getting an object array here.,I'm uncertain about the object array you're seeing. Could you share more details or the specific code that results in this array type?,Unknown,54,54,numpy_numpy,18004,issue,,1,3,3,1,I'm uncertain about the object array you're seeing. Could you share more details or the specific code that results in this array type?
484,numpy_numpy,issue,18004,Not sure how you are getting an object array here.,I'm having trouble understanding how you're getting an object array. Could you explain the steps or the code that leads to this outcome?,Unknown,54,54,numpy_numpy,18004,issue,,1,3,3,1,I'm having trouble understanding how you're getting an object array. Could you explain the steps or the code that leads to this outcome?
485,numpy_numpy,issue,18004,Not sure how you are getting an object array here.,I'm not sure how an object array is being generated in this case. Could you provide more information or a code snippet to help me understand?,Unknown,54,54,numpy_numpy,18004,issue,,1,3,3,1,I'm not sure how an object array is being generated in this case. Could you provide more information or a code snippet to help me understand?
486,numpy_numpy,issue,18004,"The error is referring to the fact that float(1).exp() fails. NumPy does not go to much effort to handle functions on object arrays. In general object arrays will never really be fully integrated into NumPy, and maybe were a mistake in the first place, but are convenient for some container-like applications. Object dtypes are ""out of scope"" for the Array API standard (point 4).","The error occurs because the `exp()` function is not supported for object arrays in NumPy. NumPy does not fully integrate object arrays, and they might have been a design mistake, but they are useful for certain container-like applications. According to the Array API standard, object dtypes are explicitly out of scope (point 4).",Knowledge,55,55,numpy_numpy,18004,issue,,0,0,0,0,"The error occurs because the `exp()` function is not supported for object arrays in NumPy. NumPy does not fully integrate object arrays, and they might have been a design mistake, but they are useful for certain container-like applications. According to the Array API standard, object dtypes are explicitly out of scope (point 4)."
487,numpy_numpy,issue,18004,"The error is referring to the fact that float(1).exp() fails. NumPy does not go to much effort to handle functions on object arrays. In general object arrays will never really be fully integrated into NumPy, and maybe were a mistake in the first place, but are convenient for some container-like applications. Object dtypes are ""out of scope"" for the Array API standard (point 4).","I'm encountering an error when trying to use `float(1).exp()` on an object array in NumPy. It seems that NumPy does not put much effort into supporting functions on object arrays, and they are generally not fully integrated. The Array API standard also excludes object dtypes (point 4).",Knowledge,55,55,numpy_numpy,18004,issue,,0,0,0,0,"I'm encountering an error when trying to use `float(1).exp()` on an object array in NumPy. It seems that NumPy does not put much effort into supporting functions on object arrays, and they are generally not fully integrated. The Array API standard also excludes object dtypes (point 4)."
488,numpy_numpy,issue,18004,"The error is referring to the fact that float(1).exp() fails. NumPy does not go to much effort to handle functions on object arrays. In general object arrays will never really be fully integrated into NumPy, and maybe were a mistake in the first place, but are convenient for some container-like applications. Object dtypes are ""out of scope"" for the Array API standard (point 4).","The error message indicates that `float(1).exp()` is not supported for object arrays in NumPy. NumPy's handling of object arrays is limited, and they are not fully integrated into the library. This is in line with the Array API standard, which explicitly states that object dtypes are out of scope (point 4).",Knowledge,55,55,numpy_numpy,18004,issue,,0,0,0,0,"The error message indicates that `float(1).exp()` is not supported for object arrays in NumPy. NumPy's handling of object arrays is limited, and they are not fully integrated into the library. This is in line with the Array API standard, which explicitly states that object dtypes are out of scope (point 4)."
489,numpy_numpy,issue,18004,"The error is referring to the fact that float(1).exp() fails. NumPy does not go to much effort to handle functions on object arrays. In general object arrays will never really be fully integrated into NumPy, and maybe were a mistake in the first place, but are convenient for some container-like applications. Object dtypes are ""out of scope"" for the Array API standard (point 4).","I'm getting an error when trying to apply the `exp()` function to an object array in NumPy. NumPy does not fully support functions on object arrays, and they are often considered a design mistake. The Array API standard (point 4) confirms that object dtypes are not within its scope.",Knowledge,55,55,numpy_numpy,18004,issue,,0,0,0,0,"I'm getting an error when trying to apply the `exp()` function to an object array in NumPy. NumPy does not fully support functions on object arrays, and they are often considered a design mistake. The Array API standard (point 4) confirms that object dtypes are not within its scope."
490,numpy_numpy,issue,18004,"The error is referring to the fact that float(1).exp() fails. NumPy does not go to much effort to handle functions on object arrays. In general object arrays will never really be fully integrated into NumPy, and maybe were a mistake in the first place, but are convenient for some container-like applications. Object dtypes are ""out of scope"" for the Array API standard (point 4).",The error is due to the fact that `float(1).exp()` is not supported for object arrays in NumPy. NumPy's object arrays are not fully integrated and are only useful for certain container-like applications. The Array API standard (point 4) explicitly excludes object dtypes from its scope.,Knowledge,55,55,numpy_numpy,18004,issue,,0,0,0,0,The error is due to the fact that `float(1).exp()` is not supported for object arrays in NumPy. NumPy's object arrays are not fully integrated and are only useful for certain container-like applications. The Array API standard (point 4) explicitly excludes object dtypes from its scope.
491,numpy_numpy,issue,18004,"The error is referring to the fact that float(1).exp() fails. NumPy does not go to much effort to handle functions on object arrays. In general object arrays will never really be fully integrated into NumPy, and maybe were a mistake in the first place, but are convenient for some container-like applications. Object dtypes are ""out of scope"" for the Array API standard (point 4).","I'm seeing an error when using `float(1).exp()` on an object array in NumPy. NumPy does not fully support functions on object arrays, and they are generally not integrated into the library. The Array API standard (point 4) confirms that object dtypes are out of scope.",Knowledge,55,55,numpy_numpy,18004,issue,,0,0,0,0,"I'm seeing an error when using `float(1).exp()` on an object array in NumPy. NumPy does not fully support functions on object arrays, and they are generally not integrated into the library. The Array API standard (point 4) confirms that object dtypes are out of scope."
492,numpy_numpy,issue,18004,"The error is referring to the fact that float(1).exp() fails. NumPy does not go to much effort to handle functions on object arrays. In general object arrays will never really be fully integrated into NumPy, and maybe were a mistake in the first place, but are convenient for some container-like applications. Object dtypes are ""out of scope"" for the Array API standard (point 4).",The error is caused by the `exp()` function not being supported for object arrays in NumPy. NumPy's object arrays are not fully integrated and are often considered a design mistake. The Array API standard (point 4) states that object dtypes are out of scope.,Knowledge,55,55,numpy_numpy,18004,issue,,0,0,0,0,The error is caused by the `exp()` function not being supported for object arrays in NumPy. NumPy's object arrays are not fully integrated and are often considered a design mistake. The Array API standard (point 4) states that object dtypes are out of scope.
493,numpy_numpy,issue,18004,"The error is referring to the fact that float(1).exp() fails. NumPy does not go to much effort to handle functions on object arrays. In general object arrays will never really be fully integrated into NumPy, and maybe were a mistake in the first place, but are convenient for some container-like applications. Object dtypes are ""out of scope"" for the Array API standard (point 4).","I'm running into an error with `float(1).exp()` on an object array in NumPy. NumPy does not fully support functions on object arrays, and they are not fully integrated into the library. The Array API standard (point 4) confirms that object dtypes are not within its scope.",Knowledge,55,55,numpy_numpy,18004,issue,,0,0,0,0,"I'm running into an error with `float(1).exp()` on an object array in NumPy. NumPy does not fully support functions on object arrays, and they are not fully integrated into the library. The Array API standard (point 4) confirms that object dtypes are not within its scope."
494,numpy_numpy,issue,18004,"The error is referring to the fact that float(1).exp() fails. NumPy does not go to much effort to handle functions on object arrays. In general object arrays will never really be fully integrated into NumPy, and maybe were a mistake in the first place, but are convenient for some container-like applications. Object dtypes are ""out of scope"" for the Array API standard (point 4).",The error is due to the `exp()` function not being supported for object arrays in NumPy. NumPy's object arrays are not fully integrated and are only useful for certain container-like applications. The Array API standard (point 4) explicitly states that object dtypes are out of scope.,Knowledge,55,55,numpy_numpy,18004,issue,,0,0,0,0,The error is due to the `exp()` function not being supported for object arrays in NumPy. NumPy's object arrays are not fully integrated and are only useful for certain container-like applications. The Array API standard (point 4) explicitly states that object dtypes are out of scope.
495,numpy_numpy,issue,18004,"@mattip under the Discussion banner, my comment was more directed at why a = np.array([1., 2.], dtype=object) would be cast as an object array in the first place. Or perhaps it was the result of some process that casts all outputs as object arrays. Accounting for all cases where things may fail is a lost cause. This may be one example of the deprecation warnings might be useful, but where do you stop? You have one for ragged arrays (VisibleDeprecationWarning: Creating an ndarray from ragged nested sequences....), do you then have to provide one for arrays that shouldn't be cast as object arrays?","I was more curious about why `a = np.array([1., 2.], dtype=object)` results in an object array. Is this a default behavior or a side effect of some other process? It seems like a common pitfall, and while deprecation warnings for ragged arrays are helpful, should we also warn about other cases where arrays are cast as object arrays?",Known,56,56,numpy_numpy,18004,issue,,1,1,1,1,"I was more curious about why `a = np.array([1., 2.], dtype=object)` results in an object array. Is this a default behavior or a side effect of some other process? It seems like a common pitfall, and while deprecation warnings for ragged arrays are helpful, should we also warn about other cases where arrays are cast as object arrays?"
496,numpy_numpy,issue,18004,"@mattip under the Discussion banner, my comment was more directed at why a = np.array([1., 2.], dtype=object) would be cast as an object array in the first place. Or perhaps it was the result of some process that casts all outputs as object arrays. Accounting for all cases where things may fail is a lost cause. This may be one example of the deprecation warnings might be useful, but where do you stop? You have one for ragged arrays (VisibleDeprecationWarning: Creating an ndarray from ragged nested sequences....), do you then have to provide one for arrays that shouldn't be cast as object arrays?","My comment was more about the reasoning behind `a = np.array([1., 2.], dtype=object)` being cast as an object array. Is this a deliberate design choice or an unintended consequence? It's challenging to account for all potential issues, but deprecation warnings for ragged arrays are a start. Should we extend this to other problematic array casts as well?",Known,56,56,numpy_numpy,18004,issue,,1,1,1,1,"My comment was more about the reasoning behind `a = np.array([1., 2.], dtype=object)` being cast as an object array. Is this a deliberate design choice or an unintended consequence? It's challenging to account for all potential issues, but deprecation warnings for ragged arrays are a start. Should we extend this to other problematic array casts as well?"
497,numpy_numpy,issue,18004,"@mattip under the Discussion banner, my comment was more directed at why a = np.array([1., 2.], dtype=object) would be cast as an object array in the first place. Or perhaps it was the result of some process that casts all outputs as object arrays. Accounting for all cases where things may fail is a lost cause. This may be one example of the deprecation warnings might be useful, but where do you stop? You have one for ragged arrays (VisibleDeprecationWarning: Creating an ndarray from ragged nested sequences....), do you then have to provide one for arrays that shouldn't be cast as object arrays?","I was wondering why `a = np.array([1., 2.], dtype=object)` ends up as an object array. Is this a result of a specific process or a general behavior? While deprecation warnings for ragged arrays are useful, it raises the question of whether we should have similar warnings for other cases where arrays are cast as object arrays.",Known,56,56,numpy_numpy,18004,issue,,1,1,1,1,"I was wondering why `a = np.array([1., 2.], dtype=object)` ends up as an object array. Is this a result of a specific process or a general behavior? While deprecation warnings for ragged arrays are useful, it raises the question of whether we should have similar warnings for other cases where arrays are cast as object arrays."
498,numpy_numpy,issue,18004,"@mattip under the Discussion banner, my comment was more directed at why a = np.array([1., 2.], dtype=object) would be cast as an object array in the first place. Or perhaps it was the result of some process that casts all outputs as object arrays. Accounting for all cases where things may fail is a lost cause. This may be one example of the deprecation warnings might be useful, but where do you stop? You have one for ragged arrays (VisibleDeprecationWarning: Creating an ndarray from ragged nested sequences....), do you then have to provide one for arrays that shouldn't be cast as object arrays?","My initial comment was focused on the behavior of `a = np.array([1., 2.], dtype=object)` being cast as an object array. Is this a default setting or a side effect of some other operation? It's difficult to cover all edge cases, but deprecation warnings for ragged arrays are a good start. Should we consider warnings for other problematic array casts?",Known,56,56,numpy_numpy,18004,issue,,1,1,1,1,"My initial comment was focused on the behavior of `a = np.array([1., 2.], dtype=object)` being cast as an object array. Is this a default setting or a side effect of some other operation? It's difficult to cover all edge cases, but deprecation warnings for ragged arrays are a good start. Should we consider warnings for other problematic array casts?"
499,numpy_numpy,issue,18004,"@mattip under the Discussion banner, my comment was more directed at why a = np.array([1., 2.], dtype=object) would be cast as an object array in the first place. Or perhaps it was the result of some process that casts all outputs as object arrays. Accounting for all cases where things may fail is a lost cause. This may be one example of the deprecation warnings might be useful, but where do you stop? You have one for ragged arrays (VisibleDeprecationWarning: Creating an ndarray from ragged nested sequences....), do you then have to provide one for arrays that shouldn't be cast as object arrays?","I was more interested in understanding why `a = np.array([1., 2.], dtype=object)` results in an object array. Is this a standard behavior or a result of some other process? While deprecation warnings for ragged arrays are helpful, it makes me wonder if we should have similar warnings for other cases where arrays are cast as object arrays.",Known,56,56,numpy_numpy,18004,issue,,1,1,1,1,"I was more interested in understanding why `a = np.array([1., 2.], dtype=object)` results in an object array. Is this a standard behavior or a result of some other process? While deprecation warnings for ragged arrays are helpful, it makes me wonder if we should have similar warnings for other cases where arrays are cast as object arrays."
500,numpy_numpy,issue,18004,"@mattip under the Discussion banner, my comment was more directed at why a = np.array([1., 2.], dtype=object) would be cast as an object array in the first place. Or perhaps it was the result of some process that casts all outputs as object arrays. Accounting for all cases where things may fail is a lost cause. This may be one example of the deprecation warnings might be useful, but where do you stop? You have one for ragged arrays (VisibleDeprecationWarning: Creating an ndarray from ragged nested sequences....), do you then have to provide one for arrays that shouldn't be cast as object arrays?","I was curious about the reasoning behind `a = np.array([1., 2.], dtype=object)` being cast as an object array. Is this a deliberate choice or an unintended consequence? It's a challenging task to account for all potential issues, but deprecation warnings for ragged arrays are a step in the right direction. Should we extend this to other problematic array casts?",Known,56,56,numpy_numpy,18004,issue,,1,1,1,1,"I was curious about the reasoning behind `a = np.array([1., 2.], dtype=object)` being cast as an object array. Is this a deliberate choice or an unintended consequence? It's a challenging task to account for all potential issues, but deprecation warnings for ragged arrays are a step in the right direction. Should we extend this to other problematic array casts?"
501,numpy_numpy,issue,18004,"@mattip under the Discussion banner, my comment was more directed at why a = np.array([1., 2.], dtype=object) would be cast as an object array in the first place. Or perhaps it was the result of some process that casts all outputs as object arrays. Accounting for all cases where things may fail is a lost cause. This may be one example of the deprecation warnings might be useful, but where do you stop? You have one for ragged arrays (VisibleDeprecationWarning: Creating an ndarray from ragged nested sequences....), do you then have to provide one for arrays that shouldn't be cast as object arrays?","My comment was directed at the behavior of `a = np.array([1., 2.], dtype=object)` being cast as an object array. Is this a default behavior or a side effect of some other process? While deprecation warnings for ragged arrays are useful, it raises the question of whether we should have similar warnings for other cases where arrays are cast as object arrays.",Known,56,56,numpy_numpy,18004,issue,,1,1,1,1,"My comment was directed at the behavior of `a = np.array([1., 2.], dtype=object)` being cast as an object array. Is this a default behavior or a side effect of some other process? While deprecation warnings for ragged arrays are useful, it raises the question of whether we should have similar warnings for other cases where arrays are cast as object arrays."
502,numpy_numpy,issue,18004,"@mattip under the Discussion banner, my comment was more directed at why a = np.array([1., 2.], dtype=object) would be cast as an object array in the first place. Or perhaps it was the result of some process that casts all outputs as object arrays. Accounting for all cases where things may fail is a lost cause. This may be one example of the deprecation warnings might be useful, but where do you stop? You have one for ragged arrays (VisibleDeprecationWarning: Creating an ndarray from ragged nested sequences....), do you then have to provide one for arrays that shouldn't be cast as object arrays?","I was more focused on why `a = np.array([1., 2.], dtype=object)` results in an object array. Is this a standard behavior or a result of some other operation? It's difficult to cover all edge cases, but deprecation warnings for ragged arrays are a good start. Should we consider warnings for other problematic array casts as well?",Known,56,56,numpy_numpy,18004,issue,,1,1,1,1,"I was more focused on why `a = np.array([1., 2.], dtype=object)` results in an object array. Is this a standard behavior or a result of some other operation? It's difficult to cover all edge cases, but deprecation warnings for ragged arrays are a good start. Should we consider warnings for other problematic array casts as well?"
503,numpy_numpy,issue,17669,"This issue is motivated by ongoing work in SciPy to improve the existing multivariate distributions and add new ones. We'd like to be sure that, if possible, enhancements that we make remain consistent with the current and future behavior of NumPy's multivariate distributions.

The specific issue is how the size parameter interacts with broadcasting of the distribution parameters, some of which are 1-d or 2-d arrays rather than scalars.

I'm going to refer to gufunc broadcasting, so it will be helpful to have the gufunc signatures of the existing multivariate distributions (even though they are not actually implemented as gufuncs):

Name                         Parameters      Gufunc signature
---------------------------  --------------  ----------------
dirichlet                    alpha           (n)       -> (n)
multinomial                  n, pvals        (),(n)    -> (n)
multivariate_normal          mean, cov       (n),(n,n) -> (n)
multivariate_hypergeometric  colors, nsample (n),()    -> (n)

@bashtage has a pull request to add broadcasting to multinomial. As an example, suppose I have four values of n, say 2, 5, 10 and 20, and I have two discrete probability distributions of length 3, say [0.1, 0.2, 0.7] and [0.8, 0.2, 0.0], and I want to generate 10 multinomial variates for each combination of those inputs. With the code in @bashtage's PR, we can write:

In [85]: n = np.array([2, 5, 10, 20]).reshape(-1, 1)

In [86]: pvals = np.array([[0.1, 0.2, 0.7], [0.8, 0.2, 0.0]])

In [87]: rng = np.random.default_rng()

In [88]: x = rng.multinomial(n, pvals, size=(10, 4, 2))

In [89]: x.shape
Out[89]: (10, 4, 2, 3)

The shape of the output has three sources: (10,) is the number of variates that we wanted for each combination of input parameters; (4, 2) is the broadcast shape of the input parameters, without the dimension associated with the gufunc signature; (3,) is the dimension associated with each variate.

Note that size is not the shape of the output array--size does not include the dimension associated with the variates themselves.

The question is how to adapt this to the other multivariate distribution methods.","We are currently working on enhancing the multivariate distributions in SciPy to ensure they remain consistent with NumPy's behavior. One key challenge is how the size parameter interacts with the broadcasting of distribution parameters, which can be 1-d or 2-d arrays. For example, in the multinomial distribution, we need to handle different values of n and pvals while generating variates. @bashtage's PR introduces broadcasting, allowing us to generate 10 multinomial variates for each combination of inputs. The output shape reflects the number of variates, the broadcast shape of the inputs, and the dimension of each variate. We need to apply this approach to other multivariate distributions as well.",Known,57,57,numpy_numpy,17669,issue,,1,1,1,1,"We are currently working on enhancing the multivariate distributions in SciPy to ensure they remain consistent with NumPy's behavior. One key challenge is how the size parameter interacts with the broadcasting of distribution parameters, which can be 1-d or 2-d arrays. For example, in the multinomial distribution, we need to handle different values of n and pvals while generating variates. @bashtage's PR introduces broadcasting, allowing us to generate 10 multinomial variates for each combination of inputs. The output shape reflects the number of variates, the broadcast shape of the inputs, and the dimension of each variate. We need to apply this approach to other multivariate distributions as well."
504,numpy_numpy,issue,17669,"This issue is motivated by ongoing work in SciPy to improve the existing multivariate distributions and add new ones. We'd like to be sure that, if possible, enhancements that we make remain consistent with the current and future behavior of NumPy's multivariate distributions.

The specific issue is how the size parameter interacts with broadcasting of the distribution parameters, some of which are 1-d or 2-d arrays rather than scalars.

I'm going to refer to gufunc broadcasting, so it will be helpful to have the gufunc signatures of the existing multivariate distributions (even though they are not actually implemented as gufuncs):

Name                         Parameters      Gufunc signature
---------------------------  --------------  ----------------
dirichlet                    alpha           (n)       -> (n)
multinomial                  n, pvals        (),(n)    -> (n)
multivariate_normal          mean, cov       (n),(n,n) -> (n)
multivariate_hypergeometric  colors, nsample (n),()    -> (n)

@bashtage has a pull request to add broadcasting to multinomial. As an example, suppose I have four values of n, say 2, 5, 10 and 20, and I have two discrete probability distributions of length 3, say [0.1, 0.2, 0.7] and [0.8, 0.2, 0.0], and I want to generate 10 multinomial variates for each combination of those inputs. With the code in @bashtage's PR, we can write:

In [85]: n = np.array([2, 5, 10, 20]).reshape(-1, 1)

In [86]: pvals = np.array([[0.1, 0.2, 0.7], [0.8, 0.2, 0.0]])

In [87]: rng = np.random.default_rng()

In [88]: x = rng.multinomial(n, pvals, size=(10, 4, 2))

In [89]: x.shape
Out[89]: (10, 4, 2, 3)

The shape of the output has three sources: (10,) is the number of variates that we wanted for each combination of input parameters; (4, 2) is the broadcast shape of the input parameters, without the dimension associated with the gufunc signature; (3,) is the dimension associated with each variate.

Note that size is not the shape of the output array--size does not include the dimension associated with the variates themselves.

The question is how to adapt this to the other multivariate distribution methods.","Our goal is to improve the multivariate distributions in SciPy and ensure they align with NumPy's future behavior. A specific issue is the interaction between the size parameter and the broadcasting of distribution parameters, which can be arrays of different dimensions. For instance, in the multinomial distribution, we need to handle multiple values of n and pvals. @bashtage's PR demonstrates how to generate variates with broadcasting, resulting in an output shape that includes the number of variates, the broadcast shape of the inputs, and the variate dimension. We need to extend this approach to other multivariate distributions.",Known,57,57,numpy_numpy,17669,issue,,1,1,1,1,"Our goal is to improve the multivariate distributions in SciPy and ensure they align with NumPy's future behavior. A specific issue is the interaction between the size parameter and the broadcasting of distribution parameters, which can be arrays of different dimensions. For instance, in the multinomial distribution, we need to handle multiple values of n and pvals. @bashtage's PR demonstrates how to generate variates with broadcasting, resulting in an output shape that includes the number of variates, the broadcast shape of the inputs, and the variate dimension. We need to extend this approach to other multivariate distributions."
505,numpy_numpy,issue,17669,"This issue is motivated by ongoing work in SciPy to improve the existing multivariate distributions and add new ones. We'd like to be sure that, if possible, enhancements that we make remain consistent with the current and future behavior of NumPy's multivariate distributions.

The specific issue is how the size parameter interacts with broadcasting of the distribution parameters, some of which are 1-d or 2-d arrays rather than scalars.

I'm going to refer to gufunc broadcasting, so it will be helpful to have the gufunc signatures of the existing multivariate distributions (even though they are not actually implemented as gufuncs):

Name                         Parameters      Gufunc signature
---------------------------  --------------  ----------------
dirichlet                    alpha           (n)       -> (n)
multinomial                  n, pvals        (),(n)    -> (n)
multivariate_normal          mean, cov       (n),(n,n) -> (n)
multivariate_hypergeometric  colors, nsample (n),()    -> (n)

@bashtage has a pull request to add broadcasting to multinomial. As an example, suppose I have four values of n, say 2, 5, 10 and 20, and I have two discrete probability distributions of length 3, say [0.1, 0.2, 0.7] and [0.8, 0.2, 0.0], and I want to generate 10 multinomial variates for each combination of those inputs. With the code in @bashtage's PR, we can write:

In [85]: n = np.array([2, 5, 10, 20]).reshape(-1, 1)

In [86]: pvals = np.array([[0.1, 0.2, 0.7], [0.8, 0.2, 0.0]])

In [87]: rng = np.random.default_rng()

In [88]: x = rng.multinomial(n, pvals, size=(10, 4, 2))

In [89]: x.shape
Out[89]: (10, 4, 2, 3)

The shape of the output has three sources: (10,) is the number of variates that we wanted for each combination of input parameters; (4, 2) is the broadcast shape of the input parameters, without the dimension associated with the gufunc signature; (3,) is the dimension associated with each variate.

Note that size is not the shape of the output array--size does not include the dimension associated with the variates themselves.

The question is how to adapt this to the other multivariate distribution methods.","We are enhancing the multivariate distributions in SciPy to maintain consistency with NumPy's behavior. One challenge is the interaction between the size parameter and the broadcasting of distribution parameters, which can be 1-d or 2-d arrays. For example, in the multinomial distribution, we need to handle different values of n and pvals. @bashtage's PR shows how to generate 10 multinomial variates for each combination of inputs, with the output shape reflecting the number of variates, the broadcast shape of the inputs, and the variate dimension. We need to apply this approach to other multivariate distributions.",Known,57,57,numpy_numpy,17669,issue,,1,1,1,1,"We are enhancing the multivariate distributions in SciPy to maintain consistency with NumPy's behavior. One challenge is the interaction between the size parameter and the broadcasting of distribution parameters, which can be 1-d or 2-d arrays. For example, in the multinomial distribution, we need to handle different values of n and pvals. @bashtage's PR shows how to generate 10 multinomial variates for each combination of inputs, with the output shape reflecting the number of variates, the broadcast shape of the inputs, and the variate dimension. We need to apply this approach to other multivariate distributions."
506,numpy_numpy,issue,17669,"This issue is motivated by ongoing work in SciPy to improve the existing multivariate distributions and add new ones. We'd like to be sure that, if possible, enhancements that we make remain consistent with the current and future behavior of NumPy's multivariate distributions.

The specific issue is how the size parameter interacts with broadcasting of the distribution parameters, some of which are 1-d or 2-d arrays rather than scalars.

I'm going to refer to gufunc broadcasting, so it will be helpful to have the gufunc signatures of the existing multivariate distributions (even though they are not actually implemented as gufuncs):

Name                         Parameters      Gufunc signature
---------------------------  --------------  ----------------
dirichlet                    alpha           (n)       -> (n)
multinomial                  n, pvals        (),(n)    -> (n)
multivariate_normal          mean, cov       (n),(n,n) -> (n)
multivariate_hypergeometric  colors, nsample (n),()    -> (n)

@bashtage has a pull request to add broadcasting to multinomial. As an example, suppose I have four values of n, say 2, 5, 10 and 20, and I have two discrete probability distributions of length 3, say [0.1, 0.2, 0.7] and [0.8, 0.2, 0.0], and I want to generate 10 multinomial variates for each combination of those inputs. With the code in @bashtage's PR, we can write:

In [85]: n = np.array([2, 5, 10, 20]).reshape(-1, 1)

In [86]: pvals = np.array([[0.1, 0.2, 0.7], [0.8, 0.2, 0.0]])

In [87]: rng = np.random.default_rng()

In [88]: x = rng.multinomial(n, pvals, size=(10, 4, 2))

In [89]: x.shape
Out[89]: (10, 4, 2, 3)

The shape of the output has three sources: (10,) is the number of variates that we wanted for each combination of input parameters; (4, 2) is the broadcast shape of the input parameters, without the dimension associated with the gufunc signature; (3,) is the dimension associated with each variate.

Note that size is not the shape of the output array--size does not include the dimension associated with the variates themselves.

The question is how to adapt this to the other multivariate distribution methods.","The ongoing work in SciPy to improve multivariate distributions involves ensuring consistency with NumPy's behavior. A key issue is how the size parameter interacts with the broadcasting of distribution parameters, which can be arrays of different dimensions. For instance, in the multinomial distribution, we need to handle multiple values of n and pvals. @bashtage's PR demonstrates how to generate variates with broadcasting, resulting in an output shape that includes the number of variates, the broadcast shape of the inputs, and the variate dimension. We need to extend this approach to other multivariate distributions.",Known,57,57,numpy_numpy,17669,issue,,1,1,1,1,"The ongoing work in SciPy to improve multivariate distributions involves ensuring consistency with NumPy's behavior. A key issue is how the size parameter interacts with the broadcasting of distribution parameters, which can be arrays of different dimensions. For instance, in the multinomial distribution, we need to handle multiple values of n and pvals. @bashtage's PR demonstrates how to generate variates with broadcasting, resulting in an output shape that includes the number of variates, the broadcast shape of the inputs, and the variate dimension. We need to extend this approach to other multivariate distributions."
507,numpy_numpy,issue,17669,"This issue is motivated by ongoing work in SciPy to improve the existing multivariate distributions and add new ones. We'd like to be sure that, if possible, enhancements that we make remain consistent with the current and future behavior of NumPy's multivariate distributions.

The specific issue is how the size parameter interacts with broadcasting of the distribution parameters, some of which are 1-d or 2-d arrays rather than scalars.

I'm going to refer to gufunc broadcasting, so it will be helpful to have the gufunc signatures of the existing multivariate distributions (even though they are not actually implemented as gufuncs):

Name                         Parameters      Gufunc signature
---------------------------  --------------  ----------------
dirichlet                    alpha           (n)       -> (n)
multinomial                  n, pvals        (),(n)    -> (n)
multivariate_normal          mean, cov       (n),(n,n) -> (n)
multivariate_hypergeometric  colors, nsample (n),()    -> (n)

@bashtage has a pull request to add broadcasting to multinomial. As an example, suppose I have four values of n, say 2, 5, 10 and 20, and I have two discrete probability distributions of length 3, say [0.1, 0.2, 0.7] and [0.8, 0.2, 0.0], and I want to generate 10 multinomial variates for each combination of those inputs. With the code in @bashtage's PR, we can write:

In [85]: n = np.array([2, 5, 10, 20]).reshape(-1, 1)

In [86]: pvals = np.array([[0.1, 0.2, 0.7], [0.8, 0.2, 0.0]])

In [87]: rng = np.random.default_rng()

In [88]: x = rng.multinomial(n, pvals, size=(10, 4, 2))

In [89]: x.shape
Out[89]: (10, 4, 2, 3)

The shape of the output has three sources: (10,) is the number of variates that we wanted for each combination of input parameters; (4, 2) is the broadcast shape of the input parameters, without the dimension associated with the gufunc signature; (3,) is the dimension associated with each variate.

Note that size is not the shape of the output array--size does not include the dimension associated with the variates themselves.

The question is how to adapt this to the other multivariate distribution methods.","We are working on enhancing the multivariate distributions in SciPy to ensure they remain consistent with NumPy's behavior. One challenge is the interaction between the size parameter and the broadcasting of distribution parameters, which can be 1-d or 2-d arrays. For example, in the multinomial distribution, we need to handle different values of n and pvals. @bashtage's PR shows how to generate 10 multinomial variates for each combination of inputs, with the output shape reflecting the number of variates, the broadcast shape of the inputs, and the variate dimension. We need to apply this approach to other multivariate distributions.",Known,57,57,numpy_numpy,17669,issue,,1,1,1,1,"We are working on enhancing the multivariate distributions in SciPy to ensure they remain consistent with NumPy's behavior. One challenge is the interaction between the size parameter and the broadcasting of distribution parameters, which can be 1-d or 2-d arrays. For example, in the multinomial distribution, we need to handle different values of n and pvals. @bashtage's PR shows how to generate 10 multinomial variates for each combination of inputs, with the output shape reflecting the number of variates, the broadcast shape of the inputs, and the variate dimension. We need to apply this approach to other multivariate distributions."
508,numpy_numpy,issue,17669,"This issue is motivated by ongoing work in SciPy to improve the existing multivariate distributions and add new ones. We'd like to be sure that, if possible, enhancements that we make remain consistent with the current and future behavior of NumPy's multivariate distributions.

The specific issue is how the size parameter interacts with broadcasting of the distribution parameters, some of which are 1-d or 2-d arrays rather than scalars.

I'm going to refer to gufunc broadcasting, so it will be helpful to have the gufunc signatures of the existing multivariate distributions (even though they are not actually implemented as gufuncs):

Name                         Parameters      Gufunc signature
---------------------------  --------------  ----------------
dirichlet                    alpha           (n)       -> (n)
multinomial                  n, pvals        (),(n)    -> (n)
multivariate_normal          mean, cov       (n),(n,n) -> (n)
multivariate_hypergeometric  colors, nsample (n),()    -> (n)

@bashtage has a pull request to add broadcasting to multinomial. As an example, suppose I have four values of n, say 2, 5, 10 and 20, and I have two discrete probability distributions of length 3, say [0.1, 0.2, 0.7] and [0.8, 0.2, 0.0], and I want to generate 10 multinomial variates for each combination of those inputs. With the code in @bashtage's PR, we can write:

In [85]: n = np.array([2, 5, 10, 20]).reshape(-1, 1)

In [86]: pvals = np.array([[0.1, 0.2, 0.7], [0.8, 0.2, 0.0]])

In [87]: rng = np.random.default_rng()

In [88]: x = rng.multinomial(n, pvals, size=(10, 4, 2))

In [89]: x.shape
Out[89]: (10, 4, 2, 3)

The shape of the output has three sources: (10,) is the number of variates that we wanted for each combination of input parameters; (4, 2) is the broadcast shape of the input parameters, without the dimension associated with the gufunc signature; (3,) is the dimension associated with each variate.

Note that size is not the shape of the output array--size does not include the dimension associated with the variates themselves.

The question is how to adapt this to the other multivariate distribution methods.","Our efforts to improve the multivariate distributions in SciPy include ensuring they remain consistent with NumPy's behavior. A specific issue is the interaction between the size parameter and the broadcasting of distribution parameters, which can be arrays of different dimensions. For instance, in the multinomial distribution, we need to handle multiple values of n and pvals. @bashtage's PR demonstrates how to generate variates with broadcasting, resulting in an output shape that includes the number of variates, the broadcast shape of the inputs, and the variate dimension. We need to extend this approach to other multivariate distributions.",Known,57,57,numpy_numpy,17669,issue,,1,1,1,1,"Our efforts to improve the multivariate distributions in SciPy include ensuring they remain consistent with NumPy's behavior. A specific issue is the interaction between the size parameter and the broadcasting of distribution parameters, which can be arrays of different dimensions. For instance, in the multinomial distribution, we need to handle multiple values of n and pvals. @bashtage's PR demonstrates how to generate variates with broadcasting, resulting in an output shape that includes the number of variates, the broadcast shape of the inputs, and the variate dimension. We need to extend this approach to other multivariate distributions."
509,numpy_numpy,issue,17669,"This issue is motivated by ongoing work in SciPy to improve the existing multivariate distributions and add new ones. We'd like to be sure that, if possible, enhancements that we make remain consistent with the current and future behavior of NumPy's multivariate distributions.

The specific issue is how the size parameter interacts with broadcasting of the distribution parameters, some of which are 1-d or 2-d arrays rather than scalars.

I'm going to refer to gufunc broadcasting, so it will be helpful to have the gufunc signatures of the existing multivariate distributions (even though they are not actually implemented as gufuncs):

Name                         Parameters      Gufunc signature
---------------------------  --------------  ----------------
dirichlet                    alpha           (n)       -> (n)
multinomial                  n, pvals        (),(n)    -> (n)
multivariate_normal          mean, cov       (n),(n,n) -> (n)
multivariate_hypergeometric  colors, nsample (n),()    -> (n)

@bashtage has a pull request to add broadcasting to multinomial. As an example, suppose I have four values of n, say 2, 5, 10 and 20, and I have two discrete probability distributions of length 3, say [0.1, 0.2, 0.7] and [0.8, 0.2, 0.0], and I want to generate 10 multinomial variates for each combination of those inputs. With the code in @bashtage's PR, we can write:

In [85]: n = np.array([2, 5, 10, 20]).reshape(-1, 1)

In [86]: pvals = np.array([[0.1, 0.2, 0.7], [0.8, 0.2, 0.0]])

In [87]: rng = np.random.default_rng()

In [88]: x = rng.multinomial(n, pvals, size=(10, 4, 2))

In [89]: x.shape
Out[89]: (10, 4, 2, 3)

The shape of the output has three sources: (10,) is the number of variates that we wanted for each combination of input parameters; (4, 2) is the broadcast shape of the input parameters, without the dimension associated with the gufunc signature; (3,) is the dimension associated with each variate.

Note that size is not the shape of the output array--size does not include the dimension associated with the variates themselves.

The question is how to adapt this to the other multivariate distribution methods.","We are enhancing the multivariate distributions in SciPy to maintain consistency with NumPy's behavior. One key challenge is the interaction between the size parameter and the broadcasting of distribution parameters, which can be 1-d or 2-d arrays. For example, in the multinomial distribution, we need to handle different values of n and pvals. @bashtage's PR shows how to generate 10 multinomial variates for each combination of inputs, with the output shape reflecting the number of variates, the broadcast shape of the inputs, and the variate dimension. We need to apply this approach to other multivariate distributions.",Known,57,57,numpy_numpy,17669,issue,,1,1,1,1,"We are enhancing the multivariate distributions in SciPy to maintain consistency with NumPy's behavior. One key challenge is the interaction between the size parameter and the broadcasting of distribution parameters, which can be 1-d or 2-d arrays. For example, in the multinomial distribution, we need to handle different values of n and pvals. @bashtage's PR shows how to generate 10 multinomial variates for each combination of inputs, with the output shape reflecting the number of variates, the broadcast shape of the inputs, and the variate dimension. We need to apply this approach to other multivariate distributions."
510,numpy_numpy,issue,17669,"This issue is motivated by ongoing work in SciPy to improve the existing multivariate distributions and add new ones. We'd like to be sure that, if possible, enhancements that we make remain consistent with the current and future behavior of NumPy's multivariate distributions.

The specific issue is how the size parameter interacts with broadcasting of the distribution parameters, some of which are 1-d or 2-d arrays rather than scalars.

I'm going to refer to gufunc broadcasting, so it will be helpful to have the gufunc signatures of the existing multivariate distributions (even though they are not actually implemented as gufuncs):

Name                         Parameters      Gufunc signature
---------------------------  --------------  ----------------
dirichlet                    alpha           (n)       -> (n)
multinomial                  n, pvals        (),(n)    -> (n)
multivariate_normal          mean, cov       (n),(n,n) -> (n)
multivariate_hypergeometric  colors, nsample (n),()    -> (n)

@bashtage has a pull request to add broadcasting to multinomial. As an example, suppose I have four values of n, say 2, 5, 10 and 20, and I have two discrete probability distributions of length 3, say [0.1, 0.2, 0.7] and [0.8, 0.2, 0.0], and I want to generate 10 multinomial variates for each combination of those inputs. With the code in @bashtage's PR, we can write:

In [85]: n = np.array([2, 5, 10, 20]).reshape(-1, 1)

In [86]: pvals = np.array([[0.1, 0.2, 0.7], [0.8, 0.2, 0.0]])

In [87]: rng = np.random.default_rng()

In [88]: x = rng.multinomial(n, pvals, size=(10, 4, 2))

In [89]: x.shape
Out[89]: (10, 4, 2, 3)

The shape of the output has three sources: (10,) is the number of variates that we wanted for each combination of input parameters; (4, 2) is the broadcast shape of the input parameters, without the dimension associated with the gufunc signature; (3,) is the dimension associated with each variate.

Note that size is not the shape of the output array--size does not include the dimension associated with the variates themselves.

The question is how to adapt this to the other multivariate distribution methods.","The ongoing work in SciPy to improve multivariate distributions involves ensuring consistency with NumPy's behavior. A key issue is the interaction between the size parameter and the broadcasting of distribution parameters, which can be arrays of different dimensions. For instance, in the multinomial distribution, we need to handle multiple values of n and pvals. @bashtage's PR demonstrates how to generate variates with broadcasting, resulting in an output shape that includes the number of variates, the broadcast shape of the inputs, and the variate dimension. We need to extend this approach to other multivariate distributions.",Known,57,57,numpy_numpy,17669,issue,,1,1,1,1,"The ongoing work in SciPy to improve multivariate distributions involves ensuring consistency with NumPy's behavior. A key issue is the interaction between the size parameter and the broadcasting of distribution parameters, which can be arrays of different dimensions. For instance, in the multinomial distribution, we need to handle multiple values of n and pvals. @bashtage's PR demonstrates how to generate variates with broadcasting, resulting in an output shape that includes the number of variates, the broadcast shape of the inputs, and the variate dimension. We need to extend this approach to other multivariate distributions."
511,numpy_numpy,issue,17669,"This issue is motivated by ongoing work in SciPy to improve the existing multivariate distributions and add new ones. We'd like to be sure that, if possible, enhancements that we make remain consistent with the current and future behavior of NumPy's multivariate distributions.

The specific issue is how the size parameter interacts with broadcasting of the distribution parameters, some of which are 1-d or 2-d arrays rather than scalars.

I'm going to refer to gufunc broadcasting, so it will be helpful to have the gufunc signatures of the existing multivariate distributions (even though they are not actually implemented as gufuncs):

Name                         Parameters      Gufunc signature
---------------------------  --------------  ----------------
dirichlet                    alpha           (n)       -> (n)
multinomial                  n, pvals        (),(n)    -> (n)
multivariate_normal          mean, cov       (n),(n,n) -> (n)
multivariate_hypergeometric  colors, nsample (n),()    -> (n)

@bashtage has a pull request to add broadcasting to multinomial. As an example, suppose I have four values of n, say 2, 5, 10 and 20, and I have two discrete probability distributions of length 3, say [0.1, 0.2, 0.7] and [0.8, 0.2, 0.0], and I want to generate 10 multinomial variates for each combination of those inputs. With the code in @bashtage's PR, we can write:

In [85]: n = np.array([2, 5, 10, 20]).reshape(-1, 1)

In [86]: pvals = np.array([[0.1, 0.2, 0.7], [0.8, 0.2, 0.0]])

In [87]: rng = np.random.default_rng()

In [88]: x = rng.multinomial(n, pvals, size=(10, 4, 2))

In [89]: x.shape
Out[89]: (10, 4, 2, 3)

The shape of the output has three sources: (10,) is the number of variates that we wanted for each combination of input parameters; (4, 2) is the broadcast shape of the input parameters, without the dimension associated with the gufunc signature; (3,) is the dimension associated with each variate.

Note that size is not the shape of the output array--size does not include the dimension associated with the variates themselves.

The question is how to adapt this to the other multivariate distribution methods.","We are working on enhancing the multivariate distributions in SciPy to ensure they remain consistent with NumPy's behavior. One challenge is the interaction between the size parameter and the broadcasting of distribution parameters, which can be 1-d or 2-d arrays. For example, in the multinomial distribution, we need to handle different values of n and pvals. @bashtage's PR shows how to generate 10 multinomial variates for each combination of inputs, with the output shape reflecting the number of variates, the broadcast shape of the inputs, and the variate dimension. We need to apply this approach to other multivariate distributions.",Known,57,57,numpy_numpy,17669,issue,,1,1,1,1,"We are working on enhancing the multivariate distributions in SciPy to ensure they remain consistent with NumPy's behavior. One challenge is the interaction between the size parameter and the broadcasting of distribution parameters, which can be 1-d or 2-d arrays. For example, in the multinomial distribution, we need to handle different values of n and pvals. @bashtage's PR shows how to generate 10 multinomial variates for each combination of inputs, with the output shape reflecting the number of variates, the broadcast shape of the inputs, and the variate dimension. We need to apply this approach to other multivariate distributions."
512,numpy_numpy,issue,17669,"This issue is motivated by ongoing work in SciPy to improve the existing multivariate distributions and add new ones. We'd like to be sure that, if possible, enhancements that we make remain consistent with the current and future behavior of NumPy's multivariate distributions.

The specific issue is how the size parameter interacts with broadcasting of the distribution parameters, some of which are 1-d or 2-d arrays rather than scalars.

I'm going to refer to gufunc broadcasting, so it will be helpful to have the gufunc signatures of the existing multivariate distributions (even though they are not actually implemented as gufuncs):

Name                         Parameters      Gufunc signature
---------------------------  --------------  ----------------
dirichlet                    alpha           (n)       -> (n)
multinomial                  n, pvals        (),(n)    -> (n)
multivariate_normal          mean, cov       (n),(n,n) -> (n)
multivariate_hypergeometric  colors, nsample (n),()    -> (n)

@bashtage has a pull request to add broadcasting to multinomial. As an example, suppose I have four values of n, say 2, 5, 10 and 20, and I have two discrete probability distributions of length 3, say [0.1, 0.2, 0.7] and [0.8, 0.2, 0.0], and I want to generate 10 multinomial variates for each combination of those inputs. With the code in @bashtage's PR, we can write:

In [85]: n = np.array([2, 5, 10, 20]).reshape(-1, 1)

In [86]: pvals = np.array([[0.1, 0.2, 0.7], [0.8, 0.2, 0.0]])

In [87]: rng = np.random.default_rng()

In [88]: x = rng.multinomial(n, pvals, size=(10, 4, 2))

In [89]: x.shape
Out[89]: (10, 4, 2, 3)

The shape of the output has three sources: (10,) is the number of variates that we wanted for each combination of input parameters; (4, 2) is the broadcast shape of the input parameters, without the dimension associated with the gufunc signature; (3,) is the dimension associated with each variate.

Note that size is not the shape of the output array--size does not include the dimension associated with the variates themselves.

The question is how to adapt this to the other multivariate distribution methods.","Our efforts to improve the multivariate distributions in SciPy include ensuring they remain consistent with NumPy's behavior. A specific issue is the interaction between the size parameter and the broadcasting of distribution parameters, which can be arrays of different dimensions. For instance, in the multinomial distribution, we need to handle multiple values of n and pvals. @bashtage's PR demonstrates how to generate variates with broadcasting, resulting in an output shape that includes the number of variates, the broadcast shape of the inputs, and the variate dimension. We need to extend this approach to other multivariate distributions.",Known,57,57,numpy_numpy,17669,issue,,1,1,1,1,"Our efforts to improve the multivariate distributions in SciPy include ensuring they remain consistent with NumPy's behavior. A specific issue is the interaction between the size parameter and the broadcasting of distribution parameters, which can be arrays of different dimensions. For instance, in the multinomial distribution, we need to handle multiple values of n and pvals. @bashtage's PR demonstrates how to generate variates with broadcasting, resulting in an output shape that includes the number of variates, the broadcast shape of the inputs, and the variate dimension. We need to extend this approach to other multivariate distributions."
